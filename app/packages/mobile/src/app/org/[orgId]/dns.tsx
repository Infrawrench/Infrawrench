import { DnsScreen } from "@/features/dns/DnsScreen";

/** Org-wide DNS inventory — every zone and record, with dangling targets flagged. */
export default function DnsRoute() {
  return <DnsScreen />;
}
