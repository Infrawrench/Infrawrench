import { PostureScreen } from "@/features/posture/PostureScreen";

/** Org-wide posture findings — plugin-declared security checks, ranked by severity. */
export default function PostureRoute() {
  return <PostureScreen />;
}
