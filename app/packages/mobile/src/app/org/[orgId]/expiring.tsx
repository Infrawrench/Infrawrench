import { ExpiringScreen } from "@/features/expiry/ExpiringScreen";

/** Org-wide expiry feed — certs, domains, tokens and key ages with deadlines. */
export default function ExpiringRoute() {
  return <ExpiringScreen />;
}
