import { ProbesScreen } from "@/features/probes/ProbesScreen";

/** Org-wide synthetic probes — uptime/latency checks with live status. */
export default function ProbesRoute() {
  return <ProbesScreen />;
}
