import { LogWorkspacesScreen } from "@/features/log-workspaces/LogWorkspacesScreen";

/** Saved log-workspace queries — read-only list; tap one to tail its streams. */
export default function LogWorkspacesRoute() {
  return <LogWorkspacesScreen />;
}
