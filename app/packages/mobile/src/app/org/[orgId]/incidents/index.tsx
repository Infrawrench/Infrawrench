import { IncidentsScreen } from "@/features/incidents/IncidentsScreen";

/** Declared incidents — the org's own, not provider status incidents. */
export default function IncidentsRoute() {
  return <IncidentsScreen />;
}
