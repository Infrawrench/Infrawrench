/**
 * The account-page section rule lives in `@infrawrench/client-core` so web,
 * desktop, and mobile all filter sections the same way. Re-exported here to
 * keep the historical `@infrawrench/ui` import path working.
 */
export { getVisibleAccountCategories, pickDefaultAccountSectionId } from "@infrawrench/client-core";
