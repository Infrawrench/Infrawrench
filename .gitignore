node_modules
dist
.next
out
release
.turbo
coverage
*.local
.env
.env.*
!.env.example
*.tsbuildinfo

# Generated client SDKs (`pnpm --filter @infrawrench/web generate:sdk`).
# Deliberately outside the pnpm workspace and never committed — the checked-in
# openapi.json is the source of truth, these are build output.
/sdk

# Astro / Wrangler
.astro
.wrangler
.dev.vars

# Tauri
src-tauri/target

# SQLite
*.db
*.db-shm
*.db-wal

# OS
.DS_Store
Thumbs.db
.reviews/

# Claude Code agent worktrees
.claude/worktrees/
