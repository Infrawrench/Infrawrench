diff --git a/clickhouse/driver.cjs b/clickhouse/driver.cjs
new file mode 100644
index 0000000..12688c4
--- /dev/null
+++ b/clickhouse/driver.cjs
@@ -0,0 +1,109 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var driver_exports = {};
+__export(driver_exports, {
+  ClickHouseDatabase: () => import_db2.ClickHouseDatabase,
+  ClickHouseDriver: () => ClickHouseDriver,
+  ClickHouseDriverDatabase: () => ClickHouseDriverDatabase,
+  drizzle: () => drizzle
+});
+module.exports = __toCommonJS(driver_exports);
+var import_client = require("@clickhouse/client");
+var import_db = require("../clickhouse-core/db.cjs");
+var import_dialect = require("../clickhouse-core/dialect.cjs");
+var import_entity = require("../entity.cjs");
+var import_logger = require("../logger.cjs");
+var import_relations = require("../relations.cjs");
+var import_utils = require("../utils.cjs");
+var import_session = require("./session.cjs");
+var import_db2 = require("../clickhouse-core/db.cjs");
+class ClickHouseDriver {
+  constructor(client, dialect, options = {}) {
+    this.client = client;
+    this.dialect = dialect;
+    this.options = options;
+  }
+  client;
+  dialect;
+  options;
+  static [import_entity.entityKind] = "ClickHouseDriver";
+  createSession() {
+    return new import_session.ClickHouseDriverSession(this.client, this.dialect, {
+      logger: this.options.logger,
+      cache: this.options.cache,
+      settings: this.options.settings
+    });
+  }
+}
+class ClickHouseDriverDatabase extends import_db.ClickHouseDatabase {
+  static [import_entity.entityKind] = "ClickHouseDriverDatabase";
+}
+function construct(client, config = {}) {
+  const dialect = new import_dialect.ClickHouseDialect({ casing: config.casing });
+  let logger;
+  if (config.logger === true) {
+    logger = new import_logger.DefaultLogger();
+  } else if (config.logger !== false) {
+    logger = config.logger;
+  }
+  let schema;
+  if (config.schema) {
+    const tablesConfig = (0, import_relations.extractTablesRelationalConfig)(config.schema, import_relations.createTableRelationsHelpers);
+    schema = {
+      fullSchema: config.schema,
+      schema: tablesConfig.tables,
+      tableNamesMap: tablesConfig.tableNamesMap
+    };
+  }
+  const driver = new ClickHouseDriver(client, dialect, { logger, cache: config.cache, settings: config.settings });
+  const session = driver.createSession();
+  const db = new ClickHouseDriverDatabase(dialect, session, schema);
+  db.$client = client;
+  db.$cache = config.cache;
+  if (db.$cache) {
+    db.$cache["invalidate"] = config.cache?.onMutate;
+  }
+  return db;
+}
+function drizzle(...params) {
+  if (typeof params[0] === "string") {
+    const instance = (0, import_client.createClient)({ url: params[0] });
+    return construct(instance, params[1]);
+  }
+  if ((0, import_utils.isConfig)(params[0])) {
+    const { connection, client, ...drizzleConfig } = params[0];
+    if (client) return construct(client, drizzleConfig);
+    const instance = typeof connection === "string" ? (0, import_client.createClient)({ url: connection }) : (0, import_client.createClient)(connection);
+    return construct(instance, drizzleConfig);
+  }
+  return construct(params[0], params[1]);
+}
+((drizzle2) => {
+  function mock(config) {
+    return construct({}, config);
+  }
+  drizzle2.mock = mock;
+})(drizzle || (drizzle = {}));
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDatabase,
+  ClickHouseDriver,
+  ClickHouseDriverDatabase,
+  drizzle
+});
diff --git a/clickhouse/driver.d.cts b/clickhouse/driver.d.cts
new file mode 100644
index 0000000..7f32e7c
--- /dev/null
+++ b/clickhouse/driver.d.cts
@@ -0,0 +1,68 @@
+import type { ClickHouseClientConfigOptions, ClickHouseSettings } from '@clickhouse/client';
+import type { Cache } from '../cache/core/cache.cjs';
+import { ClickHouseDatabase } from '../clickhouse-core/db.cjs';
+import { ClickHouseDialect } from '../clickhouse-core/dialect.cjs';
+import { entityKind } from '../entity.cjs';
+import type { Logger } from '../logger.cjs';
+import { type DrizzleConfig } from '../utils.cjs';
+import type { ClickHouseDriverClient, ClickHouseDriverPreparedQueryHKT, ClickHouseDriverQueryResultHKT } from './session.cjs';
+import { ClickHouseDriverSession } from './session.cjs';
+export interface ClickHouseDriverOptions {
+    logger?: Logger;
+    cache?: Cache;
+    settings?: ClickHouseSettings;
+}
+export declare class ClickHouseDriver {
+    private client;
+    private dialect;
+    private options;
+    static readonly [entityKind]: string;
+    constructor(client: ClickHouseDriverClient, dialect: ClickHouseDialect, options?: ClickHouseDriverOptions);
+    createSession(): ClickHouseDriverSession;
+}
+export { ClickHouseDatabase } from '../clickhouse-core/db.cjs';
+export declare class ClickHouseDriverDatabase<TSchema extends Record<string, unknown> = Record<string, never>> extends ClickHouseDatabase<ClickHouseDriverQueryResultHKT, ClickHouseDriverPreparedQueryHKT, TSchema> {
+    static readonly [entityKind]: string;
+}
+export type ClickHouseDrizzleConfig<TSchema extends Record<string, unknown> = Record<string, never>> = Omit<DrizzleConfig<TSchema>, 'schema'> & {
+    /** Settings applied to every statement Drizzle issues. */
+    settings?: ClickHouseSettings;
+} & ({
+    schema: TSchema;
+} | {
+    schema?: undefined;
+});
+/**
+ * Connects Drizzle to ClickHouse through `@clickhouse/client`.
+ *
+ * Accepts an existing client, a connection URL, or the client's own config object:
+ *
+ * ```ts
+ * const db = drizzle(process.env.CLICKHOUSE_URL!);
+ * const db = drizzle({ connection: { url: 'http://localhost:8123', username: 'default' }, schema });
+ * const db = drizzle(createClient({ url }), { schema });
+ * ```
+ */
+export declare function drizzle<TSchema extends Record<string, unknown> = Record<string, never>, TClient extends ClickHouseDriverClient = ClickHouseDriverClient>(...params: [
+    TClient | string
+] | [
+    TClient | string,
+    ClickHouseDrizzleConfig<TSchema>
+] | [
+    (ClickHouseDrizzleConfig<TSchema> & ({
+        connection: string | ClickHouseClientConfigOptions;
+    } | {
+        client: TClient;
+    }))
+]): ClickHouseDriverDatabase<TSchema> & {
+    $client: TClient;
+};
+export declare namespace drizzle {
+    /**
+     * Builds a database that throws on every query, for typing schemas in environments that must not
+     * open a connection.
+     */
+    function mock<TSchema extends Record<string, unknown> = Record<string, never>>(config?: ClickHouseDrizzleConfig<TSchema>): ClickHouseDriverDatabase<TSchema> & {
+        $client: '$client is not available on drizzle.mock()';
+    };
+}
diff --git a/clickhouse/driver.d.ts b/clickhouse/driver.d.ts
new file mode 100644
index 0000000..4ae1f17
--- /dev/null
+++ b/clickhouse/driver.d.ts
@@ -0,0 +1,68 @@
+import type { ClickHouseClientConfigOptions, ClickHouseSettings } from '@clickhouse/client';
+import type { Cache } from '../cache/core/cache.js';
+import { ClickHouseDatabase } from '../clickhouse-core/db.js';
+import { ClickHouseDialect } from '../clickhouse-core/dialect.js';
+import { entityKind } from '../entity.js';
+import type { Logger } from '../logger.js';
+import { type DrizzleConfig } from '../utils.js';
+import type { ClickHouseDriverClient, ClickHouseDriverPreparedQueryHKT, ClickHouseDriverQueryResultHKT } from './session.js';
+import { ClickHouseDriverSession } from './session.js';
+export interface ClickHouseDriverOptions {
+    logger?: Logger;
+    cache?: Cache;
+    settings?: ClickHouseSettings;
+}
+export declare class ClickHouseDriver {
+    private client;
+    private dialect;
+    private options;
+    static readonly [entityKind]: string;
+    constructor(client: ClickHouseDriverClient, dialect: ClickHouseDialect, options?: ClickHouseDriverOptions);
+    createSession(): ClickHouseDriverSession;
+}
+export { ClickHouseDatabase } from '../clickhouse-core/db.js';
+export declare class ClickHouseDriverDatabase<TSchema extends Record<string, unknown> = Record<string, never>> extends ClickHouseDatabase<ClickHouseDriverQueryResultHKT, ClickHouseDriverPreparedQueryHKT, TSchema> {
+    static readonly [entityKind]: string;
+}
+export type ClickHouseDrizzleConfig<TSchema extends Record<string, unknown> = Record<string, never>> = Omit<DrizzleConfig<TSchema>, 'schema'> & {
+    /** Settings applied to every statement Drizzle issues. */
+    settings?: ClickHouseSettings;
+} & ({
+    schema: TSchema;
+} | {
+    schema?: undefined;
+});
+/**
+ * Connects Drizzle to ClickHouse through `@clickhouse/client`.
+ *
+ * Accepts an existing client, a connection URL, or the client's own config object:
+ *
+ * ```ts
+ * const db = drizzle(process.env.CLICKHOUSE_URL!);
+ * const db = drizzle({ connection: { url: 'http://localhost:8123', username: 'default' }, schema });
+ * const db = drizzle(createClient({ url }), { schema });
+ * ```
+ */
+export declare function drizzle<TSchema extends Record<string, unknown> = Record<string, never>, TClient extends ClickHouseDriverClient = ClickHouseDriverClient>(...params: [
+    TClient | string
+] | [
+    TClient | string,
+    ClickHouseDrizzleConfig<TSchema>
+] | [
+    (ClickHouseDrizzleConfig<TSchema> & ({
+        connection: string | ClickHouseClientConfigOptions;
+    } | {
+        client: TClient;
+    }))
+]): ClickHouseDriverDatabase<TSchema> & {
+    $client: TClient;
+};
+export declare namespace drizzle {
+    /**
+     * Builds a database that throws on every query, for typing schemas in environments that must not
+     * open a connection.
+     */
+    function mock<TSchema extends Record<string, unknown> = Record<string, never>>(config?: ClickHouseDrizzleConfig<TSchema>): ClickHouseDriverDatabase<TSchema> & {
+        $client: '$client is not available on drizzle.mock()';
+    };
+}
diff --git a/clickhouse/driver.js b/clickhouse/driver.js
new file mode 100644
index 0000000..0810206
--- /dev/null
+++ b/clickhouse/driver.js
@@ -0,0 +1,85 @@
+import { createClient } from "@clickhouse/client";
+import { ClickHouseDatabase } from "../clickhouse-core/db.js";
+import { ClickHouseDialect } from "../clickhouse-core/dialect.js";
+import { entityKind } from "../entity.js";
+import { DefaultLogger } from "../logger.js";
+import {
+  createTableRelationsHelpers,
+  extractTablesRelationalConfig
+} from "../relations.js";
+import { isConfig } from "../utils.js";
+import { ClickHouseDriverSession } from "./session.js";
+class ClickHouseDriver {
+  constructor(client, dialect, options = {}) {
+    this.client = client;
+    this.dialect = dialect;
+    this.options = options;
+  }
+  client;
+  dialect;
+  options;
+  static [entityKind] = "ClickHouseDriver";
+  createSession() {
+    return new ClickHouseDriverSession(this.client, this.dialect, {
+      logger: this.options.logger,
+      cache: this.options.cache,
+      settings: this.options.settings
+    });
+  }
+}
+import { ClickHouseDatabase as ClickHouseDatabase2 } from "../clickhouse-core/db.js";
+class ClickHouseDriverDatabase extends ClickHouseDatabase {
+  static [entityKind] = "ClickHouseDriverDatabase";
+}
+function construct(client, config = {}) {
+  const dialect = new ClickHouseDialect({ casing: config.casing });
+  let logger;
+  if (config.logger === true) {
+    logger = new DefaultLogger();
+  } else if (config.logger !== false) {
+    logger = config.logger;
+  }
+  let schema;
+  if (config.schema) {
+    const tablesConfig = extractTablesRelationalConfig(config.schema, createTableRelationsHelpers);
+    schema = {
+      fullSchema: config.schema,
+      schema: tablesConfig.tables,
+      tableNamesMap: tablesConfig.tableNamesMap
+    };
+  }
+  const driver = new ClickHouseDriver(client, dialect, { logger, cache: config.cache, settings: config.settings });
+  const session = driver.createSession();
+  const db = new ClickHouseDriverDatabase(dialect, session, schema);
+  db.$client = client;
+  db.$cache = config.cache;
+  if (db.$cache) {
+    db.$cache["invalidate"] = config.cache?.onMutate;
+  }
+  return db;
+}
+function drizzle(...params) {
+  if (typeof params[0] === "string") {
+    const instance = createClient({ url: params[0] });
+    return construct(instance, params[1]);
+  }
+  if (isConfig(params[0])) {
+    const { connection, client, ...drizzleConfig } = params[0];
+    if (client) return construct(client, drizzleConfig);
+    const instance = typeof connection === "string" ? createClient({ url: connection }) : createClient(connection);
+    return construct(instance, drizzleConfig);
+  }
+  return construct(params[0], params[1]);
+}
+((drizzle2) => {
+  function mock(config) {
+    return construct({}, config);
+  }
+  drizzle2.mock = mock;
+})(drizzle || (drizzle = {}));
+export {
+  ClickHouseDatabase2 as ClickHouseDatabase,
+  ClickHouseDriver,
+  ClickHouseDriverDatabase,
+  drizzle
+};
diff --git a/clickhouse/index.cjs b/clickhouse/index.cjs
new file mode 100644
index 0000000..a04fdb2
--- /dev/null
+++ b/clickhouse/index.cjs
@@ -0,0 +1,24 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var clickhouse_exports = {};
+module.exports = __toCommonJS(clickhouse_exports);
+__reExport(clickhouse_exports, require("./driver.cjs"), module.exports);
+__reExport(clickhouse_exports, require("./session.cjs"), module.exports);
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ...require("./driver.cjs"),
+  ...require("./session.cjs")
+});
diff --git a/clickhouse/index.d.cts b/clickhouse/index.d.cts
new file mode 100644
index 0000000..ecd32d8
--- /dev/null
+++ b/clickhouse/index.d.cts
@@ -0,0 +1,2 @@
+export * from './driver.cjs';
+export * from './session.cjs';
diff --git a/clickhouse/index.d.ts b/clickhouse/index.d.ts
new file mode 100644
index 0000000..5cfe4a2
--- /dev/null
+++ b/clickhouse/index.d.ts
@@ -0,0 +1,2 @@
+export * from './driver.js';
+export * from './session.js';
diff --git a/clickhouse/index.js b/clickhouse/index.js
new file mode 100644
index 0000000..b74eace
--- /dev/null
+++ b/clickhouse/index.js
@@ -0,0 +1,2 @@
+export * from "./driver.js";
+export * from "./session.js";
diff --git a/clickhouse/migrator.cjs b/clickhouse/migrator.cjs
new file mode 100644
index 0000000..8d846b1
--- /dev/null
+++ b/clickhouse/migrator.cjs
@@ -0,0 +1,32 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var migrator_exports = {};
+__export(migrator_exports, {
+  migrate: () => migrate
+});
+module.exports = __toCommonJS(migrator_exports);
+var import_migrator = require("../migrator.cjs");
+async function migrate(db, config) {
+  const migrations = (0, import_migrator.readMigrationFiles)(config);
+  await db.dialect.migrate(migrations, db.session, config);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  migrate
+});
diff --git a/clickhouse/migrator.d.cts b/clickhouse/migrator.d.cts
new file mode 100644
index 0000000..593bcae
--- /dev/null
+++ b/clickhouse/migrator.d.cts
@@ -0,0 +1,11 @@
+import type { MigrationConfig } from '../migrator.cjs';
+import type { ClickHouseDriverDatabase } from './driver.cjs';
+/**
+ * Applies the migrations in `config.migrationsFolder`.
+ *
+ * ClickHouse has no usable transactions, so migrations are applied statement by statement and a
+ * migration is only recorded once all of its statements have succeeded — a failure part-way leaves
+ * it unrecorded and it is retried on the next run. Write migrations so that this is safe, e.g. with
+ * `CREATE TABLE IF NOT EXISTS`.
+ */
+export declare function migrate<TSchema extends Record<string, unknown>>(db: ClickHouseDriverDatabase<TSchema>, config: MigrationConfig): Promise<void>;
diff --git a/clickhouse/migrator.d.ts b/clickhouse/migrator.d.ts
new file mode 100644
index 0000000..d584dd1
--- /dev/null
+++ b/clickhouse/migrator.d.ts
@@ -0,0 +1,11 @@
+import type { MigrationConfig } from '../migrator.js';
+import type { ClickHouseDriverDatabase } from './driver.js';
+/**
+ * Applies the migrations in `config.migrationsFolder`.
+ *
+ * ClickHouse has no usable transactions, so migrations are applied statement by statement and a
+ * migration is only recorded once all of its statements have succeeded — a failure part-way leaves
+ * it unrecorded and it is retried on the next run. Write migrations so that this is safe, e.g. with
+ * `CREATE TABLE IF NOT EXISTS`.
+ */
+export declare function migrate<TSchema extends Record<string, unknown>>(db: ClickHouseDriverDatabase<TSchema>, config: MigrationConfig): Promise<void>;
diff --git a/clickhouse/migrator.js b/clickhouse/migrator.js
new file mode 100644
index 0000000..043a7a3
--- /dev/null
+++ b/clickhouse/migrator.js
@@ -0,0 +1,8 @@
+import { readMigrationFiles } from "../migrator.js";
+async function migrate(db, config) {
+  const migrations = readMigrationFiles(config);
+  await db.dialect.migrate(migrations, db.session, config);
+}
+export {
+  migrate
+};
diff --git a/clickhouse/session.cjs b/clickhouse/session.cjs
new file mode 100644
index 0000000..441f580
--- /dev/null
+++ b/clickhouse/session.cjs
@@ -0,0 +1,189 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var session_exports = {};
+__export(session_exports, {
+  ClickHouseDriverPreparedQuery: () => ClickHouseDriverPreparedQuery,
+  ClickHouseDriverSession: () => ClickHouseDriverSession,
+  DRIZZLE_CLICKHOUSE_SETTINGS: () => DRIZZLE_CLICKHOUSE_SETTINGS
+});
+module.exports = __toCommonJS(session_exports);
+var import_node_stream = require("node:stream");
+var import_cache = require("../cache/core/cache.cjs");
+var import_errors = require("../clickhouse-core/errors.cjs");
+var import_insert = require("../clickhouse-core/query-builders/insert.cjs");
+var import_session = require("../clickhouse-core/session.cjs");
+var import_entity = require("../entity.cjs");
+var import_errors2 = require("../errors.cjs");
+var import_logger = require("../logger.cjs");
+var import_sql = require("../sql/sql.cjs");
+var import_utils = require("../utils.cjs");
+const DRIZZLE_CLICKHOUSE_SETTINGS = {
+  date_time_output_format: "iso"
+};
+const ROW_RETURNING = /^\s*(?:\(|select|with|show|describe|desc|explain|exists|values)\b/i;
+function toInsertStream(rows) {
+  return import_node_stream.Readable.from(rows);
+}
+class ClickHouseDriverPreparedQuery extends import_session.ClickHousePreparedQuery {
+  constructor(client, queryString, params, logger, fields, customResultMapper, settings, cache, queryMetadata, cacheConfig) {
+    super(cache, queryMetadata, cacheConfig);
+    this.client = client;
+    this.queryString = queryString;
+    this.params = params;
+    this.logger = logger;
+    this.fields = fields;
+    this.customResultMapper = customResultMapper;
+    this.settings = settings;
+  }
+  client;
+  queryString;
+  params;
+  logger;
+  fields;
+  customResultMapper;
+  settings;
+  static [import_entity.entityKind] = "ClickHouseDriverPreparedQuery";
+  async execute(placeholderValues = {}) {
+    const params = (0, import_sql.fillPlaceholders)(this.params, placeholderValues);
+    this.logger.logQuery(this.queryString, params);
+    const { fields, customResultMapper, client, queryString, settings } = this;
+    if (!fields && !customResultMapper) {
+      return this.queryWithCache(queryString, params, async () => {
+        if (ROW_RETURNING.test(queryString)) {
+          const resultSet = await client.query({
+            query: queryString,
+            format: "JSON",
+            clickhouse_settings: settings
+          });
+          const response = await resultSet.json();
+          return {
+            rows: response.data,
+            query_id: resultSet.query_id,
+            statistics: response.statistics
+          };
+        }
+        const result = await client.command({ query: queryString, clickhouse_settings: settings });
+        return { rows: [], query_id: result.query_id };
+      });
+    }
+    return this.queryWithCache(queryString, params, async () => {
+      const resultSet = await client.query({
+        query: queryString,
+        format: "JSONCompact",
+        clickhouse_settings: settings
+      });
+      const { data } = await resultSet.json();
+      const rows = data;
+      if (customResultMapper) {
+        return customResultMapper(rows);
+      }
+      return rows.map((row) => (0, import_utils.mapResultRow)(fields, row, this.joinsNotNullableMap));
+    });
+  }
+  async *iterator(placeholderValues = {}) {
+    const params = (0, import_sql.fillPlaceholders)(this.params, placeholderValues);
+    this.logger.logQuery(this.queryString, params);
+    const resultSet = await this.client.query({
+      query: this.queryString,
+      format: "JSONCompactEachRow",
+      clickhouse_settings: this.settings
+    });
+    for await (const chunk of resultSet.stream()) {
+      for (const row of chunk) {
+        const values = row.json();
+        yield this.fields ? (0, import_utils.mapResultRow)(this.fields, values, this.joinsNotNullableMap) : values;
+      }
+    }
+  }
+}
+class ClickHouseDriverSession extends import_session.ClickHouseSession {
+  constructor(client, dialect, options = {}) {
+    super(dialect);
+    this.client = client;
+    this.options = options;
+    this.logger = options.logger ?? new import_logger.NoopLogger();
+    this.cache = options.cache ?? new import_cache.NoopCache();
+    this.settings = { ...DRIZZLE_CLICKHOUSE_SETTINGS, ...options.settings };
+  }
+  client;
+  options;
+  static [import_entity.entityKind] = "ClickHouseDriverSession";
+  logger;
+  cache;
+  settings;
+  prepareQuery(query, fields, customResultMapper, queryMetadata, cacheConfig) {
+    return new ClickHouseDriverPreparedQuery(
+      this.client,
+      query.sql,
+      query.params,
+      this.logger,
+      fields,
+      customResultMapper,
+      this.settings,
+      this.cache,
+      queryMetadata,
+      cacheConfig
+    );
+  }
+  /**
+   * Streams rows into a table through the driver's own insert, which writes them to the request
+   * body rather than into the statement.
+   *
+   * The async iterable is handed over as-is, so the driver pulls rows as the socket drains and a
+   * source larger than memory never materialises. `@clickhouse/client` accepts one directly on Node;
+   * on the web build, which has no `Readable`, it is collected first — the streaming guarantee is
+   * the platform's, not Drizzle's.
+   */
+  async insertRows(table, rows, options = {}) {
+    const settings = { ...this.settings, ...options.settings };
+    this.logger.logQuery(`insert into ${table} format ${import_insert.CLICKHOUSE_INSERT_FORMAT}`, []);
+    try {
+      const result = await this.client.insert({
+        table,
+        values: toInsertStream(rows),
+        format: import_insert.CLICKHOUSE_INSERT_FORMAT,
+        clickhouse_settings: settings
+      });
+      if (options.metadata && !(0, import_entity.is)(this.cache, import_cache.NoopCache)) {
+        await this.cache.onMutate({ tables: options.metadata.tables });
+      }
+      return { query_id: result.query_id };
+    } catch (e) {
+      if (e instanceof import_errors.ClickHouseInsertValueError) throw e;
+      throw new import_errors2.DrizzleQueryError(`insert into ${table} format ${import_insert.CLICKHOUSE_INSERT_FORMAT}`, [], e);
+    }
+  }
+  async all(query) {
+    const { sql: queryString, params } = this.dialect.sqlToQuery(query);
+    this.logger.logQuery(queryString, params);
+    const resultSet = await this.client.query({
+      query: queryString,
+      format: "JSON",
+      clickhouse_settings: this.settings
+    });
+    const { data } = await resultSet.json();
+    return data;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDriverPreparedQuery,
+  ClickHouseDriverSession,
+  DRIZZLE_CLICKHOUSE_SETTINGS
+});
diff --git a/clickhouse/session.d.cts b/clickhouse/session.d.cts
new file mode 100644
index 0000000..bcb6143
--- /dev/null
+++ b/clickhouse/session.d.cts
@@ -0,0 +1,93 @@
+import type { ClickHouseClient, ClickHouseSettings } from '@clickhouse/client';
+import type { Cache } from '../cache/core/cache.cjs';
+import type { WithCacheConfig } from '../cache/core/types.cjs';
+import type { ClickHouseDialect } from '../clickhouse-core/dialect.cjs';
+import type { SelectedFieldsOrdered } from '../clickhouse-core/query-builders/select.types.cjs';
+import { ClickHousePreparedQuery as PreparedQueryBase, type ClickHousePreparedQueryConfig, type ClickHousePreparedQueryHKT, type ClickHouseQueryMetadata, type ClickHouseQueryResultHKT, ClickHouseSession, type PreparedQueryKind } from '../clickhouse-core/session.cjs';
+import { entityKind } from '../entity.cjs';
+import type { Logger } from '../logger.cjs';
+import type { Query, SQL } from '../sql/sql.cjs';
+import { type Assume } from '../utils.cjs';
+/**
+ * The subset of `@clickhouse/client` Drizzle depends on, so that any compatible client works.
+ *
+ * `insert` is here because a row-format insert is not a statement the query interface can carry — the
+ * driver has to write the rows to the request body itself. Both the Node and the web builds provide
+ * it with the same signature.
+ */
+export type ClickHouseDriverClient = Pick<ClickHouseClient, 'query' | 'command' | 'close' | 'insert'>;
+/**
+ * The result of a statement that does not return rows — an insert, a mutation or DDL.
+ *
+ * `statistics` is ClickHouse's own accounting for the statement and is worth logging on expensive
+ * queries; it is absent for statements issued through the command interface.
+ */
+export interface ClickHouseQueryResult<TRow = never> {
+    rows: TRow[];
+    /** ClickHouse's id for the statement, which `system.query_log` can be joined on. */
+    query_id: string;
+    statistics?: {
+        elapsed: number;
+        rows_read: number;
+        bytes_read: number;
+    };
+}
+/**
+ * Format settings Drizzle relies on for unambiguous decoding.
+ *
+ * `date_time_output_format: 'iso'` makes ClickHouse render `DateTime`/`DateTime64` with an explicit
+ * zone, so a `Date` can be reconstructed without having to know the column's or the server's
+ * timezone. Callers can still override it per query.
+ */
+export declare const DRIZZLE_CLICKHOUSE_SETTINGS: ClickHouseSettings;
+export interface ClickHouseDriverSessionOptions {
+    logger?: Logger;
+    cache?: Cache;
+    /** Settings applied to every statement, merged over {@link DRIZZLE_CLICKHOUSE_SETTINGS}. */
+    settings?: ClickHouseSettings;
+}
+export declare class ClickHouseDriverPreparedQuery<T extends ClickHousePreparedQueryConfig> extends PreparedQueryBase<T> {
+    private client;
+    private queryString;
+    private params;
+    private logger;
+    private fields;
+    private customResultMapper;
+    private settings;
+    static readonly [entityKind]: string;
+    constructor(client: ClickHouseDriverClient, queryString: string, params: unknown[], logger: Logger, fields: SelectedFieldsOrdered | undefined, customResultMapper: ((rows: unknown[][]) => T['execute']) | undefined, settings: ClickHouseSettings, cache: Cache, queryMetadata: ClickHouseQueryMetadata | undefined, cacheConfig: WithCacheConfig | undefined);
+    execute(placeholderValues?: Record<string, unknown>): Promise<T['execute']>;
+    iterator(placeholderValues?: Record<string, unknown>): AsyncGenerator<T['iterator']>;
+}
+export declare class ClickHouseDriverSession extends ClickHouseSession<ClickHouseDriverQueryResultHKT, ClickHouseDriverPreparedQueryHKT> {
+    private client;
+    private options;
+    static readonly [entityKind]: string;
+    private logger;
+    private cache;
+    private settings;
+    constructor(client: ClickHouseDriverClient, dialect: ClickHouseDialect, options?: ClickHouseDriverSessionOptions);
+    prepareQuery<T extends ClickHousePreparedQueryConfig, TPreparedQueryHKT extends ClickHousePreparedQueryHKT>(query: Query, fields: SelectedFieldsOrdered | undefined, customResultMapper?: (rows: unknown[][]) => T['execute'], queryMetadata?: ClickHouseQueryMetadata, cacheConfig?: WithCacheConfig): PreparedQueryKind<TPreparedQueryHKT, T>;
+    /**
+     * Streams rows into a table through the driver's own insert, which writes them to the request
+     * body rather than into the statement.
+     *
+     * The async iterable is handed over as-is, so the driver pulls rows as the socket drains and a
+     * source larger than memory never materialises. `@clickhouse/client` accepts one directly on Node;
+     * on the web build, which has no `Readable`, it is collected first — the streaming guarantee is
+     * the platform's, not Drizzle's.
+     */
+    insertRows(table: string, rows: AsyncIterable<Record<string, unknown>>, options?: {
+        settings?: ClickHouseSettings;
+        metadata?: ClickHouseQueryMetadata;
+    }): Promise<{
+        query_id: string;
+    }>;
+    all<T = unknown>(query: SQL): Promise<T[]>;
+}
+export interface ClickHouseDriverQueryResultHKT extends ClickHouseQueryResultHKT {
+    type: ClickHouseQueryResult<Assume<this['row'], Record<string, unknown>>>;
+}
+export interface ClickHouseDriverPreparedQueryHKT extends ClickHousePreparedQueryHKT {
+    type: ClickHouseDriverPreparedQuery<Assume<this['config'], ClickHousePreparedQueryConfig>>;
+}
diff --git a/clickhouse/session.d.ts b/clickhouse/session.d.ts
new file mode 100644
index 0000000..e9c074e
--- /dev/null
+++ b/clickhouse/session.d.ts
@@ -0,0 +1,93 @@
+import type { ClickHouseClient, ClickHouseSettings } from '@clickhouse/client';
+import type { Cache } from '../cache/core/cache.js';
+import type { WithCacheConfig } from '../cache/core/types.js';
+import type { ClickHouseDialect } from '../clickhouse-core/dialect.js';
+import type { SelectedFieldsOrdered } from '../clickhouse-core/query-builders/select.types.js';
+import { ClickHousePreparedQuery as PreparedQueryBase, type ClickHousePreparedQueryConfig, type ClickHousePreparedQueryHKT, type ClickHouseQueryMetadata, type ClickHouseQueryResultHKT, ClickHouseSession, type PreparedQueryKind } from '../clickhouse-core/session.js';
+import { entityKind } from '../entity.js';
+import type { Logger } from '../logger.js';
+import type { Query, SQL } from '../sql/sql.js';
+import { type Assume } from '../utils.js';
+/**
+ * The subset of `@clickhouse/client` Drizzle depends on, so that any compatible client works.
+ *
+ * `insert` is here because a row-format insert is not a statement the query interface can carry — the
+ * driver has to write the rows to the request body itself. Both the Node and the web builds provide
+ * it with the same signature.
+ */
+export type ClickHouseDriverClient = Pick<ClickHouseClient, 'query' | 'command' | 'close' | 'insert'>;
+/**
+ * The result of a statement that does not return rows — an insert, a mutation or DDL.
+ *
+ * `statistics` is ClickHouse's own accounting for the statement and is worth logging on expensive
+ * queries; it is absent for statements issued through the command interface.
+ */
+export interface ClickHouseQueryResult<TRow = never> {
+    rows: TRow[];
+    /** ClickHouse's id for the statement, which `system.query_log` can be joined on. */
+    query_id: string;
+    statistics?: {
+        elapsed: number;
+        rows_read: number;
+        bytes_read: number;
+    };
+}
+/**
+ * Format settings Drizzle relies on for unambiguous decoding.
+ *
+ * `date_time_output_format: 'iso'` makes ClickHouse render `DateTime`/`DateTime64` with an explicit
+ * zone, so a `Date` can be reconstructed without having to know the column's or the server's
+ * timezone. Callers can still override it per query.
+ */
+export declare const DRIZZLE_CLICKHOUSE_SETTINGS: ClickHouseSettings;
+export interface ClickHouseDriverSessionOptions {
+    logger?: Logger;
+    cache?: Cache;
+    /** Settings applied to every statement, merged over {@link DRIZZLE_CLICKHOUSE_SETTINGS}. */
+    settings?: ClickHouseSettings;
+}
+export declare class ClickHouseDriverPreparedQuery<T extends ClickHousePreparedQueryConfig> extends PreparedQueryBase<T> {
+    private client;
+    private queryString;
+    private params;
+    private logger;
+    private fields;
+    private customResultMapper;
+    private settings;
+    static readonly [entityKind]: string;
+    constructor(client: ClickHouseDriverClient, queryString: string, params: unknown[], logger: Logger, fields: SelectedFieldsOrdered | undefined, customResultMapper: ((rows: unknown[][]) => T['execute']) | undefined, settings: ClickHouseSettings, cache: Cache, queryMetadata: ClickHouseQueryMetadata | undefined, cacheConfig: WithCacheConfig | undefined);
+    execute(placeholderValues?: Record<string, unknown>): Promise<T['execute']>;
+    iterator(placeholderValues?: Record<string, unknown>): AsyncGenerator<T['iterator']>;
+}
+export declare class ClickHouseDriverSession extends ClickHouseSession<ClickHouseDriverQueryResultHKT, ClickHouseDriverPreparedQueryHKT> {
+    private client;
+    private options;
+    static readonly [entityKind]: string;
+    private logger;
+    private cache;
+    private settings;
+    constructor(client: ClickHouseDriverClient, dialect: ClickHouseDialect, options?: ClickHouseDriverSessionOptions);
+    prepareQuery<T extends ClickHousePreparedQueryConfig, TPreparedQueryHKT extends ClickHousePreparedQueryHKT>(query: Query, fields: SelectedFieldsOrdered | undefined, customResultMapper?: (rows: unknown[][]) => T['execute'], queryMetadata?: ClickHouseQueryMetadata, cacheConfig?: WithCacheConfig): PreparedQueryKind<TPreparedQueryHKT, T>;
+    /**
+     * Streams rows into a table through the driver's own insert, which writes them to the request
+     * body rather than into the statement.
+     *
+     * The async iterable is handed over as-is, so the driver pulls rows as the socket drains and a
+     * source larger than memory never materialises. `@clickhouse/client` accepts one directly on Node;
+     * on the web build, which has no `Readable`, it is collected first — the streaming guarantee is
+     * the platform's, not Drizzle's.
+     */
+    insertRows(table: string, rows: AsyncIterable<Record<string, unknown>>, options?: {
+        settings?: ClickHouseSettings;
+        metadata?: ClickHouseQueryMetadata;
+    }): Promise<{
+        query_id: string;
+    }>;
+    all<T = unknown>(query: SQL): Promise<T[]>;
+}
+export interface ClickHouseDriverQueryResultHKT extends ClickHouseQueryResultHKT {
+    type: ClickHouseQueryResult<Assume<this['row'], Record<string, unknown>>>;
+}
+export interface ClickHouseDriverPreparedQueryHKT extends ClickHousePreparedQueryHKT {
+    type: ClickHouseDriverPreparedQuery<Assume<this['config'], ClickHousePreparedQueryConfig>>;
+}
diff --git a/clickhouse/session.js b/clickhouse/session.js
new file mode 100644
index 0000000..de99206
--- /dev/null
+++ b/clickhouse/session.js
@@ -0,0 +1,166 @@
+import { Readable } from "node:stream";
+import { NoopCache } from "../cache/core/cache.js";
+import { ClickHouseInsertValueError } from "../clickhouse-core/errors.js";
+import { CLICKHOUSE_INSERT_FORMAT as INSERT_FORMAT } from "../clickhouse-core/query-builders/insert.js";
+import {
+  ClickHousePreparedQuery as PreparedQueryBase,
+  ClickHouseSession
+} from "../clickhouse-core/session.js";
+import { entityKind, is } from "../entity.js";
+import { DrizzleQueryError } from "../errors.js";
+import { NoopLogger } from "../logger.js";
+import { fillPlaceholders } from "../sql/sql.js";
+import { mapResultRow } from "../utils.js";
+const DRIZZLE_CLICKHOUSE_SETTINGS = {
+  date_time_output_format: "iso"
+};
+const ROW_RETURNING = /^\s*(?:\(|select|with|show|describe|desc|explain|exists|values)\b/i;
+function toInsertStream(rows) {
+  return Readable.from(rows);
+}
+class ClickHouseDriverPreparedQuery extends PreparedQueryBase {
+  constructor(client, queryString, params, logger, fields, customResultMapper, settings, cache, queryMetadata, cacheConfig) {
+    super(cache, queryMetadata, cacheConfig);
+    this.client = client;
+    this.queryString = queryString;
+    this.params = params;
+    this.logger = logger;
+    this.fields = fields;
+    this.customResultMapper = customResultMapper;
+    this.settings = settings;
+  }
+  client;
+  queryString;
+  params;
+  logger;
+  fields;
+  customResultMapper;
+  settings;
+  static [entityKind] = "ClickHouseDriverPreparedQuery";
+  async execute(placeholderValues = {}) {
+    const params = fillPlaceholders(this.params, placeholderValues);
+    this.logger.logQuery(this.queryString, params);
+    const { fields, customResultMapper, client, queryString, settings } = this;
+    if (!fields && !customResultMapper) {
+      return this.queryWithCache(queryString, params, async () => {
+        if (ROW_RETURNING.test(queryString)) {
+          const resultSet = await client.query({
+            query: queryString,
+            format: "JSON",
+            clickhouse_settings: settings
+          });
+          const response = await resultSet.json();
+          return {
+            rows: response.data,
+            query_id: resultSet.query_id,
+            statistics: response.statistics
+          };
+        }
+        const result = await client.command({ query: queryString, clickhouse_settings: settings });
+        return { rows: [], query_id: result.query_id };
+      });
+    }
+    return this.queryWithCache(queryString, params, async () => {
+      const resultSet = await client.query({
+        query: queryString,
+        format: "JSONCompact",
+        clickhouse_settings: settings
+      });
+      const { data } = await resultSet.json();
+      const rows = data;
+      if (customResultMapper) {
+        return customResultMapper(rows);
+      }
+      return rows.map((row) => mapResultRow(fields, row, this.joinsNotNullableMap));
+    });
+  }
+  async *iterator(placeholderValues = {}) {
+    const params = fillPlaceholders(this.params, placeholderValues);
+    this.logger.logQuery(this.queryString, params);
+    const resultSet = await this.client.query({
+      query: this.queryString,
+      format: "JSONCompactEachRow",
+      clickhouse_settings: this.settings
+    });
+    for await (const chunk of resultSet.stream()) {
+      for (const row of chunk) {
+        const values = row.json();
+        yield this.fields ? mapResultRow(this.fields, values, this.joinsNotNullableMap) : values;
+      }
+    }
+  }
+}
+class ClickHouseDriverSession extends ClickHouseSession {
+  constructor(client, dialect, options = {}) {
+    super(dialect);
+    this.client = client;
+    this.options = options;
+    this.logger = options.logger ?? new NoopLogger();
+    this.cache = options.cache ?? new NoopCache();
+    this.settings = { ...DRIZZLE_CLICKHOUSE_SETTINGS, ...options.settings };
+  }
+  client;
+  options;
+  static [entityKind] = "ClickHouseDriverSession";
+  logger;
+  cache;
+  settings;
+  prepareQuery(query, fields, customResultMapper, queryMetadata, cacheConfig) {
+    return new ClickHouseDriverPreparedQuery(
+      this.client,
+      query.sql,
+      query.params,
+      this.logger,
+      fields,
+      customResultMapper,
+      this.settings,
+      this.cache,
+      queryMetadata,
+      cacheConfig
+    );
+  }
+  /**
+   * Streams rows into a table through the driver's own insert, which writes them to the request
+   * body rather than into the statement.
+   *
+   * The async iterable is handed over as-is, so the driver pulls rows as the socket drains and a
+   * source larger than memory never materialises. `@clickhouse/client` accepts one directly on Node;
+   * on the web build, which has no `Readable`, it is collected first — the streaming guarantee is
+   * the platform's, not Drizzle's.
+   */
+  async insertRows(table, rows, options = {}) {
+    const settings = { ...this.settings, ...options.settings };
+    this.logger.logQuery(`insert into ${table} format ${INSERT_FORMAT}`, []);
+    try {
+      const result = await this.client.insert({
+        table,
+        values: toInsertStream(rows),
+        format: INSERT_FORMAT,
+        clickhouse_settings: settings
+      });
+      if (options.metadata && !is(this.cache, NoopCache)) {
+        await this.cache.onMutate({ tables: options.metadata.tables });
+      }
+      return { query_id: result.query_id };
+    } catch (e) {
+      if (e instanceof ClickHouseInsertValueError) throw e;
+      throw new DrizzleQueryError(`insert into ${table} format ${INSERT_FORMAT}`, [], e);
+    }
+  }
+  async all(query) {
+    const { sql: queryString, params } = this.dialect.sqlToQuery(query);
+    this.logger.logQuery(queryString, params);
+    const resultSet = await this.client.query({
+      query: queryString,
+      format: "JSON",
+      clickhouse_settings: this.settings
+    });
+    const { data } = await resultSet.json();
+    return data;
+  }
+}
+export {
+  ClickHouseDriverPreparedQuery,
+  ClickHouseDriverSession,
+  DRIZZLE_CLICKHOUSE_SETTINGS
+};
diff --git a/clickhouse-core/alias.cjs b/clickhouse-core/alias.cjs
new file mode 100644
index 0000000..4df5138
--- /dev/null
+++ b/clickhouse-core/alias.cjs
@@ -0,0 +1,31 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var alias_exports = {};
+__export(alias_exports, {
+  alias: () => alias
+});
+module.exports = __toCommonJS(alias_exports);
+var import_alias = require("../alias.cjs");
+function alias(table, alias2) {
+  return new Proxy(table, new import_alias.TableAliasProxyHandler(alias2, false));
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  alias
+});
diff --git a/clickhouse-core/alias.d.cts b/clickhouse-core/alias.d.cts
new file mode 100644
index 0000000..ad9d418
--- /dev/null
+++ b/clickhouse-core/alias.d.cts
@@ -0,0 +1,4 @@
+import type { BuildAliasTable } from './query-builders/select.types.cjs';
+import type { ClickHouseTable } from './table.cjs';
+export declare function alias<TTable extends ClickHouseTable, TAlias extends string>(// | ClickHouseViewBase
+table: TTable, alias: TAlias): BuildAliasTable<TTable, TAlias>;
diff --git a/clickhouse-core/alias.d.ts b/clickhouse-core/alias.d.ts
new file mode 100644
index 0000000..ebdfaeb
--- /dev/null
+++ b/clickhouse-core/alias.d.ts
@@ -0,0 +1,4 @@
+import type { BuildAliasTable } from './query-builders/select.types.js';
+import type { ClickHouseTable } from './table.js';
+export declare function alias<TTable extends ClickHouseTable, TAlias extends string>(// | ClickHouseViewBase
+table: TTable, alias: TAlias): BuildAliasTable<TTable, TAlias>;
diff --git a/clickhouse-core/alias.js b/clickhouse-core/alias.js
new file mode 100644
index 0000000..d0c522d
--- /dev/null
+++ b/clickhouse-core/alias.js
@@ -0,0 +1,7 @@
+import { TableAliasProxyHandler } from "../alias.js";
+function alias(table, alias2) {
+  return new Proxy(table, new TableAliasProxyHandler(alias2, false));
+}
+export {
+  alias
+};
diff --git a/clickhouse-core/columns/all.cjs b/clickhouse-core/columns/all.cjs
new file mode 100644
index 0000000..03bf02b
--- /dev/null
+++ b/clickhouse-core/columns/all.cjs
@@ -0,0 +1,88 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var all_exports = {};
+__export(all_exports, {
+  getClickHouseColumnBuilders: () => getClickHouseColumnBuilders
+});
+module.exports = __toCommonJS(all_exports);
+var import_array = require("./array.cjs");
+var import_bigint = require("./bigint.cjs");
+var import_bool = require("./bool.cjs");
+var import_custom = require("./custom.cjs");
+var import_date = require("./date.cjs");
+var import_datetime = require("./datetime.cjs");
+var import_decimal = require("./decimal.cjs");
+var import_enum = require("./enum.cjs");
+var import_float = require("./float.cjs");
+var import_int = require("./int.cjs");
+var import_ip = require("./ip.cjs");
+var import_json = require("./json.cjs");
+var import_low_cardinality = require("./low-cardinality.cjs");
+var import_map = require("./map.cjs");
+var import_nullable = require("./nullable.cjs");
+var import_string = require("./string.cjs");
+var import_tuple = require("./tuple.cjs");
+var import_uuid = require("./uuid.cjs");
+function getClickHouseColumnBuilders() {
+  return {
+    array: import_array.array,
+    bool: import_bool.bool,
+    boolean: import_bool.boolean,
+    customType: import_custom.customType,
+    date: import_date.date,
+    date32: import_date.date32,
+    dateTime: import_datetime.dateTime,
+    dateTime64: import_datetime.dateTime64,
+    decimal: import_decimal.decimal,
+    decimal32: import_decimal.decimal32,
+    decimal64: import_decimal.decimal64,
+    decimal128: import_decimal.decimal128,
+    decimal256: import_decimal.decimal256,
+    enum8: import_enum.enum8,
+    enum16: import_enum.enum16,
+    fixedString: import_string.fixedString,
+    float32: import_float.float32,
+    float64: import_float.float64,
+    int8: import_int.int8,
+    int16: import_int.int16,
+    int32: import_int.int32,
+    int64: import_bigint.int64,
+    int128: import_bigint.int128,
+    int256: import_bigint.int256,
+    ipv4: import_ip.ipv4,
+    ipv6: import_ip.ipv6,
+    json: import_json.json,
+    lowCardinality: import_low_cardinality.lowCardinality,
+    map: import_map.map,
+    nullable: import_nullable.nullable,
+    string: import_string.string,
+    tuple: import_tuple.tuple,
+    uint8: import_int.uint8,
+    uint16: import_int.uint16,
+    uint32: import_int.uint32,
+    uint64: import_bigint.uint64,
+    uint128: import_bigint.uint128,
+    uint256: import_bigint.uint256,
+    uuid: import_uuid.uuid
+  };
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  getClickHouseColumnBuilders
+});
diff --git a/clickhouse-core/columns/all.d.cts b/clickhouse-core/columns/all.d.cts
new file mode 100644
index 0000000..4fe3766
--- /dev/null
+++ b/clickhouse-core/columns/all.d.cts
@@ -0,0 +1,138 @@
+import { array } from './array.cjs';
+import { bool } from './bool.cjs';
+import { customType } from './custom.cjs';
+import { dateTime, dateTime64 } from './datetime.cjs';
+import { decimal } from './decimal.cjs';
+import { json } from './json.cjs';
+import { lowCardinality } from './low-cardinality.cjs';
+import { map } from './map.cjs';
+import { nullable } from './nullable.cjs';
+import { fixedString, string } from './string.cjs';
+import { tuple } from './tuple.cjs';
+import { uuid } from './uuid.cjs';
+export declare function getClickHouseColumnBuilders(): {
+    array: typeof array;
+    bool: typeof bool;
+    boolean: typeof bool;
+    customType: typeof customType;
+    date: {
+        (): import("./date.cjs").ClickHouseDateBuilderInitial<"", "ClickHouseDate", "date">;
+        <TMode extends import("./date.common.cjs").ClickHouseDateMode = "date">(config?: import("./date.cjs").ClickHouseDateConfig<TMode> | undefined): import("./date.cjs").ClickHouseDateBuilderInitial<"", "ClickHouseDate", TMode>;
+        <TName extends string, TMode extends import("./date.common.cjs").ClickHouseDateMode = "date">(name: TName, config?: import("./date.cjs").ClickHouseDateConfig<TMode> | undefined): import("./date.cjs").ClickHouseDateBuilderInitial<TName, "ClickHouseDate", TMode>;
+    };
+    date32: {
+        (): import("./date.cjs").ClickHouseDateBuilderInitial<"", "ClickHouseDate32", "date">;
+        <TMode extends import("./date.common.cjs").ClickHouseDateMode = "date">(config?: import("./date.cjs").ClickHouseDateConfig<TMode> | undefined): import("./date.cjs").ClickHouseDateBuilderInitial<"", "ClickHouseDate32", TMode>;
+        <TName extends string, TMode extends import("./date.common.cjs").ClickHouseDateMode = "date">(name: TName, config?: import("./date.cjs").ClickHouseDateConfig<TMode> | undefined): import("./date.cjs").ClickHouseDateBuilderInitial<TName, "ClickHouseDate32", TMode>;
+    };
+    dateTime: typeof dateTime;
+    dateTime64: typeof dateTime64;
+    decimal: typeof decimal;
+    decimal32: {
+        <TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal32", TMode>;
+        <TName extends string, TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal32", TMode>;
+    };
+    decimal64: {
+        <TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal64", TMode>;
+        <TName extends string, TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal64", TMode>;
+    };
+    decimal128: {
+        <TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal128", TMode>;
+        <TName extends string, TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal128", TMode>;
+    };
+    decimal256: {
+        <TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal256", TMode>;
+        <TName extends string, TMode extends import("./decimal.cjs").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.cjs").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.cjs").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal256", TMode>;
+    };
+    enum8: {
+        <U extends string, T extends readonly [U, ...U[]]>(values: T | import("../../utils.cjs").Writable<T>): import("./enum.cjs").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", import("../../utils.cjs").Writable<T>>;
+        <T extends Record<string, number>>(values: T): import("./enum.cjs").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", import("./enum.cjs").ClickHouseEnumRecordValues<T>>;
+        <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | import("../../utils.cjs").Writable<T>): import("./enum.cjs").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", import("../../utils.cjs").Writable<T>>;
+        <TName extends string, T extends Record<string, number>>(name: TName, values: T): import("./enum.cjs").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", import("./enum.cjs").ClickHouseEnumRecordValues<T>>;
+    };
+    enum16: {
+        <U extends string, T extends readonly [U, ...U[]]>(values: T | import("../../utils.cjs").Writable<T>): import("./enum.cjs").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", import("../../utils.cjs").Writable<T>>;
+        <T extends Record<string, number>>(values: T): import("./enum.cjs").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", import("./enum.cjs").ClickHouseEnumRecordValues<T>>;
+        <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | import("../../utils.cjs").Writable<T>): import("./enum.cjs").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", import("../../utils.cjs").Writable<T>>;
+        <TName extends string, T extends Record<string, number>>(name: TName, values: T): import("./enum.cjs").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", import("./enum.cjs").ClickHouseEnumRecordValues<T>>;
+    };
+    fixedString: typeof fixedString;
+    float32: {
+        (): import("./float.cjs").ClickHouseFloatBuilderInitial<"", "ClickHouseFloat32">;
+        <TName extends string>(name: TName): import("./float.cjs").ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat32">;
+    };
+    float64: {
+        (): import("./float.cjs").ClickHouseFloatBuilderInitial<"", "ClickHouseFloat64">;
+        <TName extends string>(name: TName): import("./float.cjs").ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat64">;
+    };
+    int8: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseInt8">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseInt8">;
+    };
+    int16: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseInt16">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseInt16">;
+    };
+    int32: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseInt32">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseInt32">;
+    };
+    int64: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt64", TMode>;
+    };
+    int128: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt128", TMode>;
+    };
+    int256: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt256", TMode>;
+    };
+    ipv4: {
+        (): import("./ip.cjs").ClickHouseIpBuilderInitial<"", "ClickHouseIPv4">;
+        <TName extends string>(name: TName): import("./ip.cjs").ClickHouseIpBuilderInitial<TName, "ClickHouseIPv4">;
+    };
+    ipv6: {
+        (): import("./ip.cjs").ClickHouseIpBuilderInitial<"", "ClickHouseIPv6">;
+        <TName extends string>(name: TName): import("./ip.cjs").ClickHouseIpBuilderInitial<TName, "ClickHouseIPv6">;
+    };
+    json: typeof json;
+    lowCardinality: typeof lowCardinality;
+    map: typeof map;
+    nullable: typeof nullable;
+    string: typeof string;
+    tuple: typeof tuple;
+    uint8: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseUInt8">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt8">;
+    };
+    uint16: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseUInt16">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt16">;
+    };
+    uint32: {
+        (): import("./int.cjs").ClickHouseIntBuilderInitial<"", "ClickHouseUInt32">;
+        <TName extends string>(name: TName): import("./int.cjs").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt32">;
+    };
+    uint64: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt64", TMode>;
+    };
+    uint128: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt128", TMode>;
+    };
+    uint256: {
+        (): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", "bigint">;
+        <TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", TMode>;
+        <TName extends string, TMode extends import("./bigint.cjs").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.cjs").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.cjs").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt256", TMode>;
+    };
+    uuid: typeof uuid;
+};
+export type ClickHouseColumnBuilders = ReturnType<typeof getClickHouseColumnBuilders>;
diff --git a/clickhouse-core/columns/all.d.ts b/clickhouse-core/columns/all.d.ts
new file mode 100644
index 0000000..3a0b9c6
--- /dev/null
+++ b/clickhouse-core/columns/all.d.ts
@@ -0,0 +1,138 @@
+import { array } from './array.js';
+import { bool } from './bool.js';
+import { customType } from './custom.js';
+import { dateTime, dateTime64 } from './datetime.js';
+import { decimal } from './decimal.js';
+import { json } from './json.js';
+import { lowCardinality } from './low-cardinality.js';
+import { map } from './map.js';
+import { nullable } from './nullable.js';
+import { fixedString, string } from './string.js';
+import { tuple } from './tuple.js';
+import { uuid } from './uuid.js';
+export declare function getClickHouseColumnBuilders(): {
+    array: typeof array;
+    bool: typeof bool;
+    boolean: typeof bool;
+    customType: typeof customType;
+    date: {
+        (): import("./date.js").ClickHouseDateBuilderInitial<"", "ClickHouseDate", "date">;
+        <TMode extends import("./date.common.js").ClickHouseDateMode = "date">(config?: import("./date.js").ClickHouseDateConfig<TMode> | undefined): import("./date.js").ClickHouseDateBuilderInitial<"", "ClickHouseDate", TMode>;
+        <TName extends string, TMode extends import("./date.common.js").ClickHouseDateMode = "date">(name: TName, config?: import("./date.js").ClickHouseDateConfig<TMode> | undefined): import("./date.js").ClickHouseDateBuilderInitial<TName, "ClickHouseDate", TMode>;
+    };
+    date32: {
+        (): import("./date.js").ClickHouseDateBuilderInitial<"", "ClickHouseDate32", "date">;
+        <TMode extends import("./date.common.js").ClickHouseDateMode = "date">(config?: import("./date.js").ClickHouseDateConfig<TMode> | undefined): import("./date.js").ClickHouseDateBuilderInitial<"", "ClickHouseDate32", TMode>;
+        <TName extends string, TMode extends import("./date.common.js").ClickHouseDateMode = "date">(name: TName, config?: import("./date.js").ClickHouseDateConfig<TMode> | undefined): import("./date.js").ClickHouseDateBuilderInitial<TName, "ClickHouseDate32", TMode>;
+    };
+    dateTime: typeof dateTime;
+    dateTime64: typeof dateTime64;
+    decimal: typeof decimal;
+    decimal32: {
+        <TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal32", TMode>;
+        <TName extends string, TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal32", TMode>;
+    };
+    decimal64: {
+        <TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal64", TMode>;
+        <TName extends string, TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal64", TMode>;
+    };
+    decimal128: {
+        <TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal128", TMode>;
+        <TName extends string, TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal128", TMode>;
+    };
+    decimal256: {
+        <TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal256", TMode>;
+        <TName extends string, TMode extends import("./decimal.js").ClickHouseDecimalMode = "string">(name: TName, config?: Omit<import("./decimal.js").ClickHouseDecimalConfig<TMode>, "precision"> | undefined): import("./decimal.js").ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal256", TMode>;
+    };
+    enum8: {
+        <U extends string, T extends readonly [U, ...U[]]>(values: T | import("../../utils.js").Writable<T>): import("./enum.js").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", import("../../utils.js").Writable<T>>;
+        <T extends Record<string, number>>(values: T): import("./enum.js").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", import("./enum.js").ClickHouseEnumRecordValues<T>>;
+        <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | import("../../utils.js").Writable<T>): import("./enum.js").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", import("../../utils.js").Writable<T>>;
+        <TName extends string, T extends Record<string, number>>(name: TName, values: T): import("./enum.js").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", import("./enum.js").ClickHouseEnumRecordValues<T>>;
+    };
+    enum16: {
+        <U extends string, T extends readonly [U, ...U[]]>(values: T | import("../../utils.js").Writable<T>): import("./enum.js").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", import("../../utils.js").Writable<T>>;
+        <T extends Record<string, number>>(values: T): import("./enum.js").ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", import("./enum.js").ClickHouseEnumRecordValues<T>>;
+        <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | import("../../utils.js").Writable<T>): import("./enum.js").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", import("../../utils.js").Writable<T>>;
+        <TName extends string, T extends Record<string, number>>(name: TName, values: T): import("./enum.js").ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", import("./enum.js").ClickHouseEnumRecordValues<T>>;
+    };
+    fixedString: typeof fixedString;
+    float32: {
+        (): import("./float.js").ClickHouseFloatBuilderInitial<"", "ClickHouseFloat32">;
+        <TName extends string>(name: TName): import("./float.js").ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat32">;
+    };
+    float64: {
+        (): import("./float.js").ClickHouseFloatBuilderInitial<"", "ClickHouseFloat64">;
+        <TName extends string>(name: TName): import("./float.js").ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat64">;
+    };
+    int8: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseInt8">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseInt8">;
+    };
+    int16: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseInt16">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseInt16">;
+    };
+    int32: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseInt32">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseInt32">;
+    };
+    int64: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt64", TMode>;
+    };
+    int128: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt128", TMode>;
+    };
+    int256: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt256", TMode>;
+    };
+    ipv4: {
+        (): import("./ip.js").ClickHouseIpBuilderInitial<"", "ClickHouseIPv4">;
+        <TName extends string>(name: TName): import("./ip.js").ClickHouseIpBuilderInitial<TName, "ClickHouseIPv4">;
+    };
+    ipv6: {
+        (): import("./ip.js").ClickHouseIpBuilderInitial<"", "ClickHouseIPv6">;
+        <TName extends string>(name: TName): import("./ip.js").ClickHouseIpBuilderInitial<TName, "ClickHouseIPv6">;
+    };
+    json: typeof json;
+    lowCardinality: typeof lowCardinality;
+    map: typeof map;
+    nullable: typeof nullable;
+    string: typeof string;
+    tuple: typeof tuple;
+    uint8: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseUInt8">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt8">;
+    };
+    uint16: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseUInt16">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt16">;
+    };
+    uint32: {
+        (): import("./int.js").ClickHouseIntBuilderInitial<"", "ClickHouseUInt32">;
+        <TName extends string>(name: TName): import("./int.js").ClickHouseIntBuilderInitial<TName, "ClickHouseUInt32">;
+    };
+    uint64: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt64", TMode>;
+    };
+    uint128: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt128", TMode>;
+    };
+    uint256: {
+        (): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", "bigint">;
+        <TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", TMode>;
+        <TName extends string, TMode extends import("./bigint.js").ClickHouseBigIntMode = "bigint">(name: TName, config?: import("./bigint.js").ClickHouseBigIntConfig<TMode> | undefined): import("./bigint.js").ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt256", TMode>;
+    };
+    uuid: typeof uuid;
+};
+export type ClickHouseColumnBuilders = ReturnType<typeof getClickHouseColumnBuilders>;
diff --git a/clickhouse-core/columns/all.js b/clickhouse-core/columns/all.js
new file mode 100644
index 0000000..57072e0
--- /dev/null
+++ b/clickhouse-core/columns/all.js
@@ -0,0 +1,64 @@
+import { array } from "./array.js";
+import { int128, int256, int64, uint128, uint256, uint64 } from "./bigint.js";
+import { bool, boolean } from "./bool.js";
+import { customType } from "./custom.js";
+import { date, date32 } from "./date.js";
+import { dateTime, dateTime64 } from "./datetime.js";
+import { decimal, decimal128, decimal256, decimal32, decimal64 } from "./decimal.js";
+import { enum16, enum8 } from "./enum.js";
+import { float32, float64 } from "./float.js";
+import { int16, int32, int8, uint16, uint32, uint8 } from "./int.js";
+import { ipv4, ipv6 } from "./ip.js";
+import { json } from "./json.js";
+import { lowCardinality } from "./low-cardinality.js";
+import { map } from "./map.js";
+import { nullable } from "./nullable.js";
+import { fixedString, string } from "./string.js";
+import { tuple } from "./tuple.js";
+import { uuid } from "./uuid.js";
+function getClickHouseColumnBuilders() {
+  return {
+    array,
+    bool,
+    boolean,
+    customType,
+    date,
+    date32,
+    dateTime,
+    dateTime64,
+    decimal,
+    decimal32,
+    decimal64,
+    decimal128,
+    decimal256,
+    enum8,
+    enum16,
+    fixedString,
+    float32,
+    float64,
+    int8,
+    int16,
+    int32,
+    int64,
+    int128,
+    int256,
+    ipv4,
+    ipv6,
+    json,
+    lowCardinality,
+    map,
+    nullable,
+    string,
+    tuple,
+    uint8,
+    uint16,
+    uint32,
+    uint64,
+    uint128,
+    uint256,
+    uuid
+  };
+}
+export {
+  getClickHouseColumnBuilders
+};
diff --git a/clickhouse-core/columns/array.cjs b/clickhouse-core/columns/array.cjs
new file mode 100644
index 0000000..0ab6ce2
--- /dev/null
+++ b/clickhouse-core/columns/array.cjs
@@ -0,0 +1,81 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var array_exports = {};
+__export(array_exports, {
+  ClickHouseArray: () => ClickHouseArray,
+  ClickHouseArrayBuilder: () => ClickHouseArrayBuilder,
+  array: () => array
+});
+module.exports = __toCommonJS(array_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseArrayBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseArrayBuilder";
+  constructor(name, baseBuilder) {
+    super(name, "array", "ClickHouseArray");
+    this.config.baseBuilder = baseBuilder;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const baseColumn = this.config.baseBuilder.markAsElement().build(table);
+    return new ClickHouseArray(
+      table,
+      this.config,
+      baseColumn
+    );
+  }
+}
+class ClickHouseArray extends import_common.ClickHouseColumn {
+  constructor(table, config, baseColumn) {
+    super(table, config);
+    this.baseColumn = baseColumn;
+  }
+  baseColumn;
+  static [import_entity.entityKind] = "ClickHouseArray";
+  /** ClickHouse rejects `Nullable(Array(...))`; use an empty array instead of null. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return `Array(${this.baseColumn.getSQLType()})`;
+  }
+  mapFromDriverValue(value) {
+    return value.map((element) => element === null ? null : this.baseColumn.mapFromDriverValue(element));
+  }
+  mapToDriverValue(value) {
+    const elements = value.map(
+      (element) => element === null ? import_sql.sql`NULL` : import_sql.sql`${this.baseColumn.mapToDriverValue(element)}`
+    );
+    return import_sql.sql`[${import_sql.sql.join(elements, import_sql.sql`, `)}]`;
+  }
+  mapToRowValue(value) {
+    return value.map((element) => element === null ? null : this.baseColumn.mapToRowValue(element));
+  }
+}
+function array(a, b) {
+  return typeof a === "string" ? new ClickHouseArrayBuilder(a, b) : new ClickHouseArrayBuilder("", a);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseArray,
+  ClickHouseArrayBuilder,
+  array
+});
diff --git a/clickhouse-core/columns/array.d.cts b/clickhouse-core/columns/array.d.cts
new file mode 100644
index 0000000..8dc7516
--- /dev/null
+++ b/clickhouse-core/columns/array.d.cts
@@ -0,0 +1,46 @@
+import type { AnyClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.cjs';
+import type { ElementData } from './nullable.cjs';
+export type ClickHouseArrayBuilderInitial<TName extends string, TBase extends ClickHouseColumnBuilderBase> = ClickHouseArrayBuilder<{
+    name: TName;
+    dataType: 'array';
+    columnType: 'ClickHouseArray';
+    data: ElementData<TBase>[];
+    driverParam: unknown[];
+    enumValues: undefined;
+    notNull: true;
+}, TBase>;
+export declare class ClickHouseArrayBuilder<T extends ColumnBuilderBaseConfig<'array', 'ClickHouseArray'>, TBase extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    baseBuilder: TBase;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], baseBuilder: TBase);
+}
+export declare class ClickHouseArray<T extends ColumnBaseConfig<'array', 'ClickHouseArray'>> extends ClickHouseColumn<T> {
+    readonly baseColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, baseColumn: ClickHouseColumn);
+    /** ClickHouse rejects `Nullable(Array(...))`; use an empty array instead of null. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown[]): unknown[];
+    mapToDriverValue(value: unknown[]): SQL;
+    mapToRowValue(value: unknown[]): unknown[];
+}
+/**
+ * `Array(T)` — an ordered list. Elements are non-nullable unless wrapped in
+ * {@link nullable}, and arrays nest, so `array(array(int32()))` is `Array(Array(Int32))`.
+ *
+ * ```ts
+ * tags: array(string()),
+ * scores: array(nullable(float64())),
+ * ```
+ */
+export declare function array<TBase extends ClickHouseColumnBuilderBase>(baseBuilder: TBase): ClickHouseArrayBuilderInitial<'', TBase>;
+export declare function array<TName extends string, TBase extends ClickHouseColumnBuilderBase>(name: TName, baseBuilder: TBase): ClickHouseArrayBuilderInitial<TName, TBase>;
diff --git a/clickhouse-core/columns/array.d.ts b/clickhouse-core/columns/array.d.ts
new file mode 100644
index 0000000..f373762
--- /dev/null
+++ b/clickhouse-core/columns/array.d.ts
@@ -0,0 +1,46 @@
+import type { AnyClickHouseTable } from '../table.js';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.js';
+import type { ElementData } from './nullable.js';
+export type ClickHouseArrayBuilderInitial<TName extends string, TBase extends ClickHouseColumnBuilderBase> = ClickHouseArrayBuilder<{
+    name: TName;
+    dataType: 'array';
+    columnType: 'ClickHouseArray';
+    data: ElementData<TBase>[];
+    driverParam: unknown[];
+    enumValues: undefined;
+    notNull: true;
+}, TBase>;
+export declare class ClickHouseArrayBuilder<T extends ColumnBuilderBaseConfig<'array', 'ClickHouseArray'>, TBase extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    baseBuilder: TBase;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], baseBuilder: TBase);
+}
+export declare class ClickHouseArray<T extends ColumnBaseConfig<'array', 'ClickHouseArray'>> extends ClickHouseColumn<T> {
+    readonly baseColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, baseColumn: ClickHouseColumn);
+    /** ClickHouse rejects `Nullable(Array(...))`; use an empty array instead of null. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown[]): unknown[];
+    mapToDriverValue(value: unknown[]): SQL;
+    mapToRowValue(value: unknown[]): unknown[];
+}
+/**
+ * `Array(T)` — an ordered list. Elements are non-nullable unless wrapped in
+ * {@link nullable}, and arrays nest, so `array(array(int32()))` is `Array(Array(Int32))`.
+ *
+ * ```ts
+ * tags: array(string()),
+ * scores: array(nullable(float64())),
+ * ```
+ */
+export declare function array<TBase extends ClickHouseColumnBuilderBase>(baseBuilder: TBase): ClickHouseArrayBuilderInitial<'', TBase>;
+export declare function array<TName extends string, TBase extends ClickHouseColumnBuilderBase>(name: TName, baseBuilder: TBase): ClickHouseArrayBuilderInitial<TName, TBase>;
diff --git a/clickhouse-core/columns/array.js b/clickhouse-core/columns/array.js
new file mode 100644
index 0000000..26ac5bb
--- /dev/null
+++ b/clickhouse-core/columns/array.js
@@ -0,0 +1,55 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseArrayBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseArrayBuilder";
+  constructor(name, baseBuilder) {
+    super(name, "array", "ClickHouseArray");
+    this.config.baseBuilder = baseBuilder;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const baseColumn = this.config.baseBuilder.markAsElement().build(table);
+    return new ClickHouseArray(
+      table,
+      this.config,
+      baseColumn
+    );
+  }
+}
+class ClickHouseArray extends ClickHouseColumn {
+  constructor(table, config, baseColumn) {
+    super(table, config);
+    this.baseColumn = baseColumn;
+  }
+  baseColumn;
+  static [entityKind] = "ClickHouseArray";
+  /** ClickHouse rejects `Nullable(Array(...))`; use an empty array instead of null. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return `Array(${this.baseColumn.getSQLType()})`;
+  }
+  mapFromDriverValue(value) {
+    return value.map((element) => element === null ? null : this.baseColumn.mapFromDriverValue(element));
+  }
+  mapToDriverValue(value) {
+    const elements = value.map(
+      (element) => element === null ? sql`NULL` : sql`${this.baseColumn.mapToDriverValue(element)}`
+    );
+    return sql`[${sql.join(elements, sql`, `)}]`;
+  }
+  mapToRowValue(value) {
+    return value.map((element) => element === null ? null : this.baseColumn.mapToRowValue(element));
+  }
+}
+function array(a, b) {
+  return typeof a === "string" ? new ClickHouseArrayBuilder(a, b) : new ClickHouseArrayBuilder("", a);
+}
+export {
+  ClickHouseArray,
+  ClickHouseArrayBuilder,
+  array
+};
diff --git a/clickhouse-core/columns/bigint.cjs b/clickhouse-core/columns/bigint.cjs
new file mode 100644
index 0000000..8aaeebb
--- /dev/null
+++ b/clickhouse-core/columns/bigint.cjs
@@ -0,0 +1,105 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var bigint_exports = {};
+__export(bigint_exports, {
+  ClickHouseBigInt: () => ClickHouseBigInt,
+  ClickHouseBigIntBuilder: () => ClickHouseBigIntBuilder,
+  int128: () => int128,
+  int256: () => int256,
+  int64: () => int64,
+  uint128: () => uint128,
+  uint256: () => uint256,
+  uint64: () => uint64
+});
+module.exports = __toCommonJS(bigint_exports);
+var import_entity = require("../../entity.cjs");
+var import_utils = require("../../utils.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseBigIntBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseBigIntBuilder";
+  constructor(name, columnType, chType, mode) {
+    super(name, mode === "number" ? "number" : mode === "string" ? "string" : "bigint", columnType);
+    this.config.chType = chType;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseBigInt(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseBigInt extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseBigInt";
+  chType = this.config.chType;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    switch (this.mode) {
+      case "number": {
+        return typeof value === "number" ? value : Number(value);
+      }
+      case "string": {
+        return typeof value === "string" ? value : String(value);
+      }
+      default: {
+        return typeof value === "bigint" ? value : BigInt(value);
+      }
+    }
+  }
+  mapToDriverValue(value) {
+    return (0, import_literals.numericLiteral)(value);
+  }
+  /**
+   * A decimal *string*. `Int64` and wider do not survive JSON's number type — a `UInt64` past
+   * `Number.MAX_SAFE_INTEGER` would be silently rounded on the way in — and ClickHouse accepts a
+   * quoted integer for these columns, which is why its own JSON output quotes them too.
+   */
+  mapToRowValue(value) {
+    return String(value);
+  }
+}
+function bigIntFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+    return new ClickHouseBigIntBuilder(name, columnType, chType, config?.mode ?? "bigint");
+  }
+  return column;
+}
+const int64 = bigIntFactory("ClickHouseInt64", "Int64");
+const int128 = bigIntFactory("ClickHouseInt128", "Int128");
+const int256 = bigIntFactory("ClickHouseInt256", "Int256");
+const uint64 = bigIntFactory("ClickHouseUInt64", "UInt64");
+const uint128 = bigIntFactory("ClickHouseUInt128", "UInt128");
+const uint256 = bigIntFactory("ClickHouseUInt256", "UInt256");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseBigInt,
+  ClickHouseBigIntBuilder,
+  int128,
+  int256,
+  int64,
+  uint128,
+  uint256,
+  uint64
+});
diff --git a/clickhouse-core/columns/bigint.d.cts b/clickhouse-core/columns/bigint.d.cts
new file mode 100644
index 0000000..766cf0f
--- /dev/null
+++ b/clickhouse-core/columns/bigint.d.cts
@@ -0,0 +1,85 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+/** ClickHouse integer types too wide to fit losslessly into a JavaScript `number`. */
+export type ClickHouseWideIntTypeName = 'Int64' | 'Int128' | 'Int256' | 'UInt64' | 'UInt128' | 'UInt256';
+/**
+ * How a wide integer is surfaced in TypeScript.
+ *
+ * ClickHouse's JSON output formats quote 64-bit-and-wider integers, so the driver always hands us a
+ * string. `bigint` (the default) is lossless; `number` is convenient but loses precision beyond
+ * `Number.MAX_SAFE_INTEGER`; `string` passes the driver value through untouched.
+ */
+export type ClickHouseBigIntMode = 'bigint' | 'number' | 'string';
+export interface ClickHouseBigIntConfig<TMode extends ClickHouseBigIntMode = ClickHouseBigIntMode> {
+    mode?: TMode;
+}
+interface ClickHouseBigIntRuntimeConfig {
+    chType: ClickHouseWideIntTypeName;
+    mode: ClickHouseBigIntMode;
+}
+export type ClickHouseBigIntBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseBigIntMode> = ClickHouseBigIntBuilder<{
+    name: TName;
+    dataType: TMode extends 'number' ? 'number' : TMode extends 'string' ? 'string' : 'bigint';
+    columnType: TColumnType;
+    data: TMode extends 'number' ? number : TMode extends 'string' ? string : bigint;
+    driverParam: string | number;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseBigIntBuilder<T extends ColumnBuilderBaseConfig<'bigint' | 'number' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseBigIntRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseWideIntTypeName, mode: ClickHouseBigIntMode);
+}
+export declare class ClickHouseBigInt<T extends ColumnBaseConfig<'bigint' | 'number' | 'string', string>> extends ClickHouseColumn<T, ClickHouseBigIntRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseWideIntTypeName;
+    readonly mode: ClickHouseBigIntMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string | number | bigint): bigint | number | string;
+    mapToDriverValue(value: bigint | number | string): SQL;
+    /**
+     * A decimal *string*. `Int64` and wider do not survive JSON's number type — a `UInt64` past
+     * `Number.MAX_SAFE_INTEGER` would be silently rounded on the way in — and ClickHouse accepts a
+     * quoted integer for these columns, which is why its own JSON output quotes them too.
+     */
+    mapToRowValue(value: bigint | number | string): string;
+}
+/** `Int64` — signed 64-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int64: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt64", TMode>;
+};
+/** `Int128` — signed 128-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int128: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt128", TMode>;
+};
+/** `Int256` — signed 256-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int256: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt256", TMode>;
+};
+/** `UInt64` — unsigned 64-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint64: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt64", TMode>;
+};
+/** `UInt128` — unsigned 128-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint128: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt128", TMode>;
+};
+/** `UInt256` — unsigned 256-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint256: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt256", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/bigint.d.ts b/clickhouse-core/columns/bigint.d.ts
new file mode 100644
index 0000000..6e4d8bd
--- /dev/null
+++ b/clickhouse-core/columns/bigint.d.ts
@@ -0,0 +1,85 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+/** ClickHouse integer types too wide to fit losslessly into a JavaScript `number`. */
+export type ClickHouseWideIntTypeName = 'Int64' | 'Int128' | 'Int256' | 'UInt64' | 'UInt128' | 'UInt256';
+/**
+ * How a wide integer is surfaced in TypeScript.
+ *
+ * ClickHouse's JSON output formats quote 64-bit-and-wider integers, so the driver always hands us a
+ * string. `bigint` (the default) is lossless; `number` is convenient but loses precision beyond
+ * `Number.MAX_SAFE_INTEGER`; `string` passes the driver value through untouched.
+ */
+export type ClickHouseBigIntMode = 'bigint' | 'number' | 'string';
+export interface ClickHouseBigIntConfig<TMode extends ClickHouseBigIntMode = ClickHouseBigIntMode> {
+    mode?: TMode;
+}
+interface ClickHouseBigIntRuntimeConfig {
+    chType: ClickHouseWideIntTypeName;
+    mode: ClickHouseBigIntMode;
+}
+export type ClickHouseBigIntBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseBigIntMode> = ClickHouseBigIntBuilder<{
+    name: TName;
+    dataType: TMode extends 'number' ? 'number' : TMode extends 'string' ? 'string' : 'bigint';
+    columnType: TColumnType;
+    data: TMode extends 'number' ? number : TMode extends 'string' ? string : bigint;
+    driverParam: string | number;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseBigIntBuilder<T extends ColumnBuilderBaseConfig<'bigint' | 'number' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseBigIntRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseWideIntTypeName, mode: ClickHouseBigIntMode);
+}
+export declare class ClickHouseBigInt<T extends ColumnBaseConfig<'bigint' | 'number' | 'string', string>> extends ClickHouseColumn<T, ClickHouseBigIntRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseWideIntTypeName;
+    readonly mode: ClickHouseBigIntMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string | number | bigint): bigint | number | string;
+    mapToDriverValue(value: bigint | number | string): SQL;
+    /**
+     * A decimal *string*. `Int64` and wider do not survive JSON's number type — a `UInt64` past
+     * `Number.MAX_SAFE_INTEGER` would be silently rounded on the way in — and ClickHouse accepts a
+     * quoted integer for these columns, which is why its own JSON output quotes them too.
+     */
+    mapToRowValue(value: bigint | number | string): string;
+}
+/** `Int64` — signed 64-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int64: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt64", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt64", TMode>;
+};
+/** `Int128` — signed 128-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int128: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt128", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt128", TMode>;
+};
+/** `Int256` — signed 256-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const int256: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseInt256", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseInt256", TMode>;
+};
+/** `UInt64` — unsigned 64-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint64: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt64", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt64", TMode>;
+};
+/** `UInt128` — unsigned 128-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint128: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt128", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt128", TMode>;
+};
+/** `UInt256` — unsigned 256-bit integer. Surfaced as `bigint` unless a different `mode` is given. */
+export declare const uint256: {
+    (): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", "bigint">;
+    <TMode extends ClickHouseBigIntMode = "bigint">(config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<"", "ClickHouseUInt256", TMode>;
+    <TName extends string, TMode extends ClickHouseBigIntMode = "bigint">(name: TName, config?: ClickHouseBigIntConfig<TMode> | undefined): ClickHouseBigIntBuilderInitial<TName, "ClickHouseUInt256", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/bigint.js b/clickhouse-core/columns/bigint.js
new file mode 100644
index 0000000..0435b85
--- /dev/null
+++ b/clickhouse-core/columns/bigint.js
@@ -0,0 +1,74 @@
+import { entityKind } from "../../entity.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { numericLiteral } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseBigIntBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseBigIntBuilder";
+  constructor(name, columnType, chType, mode) {
+    super(name, mode === "number" ? "number" : mode === "string" ? "string" : "bigint", columnType);
+    this.config.chType = chType;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseBigInt(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseBigInt extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseBigInt";
+  chType = this.config.chType;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    switch (this.mode) {
+      case "number": {
+        return typeof value === "number" ? value : Number(value);
+      }
+      case "string": {
+        return typeof value === "string" ? value : String(value);
+      }
+      default: {
+        return typeof value === "bigint" ? value : BigInt(value);
+      }
+    }
+  }
+  mapToDriverValue(value) {
+    return numericLiteral(value);
+  }
+  /**
+   * A decimal *string*. `Int64` and wider do not survive JSON's number type — a `UInt64` past
+   * `Number.MAX_SAFE_INTEGER` would be silently rounded on the way in — and ClickHouse accepts a
+   * quoted integer for these columns, which is why its own JSON output quotes them too.
+   */
+  mapToRowValue(value) {
+    return String(value);
+  }
+}
+function bigIntFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = getColumnNameAndConfig(a, b);
+    return new ClickHouseBigIntBuilder(name, columnType, chType, config?.mode ?? "bigint");
+  }
+  return column;
+}
+const int64 = bigIntFactory("ClickHouseInt64", "Int64");
+const int128 = bigIntFactory("ClickHouseInt128", "Int128");
+const int256 = bigIntFactory("ClickHouseInt256", "Int256");
+const uint64 = bigIntFactory("ClickHouseUInt64", "UInt64");
+const uint128 = bigIntFactory("ClickHouseUInt128", "UInt128");
+const uint256 = bigIntFactory("ClickHouseUInt256", "UInt256");
+export {
+  ClickHouseBigInt,
+  ClickHouseBigIntBuilder,
+  int128,
+  int256,
+  int64,
+  uint128,
+  uint256,
+  uint64
+};
diff --git a/clickhouse-core/columns/bool.cjs b/clickhouse-core/columns/bool.cjs
new file mode 100644
index 0000000..ba8e8d3
--- /dev/null
+++ b/clickhouse-core/columns/bool.cjs
@@ -0,0 +1,66 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var bool_exports = {};
+__export(bool_exports, {
+  ClickHouseBoolean: () => ClickHouseBoolean,
+  ClickHouseBooleanBuilder: () => ClickHouseBooleanBuilder,
+  bool: () => bool,
+  boolean: () => boolean
+});
+module.exports = __toCommonJS(bool_exports);
+var import_entity = require("../../entity.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseBooleanBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseBooleanBuilder";
+  constructor(name) {
+    super(name, "boolean", "ClickHouseBoolean");
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseBoolean(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseBoolean extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseBoolean";
+  getBaseSQLType() {
+    return "Bool";
+  }
+  mapFromDriverValue(value) {
+    if (typeof value === "boolean") return value;
+    if (typeof value === "number") return value !== 0;
+    return value === "true" || value === "1";
+  }
+  mapToDriverValue(value) {
+    return value;
+  }
+}
+function bool(name) {
+  return new ClickHouseBooleanBuilder(name ?? "");
+}
+const boolean = bool;
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseBoolean,
+  ClickHouseBooleanBuilder,
+  bool,
+  boolean
+});
diff --git a/clickhouse-core/columns/bool.d.cts b/clickhouse-core/columns/bool.d.cts
new file mode 100644
index 0000000..da8959c
--- /dev/null
+++ b/clickhouse-core/columns/bool.d.cts
@@ -0,0 +1,27 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseBooleanBuilderInitial<TName extends string> = ClickHouseBooleanBuilder<{
+    name: TName;
+    dataType: 'boolean';
+    columnType: 'ClickHouseBoolean';
+    data: boolean;
+    driverParam: boolean | number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseBooleanBuilder<T extends ColumnBuilderBaseConfig<'boolean', 'ClickHouseBoolean'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+}
+export declare class ClickHouseBoolean<T extends ColumnBaseConfig<'boolean', 'ClickHouseBoolean'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: boolean | number | string): boolean;
+    mapToDriverValue(value: boolean): boolean;
+}
+/** `Bool` — a boolean stored as `UInt8` under the hood. */
+export declare function bool(): ClickHouseBooleanBuilderInitial<''>;
+export declare function bool<TName extends string>(name: TName): ClickHouseBooleanBuilderInitial<TName>;
+/** Alias for {@link bool}. */
+export declare const boolean: typeof bool;
diff --git a/clickhouse-core/columns/bool.d.ts b/clickhouse-core/columns/bool.d.ts
new file mode 100644
index 0000000..2d69d9a
--- /dev/null
+++ b/clickhouse-core/columns/bool.d.ts
@@ -0,0 +1,27 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseBooleanBuilderInitial<TName extends string> = ClickHouseBooleanBuilder<{
+    name: TName;
+    dataType: 'boolean';
+    columnType: 'ClickHouseBoolean';
+    data: boolean;
+    driverParam: boolean | number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseBooleanBuilder<T extends ColumnBuilderBaseConfig<'boolean', 'ClickHouseBoolean'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+}
+export declare class ClickHouseBoolean<T extends ColumnBaseConfig<'boolean', 'ClickHouseBoolean'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: boolean | number | string): boolean;
+    mapToDriverValue(value: boolean): boolean;
+}
+/** `Bool` — a boolean stored as `UInt8` under the hood. */
+export declare function bool(): ClickHouseBooleanBuilderInitial<''>;
+export declare function bool<TName extends string>(name: TName): ClickHouseBooleanBuilderInitial<TName>;
+/** Alias for {@link bool}. */
+export declare const boolean: typeof bool;
diff --git a/clickhouse-core/columns/bool.js b/clickhouse-core/columns/bool.js
new file mode 100644
index 0000000..6521405
--- /dev/null
+++ b/clickhouse-core/columns/bool.js
@@ -0,0 +1,39 @@
+import { entityKind } from "../../entity.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseBooleanBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseBooleanBuilder";
+  constructor(name) {
+    super(name, "boolean", "ClickHouseBoolean");
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseBoolean(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseBoolean extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseBoolean";
+  getBaseSQLType() {
+    return "Bool";
+  }
+  mapFromDriverValue(value) {
+    if (typeof value === "boolean") return value;
+    if (typeof value === "number") return value !== 0;
+    return value === "true" || value === "1";
+  }
+  mapToDriverValue(value) {
+    return value;
+  }
+}
+function bool(name) {
+  return new ClickHouseBooleanBuilder(name ?? "");
+}
+const boolean = bool;
+export {
+  ClickHouseBoolean,
+  ClickHouseBooleanBuilder,
+  bool,
+  boolean
+};
diff --git a/clickhouse-core/columns/common.cjs b/clickhouse-core/columns/common.cjs
new file mode 100644
index 0000000..e4ab885
--- /dev/null
+++ b/clickhouse-core/columns/common.cjs
@@ -0,0 +1,207 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var common_exports = {};
+__export(common_exports, {
+  COMPUTED_COLUMN_KEYWORD: () => COMPUTED_COLUMN_KEYWORD,
+  ClickHouseColumn: () => ClickHouseColumn,
+  ClickHouseColumnBuilder: () => ClickHouseColumnBuilder
+});
+module.exports = __toCommonJS(common_exports);
+var import_column_builder = require("../../column-builder.cjs");
+var import_column = require("../../column.cjs");
+var import_entity = require("../../entity.cjs");
+const COMPUTED_COLUMN_KEYWORD = {
+  stored: "MATERIALIZED",
+  virtual: "ALIAS"
+};
+class ClickHouseColumnBuilder extends import_column_builder.ColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseColumnBuilder";
+  constructor(name, dataType, columnType) {
+    super(name, dataType, columnType);
+    this.config.codec = void 0;
+    this.config.ttl = void 0;
+    this.config.comment = void 0;
+    this.config.ephemeral = false;
+    this.config.explicitNullable = false;
+  }
+  /**
+   * @internal The builder's `dataType`.
+   *
+   * `builder['_']` is a type-only declaration and is `undefined` at runtime, so wrapper types such as
+   * `LowCardinality(T)` have to read it off the runtime config instead.
+   */
+  getDataType() {
+    return this.config.dataType;
+  }
+  /** @internal The `enumValues` a wrapper type should inherit from the type it wraps. */
+  getEnumValues() {
+    return this.config.enumValues;
+  }
+  /** @internal Marks the builder as explicitly `Nullable(T)`. See {@link nullable}. */
+  markNullable() {
+    this.config.notNull = false;
+    this.config.explicitNullable = true;
+    return this;
+  }
+  /**
+   * @internal Prepares the builder for use as an `Array`/`Map`/`Tuple` element.
+   *
+   * Element types are non-nullable unless wrapped in {@link nullable}, which is the opposite of the
+   * default for table columns but matches how ClickHouse types are normally written.
+   */
+  markAsElement() {
+    if (!this.config.explicitNullable) {
+      this.config.notNull = true;
+    }
+    return this;
+  }
+  /**
+   * Attaches a compression `CODEC` to the column.
+   *
+   * @example
+   * ```ts
+   * const events = clickhouseTable('events', {
+   * 	body: string().codec('ZSTD(3)'),
+   * 	ts: dateTime().codec('Delta', 'LZ4'),
+   * });
+   * ```
+   */
+  codec(...codecs) {
+    this.config.codec = codecs;
+    return this;
+  }
+  /**
+   * Adds a column-level `TTL` expression. Rows are not dropped; the column value is reset to its
+   * default once the expression elapses.
+   *
+   * @example
+   * ```ts
+   * const events = clickhouseTable('events', {
+   * 	createdAt: dateTime().notNull(),
+   * 	payload: string().ttl(sql`createdAt + INTERVAL 1 MONTH`),
+   * });
+   * ```
+   */
+  ttl(expression) {
+    this.config.ttl = expression;
+    return this;
+  }
+  /** Adds a `COMMENT` to the column definition. */
+  comment(value) {
+    this.config.comment = value;
+    return this;
+  }
+  /**
+   * Marks the column as `EPHEMERAL`. Ephemeral columns are accepted on insert and can be referenced
+   * by other columns' `DEFAULT`/`MATERIALIZED` expressions, but are never stored.
+   */
+  ephemeral() {
+    this.config.ephemeral = true;
+    return this;
+  }
+  /**
+   * Declares the column as `MATERIALIZED`: the expression is evaluated on insert and stored on disk.
+   * Materialized columns cannot be written to directly, so they are omitted from the insert model.
+   */
+  materialized(expression) {
+    return this.generatedAlwaysAs(expression, { mode: "stored" });
+  }
+  /**
+   * Declares the column as an `ALIAS`: the expression is not stored and is evaluated on read.
+   * Alias columns cannot be written to directly, so they are omitted from the insert model.
+   */
+  aliasedAs(expression) {
+    return this.generatedAlwaysAs(expression, { mode: "virtual" });
+  }
+  generatedAlwaysAs(as, config) {
+    this.config.generated = {
+      as,
+      type: "always",
+      mode: config?.mode ?? "stored"
+    };
+    return this;
+  }
+}
+class ClickHouseColumn extends import_column.Column {
+  constructor(table, config) {
+    super(table, config);
+    this.table = table;
+  }
+  table;
+  static [import_entity.entityKind] = "ClickHouseColumn";
+  /**
+   * ClickHouse forbids `Nullable(T)` when `T` is `Array`, `Map`, `Tuple`, `JSON` or itself `Nullable`.
+   * Columns of those types are always emitted bare, even when the Drizzle model marks them nullable.
+   */
+  get supportsNullable() {
+    return true;
+  }
+  getSQLType() {
+    const baseType = this.getBaseSQLType();
+    return this.notNull || !this.supportsNullable ? baseType : `Nullable(${baseType})`;
+  }
+  /** The compression codecs attached with `.codec()`, if any. */
+  get codec() {
+    return this.config.codec;
+  }
+  /** The column-level `TTL` expression attached with `.ttl()`, if any. */
+  get ttl() {
+    return this.config.ttl;
+  }
+  /** The `COMMENT` attached with `.comment()`, if any. */
+  get comment() {
+    return this.config.comment;
+  }
+  /** Whether the column was declared `EPHEMERAL`. */
+  get isEphemeral() {
+    return this.config.ephemeral;
+  }
+  /** The ClickHouse keyword for a computed column — `MATERIALIZED` or `ALIAS` — if it is one. */
+  get computedKeyword() {
+    const mode = this.config.generated?.mode;
+    return mode === void 0 ? void 0 : COMPUTED_COLUMN_KEYWORD[mode];
+  }
+  /**
+   * The value as a *row format* carries it — the JSON a `JSONEachRow` body holds — as opposed to
+   * {@link mapToDriverValue}, which renders a SQL literal for a statement.
+   *
+   * ClickHouse takes data by two routes and they are not the same encoding. A statement needs
+   * `toDateTime64('2026-08-11 12:00:00.000', 3, 'UTC')`, because a bare string compared against a
+   * `DateTime64` column is a hard error; a JSON body needs the bare `"2026-08-11 12:00:00.000"` and
+   * lets the column's own type do the parsing, because there is no expression to type-check. The
+   * two paths exist because inlining a bulk load into one statement makes the client build the
+   * whole batch as a string and the server re-parse every field as a SQL expression.
+   *
+   * **Overriding one without the other is the bug this pair invites** — a column that filters
+   * correctly and inserts wrong, or vice versa. The default returns the value untouched, which is
+   * already correct for every type whose JavaScript form is its JSON form (`String`, `Int32`,
+   * `Float64`, `Bool`, `UUID`, `IPv4`, `JSON`); the types that need more override it next to their
+   * `mapToDriverValue`, and `clickhouse-core/columns/__tests__` asserts the pair round-trips for
+   * each of them.
+   */
+  mapToRowValue(value) {
+    return value;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  COMPUTED_COLUMN_KEYWORD,
+  ClickHouseColumn,
+  ClickHouseColumnBuilder
+});
diff --git a/clickhouse-core/columns/common.d.cts b/clickhouse-core/columns/common.d.cts
new file mode 100644
index 0000000..d0fa586
--- /dev/null
+++ b/clickhouse-core/columns/common.d.cts
@@ -0,0 +1,142 @@
+import type { ClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBase, ColumnBuilderBaseConfig, ColumnBuilderExtraConfig, ColumnBuilderRuntimeConfig, ColumnDataType, GeneratedColumnConfig, GeneratedStorageMode, HasGenerated } from '../../column-builder.cjs';
+import { ColumnBuilder } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { Column } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import type { Update } from '../../utils.cjs';
+export interface ClickHouseColumnBuilderBase<T extends ColumnBuilderBaseConfig<ColumnDataType, string> = ColumnBuilderBaseConfig<ColumnDataType, string>, TTypeConfig extends object = object> extends ColumnBuilderBase<T, TTypeConfig & {
+    dialect: 'clickhouse';
+}> {
+}
+/**
+ * How a computed ClickHouse column is stored, spelled in Drizzle's vocabulary.
+ *
+ * - `stored` — ClickHouse's `MATERIALIZED`: evaluated on insert and written to disk.
+ * - `virtual` — ClickHouse's `ALIAS`: not stored, evaluated on every read.
+ */
+export type ClickHouseComputedColumnMode = GeneratedStorageMode;
+/** The ClickHouse keyword corresponding to each {@link ClickHouseComputedColumnMode}. */
+export declare const COMPUTED_COLUMN_KEYWORD: {
+    readonly stored: "MATERIALIZED";
+    readonly virtual: "ALIAS";
+};
+export interface ClickHouseColumnConfig {
+    codec: (string | SQL)[] | undefined;
+    ttl: SQL | undefined;
+    comment: string | undefined;
+    /** Set for `EPHEMERAL` columns, which are accepted on insert but never stored. */
+    ephemeral: boolean;
+    /**
+     * Set by {@link nullable}. Distinguishes "the user asked for `Nullable(T)`" from "the user never
+     * said", which matters for `Array`/`Map`/`Tuple` elements — those default to non-nullable to match
+     * ClickHouse, rather than to Drizzle's nullable-unless-`notNull` default for table columns.
+     */
+    explicitNullable: boolean;
+}
+export declare abstract class ClickHouseColumnBuilder<T extends ColumnBuilderBaseConfig<ColumnDataType, string> = ColumnBuilderBaseConfig<ColumnDataType, string> & {
+    data: any;
+}, TRuntimeConfig extends object = object, TTypeConfig extends object = object, TExtraConfig extends ColumnBuilderExtraConfig = ColumnBuilderExtraConfig> extends ColumnBuilder<T, TRuntimeConfig & ClickHouseColumnConfig, TTypeConfig & {
+    dialect: 'clickhouse';
+}, TExtraConfig> implements ClickHouseColumnBuilderBase<T, TTypeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], dataType: T['dataType'], columnType: T['columnType']);
+    /**
+     * Attaches a compression `CODEC` to the column.
+     *
+     * @example
+     * ```ts
+     * const events = clickhouseTable('events', {
+     * 	body: string().codec('ZSTD(3)'),
+     * 	ts: dateTime().codec('Delta', 'LZ4'),
+     * });
+     * ```
+     */
+    codec(...codecs: [string | SQL, ...(string | SQL)[]]): this;
+    /**
+     * Adds a column-level `TTL` expression. Rows are not dropped; the column value is reset to its
+     * default once the expression elapses.
+     *
+     * @example
+     * ```ts
+     * const events = clickhouseTable('events', {
+     * 	createdAt: dateTime().notNull(),
+     * 	payload: string().ttl(sql`createdAt + INTERVAL 1 MONTH`),
+     * });
+     * ```
+     */
+    ttl(expression: SQL): this;
+    /** Adds a `COMMENT` to the column definition. */
+    comment(value: string): this;
+    /**
+     * Marks the column as `EPHEMERAL`. Ephemeral columns are accepted on insert and can be referenced
+     * by other columns' `DEFAULT`/`MATERIALIZED` expressions, but are never stored.
+     */
+    ephemeral(): this;
+    /**
+     * Declares the column as `MATERIALIZED`: the expression is evaluated on insert and stored on disk.
+     * Materialized columns cannot be written to directly, so they are omitted from the insert model.
+     */
+    materialized(expression: SQL | (() => SQL)): HasGenerated<this, {
+        type: 'always';
+    }>;
+    /**
+     * Declares the column as an `ALIAS`: the expression is not stored and is evaluated on read.
+     * Alias columns cannot be written to directly, so they are omitted from the insert model.
+     */
+    aliasedAs(expression: SQL | (() => SQL)): HasGenerated<this, {
+        type: 'always';
+    }>;
+    generatedAlwaysAs(as: SQL | T['data'] | (() => SQL), config?: Partial<GeneratedColumnConfig<unknown>>): HasGenerated<this, {
+        type: 'always';
+    }>;
+}
+export declare abstract class ClickHouseColumn<T extends ColumnBaseConfig<ColumnDataType, string> = ColumnBaseConfig<ColumnDataType, string>, TRuntimeConfig extends object = {}, TTypeConfig extends object = {}> extends Column<T, TRuntimeConfig & ClickHouseColumnConfig, TTypeConfig & {
+    dialect: 'clickhouse';
+}> {
+    readonly table: ClickHouseTable;
+    static readonly [entityKind]: string;
+    constructor(table: ClickHouseTable, config: ColumnBuilderRuntimeConfig<T['data'], TRuntimeConfig & ClickHouseColumnConfig>);
+    /**
+     * The ClickHouse type of this column without the `Nullable(...)` wrapper — for example `Int32`,
+     * `DateTime64(3, 'UTC')` or `Array(String)`.
+     */
+    abstract getBaseSQLType(): string;
+    /**
+     * ClickHouse forbids `Nullable(T)` when `T` is `Array`, `Map`, `Tuple`, `JSON` or itself `Nullable`.
+     * Columns of those types are always emitted bare, even when the Drizzle model marks them nullable.
+     */
+    get supportsNullable(): boolean;
+    getSQLType(): string;
+    /** The compression codecs attached with `.codec()`, if any. */
+    get codec(): (string | SQL)[] | undefined;
+    /** The column-level `TTL` expression attached with `.ttl()`, if any. */
+    get ttl(): SQL | undefined;
+    /** The `COMMENT` attached with `.comment()`, if any. */
+    get comment(): string | undefined;
+    /** Whether the column was declared `EPHEMERAL`. */
+    get isEphemeral(): boolean;
+    /** The ClickHouse keyword for a computed column — `MATERIALIZED` or `ALIAS` — if it is one. */
+    get computedKeyword(): 'MATERIALIZED' | 'ALIAS' | undefined;
+    /**
+     * The value as a *row format* carries it — the JSON a `JSONEachRow` body holds — as opposed to
+     * {@link mapToDriverValue}, which renders a SQL literal for a statement.
+     *
+     * ClickHouse takes data by two routes and they are not the same encoding. A statement needs
+     * `toDateTime64('2026-08-11 12:00:00.000', 3, 'UTC')`, because a bare string compared against a
+     * `DateTime64` column is a hard error; a JSON body needs the bare `"2026-08-11 12:00:00.000"` and
+     * lets the column's own type do the parsing, because there is no expression to type-check. The
+     * two paths exist because inlining a bulk load into one statement makes the client build the
+     * whole batch as a string and the server re-parse every field as a SQL expression.
+     *
+     * **Overriding one without the other is the bug this pair invites** — a column that filters
+     * correctly and inserts wrong, or vice versa. The default returns the value untouched, which is
+     * already correct for every type whose JavaScript form is its JSON form (`String`, `Int32`,
+     * `Float64`, `Bool`, `UUID`, `IPv4`, `JSON`); the types that need more override it next to their
+     * `mapToDriverValue`, and `clickhouse-core/columns/__tests__` asserts the pair round-trips for
+     * each of them.
+     */
+    mapToRowValue(value: unknown): unknown;
+}
+export type AnyClickHouseColumn<TPartial extends Partial<ColumnBaseConfig<ColumnDataType, string>> = {}> = ClickHouseColumn<Required<Update<ColumnBaseConfig<ColumnDataType, string>, TPartial>>>;
diff --git a/clickhouse-core/columns/common.d.ts b/clickhouse-core/columns/common.d.ts
new file mode 100644
index 0000000..51305f0
--- /dev/null
+++ b/clickhouse-core/columns/common.d.ts
@@ -0,0 +1,142 @@
+import type { ClickHouseTable } from '../table.js';
+import type { ColumnBuilderBase, ColumnBuilderBaseConfig, ColumnBuilderExtraConfig, ColumnBuilderRuntimeConfig, ColumnDataType, GeneratedColumnConfig, GeneratedStorageMode, HasGenerated } from '../../column-builder.js';
+import { ColumnBuilder } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { Column } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import type { Update } from '../../utils.js';
+export interface ClickHouseColumnBuilderBase<T extends ColumnBuilderBaseConfig<ColumnDataType, string> = ColumnBuilderBaseConfig<ColumnDataType, string>, TTypeConfig extends object = object> extends ColumnBuilderBase<T, TTypeConfig & {
+    dialect: 'clickhouse';
+}> {
+}
+/**
+ * How a computed ClickHouse column is stored, spelled in Drizzle's vocabulary.
+ *
+ * - `stored` — ClickHouse's `MATERIALIZED`: evaluated on insert and written to disk.
+ * - `virtual` — ClickHouse's `ALIAS`: not stored, evaluated on every read.
+ */
+export type ClickHouseComputedColumnMode = GeneratedStorageMode;
+/** The ClickHouse keyword corresponding to each {@link ClickHouseComputedColumnMode}. */
+export declare const COMPUTED_COLUMN_KEYWORD: {
+    readonly stored: "MATERIALIZED";
+    readonly virtual: "ALIAS";
+};
+export interface ClickHouseColumnConfig {
+    codec: (string | SQL)[] | undefined;
+    ttl: SQL | undefined;
+    comment: string | undefined;
+    /** Set for `EPHEMERAL` columns, which are accepted on insert but never stored. */
+    ephemeral: boolean;
+    /**
+     * Set by {@link nullable}. Distinguishes "the user asked for `Nullable(T)`" from "the user never
+     * said", which matters for `Array`/`Map`/`Tuple` elements — those default to non-nullable to match
+     * ClickHouse, rather than to Drizzle's nullable-unless-`notNull` default for table columns.
+     */
+    explicitNullable: boolean;
+}
+export declare abstract class ClickHouseColumnBuilder<T extends ColumnBuilderBaseConfig<ColumnDataType, string> = ColumnBuilderBaseConfig<ColumnDataType, string> & {
+    data: any;
+}, TRuntimeConfig extends object = object, TTypeConfig extends object = object, TExtraConfig extends ColumnBuilderExtraConfig = ColumnBuilderExtraConfig> extends ColumnBuilder<T, TRuntimeConfig & ClickHouseColumnConfig, TTypeConfig & {
+    dialect: 'clickhouse';
+}, TExtraConfig> implements ClickHouseColumnBuilderBase<T, TTypeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], dataType: T['dataType'], columnType: T['columnType']);
+    /**
+     * Attaches a compression `CODEC` to the column.
+     *
+     * @example
+     * ```ts
+     * const events = clickhouseTable('events', {
+     * 	body: string().codec('ZSTD(3)'),
+     * 	ts: dateTime().codec('Delta', 'LZ4'),
+     * });
+     * ```
+     */
+    codec(...codecs: [string | SQL, ...(string | SQL)[]]): this;
+    /**
+     * Adds a column-level `TTL` expression. Rows are not dropped; the column value is reset to its
+     * default once the expression elapses.
+     *
+     * @example
+     * ```ts
+     * const events = clickhouseTable('events', {
+     * 	createdAt: dateTime().notNull(),
+     * 	payload: string().ttl(sql`createdAt + INTERVAL 1 MONTH`),
+     * });
+     * ```
+     */
+    ttl(expression: SQL): this;
+    /** Adds a `COMMENT` to the column definition. */
+    comment(value: string): this;
+    /**
+     * Marks the column as `EPHEMERAL`. Ephemeral columns are accepted on insert and can be referenced
+     * by other columns' `DEFAULT`/`MATERIALIZED` expressions, but are never stored.
+     */
+    ephemeral(): this;
+    /**
+     * Declares the column as `MATERIALIZED`: the expression is evaluated on insert and stored on disk.
+     * Materialized columns cannot be written to directly, so they are omitted from the insert model.
+     */
+    materialized(expression: SQL | (() => SQL)): HasGenerated<this, {
+        type: 'always';
+    }>;
+    /**
+     * Declares the column as an `ALIAS`: the expression is not stored and is evaluated on read.
+     * Alias columns cannot be written to directly, so they are omitted from the insert model.
+     */
+    aliasedAs(expression: SQL | (() => SQL)): HasGenerated<this, {
+        type: 'always';
+    }>;
+    generatedAlwaysAs(as: SQL | T['data'] | (() => SQL), config?: Partial<GeneratedColumnConfig<unknown>>): HasGenerated<this, {
+        type: 'always';
+    }>;
+}
+export declare abstract class ClickHouseColumn<T extends ColumnBaseConfig<ColumnDataType, string> = ColumnBaseConfig<ColumnDataType, string>, TRuntimeConfig extends object = {}, TTypeConfig extends object = {}> extends Column<T, TRuntimeConfig & ClickHouseColumnConfig, TTypeConfig & {
+    dialect: 'clickhouse';
+}> {
+    readonly table: ClickHouseTable;
+    static readonly [entityKind]: string;
+    constructor(table: ClickHouseTable, config: ColumnBuilderRuntimeConfig<T['data'], TRuntimeConfig & ClickHouseColumnConfig>);
+    /**
+     * The ClickHouse type of this column without the `Nullable(...)` wrapper — for example `Int32`,
+     * `DateTime64(3, 'UTC')` or `Array(String)`.
+     */
+    abstract getBaseSQLType(): string;
+    /**
+     * ClickHouse forbids `Nullable(T)` when `T` is `Array`, `Map`, `Tuple`, `JSON` or itself `Nullable`.
+     * Columns of those types are always emitted bare, even when the Drizzle model marks them nullable.
+     */
+    get supportsNullable(): boolean;
+    getSQLType(): string;
+    /** The compression codecs attached with `.codec()`, if any. */
+    get codec(): (string | SQL)[] | undefined;
+    /** The column-level `TTL` expression attached with `.ttl()`, if any. */
+    get ttl(): SQL | undefined;
+    /** The `COMMENT` attached with `.comment()`, if any. */
+    get comment(): string | undefined;
+    /** Whether the column was declared `EPHEMERAL`. */
+    get isEphemeral(): boolean;
+    /** The ClickHouse keyword for a computed column — `MATERIALIZED` or `ALIAS` — if it is one. */
+    get computedKeyword(): 'MATERIALIZED' | 'ALIAS' | undefined;
+    /**
+     * The value as a *row format* carries it — the JSON a `JSONEachRow` body holds — as opposed to
+     * {@link mapToDriverValue}, which renders a SQL literal for a statement.
+     *
+     * ClickHouse takes data by two routes and they are not the same encoding. A statement needs
+     * `toDateTime64('2026-08-11 12:00:00.000', 3, 'UTC')`, because a bare string compared against a
+     * `DateTime64` column is a hard error; a JSON body needs the bare `"2026-08-11 12:00:00.000"` and
+     * lets the column's own type do the parsing, because there is no expression to type-check. The
+     * two paths exist because inlining a bulk load into one statement makes the client build the
+     * whole batch as a string and the server re-parse every field as a SQL expression.
+     *
+     * **Overriding one without the other is the bug this pair invites** — a column that filters
+     * correctly and inserts wrong, or vice versa. The default returns the value untouched, which is
+     * already correct for every type whose JavaScript form is its JSON form (`String`, `Int32`,
+     * `Float64`, `Bool`, `UUID`, `IPv4`, `JSON`); the types that need more override it next to their
+     * `mapToDriverValue`, and `clickhouse-core/columns/__tests__` asserts the pair round-trips for
+     * each of them.
+     */
+    mapToRowValue(value: unknown): unknown;
+}
+export type AnyClickHouseColumn<TPartial extends Partial<ColumnBaseConfig<ColumnDataType, string>> = {}> = ClickHouseColumn<Required<Update<ColumnBaseConfig<ColumnDataType, string>, TPartial>>>;
diff --git a/clickhouse-core/columns/common.js b/clickhouse-core/columns/common.js
new file mode 100644
index 0000000..7f03244
--- /dev/null
+++ b/clickhouse-core/columns/common.js
@@ -0,0 +1,181 @@
+import { ColumnBuilder } from "../../column-builder.js";
+import { Column } from "../../column.js";
+import { entityKind } from "../../entity.js";
+const COMPUTED_COLUMN_KEYWORD = {
+  stored: "MATERIALIZED",
+  virtual: "ALIAS"
+};
+class ClickHouseColumnBuilder extends ColumnBuilder {
+  static [entityKind] = "ClickHouseColumnBuilder";
+  constructor(name, dataType, columnType) {
+    super(name, dataType, columnType);
+    this.config.codec = void 0;
+    this.config.ttl = void 0;
+    this.config.comment = void 0;
+    this.config.ephemeral = false;
+    this.config.explicitNullable = false;
+  }
+  /**
+   * @internal The builder's `dataType`.
+   *
+   * `builder['_']` is a type-only declaration and is `undefined` at runtime, so wrapper types such as
+   * `LowCardinality(T)` have to read it off the runtime config instead.
+   */
+  getDataType() {
+    return this.config.dataType;
+  }
+  /** @internal The `enumValues` a wrapper type should inherit from the type it wraps. */
+  getEnumValues() {
+    return this.config.enumValues;
+  }
+  /** @internal Marks the builder as explicitly `Nullable(T)`. See {@link nullable}. */
+  markNullable() {
+    this.config.notNull = false;
+    this.config.explicitNullable = true;
+    return this;
+  }
+  /**
+   * @internal Prepares the builder for use as an `Array`/`Map`/`Tuple` element.
+   *
+   * Element types are non-nullable unless wrapped in {@link nullable}, which is the opposite of the
+   * default for table columns but matches how ClickHouse types are normally written.
+   */
+  markAsElement() {
+    if (!this.config.explicitNullable) {
+      this.config.notNull = true;
+    }
+    return this;
+  }
+  /**
+   * Attaches a compression `CODEC` to the column.
+   *
+   * @example
+   * ```ts
+   * const events = clickhouseTable('events', {
+   * 	body: string().codec('ZSTD(3)'),
+   * 	ts: dateTime().codec('Delta', 'LZ4'),
+   * });
+   * ```
+   */
+  codec(...codecs) {
+    this.config.codec = codecs;
+    return this;
+  }
+  /**
+   * Adds a column-level `TTL` expression. Rows are not dropped; the column value is reset to its
+   * default once the expression elapses.
+   *
+   * @example
+   * ```ts
+   * const events = clickhouseTable('events', {
+   * 	createdAt: dateTime().notNull(),
+   * 	payload: string().ttl(sql`createdAt + INTERVAL 1 MONTH`),
+   * });
+   * ```
+   */
+  ttl(expression) {
+    this.config.ttl = expression;
+    return this;
+  }
+  /** Adds a `COMMENT` to the column definition. */
+  comment(value) {
+    this.config.comment = value;
+    return this;
+  }
+  /**
+   * Marks the column as `EPHEMERAL`. Ephemeral columns are accepted on insert and can be referenced
+   * by other columns' `DEFAULT`/`MATERIALIZED` expressions, but are never stored.
+   */
+  ephemeral() {
+    this.config.ephemeral = true;
+    return this;
+  }
+  /**
+   * Declares the column as `MATERIALIZED`: the expression is evaluated on insert and stored on disk.
+   * Materialized columns cannot be written to directly, so they are omitted from the insert model.
+   */
+  materialized(expression) {
+    return this.generatedAlwaysAs(expression, { mode: "stored" });
+  }
+  /**
+   * Declares the column as an `ALIAS`: the expression is not stored and is evaluated on read.
+   * Alias columns cannot be written to directly, so they are omitted from the insert model.
+   */
+  aliasedAs(expression) {
+    return this.generatedAlwaysAs(expression, { mode: "virtual" });
+  }
+  generatedAlwaysAs(as, config) {
+    this.config.generated = {
+      as,
+      type: "always",
+      mode: config?.mode ?? "stored"
+    };
+    return this;
+  }
+}
+class ClickHouseColumn extends Column {
+  constructor(table, config) {
+    super(table, config);
+    this.table = table;
+  }
+  table;
+  static [entityKind] = "ClickHouseColumn";
+  /**
+   * ClickHouse forbids `Nullable(T)` when `T` is `Array`, `Map`, `Tuple`, `JSON` or itself `Nullable`.
+   * Columns of those types are always emitted bare, even when the Drizzle model marks them nullable.
+   */
+  get supportsNullable() {
+    return true;
+  }
+  getSQLType() {
+    const baseType = this.getBaseSQLType();
+    return this.notNull || !this.supportsNullable ? baseType : `Nullable(${baseType})`;
+  }
+  /** The compression codecs attached with `.codec()`, if any. */
+  get codec() {
+    return this.config.codec;
+  }
+  /** The column-level `TTL` expression attached with `.ttl()`, if any. */
+  get ttl() {
+    return this.config.ttl;
+  }
+  /** The `COMMENT` attached with `.comment()`, if any. */
+  get comment() {
+    return this.config.comment;
+  }
+  /** Whether the column was declared `EPHEMERAL`. */
+  get isEphemeral() {
+    return this.config.ephemeral;
+  }
+  /** The ClickHouse keyword for a computed column — `MATERIALIZED` or `ALIAS` — if it is one. */
+  get computedKeyword() {
+    const mode = this.config.generated?.mode;
+    return mode === void 0 ? void 0 : COMPUTED_COLUMN_KEYWORD[mode];
+  }
+  /**
+   * The value as a *row format* carries it — the JSON a `JSONEachRow` body holds — as opposed to
+   * {@link mapToDriverValue}, which renders a SQL literal for a statement.
+   *
+   * ClickHouse takes data by two routes and they are not the same encoding. A statement needs
+   * `toDateTime64('2026-08-11 12:00:00.000', 3, 'UTC')`, because a bare string compared against a
+   * `DateTime64` column is a hard error; a JSON body needs the bare `"2026-08-11 12:00:00.000"` and
+   * lets the column's own type do the parsing, because there is no expression to type-check. The
+   * two paths exist because inlining a bulk load into one statement makes the client build the
+   * whole batch as a string and the server re-parse every field as a SQL expression.
+   *
+   * **Overriding one without the other is the bug this pair invites** — a column that filters
+   * correctly and inserts wrong, or vice versa. The default returns the value untouched, which is
+   * already correct for every type whose JavaScript form is its JSON form (`String`, `Int32`,
+   * `Float64`, `Bool`, `UUID`, `IPv4`, `JSON`); the types that need more override it next to their
+   * `mapToDriverValue`, and `clickhouse-core/columns/__tests__` asserts the pair round-trips for
+   * each of them.
+   */
+  mapToRowValue(value) {
+    return value;
+  }
+}
+export {
+  COMPUTED_COLUMN_KEYWORD,
+  ClickHouseColumn,
+  ClickHouseColumnBuilder
+};
diff --git a/clickhouse-core/columns/custom.cjs b/clickhouse-core/columns/custom.cjs
new file mode 100644
index 0000000..ee2aa78
--- /dev/null
+++ b/clickhouse-core/columns/custom.cjs
@@ -0,0 +1,94 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var custom_exports = {};
+__export(custom_exports, {
+  ClickHouseCustomColumn: () => ClickHouseCustomColumn,
+  ClickHouseCustomColumnBuilder: () => ClickHouseCustomColumnBuilder,
+  customType: () => customType
+});
+module.exports = __toCommonJS(custom_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_utils = require("../../utils.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseCustomColumnBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseCustomColumnBuilder";
+  constructor(name, fieldConfig, customTypeParams) {
+    super(name, "custom", "ClickHouseCustomColumn");
+    this.config.fieldConfig = fieldConfig;
+    this.config.customTypeParams = customTypeParams;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseCustomColumn(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseCustomColumn extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseCustomColumn";
+  sqlName;
+  mapTo;
+  mapRow;
+  mapFrom;
+  constructor(table, config) {
+    super(table, config);
+    this.sqlName = config.customTypeParams.dataType(config.fieldConfig);
+    this.mapTo = config.customTypeParams.toDriver;
+    this.mapRow = config.customTypeParams.toRow;
+    this.mapFrom = config.customTypeParams.fromDriver;
+  }
+  getBaseSQLType() {
+    return this.sqlName;
+  }
+  mapFromDriverValue(value) {
+    return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
+  }
+  mapToDriverValue(value) {
+    return typeof this.mapTo === "function" ? this.mapTo(value) : value;
+  }
+  /**
+   * `toRow` when the type declares one, otherwise `toDriver` — unless `toDriver` returned SQL, which
+   * a row format has nowhere to put, in which case the value passes through untouched.
+   *
+   * A custom type whose `toDriver` builds an expression (`toIPv6('…')`, `map(…)`) and whose data is
+   * not already its own JSON form needs `toRow`; without it the fallback would send the raw value
+   * and let the server's column type decide, which is right often enough to be worth doing and
+   * wrong quietly enough to be worth saying out loud.
+   */
+  mapToRowValue(value) {
+    if (typeof this.mapRow === "function") return this.mapRow(value);
+    if (typeof this.mapTo !== "function") return value;
+    const mapped = this.mapTo(value);
+    return (0, import_entity.is)(mapped, import_sql.SQL) ? value : mapped;
+  }
+}
+function customType(customTypeParams) {
+  return ((a, b) => {
+    const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+    return new ClickHouseCustomColumnBuilder(name, config, customTypeParams);
+  });
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseCustomColumn,
+  ClickHouseCustomColumnBuilder,
+  customType
+});
diff --git a/clickhouse-core/columns/custom.d.cts b/clickhouse-core/columns/custom.d.cts
new file mode 100644
index 0000000..dfa7969
--- /dev/null
+++ b/clickhouse-core/columns/custom.d.cts
@@ -0,0 +1,111 @@
+import type { AnyClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import { SQL } from '../../sql/sql.cjs';
+import type { Equal } from '../../utils.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ConvertCustomConfig<TName extends string, T extends Partial<CustomTypeValues>> = {
+    name: TName;
+    dataType: 'custom';
+    columnType: 'ClickHouseCustomColumn';
+    data: T['data'];
+    driverParam: T['driverData'];
+    enumValues: undefined;
+} & (T['notNull'] extends true ? {
+    notNull: true;
+} : {}) & (T['default'] extends true ? {
+    hasDefault: true;
+} : {});
+export interface ClickHouseCustomColumnInnerConfig {
+    customTypeParams: CustomTypeParams<any>;
+}
+export declare class ClickHouseCustomColumnBuilder<T extends ColumnBuilderBaseConfig<'custom', 'ClickHouseCustomColumn'>> extends ClickHouseColumnBuilder<T, {
+    fieldConfig: CustomTypeValues['config'];
+    customTypeParams: CustomTypeParams<any>;
+}, {
+    clickhouseColumnBuilderBrand: 'ClickHouseCustomColumnBuilderBrand';
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], fieldConfig: CustomTypeValues['config'], customTypeParams: CustomTypeParams<any>);
+}
+export declare class ClickHouseCustomColumn<T extends ColumnBaseConfig<'custom', 'ClickHouseCustomColumn'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    private sqlName;
+    private mapTo?;
+    private mapRow?;
+    private mapFrom?;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ClickHouseCustomColumnBuilder<T>['config']);
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: T['driverParam']): T['data'];
+    mapToDriverValue(value: T['data']): T['driverParam'] | SQL;
+    /**
+     * `toRow` when the type declares one, otherwise `toDriver` — unless `toDriver` returned SQL, which
+     * a row format has nowhere to put, in which case the value passes through untouched.
+     *
+     * A custom type whose `toDriver` builds an expression (`toIPv6('…')`, `map(…)`) and whose data is
+     * not already its own JSON form needs `toRow`; without it the fallback would send the raw value
+     * and let the server's column type decide, which is right often enough to be worth doing and
+     * wrong quietly enough to be worth saying out loud.
+     */
+    mapToRowValue(value: T['data']): unknown;
+}
+export type CustomTypeValues = {
+    /** The TypeScript type this column reads and writes. */
+    data: unknown;
+    /**
+     * The value handed to and received from the driver.
+     *
+     * Returning a {@link SQL} from `toDriver` is the way to emit a ClickHouse function call such as
+     * `toIPv6('…')` rather than a bare literal.
+     */
+    driverData?: unknown;
+    /** Extra options accepted by the type, surfaced to `dataType()`. */
+    config?: Record<string, any>;
+    /** Set to `true` to make `config` required. */
+    configRequired?: boolean;
+    /** Set to `true` to have the type imply `.notNull()`. */
+    notNull?: boolean;
+    /** Set to `true` to have the type imply a default. */
+    default?: boolean;
+};
+export interface CustomTypeParams<T extends CustomTypeValues> {
+    /**
+     * Returns the ClickHouse type as it should appear in DDL, e.g. `Point` or
+     * `AggregateFunction(sum, UInt64)`.
+     */
+    dataType: (config: T['config'] | (Equal<T['configRequired'], true> extends true ? never : undefined)) => string;
+    /** Maps a TypeScript value to the SQL literal a statement should carry. */
+    toDriver?: (value: T['data']) => T['driverData'] | SQL;
+    /**
+     * Maps a TypeScript value to what a row format (`JSONEachRow`) should carry.
+     *
+     * Needed when {@link toDriver} builds an expression rather than returning a plain value — a body
+     * has nowhere to put `toIPv6('…')`. Without it the value is sent untouched and the column's own
+     * type does the parsing, which is right for most custom types and quietly wrong for the rest.
+     */
+    toRow?: (value: T['data']) => unknown;
+    /** Maps what ClickHouse returns back to a TypeScript value. */
+    fromDriver?: (value: T['driverData']) => T['data'];
+}
+/**
+ * Defines a column type Drizzle does not ship out of the box.
+ *
+ * ```ts
+ * const point = customType<{ data: [number, number]; driverData: string }>({
+ * 	dataType: () => 'Point',
+ * 	toDriver: ([x, y]) => sql`(${x}, ${y})`,
+ * 	fromDriver: (value) => JSON.parse(value),
+ * });
+ * ```
+ */
+export declare function customType<T extends CustomTypeValues = CustomTypeValues>(customTypeParams: CustomTypeParams<T>): Equal<T['configRequired'], true> extends true ? {
+    <TConfig extends Record<string, any> & T['config']>(fieldConfig: TConfig): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TName extends string>(dbName: TName, fieldConfig: T['config']): ClickHouseCustomColumnBuilder<ConvertCustomConfig<TName, T>>;
+} : {
+    (): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TConfig extends Record<string, any> & T['config']>(fieldConfig?: TConfig): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TName extends string>(dbName: TName, fieldConfig?: T['config']): ClickHouseCustomColumnBuilder<ConvertCustomConfig<TName, T>>;
+};
diff --git a/clickhouse-core/columns/custom.d.ts b/clickhouse-core/columns/custom.d.ts
new file mode 100644
index 0000000..555f19a
--- /dev/null
+++ b/clickhouse-core/columns/custom.d.ts
@@ -0,0 +1,111 @@
+import type { AnyClickHouseTable } from '../table.js';
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import { SQL } from '../../sql/sql.js';
+import type { Equal } from '../../utils.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ConvertCustomConfig<TName extends string, T extends Partial<CustomTypeValues>> = {
+    name: TName;
+    dataType: 'custom';
+    columnType: 'ClickHouseCustomColumn';
+    data: T['data'];
+    driverParam: T['driverData'];
+    enumValues: undefined;
+} & (T['notNull'] extends true ? {
+    notNull: true;
+} : {}) & (T['default'] extends true ? {
+    hasDefault: true;
+} : {});
+export interface ClickHouseCustomColumnInnerConfig {
+    customTypeParams: CustomTypeParams<any>;
+}
+export declare class ClickHouseCustomColumnBuilder<T extends ColumnBuilderBaseConfig<'custom', 'ClickHouseCustomColumn'>> extends ClickHouseColumnBuilder<T, {
+    fieldConfig: CustomTypeValues['config'];
+    customTypeParams: CustomTypeParams<any>;
+}, {
+    clickhouseColumnBuilderBrand: 'ClickHouseCustomColumnBuilderBrand';
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], fieldConfig: CustomTypeValues['config'], customTypeParams: CustomTypeParams<any>);
+}
+export declare class ClickHouseCustomColumn<T extends ColumnBaseConfig<'custom', 'ClickHouseCustomColumn'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    private sqlName;
+    private mapTo?;
+    private mapRow?;
+    private mapFrom?;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ClickHouseCustomColumnBuilder<T>['config']);
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: T['driverParam']): T['data'];
+    mapToDriverValue(value: T['data']): T['driverParam'] | SQL;
+    /**
+     * `toRow` when the type declares one, otherwise `toDriver` — unless `toDriver` returned SQL, which
+     * a row format has nowhere to put, in which case the value passes through untouched.
+     *
+     * A custom type whose `toDriver` builds an expression (`toIPv6('…')`, `map(…)`) and whose data is
+     * not already its own JSON form needs `toRow`; without it the fallback would send the raw value
+     * and let the server's column type decide, which is right often enough to be worth doing and
+     * wrong quietly enough to be worth saying out loud.
+     */
+    mapToRowValue(value: T['data']): unknown;
+}
+export type CustomTypeValues = {
+    /** The TypeScript type this column reads and writes. */
+    data: unknown;
+    /**
+     * The value handed to and received from the driver.
+     *
+     * Returning a {@link SQL} from `toDriver` is the way to emit a ClickHouse function call such as
+     * `toIPv6('…')` rather than a bare literal.
+     */
+    driverData?: unknown;
+    /** Extra options accepted by the type, surfaced to `dataType()`. */
+    config?: Record<string, any>;
+    /** Set to `true` to make `config` required. */
+    configRequired?: boolean;
+    /** Set to `true` to have the type imply `.notNull()`. */
+    notNull?: boolean;
+    /** Set to `true` to have the type imply a default. */
+    default?: boolean;
+};
+export interface CustomTypeParams<T extends CustomTypeValues> {
+    /**
+     * Returns the ClickHouse type as it should appear in DDL, e.g. `Point` or
+     * `AggregateFunction(sum, UInt64)`.
+     */
+    dataType: (config: T['config'] | (Equal<T['configRequired'], true> extends true ? never : undefined)) => string;
+    /** Maps a TypeScript value to the SQL literal a statement should carry. */
+    toDriver?: (value: T['data']) => T['driverData'] | SQL;
+    /**
+     * Maps a TypeScript value to what a row format (`JSONEachRow`) should carry.
+     *
+     * Needed when {@link toDriver} builds an expression rather than returning a plain value — a body
+     * has nowhere to put `toIPv6('…')`. Without it the value is sent untouched and the column's own
+     * type does the parsing, which is right for most custom types and quietly wrong for the rest.
+     */
+    toRow?: (value: T['data']) => unknown;
+    /** Maps what ClickHouse returns back to a TypeScript value. */
+    fromDriver?: (value: T['driverData']) => T['data'];
+}
+/**
+ * Defines a column type Drizzle does not ship out of the box.
+ *
+ * ```ts
+ * const point = customType<{ data: [number, number]; driverData: string }>({
+ * 	dataType: () => 'Point',
+ * 	toDriver: ([x, y]) => sql`(${x}, ${y})`,
+ * 	fromDriver: (value) => JSON.parse(value),
+ * });
+ * ```
+ */
+export declare function customType<T extends CustomTypeValues = CustomTypeValues>(customTypeParams: CustomTypeParams<T>): Equal<T['configRequired'], true> extends true ? {
+    <TConfig extends Record<string, any> & T['config']>(fieldConfig: TConfig): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TName extends string>(dbName: TName, fieldConfig: T['config']): ClickHouseCustomColumnBuilder<ConvertCustomConfig<TName, T>>;
+} : {
+    (): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TConfig extends Record<string, any> & T['config']>(fieldConfig?: TConfig): ClickHouseCustomColumnBuilder<ConvertCustomConfig<'', T>>;
+    <TName extends string>(dbName: TName, fieldConfig?: T['config']): ClickHouseCustomColumnBuilder<ConvertCustomConfig<TName, T>>;
+};
diff --git a/clickhouse-core/columns/custom.js b/clickhouse-core/columns/custom.js
new file mode 100644
index 0000000..38507dc
--- /dev/null
+++ b/clickhouse-core/columns/custom.js
@@ -0,0 +1,68 @@
+import { entityKind, is } from "../../entity.js";
+import { SQL } from "../../sql/sql.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseCustomColumnBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseCustomColumnBuilder";
+  constructor(name, fieldConfig, customTypeParams) {
+    super(name, "custom", "ClickHouseCustomColumn");
+    this.config.fieldConfig = fieldConfig;
+    this.config.customTypeParams = customTypeParams;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseCustomColumn(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseCustomColumn extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseCustomColumn";
+  sqlName;
+  mapTo;
+  mapRow;
+  mapFrom;
+  constructor(table, config) {
+    super(table, config);
+    this.sqlName = config.customTypeParams.dataType(config.fieldConfig);
+    this.mapTo = config.customTypeParams.toDriver;
+    this.mapRow = config.customTypeParams.toRow;
+    this.mapFrom = config.customTypeParams.fromDriver;
+  }
+  getBaseSQLType() {
+    return this.sqlName;
+  }
+  mapFromDriverValue(value) {
+    return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
+  }
+  mapToDriverValue(value) {
+    return typeof this.mapTo === "function" ? this.mapTo(value) : value;
+  }
+  /**
+   * `toRow` when the type declares one, otherwise `toDriver` — unless `toDriver` returned SQL, which
+   * a row format has nowhere to put, in which case the value passes through untouched.
+   *
+   * A custom type whose `toDriver` builds an expression (`toIPv6('…')`, `map(…)`) and whose data is
+   * not already its own JSON form needs `toRow`; without it the fallback would send the raw value
+   * and let the server's column type decide, which is right often enough to be worth doing and
+   * wrong quietly enough to be worth saying out loud.
+   */
+  mapToRowValue(value) {
+    if (typeof this.mapRow === "function") return this.mapRow(value);
+    if (typeof this.mapTo !== "function") return value;
+    const mapped = this.mapTo(value);
+    return is(mapped, SQL) ? value : mapped;
+  }
+}
+function customType(customTypeParams) {
+  return ((a, b) => {
+    const { name, config } = getColumnNameAndConfig(a, b);
+    return new ClickHouseCustomColumnBuilder(name, config, customTypeParams);
+  });
+}
+export {
+  ClickHouseCustomColumn,
+  ClickHouseCustomColumnBuilder,
+  customType
+};
diff --git a/clickhouse-core/columns/date.cjs b/clickhouse-core/columns/date.cjs
new file mode 100644
index 0000000..8723cce
--- /dev/null
+++ b/clickhouse-core/columns/date.cjs
@@ -0,0 +1,80 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var date_exports = {};
+__export(date_exports, {
+  ClickHouseDate: () => ClickHouseDate,
+  ClickHouseDateBuilder: () => ClickHouseDateBuilder,
+  date: () => date,
+  date32: () => date32
+});
+module.exports = __toCommonJS(date_exports);
+var import_entity = require("../../entity.cjs");
+var import_utils = require("../../utils.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+var import_date_common = require("./date.common.cjs");
+class ClickHouseDateBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseDateBuilder";
+  constructor(name, columnType, chType, mode) {
+    super(name, mode === "string" ? "string" : "date", columnType);
+    this.config.chType = chType;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDate(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDate extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseDate";
+  chType = this.config.chType;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "string" ? value : (0, import_date_common.parseClickHouseDate)(value);
+  }
+  mapToDriverValue(value) {
+    const text = typeof value === "string" ? value : (0, import_date_common.formatClickHouseDate)(value);
+    return (0, import_literals.castFromString)(this.chType === "Date32" ? "toDate32" : "toDate", text);
+  }
+  mapToRowValue(value) {
+    return typeof value === "string" ? value : (0, import_date_common.formatClickHouseDate)(value);
+  }
+}
+function dateFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+    return new ClickHouseDateBuilder(name, columnType, chType, config?.mode ?? "date");
+  }
+  return column;
+}
+const date = dateFactory("ClickHouseDate", "Date");
+const date32 = dateFactory("ClickHouseDate32", "Date32");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDate,
+  ClickHouseDateBuilder,
+  date,
+  date32
+});
diff --git a/clickhouse-core/columns/date.common.cjs b/clickhouse-core/columns/date.common.cjs
new file mode 100644
index 0000000..3927042
--- /dev/null
+++ b/clickhouse-core/columns/date.common.cjs
@@ -0,0 +1,56 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var date_common_exports = {};
+__export(date_common_exports, {
+  formatClickHouseDate: () => formatClickHouseDate,
+  formatClickHouseDateTime: () => formatClickHouseDateTime,
+  parseClickHouseDate: () => parseClickHouseDate,
+  parseClickHouseDateTime: () => parseClickHouseDateTime
+});
+module.exports = __toCommonJS(date_common_exports);
+function pad(value, length = 2) {
+  return String(value).padStart(length, "0");
+}
+function formatClickHouseDate(value) {
+  return `${value.getUTCFullYear()}-${pad(value.getUTCMonth() + 1)}-${pad(value.getUTCDate())}`;
+}
+function formatClickHouseDateTime(value, precision = 0) {
+  const base = `${formatClickHouseDate(value)} ${pad(value.getUTCHours())}:${pad(value.getUTCMinutes())}:${pad(value.getUTCSeconds())}`;
+  if (precision === 0) return base;
+  const fraction = pad(value.getUTCMilliseconds(), 3).slice(0, Math.min(precision, 3)).padEnd(precision, "0");
+  return `${base}.${fraction}`;
+}
+const ISO_LIKE = /[Tt]/;
+const HAS_ZONE = /(?:[Zz]|[+-]\d{2}:?\d{2})$/;
+function parseClickHouseDateTime(value) {
+  if (ISO_LIKE.test(value)) {
+    return new Date(HAS_ZONE.test(value) ? value : `${value}Z`);
+  }
+  return /* @__PURE__ */ new Date(`${value.replace(" ", "T")}Z`);
+}
+function parseClickHouseDate(value) {
+  return /* @__PURE__ */ new Date(`${value}T00:00:00Z`);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  formatClickHouseDate,
+  formatClickHouseDateTime,
+  parseClickHouseDate,
+  parseClickHouseDateTime
+});
diff --git a/clickhouse-core/columns/date.common.d.cts b/clickhouse-core/columns/date.common.d.cts
new file mode 100644
index 0000000..9047ce9
--- /dev/null
+++ b/clickhouse-core/columns/date.common.d.cts
@@ -0,0 +1,24 @@
+/**
+ * How a date/time column is surfaced in TypeScript.
+ *
+ * - `date` (the default) hands back a JavaScript `Date`.
+ * - `string` passes ClickHouse's textual form through untouched, which avoids the round-trip through
+ *   `Date` for callers that only ever forward the value on.
+ */
+export type ClickHouseDateMode = 'date' | 'string';
+/** Renders a `Date` as ClickHouse's `YYYY-MM-DD`, in UTC. */
+export declare function formatClickHouseDate(value: Date): string;
+/** Renders a `Date` as ClickHouse's `YYYY-MM-DD hh:mm:ss[.fff]`, in UTC. */
+export declare function formatClickHouseDateTime(value: Date, precision?: number): string;
+/**
+ * Parses the textual date/time ClickHouse returns.
+ *
+ * The driver asks for `date_time_output_format = 'iso'`, so values normally arrive zone-qualified and
+ * are unambiguous. When a caller has overridden that setting ClickHouse falls back to the bare
+ * `YYYY-MM-DD hh:mm:ss` form, which is rendered in the column's own timezone — we read it as UTC,
+ * which is correct for the `UTC`-typed and untyped-but-UTC-server cases and is why declaring a
+ * timezone on the column is worth doing.
+ */
+export declare function parseClickHouseDateTime(value: string): Date;
+/** Parses ClickHouse's `YYYY-MM-DD` into a `Date` pinned to midnight UTC. */
+export declare function parseClickHouseDate(value: string): Date;
diff --git a/clickhouse-core/columns/date.common.d.ts b/clickhouse-core/columns/date.common.d.ts
new file mode 100644
index 0000000..9047ce9
--- /dev/null
+++ b/clickhouse-core/columns/date.common.d.ts
@@ -0,0 +1,24 @@
+/**
+ * How a date/time column is surfaced in TypeScript.
+ *
+ * - `date` (the default) hands back a JavaScript `Date`.
+ * - `string` passes ClickHouse's textual form through untouched, which avoids the round-trip through
+ *   `Date` for callers that only ever forward the value on.
+ */
+export type ClickHouseDateMode = 'date' | 'string';
+/** Renders a `Date` as ClickHouse's `YYYY-MM-DD`, in UTC. */
+export declare function formatClickHouseDate(value: Date): string;
+/** Renders a `Date` as ClickHouse's `YYYY-MM-DD hh:mm:ss[.fff]`, in UTC. */
+export declare function formatClickHouseDateTime(value: Date, precision?: number): string;
+/**
+ * Parses the textual date/time ClickHouse returns.
+ *
+ * The driver asks for `date_time_output_format = 'iso'`, so values normally arrive zone-qualified and
+ * are unambiguous. When a caller has overridden that setting ClickHouse falls back to the bare
+ * `YYYY-MM-DD hh:mm:ss` form, which is rendered in the column's own timezone — we read it as UTC,
+ * which is correct for the `UTC`-typed and untyped-but-UTC-server cases and is why declaring a
+ * timezone on the column is worth doing.
+ */
+export declare function parseClickHouseDateTime(value: string): Date;
+/** Parses ClickHouse's `YYYY-MM-DD` into a `Date` pinned to midnight UTC. */
+export declare function parseClickHouseDate(value: string): Date;
diff --git a/clickhouse-core/columns/date.common.js b/clickhouse-core/columns/date.common.js
new file mode 100644
index 0000000..7cbd92d
--- /dev/null
+++ b/clickhouse-core/columns/date.common.js
@@ -0,0 +1,29 @@
+function pad(value, length = 2) {
+  return String(value).padStart(length, "0");
+}
+function formatClickHouseDate(value) {
+  return `${value.getUTCFullYear()}-${pad(value.getUTCMonth() + 1)}-${pad(value.getUTCDate())}`;
+}
+function formatClickHouseDateTime(value, precision = 0) {
+  const base = `${formatClickHouseDate(value)} ${pad(value.getUTCHours())}:${pad(value.getUTCMinutes())}:${pad(value.getUTCSeconds())}`;
+  if (precision === 0) return base;
+  const fraction = pad(value.getUTCMilliseconds(), 3).slice(0, Math.min(precision, 3)).padEnd(precision, "0");
+  return `${base}.${fraction}`;
+}
+const ISO_LIKE = /[Tt]/;
+const HAS_ZONE = /(?:[Zz]|[+-]\d{2}:?\d{2})$/;
+function parseClickHouseDateTime(value) {
+  if (ISO_LIKE.test(value)) {
+    return new Date(HAS_ZONE.test(value) ? value : `${value}Z`);
+  }
+  return /* @__PURE__ */ new Date(`${value.replace(" ", "T")}Z`);
+}
+function parseClickHouseDate(value) {
+  return /* @__PURE__ */ new Date(`${value}T00:00:00Z`);
+}
+export {
+  formatClickHouseDate,
+  formatClickHouseDateTime,
+  parseClickHouseDate,
+  parseClickHouseDateTime
+};
diff --git a/clickhouse-core/columns/date.d.cts b/clickhouse-core/columns/date.d.cts
new file mode 100644
index 0000000..632b4d3
--- /dev/null
+++ b/clickhouse-core/columns/date.d.cts
@@ -0,0 +1,48 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+import { type ClickHouseDateMode } from './date.common.cjs';
+export type ClickHouseDateTypeName = 'Date' | 'Date32';
+export interface ClickHouseDateConfig<TMode extends ClickHouseDateMode = ClickHouseDateMode> {
+    mode?: TMode;
+}
+interface ClickHouseDateRuntimeConfig {
+    chType: ClickHouseDateTypeName;
+    mode: ClickHouseDateMode;
+}
+export type ClickHouseDateBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDateMode> = ClickHouseDateBuilder<{
+    name: TName;
+    dataType: TMode extends 'string' ? 'string' : 'date';
+    columnType: TColumnType;
+    data: TMode extends 'string' ? string : Date;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDateBuilder<T extends ColumnBuilderBaseConfig<'date' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseDateRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseDateTypeName, mode: ClickHouseDateMode);
+}
+export declare class ClickHouseDate<T extends ColumnBaseConfig<'date' | 'string', string>> extends ClickHouseColumn<T, ClickHouseDateRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseDateTypeName;
+    readonly mode: ClickHouseDateMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string): Date | string;
+    mapToDriverValue(value: Date | string): SQL;
+    mapToRowValue(value: Date | string): string;
+}
+/** `Date` — a two-byte calendar date, limited to the range `1970-01-01` … `2149-06-06`. */
+export declare const date: {
+    (): ClickHouseDateBuilderInitial<"", "ClickHouseDate", "date">;
+    <TMode extends ClickHouseDateMode = "date">(config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<"", "ClickHouseDate", TMode>;
+    <TName extends string, TMode extends ClickHouseDateMode = "date">(name: TName, config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<TName, "ClickHouseDate", TMode>;
+};
+/** `Date32` — a four-byte calendar date covering `1900-01-01` … `2299-12-31`. */
+export declare const date32: {
+    (): ClickHouseDateBuilderInitial<"", "ClickHouseDate32", "date">;
+    <TMode extends ClickHouseDateMode = "date">(config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<"", "ClickHouseDate32", TMode>;
+    <TName extends string, TMode extends ClickHouseDateMode = "date">(name: TName, config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<TName, "ClickHouseDate32", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/date.d.ts b/clickhouse-core/columns/date.d.ts
new file mode 100644
index 0000000..977cc5b
--- /dev/null
+++ b/clickhouse-core/columns/date.d.ts
@@ -0,0 +1,48 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+import { type ClickHouseDateMode } from './date.common.js';
+export type ClickHouseDateTypeName = 'Date' | 'Date32';
+export interface ClickHouseDateConfig<TMode extends ClickHouseDateMode = ClickHouseDateMode> {
+    mode?: TMode;
+}
+interface ClickHouseDateRuntimeConfig {
+    chType: ClickHouseDateTypeName;
+    mode: ClickHouseDateMode;
+}
+export type ClickHouseDateBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDateMode> = ClickHouseDateBuilder<{
+    name: TName;
+    dataType: TMode extends 'string' ? 'string' : 'date';
+    columnType: TColumnType;
+    data: TMode extends 'string' ? string : Date;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDateBuilder<T extends ColumnBuilderBaseConfig<'date' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseDateRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseDateTypeName, mode: ClickHouseDateMode);
+}
+export declare class ClickHouseDate<T extends ColumnBaseConfig<'date' | 'string', string>> extends ClickHouseColumn<T, ClickHouseDateRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseDateTypeName;
+    readonly mode: ClickHouseDateMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string): Date | string;
+    mapToDriverValue(value: Date | string): SQL;
+    mapToRowValue(value: Date | string): string;
+}
+/** `Date` — a two-byte calendar date, limited to the range `1970-01-01` … `2149-06-06`. */
+export declare const date: {
+    (): ClickHouseDateBuilderInitial<"", "ClickHouseDate", "date">;
+    <TMode extends ClickHouseDateMode = "date">(config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<"", "ClickHouseDate", TMode>;
+    <TName extends string, TMode extends ClickHouseDateMode = "date">(name: TName, config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<TName, "ClickHouseDate", TMode>;
+};
+/** `Date32` — a four-byte calendar date covering `1900-01-01` … `2299-12-31`. */
+export declare const date32: {
+    (): ClickHouseDateBuilderInitial<"", "ClickHouseDate32", "date">;
+    <TMode extends ClickHouseDateMode = "date">(config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<"", "ClickHouseDate32", TMode>;
+    <TName extends string, TMode extends ClickHouseDateMode = "date">(name: TName, config?: ClickHouseDateConfig<TMode> | undefined): ClickHouseDateBuilderInitial<TName, "ClickHouseDate32", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/date.js b/clickhouse-core/columns/date.js
new file mode 100644
index 0000000..892cb4b
--- /dev/null
+++ b/clickhouse-core/columns/date.js
@@ -0,0 +1,53 @@
+import { entityKind } from "../../entity.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { castFromString } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+import { formatClickHouseDate, parseClickHouseDate } from "./date.common.js";
+class ClickHouseDateBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseDateBuilder";
+  constructor(name, columnType, chType, mode) {
+    super(name, mode === "string" ? "string" : "date", columnType);
+    this.config.chType = chType;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDate(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDate extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseDate";
+  chType = this.config.chType;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "string" ? value : parseClickHouseDate(value);
+  }
+  mapToDriverValue(value) {
+    const text = typeof value === "string" ? value : formatClickHouseDate(value);
+    return castFromString(this.chType === "Date32" ? "toDate32" : "toDate", text);
+  }
+  mapToRowValue(value) {
+    return typeof value === "string" ? value : formatClickHouseDate(value);
+  }
+}
+function dateFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = getColumnNameAndConfig(a, b);
+    return new ClickHouseDateBuilder(name, columnType, chType, config?.mode ?? "date");
+  }
+  return column;
+}
+const date = dateFactory("ClickHouseDate", "Date");
+const date32 = dateFactory("ClickHouseDate32", "Date32");
+export {
+  ClickHouseDate,
+  ClickHouseDateBuilder,
+  date,
+  date32
+};
diff --git a/clickhouse-core/columns/datetime.cjs b/clickhouse-core/columns/datetime.cjs
new file mode 100644
index 0000000..77c40d2
--- /dev/null
+++ b/clickhouse-core/columns/datetime.cjs
@@ -0,0 +1,108 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var datetime_exports = {};
+__export(datetime_exports, {
+  ClickHouseDateTime: () => ClickHouseDateTime,
+  ClickHouseDateTimeBuilder: () => ClickHouseDateTimeBuilder,
+  dateTime: () => dateTime,
+  dateTime64: () => dateTime64
+});
+module.exports = __toCommonJS(datetime_exports);
+var import_entity = require("../../entity.cjs");
+var import_utils = require("../../utils.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+var import_date_common = require("./date.common.cjs");
+class ClickHouseDateTimeBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseDateTimeBuilder";
+  constructor(name, columnType, precision, timezone, mode) {
+    super(name, mode === "string" ? "string" : "date", columnType);
+    this.config.precision = precision;
+    this.config.timezone = timezone;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDateTime(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDateTime extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseDateTime";
+  precision = this.config.precision;
+  timezone = this.config.timezone;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    const args = [];
+    if (this.precision !== void 0) args.push(String(this.precision));
+    if (this.timezone !== void 0) args.push(`'${this.timezone}'`);
+    if (this.precision === void 0) {
+      return args.length > 0 ? `DateTime(${args.join(", ")})` : "DateTime";
+    }
+    return `DateTime64(${args.join(", ")})`;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "string" ? value : (0, import_date_common.parseClickHouseDateTime)(value);
+  }
+  mapToDriverValue(value) {
+    const text = typeof value === "string" ? value : (0, import_date_common.formatClickHouseDateTime)(value, this.precision ?? 0);
+    return this.precision === void 0 ? (0, import_literals.castFromString)("toDateTime", text, "UTC") : (0, import_literals.castFromString)("toDateTime64", text, this.precision, "UTC");
+  }
+  /**
+   * The textual form without the cast — a row format has no expression to build, and the server
+   * parses it against the column's own type.
+   *
+   * **A column with no declared timezone is parsed in the server's**, so an untyped `DateTime`
+   * written this way lands wherever the deployment happens to be configured. That is the hazard the
+   * literal path sidesteps by passing `'UTC'` explicitly, and it cannot be sidestepped here — one
+   * more reason to declare a timezone on the column.
+   */
+  mapToRowValue(value) {
+    return typeof value === "string" ? value : (0, import_date_common.formatClickHouseDateTime)(value, this.precision ?? 0);
+  }
+}
+function dateTime(a, b) {
+  const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+  return new ClickHouseDateTimeBuilder(
+    name,
+    "ClickHouseDateTime",
+    void 0,
+    config?.timezone,
+    config?.mode ?? "date"
+  );
+}
+function dateTime64(a, b) {
+  const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+  return new ClickHouseDateTimeBuilder(
+    name,
+    "ClickHouseDateTime64",
+    config?.precision ?? 3,
+    config?.timezone,
+    config?.mode ?? "date"
+  );
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDateTime,
+  ClickHouseDateTimeBuilder,
+  dateTime,
+  dateTime64
+});
diff --git a/clickhouse-core/columns/datetime.d.cts b/clickhouse-core/columns/datetime.d.cts
new file mode 100644
index 0000000..eb4b0d0
--- /dev/null
+++ b/clickhouse-core/columns/datetime.d.cts
@@ -0,0 +1,77 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+import { type ClickHouseDateMode } from './date.common.cjs';
+export interface ClickHouseDateTimeConfig<TMode extends ClickHouseDateMode = ClickHouseDateMode> {
+    /**
+     * An IANA timezone name, e.g. `'UTC'` or `'Europe/Berlin'`.
+     *
+     * Declaring one is strongly recommended: an untyped `DateTime` is rendered in whatever timezone
+     * the *server* is configured with, which makes the stored value depend on deployment config.
+     */
+    timezone?: string;
+    mode?: TMode;
+}
+export interface ClickHouseDateTime64Config<TMode extends ClickHouseDateMode = ClickHouseDateMode> extends ClickHouseDateTimeConfig<TMode> {
+    /**
+     * Digits of sub-second precision, `0`–`9`. Defaults to `3` (milliseconds), which is as fine as a
+     * JavaScript `Date` can represent — higher precisions round-trip correctly only in `string` mode.
+     */
+    precision?: number;
+}
+interface ClickHouseDateTimeRuntimeConfig {
+    precision: number | undefined;
+    timezone: string | undefined;
+    mode: ClickHouseDateMode;
+}
+export type ClickHouseDateTimeBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDateMode> = ClickHouseDateTimeBuilder<{
+    name: TName;
+    dataType: TMode extends 'string' ? 'string' : 'date';
+    columnType: TColumnType;
+    data: TMode extends 'string' ? string : Date;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDateTimeBuilder<T extends ColumnBuilderBaseConfig<'date' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseDateTimeRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], precision: number | undefined, timezone: string | undefined, mode: ClickHouseDateMode);
+}
+export declare class ClickHouseDateTime<T extends ColumnBaseConfig<'date' | 'string', string>> extends ClickHouseColumn<T, ClickHouseDateTimeRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly precision: number | undefined;
+    readonly timezone: string | undefined;
+    readonly mode: ClickHouseDateMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string): Date | string;
+    mapToDriverValue(value: Date | string): SQL;
+    /**
+     * The textual form without the cast — a row format has no expression to build, and the server
+     * parses it against the column's own type.
+     *
+     * **A column with no declared timezone is parsed in the server's**, so an untyped `DateTime`
+     * written this way lands wherever the deployment happens to be configured. That is the hazard the
+     * literal path sidesteps by passing `'UTC'` explicitly, and it cannot be sidestepped here — one
+     * more reason to declare a timezone on the column.
+     */
+    mapToRowValue(value: Date | string): string;
+}
+/**
+ * `DateTime([timezone])` — a second-resolution timestamp stored as a 4-byte Unix time.
+ *
+ * Pass a `timezone` unless you are certain of the server's; it determines how the value is rendered
+ * on read and interpreted when written as text.
+ */
+export declare function dateTime(): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime', 'date'>;
+export declare function dateTime<TMode extends ClickHouseDateMode = 'date'>(config?: ClickHouseDateTimeConfig<TMode>): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime', TMode>;
+export declare function dateTime<TName extends string, TMode extends ClickHouseDateMode = 'date'>(name: TName, config?: ClickHouseDateTimeConfig<TMode>): ClickHouseDateTimeBuilderInitial<TName, 'ClickHouseDateTime', TMode>;
+/**
+ * `DateTime64(precision, [timezone])` — a sub-second-resolution timestamp.
+ *
+ * Defaults to millisecond precision, matching what a JavaScript `Date` can hold.
+ */
+export declare function dateTime64(): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime64', 'date'>;
+export declare function dateTime64<TMode extends ClickHouseDateMode = 'date'>(config?: ClickHouseDateTime64Config<TMode>): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime64', TMode>;
+export declare function dateTime64<TName extends string, TMode extends ClickHouseDateMode = 'date'>(name: TName, config?: ClickHouseDateTime64Config<TMode>): ClickHouseDateTimeBuilderInitial<TName, 'ClickHouseDateTime64', TMode>;
+export {};
diff --git a/clickhouse-core/columns/datetime.d.ts b/clickhouse-core/columns/datetime.d.ts
new file mode 100644
index 0000000..e5acd6a
--- /dev/null
+++ b/clickhouse-core/columns/datetime.d.ts
@@ -0,0 +1,77 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+import { type ClickHouseDateMode } from './date.common.js';
+export interface ClickHouseDateTimeConfig<TMode extends ClickHouseDateMode = ClickHouseDateMode> {
+    /**
+     * An IANA timezone name, e.g. `'UTC'` or `'Europe/Berlin'`.
+     *
+     * Declaring one is strongly recommended: an untyped `DateTime` is rendered in whatever timezone
+     * the *server* is configured with, which makes the stored value depend on deployment config.
+     */
+    timezone?: string;
+    mode?: TMode;
+}
+export interface ClickHouseDateTime64Config<TMode extends ClickHouseDateMode = ClickHouseDateMode> extends ClickHouseDateTimeConfig<TMode> {
+    /**
+     * Digits of sub-second precision, `0`–`9`. Defaults to `3` (milliseconds), which is as fine as a
+     * JavaScript `Date` can represent — higher precisions round-trip correctly only in `string` mode.
+     */
+    precision?: number;
+}
+interface ClickHouseDateTimeRuntimeConfig {
+    precision: number | undefined;
+    timezone: string | undefined;
+    mode: ClickHouseDateMode;
+}
+export type ClickHouseDateTimeBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDateMode> = ClickHouseDateTimeBuilder<{
+    name: TName;
+    dataType: TMode extends 'string' ? 'string' : 'date';
+    columnType: TColumnType;
+    data: TMode extends 'string' ? string : Date;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDateTimeBuilder<T extends ColumnBuilderBaseConfig<'date' | 'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseDateTimeRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], precision: number | undefined, timezone: string | undefined, mode: ClickHouseDateMode);
+}
+export declare class ClickHouseDateTime<T extends ColumnBaseConfig<'date' | 'string', string>> extends ClickHouseColumn<T, ClickHouseDateTimeRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly precision: number | undefined;
+    readonly timezone: string | undefined;
+    readonly mode: ClickHouseDateMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string): Date | string;
+    mapToDriverValue(value: Date | string): SQL;
+    /**
+     * The textual form without the cast — a row format has no expression to build, and the server
+     * parses it against the column's own type.
+     *
+     * **A column with no declared timezone is parsed in the server's**, so an untyped `DateTime`
+     * written this way lands wherever the deployment happens to be configured. That is the hazard the
+     * literal path sidesteps by passing `'UTC'` explicitly, and it cannot be sidestepped here — one
+     * more reason to declare a timezone on the column.
+     */
+    mapToRowValue(value: Date | string): string;
+}
+/**
+ * `DateTime([timezone])` — a second-resolution timestamp stored as a 4-byte Unix time.
+ *
+ * Pass a `timezone` unless you are certain of the server's; it determines how the value is rendered
+ * on read and interpreted when written as text.
+ */
+export declare function dateTime(): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime', 'date'>;
+export declare function dateTime<TMode extends ClickHouseDateMode = 'date'>(config?: ClickHouseDateTimeConfig<TMode>): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime', TMode>;
+export declare function dateTime<TName extends string, TMode extends ClickHouseDateMode = 'date'>(name: TName, config?: ClickHouseDateTimeConfig<TMode>): ClickHouseDateTimeBuilderInitial<TName, 'ClickHouseDateTime', TMode>;
+/**
+ * `DateTime64(precision, [timezone])` — a sub-second-resolution timestamp.
+ *
+ * Defaults to millisecond precision, matching what a JavaScript `Date` can hold.
+ */
+export declare function dateTime64(): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime64', 'date'>;
+export declare function dateTime64<TMode extends ClickHouseDateMode = 'date'>(config?: ClickHouseDateTime64Config<TMode>): ClickHouseDateTimeBuilderInitial<'', 'ClickHouseDateTime64', TMode>;
+export declare function dateTime64<TName extends string, TMode extends ClickHouseDateMode = 'date'>(name: TName, config?: ClickHouseDateTime64Config<TMode>): ClickHouseDateTimeBuilderInitial<TName, 'ClickHouseDateTime64', TMode>;
+export {};
diff --git a/clickhouse-core/columns/datetime.js b/clickhouse-core/columns/datetime.js
new file mode 100644
index 0000000..7d32791
--- /dev/null
+++ b/clickhouse-core/columns/datetime.js
@@ -0,0 +1,81 @@
+import { entityKind } from "../../entity.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { castFromString } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+import { formatClickHouseDateTime, parseClickHouseDateTime } from "./date.common.js";
+class ClickHouseDateTimeBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseDateTimeBuilder";
+  constructor(name, columnType, precision, timezone, mode) {
+    super(name, mode === "string" ? "string" : "date", columnType);
+    this.config.precision = precision;
+    this.config.timezone = timezone;
+    this.config.mode = mode;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDateTime(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDateTime extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseDateTime";
+  precision = this.config.precision;
+  timezone = this.config.timezone;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    const args = [];
+    if (this.precision !== void 0) args.push(String(this.precision));
+    if (this.timezone !== void 0) args.push(`'${this.timezone}'`);
+    if (this.precision === void 0) {
+      return args.length > 0 ? `DateTime(${args.join(", ")})` : "DateTime";
+    }
+    return `DateTime64(${args.join(", ")})`;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "string" ? value : parseClickHouseDateTime(value);
+  }
+  mapToDriverValue(value) {
+    const text = typeof value === "string" ? value : formatClickHouseDateTime(value, this.precision ?? 0);
+    return this.precision === void 0 ? castFromString("toDateTime", text, "UTC") : castFromString("toDateTime64", text, this.precision, "UTC");
+  }
+  /**
+   * The textual form without the cast — a row format has no expression to build, and the server
+   * parses it against the column's own type.
+   *
+   * **A column with no declared timezone is parsed in the server's**, so an untyped `DateTime`
+   * written this way lands wherever the deployment happens to be configured. That is the hazard the
+   * literal path sidesteps by passing `'UTC'` explicitly, and it cannot be sidestepped here — one
+   * more reason to declare a timezone on the column.
+   */
+  mapToRowValue(value) {
+    return typeof value === "string" ? value : formatClickHouseDateTime(value, this.precision ?? 0);
+  }
+}
+function dateTime(a, b) {
+  const { name, config } = getColumnNameAndConfig(a, b);
+  return new ClickHouseDateTimeBuilder(
+    name,
+    "ClickHouseDateTime",
+    void 0,
+    config?.timezone,
+    config?.mode ?? "date"
+  );
+}
+function dateTime64(a, b) {
+  const { name, config } = getColumnNameAndConfig(a, b);
+  return new ClickHouseDateTimeBuilder(
+    name,
+    "ClickHouseDateTime64",
+    config?.precision ?? 3,
+    config?.timezone,
+    config?.mode ?? "date"
+  );
+}
+export {
+  ClickHouseDateTime,
+  ClickHouseDateTimeBuilder,
+  dateTime,
+  dateTime64
+};
diff --git a/clickhouse-core/columns/decimal.cjs b/clickhouse-core/columns/decimal.cjs
new file mode 100644
index 0000000..0da7a8e
--- /dev/null
+++ b/clickhouse-core/columns/decimal.cjs
@@ -0,0 +1,105 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var decimal_exports = {};
+__export(decimal_exports, {
+  ClickHouseDecimal: () => ClickHouseDecimal,
+  ClickHouseDecimalBuilder: () => ClickHouseDecimalBuilder,
+  decimal: () => decimal,
+  decimal128: () => decimal128,
+  decimal256: () => decimal256,
+  decimal32: () => decimal32,
+  decimal64: () => decimal64
+});
+module.exports = __toCommonJS(decimal_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_utils = require("../../utils.cjs");
+var import_common = require("./common.cjs");
+const IMPLIED_PRECISION = {
+  Decimal32: 9,
+  Decimal64: 18,
+  Decimal128: 38,
+  Decimal256: 76
+};
+class ClickHouseDecimalBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseDecimalBuilder";
+  constructor(name, columnType, chType, config) {
+    super(name, config?.mode === "number" ? "number" : "string", columnType);
+    this.config.chType = chType;
+    this.config.precision = chType === "Decimal" ? config?.precision ?? 10 : void 0;
+    this.config.scale = config?.scale ?? 0;
+    this.config.mode = config?.mode ?? "string";
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDecimal(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDecimal extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseDecimal";
+  chType = this.config.chType;
+  precision = this.config.precision;
+  scale = this.config.scale;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType === "Decimal" ? `Decimal(${this.precision}, ${this.scale})` : `${this.chType}(${this.scale})`;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "number" ? typeof value === "number" ? value : Number(value) : typeof value === "string" ? value : String(value);
+  }
+  mapToDriverValue(value) {
+    return import_sql.sql`CAST(${String(value)} AS ${import_sql.sql.raw(this.getBaseSQLType())})`;
+  }
+  /** A string, for the same reason the literal path casts from one: a JSON number loses the scale. */
+  mapToRowValue(value) {
+    return String(value);
+  }
+  /** The total number of significant digits ClickHouse allows for this column. */
+  get effectivePrecision() {
+    return this.chType === "Decimal" ? this.precision : IMPLIED_PRECISION[this.chType];
+  }
+}
+function decimal(a, b) {
+  const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+  return new ClickHouseDecimalBuilder(name, "ClickHouseDecimal", "Decimal", config);
+}
+function fixedDecimalFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+    return new ClickHouseDecimalBuilder(name, columnType, chType, config);
+  }
+  return column;
+}
+const decimal32 = fixedDecimalFactory("ClickHouseDecimal32", "Decimal32");
+const decimal64 = fixedDecimalFactory("ClickHouseDecimal64", "Decimal64");
+const decimal128 = fixedDecimalFactory("ClickHouseDecimal128", "Decimal128");
+const decimal256 = fixedDecimalFactory("ClickHouseDecimal256", "Decimal256");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDecimal,
+  ClickHouseDecimalBuilder,
+  decimal,
+  decimal128,
+  decimal256,
+  decimal32,
+  decimal64
+});
diff --git a/clickhouse-core/columns/decimal.d.cts b/clickhouse-core/columns/decimal.d.cts
new file mode 100644
index 0000000..15239d7
--- /dev/null
+++ b/clickhouse-core/columns/decimal.d.cts
@@ -0,0 +1,75 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+/**
+ * How a decimal is surfaced in TypeScript. `string` (the default) is lossless; `number` is
+ * convenient but silently rounds anything beyond double precision.
+ */
+export type ClickHouseDecimalMode = 'string' | 'number';
+/** The fixed-width `DecimalN` types, or `Decimal` when an explicit precision is given. */
+export type ClickHouseDecimalTypeName = 'Decimal' | 'Decimal32' | 'Decimal64' | 'Decimal128' | 'Decimal256';
+export interface ClickHouseDecimalConfig<TMode extends ClickHouseDecimalMode = ClickHouseDecimalMode> {
+    /** Total number of significant digits. Only accepted by {@link decimal}. */
+    precision?: number;
+    /** Number of digits after the decimal point. */
+    scale?: number;
+    mode?: TMode;
+}
+interface ClickHouseDecimalRuntimeConfig {
+    chType: ClickHouseDecimalTypeName;
+    precision: number | undefined;
+    scale: number;
+    mode: ClickHouseDecimalMode;
+}
+export type ClickHouseDecimalBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDecimalMode> = ClickHouseDecimalBuilder<{
+    name: TName;
+    dataType: TMode extends 'number' ? 'number' : 'string';
+    columnType: TColumnType;
+    data: TMode extends 'number' ? number : string;
+    driverParam: string | number;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDecimalBuilder<T extends ColumnBuilderBaseConfig<'string' | 'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseDecimalRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseDecimalTypeName, config: ClickHouseDecimalConfig | undefined);
+}
+export declare class ClickHouseDecimal<T extends ColumnBaseConfig<'string' | 'number', string>> extends ClickHouseColumn<T, ClickHouseDecimalRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseDecimalTypeName;
+    readonly precision: number | undefined;
+    readonly scale: number;
+    readonly mode: ClickHouseDecimalMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string | number): string | number;
+    mapToDriverValue(value: string | number): SQL;
+    /** A string, for the same reason the literal path casts from one: a JSON number loses the scale. */
+    mapToRowValue(value: string | number): string;
+    /** The total number of significant digits ClickHouse allows for this column. */
+    get effectivePrecision(): number;
+}
+/** `Decimal(P, S)` — a fixed-point number with `P` significant digits, `S` of them after the point. */
+export declare function decimal<TMode extends ClickHouseDecimalMode = 'string'>(config?: ClickHouseDecimalConfig<TMode>): ClickHouseDecimalBuilderInitial<'', 'ClickHouseDecimal', TMode>;
+export declare function decimal<TName extends string, TMode extends ClickHouseDecimalMode = 'string'>(name: TName, config?: ClickHouseDecimalConfig<TMode>): ClickHouseDecimalBuilderInitial<TName, 'ClickHouseDecimal', TMode>;
+/** `Decimal32(S)` — up to 9 significant digits. */
+export declare const decimal32: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal32", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal32", TMode>;
+};
+/** `Decimal64(S)` — up to 18 significant digits. */
+export declare const decimal64: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal64", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal64", TMode>;
+};
+/** `Decimal128(S)` — up to 38 significant digits. */
+export declare const decimal128: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal128", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal128", TMode>;
+};
+/** `Decimal256(S)` — up to 76 significant digits. */
+export declare const decimal256: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal256", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal256", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/decimal.d.ts b/clickhouse-core/columns/decimal.d.ts
new file mode 100644
index 0000000..a86811f
--- /dev/null
+++ b/clickhouse-core/columns/decimal.d.ts
@@ -0,0 +1,75 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+/**
+ * How a decimal is surfaced in TypeScript. `string` (the default) is lossless; `number` is
+ * convenient but silently rounds anything beyond double precision.
+ */
+export type ClickHouseDecimalMode = 'string' | 'number';
+/** The fixed-width `DecimalN` types, or `Decimal` when an explicit precision is given. */
+export type ClickHouseDecimalTypeName = 'Decimal' | 'Decimal32' | 'Decimal64' | 'Decimal128' | 'Decimal256';
+export interface ClickHouseDecimalConfig<TMode extends ClickHouseDecimalMode = ClickHouseDecimalMode> {
+    /** Total number of significant digits. Only accepted by {@link decimal}. */
+    precision?: number;
+    /** Number of digits after the decimal point. */
+    scale?: number;
+    mode?: TMode;
+}
+interface ClickHouseDecimalRuntimeConfig {
+    chType: ClickHouseDecimalTypeName;
+    precision: number | undefined;
+    scale: number;
+    mode: ClickHouseDecimalMode;
+}
+export type ClickHouseDecimalBuilderInitial<TName extends string, TColumnType extends string, TMode extends ClickHouseDecimalMode> = ClickHouseDecimalBuilder<{
+    name: TName;
+    dataType: TMode extends 'number' ? 'number' : 'string';
+    columnType: TColumnType;
+    data: TMode extends 'number' ? number : string;
+    driverParam: string | number;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseDecimalBuilder<T extends ColumnBuilderBaseConfig<'string' | 'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseDecimalRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseDecimalTypeName, config: ClickHouseDecimalConfig | undefined);
+}
+export declare class ClickHouseDecimal<T extends ColumnBaseConfig<'string' | 'number', string>> extends ClickHouseColumn<T, ClickHouseDecimalRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseDecimalTypeName;
+    readonly precision: number | undefined;
+    readonly scale: number;
+    readonly mode: ClickHouseDecimalMode;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: string | number): string | number;
+    mapToDriverValue(value: string | number): SQL;
+    /** A string, for the same reason the literal path casts from one: a JSON number loses the scale. */
+    mapToRowValue(value: string | number): string;
+    /** The total number of significant digits ClickHouse allows for this column. */
+    get effectivePrecision(): number;
+}
+/** `Decimal(P, S)` — a fixed-point number with `P` significant digits, `S` of them after the point. */
+export declare function decimal<TMode extends ClickHouseDecimalMode = 'string'>(config?: ClickHouseDecimalConfig<TMode>): ClickHouseDecimalBuilderInitial<'', 'ClickHouseDecimal', TMode>;
+export declare function decimal<TName extends string, TMode extends ClickHouseDecimalMode = 'string'>(name: TName, config?: ClickHouseDecimalConfig<TMode>): ClickHouseDecimalBuilderInitial<TName, 'ClickHouseDecimal', TMode>;
+/** `Decimal32(S)` — up to 9 significant digits. */
+export declare const decimal32: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal32", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal32", TMode>;
+};
+/** `Decimal64(S)` — up to 18 significant digits. */
+export declare const decimal64: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal64", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal64", TMode>;
+};
+/** `Decimal128(S)` — up to 38 significant digits. */
+export declare const decimal128: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal128", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal128", TMode>;
+};
+/** `Decimal256(S)` — up to 76 significant digits. */
+export declare const decimal256: {
+    <TMode extends ClickHouseDecimalMode = "string">(config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<"", "ClickHouseDecimal256", TMode>;
+    <TName extends string, TMode extends ClickHouseDecimalMode = "string">(name: TName, config?: Omit<ClickHouseDecimalConfig<TMode>, "precision"> | undefined): ClickHouseDecimalBuilderInitial<TName, "ClickHouseDecimal256", TMode>;
+};
+export {};
diff --git a/clickhouse-core/columns/decimal.js b/clickhouse-core/columns/decimal.js
new file mode 100644
index 0000000..bcd53b1
--- /dev/null
+++ b/clickhouse-core/columns/decimal.js
@@ -0,0 +1,75 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+const IMPLIED_PRECISION = {
+  Decimal32: 9,
+  Decimal64: 18,
+  Decimal128: 38,
+  Decimal256: 76
+};
+class ClickHouseDecimalBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseDecimalBuilder";
+  constructor(name, columnType, chType, config) {
+    super(name, config?.mode === "number" ? "number" : "string", columnType);
+    this.config.chType = chType;
+    this.config.precision = chType === "Decimal" ? config?.precision ?? 10 : void 0;
+    this.config.scale = config?.scale ?? 0;
+    this.config.mode = config?.mode ?? "string";
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseDecimal(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseDecimal extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseDecimal";
+  chType = this.config.chType;
+  precision = this.config.precision;
+  scale = this.config.scale;
+  mode = this.config.mode;
+  getBaseSQLType() {
+    return this.chType === "Decimal" ? `Decimal(${this.precision}, ${this.scale})` : `${this.chType}(${this.scale})`;
+  }
+  mapFromDriverValue(value) {
+    return this.mode === "number" ? typeof value === "number" ? value : Number(value) : typeof value === "string" ? value : String(value);
+  }
+  mapToDriverValue(value) {
+    return sql`CAST(${String(value)} AS ${sql.raw(this.getBaseSQLType())})`;
+  }
+  /** A string, for the same reason the literal path casts from one: a JSON number loses the scale. */
+  mapToRowValue(value) {
+    return String(value);
+  }
+  /** The total number of significant digits ClickHouse allows for this column. */
+  get effectivePrecision() {
+    return this.chType === "Decimal" ? this.precision : IMPLIED_PRECISION[this.chType];
+  }
+}
+function decimal(a, b) {
+  const { name, config } = getColumnNameAndConfig(a, b);
+  return new ClickHouseDecimalBuilder(name, "ClickHouseDecimal", "Decimal", config);
+}
+function fixedDecimalFactory(columnType, chType) {
+  function column(a, b) {
+    const { name, config } = getColumnNameAndConfig(a, b);
+    return new ClickHouseDecimalBuilder(name, columnType, chType, config);
+  }
+  return column;
+}
+const decimal32 = fixedDecimalFactory("ClickHouseDecimal32", "Decimal32");
+const decimal64 = fixedDecimalFactory("ClickHouseDecimal64", "Decimal64");
+const decimal128 = fixedDecimalFactory("ClickHouseDecimal128", "Decimal128");
+const decimal256 = fixedDecimalFactory("ClickHouseDecimal256", "Decimal256");
+export {
+  ClickHouseDecimal,
+  ClickHouseDecimalBuilder,
+  decimal,
+  decimal128,
+  decimal256,
+  decimal32,
+  decimal64
+};
diff --git a/clickhouse-core/columns/enum.cjs b/clickhouse-core/columns/enum.cjs
new file mode 100644
index 0000000..4eacc42
--- /dev/null
+++ b/clickhouse-core/columns/enum.cjs
@@ -0,0 +1,86 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var enum_exports = {};
+__export(enum_exports, {
+  ClickHouseEnum: () => ClickHouseEnum,
+  ClickHouseEnumBuilder: () => ClickHouseEnumBuilder,
+  enum16: () => enum16,
+  enum8: () => enum8
+});
+module.exports = __toCommonJS(enum_exports);
+var import_entity = require("../../entity.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+function toMembers(values) {
+  if (Array.isArray(values)) {
+    return values.map((name, index) => [name, index + 1]);
+  }
+  return Object.entries(values);
+}
+class ClickHouseEnumBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseEnumBuilder";
+  constructor(name, columnType, chType, values) {
+    super(name, "string", columnType);
+    const members = toMembers(values);
+    if (members.length === 0) {
+      throw new Error(`${chType} column "${name}" must declare at least one member`);
+    }
+    this.config.chType = chType;
+    this.config.members = members;
+    this.config.enumValues = members.map(([member]) => member);
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseEnum(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseEnum extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseEnum";
+  chType = this.config.chType;
+  members = this.config.members;
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    const members = this.members.map(([member, value]) => `${(0, import_literals.escapeClickHouseString)(member)} = ${value}`).join(", ");
+    return `${this.chType}(${members})`;
+  }
+}
+function enumFactory(columnType, chType) {
+  function column(a, b) {
+    const isNamed = typeof a === "string";
+    return new ClickHouseEnumBuilder(
+      isNamed ? a : "",
+      columnType,
+      chType,
+      isNamed ? b : a
+    );
+  }
+  return column;
+}
+const enum8 = enumFactory("ClickHouseEnum8", "Enum8");
+const enum16 = enumFactory("ClickHouseEnum16", "Enum16");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseEnum,
+  ClickHouseEnumBuilder,
+  enum16,
+  enum8
+});
diff --git a/clickhouse-core/columns/enum.d.cts b/clickhouse-core/columns/enum.d.cts
new file mode 100644
index 0000000..dc43fd4
--- /dev/null
+++ b/clickhouse-core/columns/enum.d.cts
@@ -0,0 +1,63 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { Writable } from '../../utils.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseEnumTypeName = 'Enum8' | 'Enum16';
+/**
+ * The members of an enum, either as a list (numbered from 1 in order) or as an explicit
+ * name-to-number mapping.
+ */
+export type ClickHouseEnumValues<TValues extends string = string> = readonly [TValues, ...TValues[]] | Record<TValues, number>;
+interface ClickHouseEnumRuntimeConfig<TEnum = string[] | undefined> {
+    chType: ClickHouseEnumTypeName;
+    /** Members in declaration order, paired with the numeric value ClickHouse stores. */
+    members: [string, number][];
+    enumValues: TEnum;
+}
+/** The member names of an enum declared as an explicit name-to-number mapping. */
+export type ClickHouseEnumRecordValues<T extends Record<string, number>> = [
+    Extract<keyof T, string>,
+    ...Extract<keyof T, string>[]
+];
+export type ClickHouseEnumBuilderInitial<TName extends string, TColumnType extends string, TValues extends [string, ...string[]]> = ClickHouseEnumBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: TColumnType;
+    data: TValues[number];
+    driverParam: string;
+    enumValues: TValues;
+}>;
+export declare class ClickHouseEnumBuilder<T extends ColumnBuilderBaseConfig<'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseEnumRuntimeConfig<T['enumValues']>> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseEnumTypeName, values: ClickHouseEnumValues);
+}
+export declare class ClickHouseEnum<T extends ColumnBaseConfig<'string', string>> extends ClickHouseColumn<T, ClickHouseEnumRuntimeConfig<T['enumValues']>> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseEnumTypeName;
+    readonly members: [string, number][];
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `Enum8` — a string enum stored as a signed byte, so up to 256 members.
+ *
+ * ```ts
+ * status: enum8(['pending', 'done']),        // 'pending' = 1, 'done' = 2
+ * level: enum8({ debug: 0, error: 10 }),     // explicit numbering
+ * ```
+ */
+export declare const enum8: {
+    <U extends string, T extends readonly [U, ...U[]]>(values: T | Writable<T>): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", Writable<T>>;
+    <T extends Record<string, number>>(values: T): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", ClickHouseEnumRecordValues<T>>;
+    <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | Writable<T>): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", Writable<T>>;
+    <TName extends string, T extends Record<string, number>>(name: TName, values: T): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", ClickHouseEnumRecordValues<T>>;
+};
+/** `Enum16` — a string enum stored as a signed 16-bit integer, so up to 65536 members. */
+export declare const enum16: {
+    <U extends string, T extends readonly [U, ...U[]]>(values: T | Writable<T>): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", Writable<T>>;
+    <T extends Record<string, number>>(values: T): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", ClickHouseEnumRecordValues<T>>;
+    <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | Writable<T>): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", Writable<T>>;
+    <TName extends string, T extends Record<string, number>>(name: TName, values: T): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", ClickHouseEnumRecordValues<T>>;
+};
+export {};
diff --git a/clickhouse-core/columns/enum.d.ts b/clickhouse-core/columns/enum.d.ts
new file mode 100644
index 0000000..d4514aa
--- /dev/null
+++ b/clickhouse-core/columns/enum.d.ts
@@ -0,0 +1,63 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { Writable } from '../../utils.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseEnumTypeName = 'Enum8' | 'Enum16';
+/**
+ * The members of an enum, either as a list (numbered from 1 in order) or as an explicit
+ * name-to-number mapping.
+ */
+export type ClickHouseEnumValues<TValues extends string = string> = readonly [TValues, ...TValues[]] | Record<TValues, number>;
+interface ClickHouseEnumRuntimeConfig<TEnum = string[] | undefined> {
+    chType: ClickHouseEnumTypeName;
+    /** Members in declaration order, paired with the numeric value ClickHouse stores. */
+    members: [string, number][];
+    enumValues: TEnum;
+}
+/** The member names of an enum declared as an explicit name-to-number mapping. */
+export type ClickHouseEnumRecordValues<T extends Record<string, number>> = [
+    Extract<keyof T, string>,
+    ...Extract<keyof T, string>[]
+];
+export type ClickHouseEnumBuilderInitial<TName extends string, TColumnType extends string, TValues extends [string, ...string[]]> = ClickHouseEnumBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: TColumnType;
+    data: TValues[number];
+    driverParam: string;
+    enumValues: TValues;
+}>;
+export declare class ClickHouseEnumBuilder<T extends ColumnBuilderBaseConfig<'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseEnumRuntimeConfig<T['enumValues']>> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseEnumTypeName, values: ClickHouseEnumValues);
+}
+export declare class ClickHouseEnum<T extends ColumnBaseConfig<'string', string>> extends ClickHouseColumn<T, ClickHouseEnumRuntimeConfig<T['enumValues']>> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseEnumTypeName;
+    readonly members: [string, number][];
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `Enum8` — a string enum stored as a signed byte, so up to 256 members.
+ *
+ * ```ts
+ * status: enum8(['pending', 'done']),        // 'pending' = 1, 'done' = 2
+ * level: enum8({ debug: 0, error: 10 }),     // explicit numbering
+ * ```
+ */
+export declare const enum8: {
+    <U extends string, T extends readonly [U, ...U[]]>(values: T | Writable<T>): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", Writable<T>>;
+    <T extends Record<string, number>>(values: T): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum8", ClickHouseEnumRecordValues<T>>;
+    <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | Writable<T>): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", Writable<T>>;
+    <TName extends string, T extends Record<string, number>>(name: TName, values: T): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum8", ClickHouseEnumRecordValues<T>>;
+};
+/** `Enum16` — a string enum stored as a signed 16-bit integer, so up to 65536 members. */
+export declare const enum16: {
+    <U extends string, T extends readonly [U, ...U[]]>(values: T | Writable<T>): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", Writable<T>>;
+    <T extends Record<string, number>>(values: T): ClickHouseEnumBuilderInitial<"", "ClickHouseEnum16", ClickHouseEnumRecordValues<T>>;
+    <TName extends string, U extends string, T extends readonly [U, ...U[]]>(name: TName, values: T | Writable<T>): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", Writable<T>>;
+    <TName extends string, T extends Record<string, number>>(name: TName, values: T): ClickHouseEnumBuilderInitial<TName, "ClickHouseEnum16", ClickHouseEnumRecordValues<T>>;
+};
+export {};
diff --git a/clickhouse-core/columns/enum.js b/clickhouse-core/columns/enum.js
new file mode 100644
index 0000000..ff81b44
--- /dev/null
+++ b/clickhouse-core/columns/enum.js
@@ -0,0 +1,59 @@
+import { entityKind } from "../../entity.js";
+import { escapeClickHouseString } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+function toMembers(values) {
+  if (Array.isArray(values)) {
+    return values.map((name, index) => [name, index + 1]);
+  }
+  return Object.entries(values);
+}
+class ClickHouseEnumBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseEnumBuilder";
+  constructor(name, columnType, chType, values) {
+    super(name, "string", columnType);
+    const members = toMembers(values);
+    if (members.length === 0) {
+      throw new Error(`${chType} column "${name}" must declare at least one member`);
+    }
+    this.config.chType = chType;
+    this.config.members = members;
+    this.config.enumValues = members.map(([member]) => member);
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseEnum(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseEnum extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseEnum";
+  chType = this.config.chType;
+  members = this.config.members;
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    const members = this.members.map(([member, value]) => `${escapeClickHouseString(member)} = ${value}`).join(", ");
+    return `${this.chType}(${members})`;
+  }
+}
+function enumFactory(columnType, chType) {
+  function column(a, b) {
+    const isNamed = typeof a === "string";
+    return new ClickHouseEnumBuilder(
+      isNamed ? a : "",
+      columnType,
+      chType,
+      isNamed ? b : a
+    );
+  }
+  return column;
+}
+const enum8 = enumFactory("ClickHouseEnum8", "Enum8");
+const enum16 = enumFactory("ClickHouseEnum16", "Enum16");
+export {
+  ClickHouseEnum,
+  ClickHouseEnumBuilder,
+  enum16,
+  enum8
+};
diff --git a/clickhouse-core/columns/float.cjs b/clickhouse-core/columns/float.cjs
new file mode 100644
index 0000000..4b98aa5
--- /dev/null
+++ b/clickhouse-core/columns/float.cjs
@@ -0,0 +1,83 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var float_exports = {};
+__export(float_exports, {
+  ClickHouseFloat: () => ClickHouseFloat,
+  ClickHouseFloatBuilder: () => ClickHouseFloatBuilder,
+  float32: () => float32,
+  float64: () => float64
+});
+module.exports = __toCommonJS(float_exports);
+var import_entity = require("../../entity.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseFloatBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseFloatBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "number", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseFloat(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseFloat extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseFloat";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    if (typeof value === "number") return value;
+    if (value === "inf") return Number.POSITIVE_INFINITY;
+    if (value === "-inf") return Number.NEGATIVE_INFINITY;
+    return Number(value);
+  }
+  mapToDriverValue(value) {
+    return (0, import_literals.numericLiteral)(value);
+  }
+  /**
+   * Finite values pass through as JSON numbers. `NaN` and the infinities do not exist in JSON —
+   * `JSON.stringify` turns them into `null`, which is a rejected insert for a non-nullable column
+   * and a silently wrong value for a nullable one — so they go as the strings ClickHouse spells
+   * them with, which it accepts for a `Float` column and which `mapFromDriverValue` reads back.
+   */
+  mapToRowValue(value) {
+    return Number.isFinite(value) ? value : (0, import_literals.numberToText)(value);
+  }
+}
+function floatFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseFloatBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const float32 = floatFactory("ClickHouseFloat32", "Float32");
+const float64 = floatFactory("ClickHouseFloat64", "Float64");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseFloat,
+  ClickHouseFloatBuilder,
+  float32,
+  float64
+});
diff --git a/clickhouse-core/columns/float.d.cts b/clickhouse-core/columns/float.d.cts
new file mode 100644
index 0000000..817154a
--- /dev/null
+++ b/clickhouse-core/columns/float.d.cts
@@ -0,0 +1,46 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseFloatTypeName = 'Float32' | 'Float64';
+interface ClickHouseFloatRuntimeConfig {
+    chType: ClickHouseFloatTypeName;
+}
+export type ClickHouseFloatBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseFloatBuilder<{
+    name: TName;
+    dataType: 'number';
+    columnType: TColumnType;
+    data: number;
+    driverParam: number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseFloatBuilder<T extends ColumnBuilderBaseConfig<'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseFloatRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseFloatTypeName);
+}
+export declare class ClickHouseFloat<T extends ColumnBaseConfig<'number', string>> extends ClickHouseColumn<T, ClickHouseFloatRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseFloatTypeName;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: number | string): number;
+    mapToDriverValue(value: number): SQL;
+    /**
+     * Finite values pass through as JSON numbers. `NaN` and the infinities do not exist in JSON —
+     * `JSON.stringify` turns them into `null`, which is a rejected insert for a non-nullable column
+     * and a silently wrong value for a nullable one — so they go as the strings ClickHouse spells
+     * them with, which it accepts for a `Float` column and which `mapFromDriverValue` reads back.
+     */
+    mapToRowValue(value: number): number | string;
+}
+/** `Float32` — single-precision IEEE-754 floating point number. */
+export declare const float32: {
+    (): ClickHouseFloatBuilderInitial<"", "ClickHouseFloat32">;
+    <TName extends string>(name: TName): ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat32">;
+};
+/** `Float64` — double-precision IEEE-754 floating point number. */
+export declare const float64: {
+    (): ClickHouseFloatBuilderInitial<"", "ClickHouseFloat64">;
+    <TName extends string>(name: TName): ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat64">;
+};
+export {};
diff --git a/clickhouse-core/columns/float.d.ts b/clickhouse-core/columns/float.d.ts
new file mode 100644
index 0000000..190013c
--- /dev/null
+++ b/clickhouse-core/columns/float.d.ts
@@ -0,0 +1,46 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseFloatTypeName = 'Float32' | 'Float64';
+interface ClickHouseFloatRuntimeConfig {
+    chType: ClickHouseFloatTypeName;
+}
+export type ClickHouseFloatBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseFloatBuilder<{
+    name: TName;
+    dataType: 'number';
+    columnType: TColumnType;
+    data: number;
+    driverParam: number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseFloatBuilder<T extends ColumnBuilderBaseConfig<'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseFloatRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseFloatTypeName);
+}
+export declare class ClickHouseFloat<T extends ColumnBaseConfig<'number', string>> extends ClickHouseColumn<T, ClickHouseFloatRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseFloatTypeName;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: number | string): number;
+    mapToDriverValue(value: number): SQL;
+    /**
+     * Finite values pass through as JSON numbers. `NaN` and the infinities do not exist in JSON —
+     * `JSON.stringify` turns them into `null`, which is a rejected insert for a non-nullable column
+     * and a silently wrong value for a nullable one — so they go as the strings ClickHouse spells
+     * them with, which it accepts for a `Float` column and which `mapFromDriverValue` reads back.
+     */
+    mapToRowValue(value: number): number | string;
+}
+/** `Float32` — single-precision IEEE-754 floating point number. */
+export declare const float32: {
+    (): ClickHouseFloatBuilderInitial<"", "ClickHouseFloat32">;
+    <TName extends string>(name: TName): ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat32">;
+};
+/** `Float64` — double-precision IEEE-754 floating point number. */
+export declare const float64: {
+    (): ClickHouseFloatBuilderInitial<"", "ClickHouseFloat64">;
+    <TName extends string>(name: TName): ClickHouseFloatBuilderInitial<TName, "ClickHouseFloat64">;
+};
+export {};
diff --git a/clickhouse-core/columns/float.js b/clickhouse-core/columns/float.js
new file mode 100644
index 0000000..efb99f2
--- /dev/null
+++ b/clickhouse-core/columns/float.js
@@ -0,0 +1,56 @@
+import { entityKind } from "../../entity.js";
+import { numberToText, numericLiteral } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseFloatBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseFloatBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "number", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseFloat(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseFloat extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseFloat";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    if (typeof value === "number") return value;
+    if (value === "inf") return Number.POSITIVE_INFINITY;
+    if (value === "-inf") return Number.NEGATIVE_INFINITY;
+    return Number(value);
+  }
+  mapToDriverValue(value) {
+    return numericLiteral(value);
+  }
+  /**
+   * Finite values pass through as JSON numbers. `NaN` and the infinities do not exist in JSON —
+   * `JSON.stringify` turns them into `null`, which is a rejected insert for a non-nullable column
+   * and a silently wrong value for a nullable one — so they go as the strings ClickHouse spells
+   * them with, which it accepts for a `Float` column and which `mapFromDriverValue` reads back.
+   */
+  mapToRowValue(value) {
+    return Number.isFinite(value) ? value : numberToText(value);
+  }
+}
+function floatFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseFloatBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const float32 = floatFactory("ClickHouseFloat32", "Float32");
+const float64 = floatFactory("ClickHouseFloat64", "Float64");
+export {
+  ClickHouseFloat,
+  ClickHouseFloatBuilder,
+  float32,
+  float64
+};
diff --git a/clickhouse-core/columns/index.cjs b/clickhouse-core/columns/index.cjs
new file mode 100644
index 0000000..222e6df
--- /dev/null
+++ b/clickhouse-core/columns/index.cjs
@@ -0,0 +1,60 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var columns_exports = {};
+module.exports = __toCommonJS(columns_exports);
+__reExport(columns_exports, require("./array.cjs"), module.exports);
+__reExport(columns_exports, require("./bigint.cjs"), module.exports);
+__reExport(columns_exports, require("./bool.cjs"), module.exports);
+__reExport(columns_exports, require("./common.cjs"), module.exports);
+__reExport(columns_exports, require("./custom.cjs"), module.exports);
+__reExport(columns_exports, require("./date.common.cjs"), module.exports);
+__reExport(columns_exports, require("./date.cjs"), module.exports);
+__reExport(columns_exports, require("./datetime.cjs"), module.exports);
+__reExport(columns_exports, require("./decimal.cjs"), module.exports);
+__reExport(columns_exports, require("./enum.cjs"), module.exports);
+__reExport(columns_exports, require("./float.cjs"), module.exports);
+__reExport(columns_exports, require("./int.cjs"), module.exports);
+__reExport(columns_exports, require("./ip.cjs"), module.exports);
+__reExport(columns_exports, require("./json.cjs"), module.exports);
+__reExport(columns_exports, require("./low-cardinality.cjs"), module.exports);
+__reExport(columns_exports, require("./map.cjs"), module.exports);
+__reExport(columns_exports, require("./nullable.cjs"), module.exports);
+__reExport(columns_exports, require("./string.cjs"), module.exports);
+__reExport(columns_exports, require("./tuple.cjs"), module.exports);
+__reExport(columns_exports, require("./uuid.cjs"), module.exports);
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ...require("./array.cjs"),
+  ...require("./bigint.cjs"),
+  ...require("./bool.cjs"),
+  ...require("./common.cjs"),
+  ...require("./custom.cjs"),
+  ...require("./date.common.cjs"),
+  ...require("./date.cjs"),
+  ...require("./datetime.cjs"),
+  ...require("./decimal.cjs"),
+  ...require("./enum.cjs"),
+  ...require("./float.cjs"),
+  ...require("./int.cjs"),
+  ...require("./ip.cjs"),
+  ...require("./json.cjs"),
+  ...require("./low-cardinality.cjs"),
+  ...require("./map.cjs"),
+  ...require("./nullable.cjs"),
+  ...require("./string.cjs"),
+  ...require("./tuple.cjs"),
+  ...require("./uuid.cjs")
+});
diff --git a/clickhouse-core/columns/index.d.cts b/clickhouse-core/columns/index.d.cts
new file mode 100644
index 0000000..08f8d8b
--- /dev/null
+++ b/clickhouse-core/columns/index.d.cts
@@ -0,0 +1,20 @@
+export * from './array.cjs';
+export * from './bigint.cjs';
+export * from './bool.cjs';
+export * from './common.cjs';
+export * from './custom.cjs';
+export * from './date.common.cjs';
+export * from './date.cjs';
+export * from './datetime.cjs';
+export * from './decimal.cjs';
+export * from './enum.cjs';
+export * from './float.cjs';
+export * from './int.cjs';
+export * from './ip.cjs';
+export * from './json.cjs';
+export * from './low-cardinality.cjs';
+export * from './map.cjs';
+export * from './nullable.cjs';
+export * from './string.cjs';
+export * from './tuple.cjs';
+export * from './uuid.cjs';
diff --git a/clickhouse-core/columns/index.d.ts b/clickhouse-core/columns/index.d.ts
new file mode 100644
index 0000000..b405c45
--- /dev/null
+++ b/clickhouse-core/columns/index.d.ts
@@ -0,0 +1,20 @@
+export * from './array.js';
+export * from './bigint.js';
+export * from './bool.js';
+export * from './common.js';
+export * from './custom.js';
+export * from './date.common.js';
+export * from './date.js';
+export * from './datetime.js';
+export * from './decimal.js';
+export * from './enum.js';
+export * from './float.js';
+export * from './int.js';
+export * from './ip.js';
+export * from './json.js';
+export * from './low-cardinality.js';
+export * from './map.js';
+export * from './nullable.js';
+export * from './string.js';
+export * from './tuple.js';
+export * from './uuid.js';
diff --git a/clickhouse-core/columns/index.js b/clickhouse-core/columns/index.js
new file mode 100644
index 0000000..996b5d9
--- /dev/null
+++ b/clickhouse-core/columns/index.js
@@ -0,0 +1,20 @@
+export * from "./array.js";
+export * from "./bigint.js";
+export * from "./bool.js";
+export * from "./common.js";
+export * from "./custom.js";
+export * from "./date.common.js";
+export * from "./date.js";
+export * from "./datetime.js";
+export * from "./decimal.js";
+export * from "./enum.js";
+export * from "./float.js";
+export * from "./int.js";
+export * from "./ip.js";
+export * from "./json.js";
+export * from "./low-cardinality.js";
+export * from "./map.js";
+export * from "./nullable.js";
+export * from "./string.js";
+export * from "./tuple.js";
+export * from "./uuid.js";
diff --git a/clickhouse-core/columns/int.cjs b/clickhouse-core/columns/int.cjs
new file mode 100644
index 0000000..3210c87
--- /dev/null
+++ b/clickhouse-core/columns/int.cjs
@@ -0,0 +1,79 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var int_exports = {};
+__export(int_exports, {
+  ClickHouseInt: () => ClickHouseInt,
+  ClickHouseIntBuilder: () => ClickHouseIntBuilder,
+  int16: () => int16,
+  int32: () => int32,
+  int8: () => int8,
+  uint16: () => uint16,
+  uint32: () => uint32,
+  uint8: () => uint8
+});
+module.exports = __toCommonJS(int_exports);
+var import_entity = require("../../entity.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseIntBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseIntBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "number", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseInt(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseInt extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseInt";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    return typeof value === "string" ? Number(value) : value;
+  }
+}
+function intFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseIntBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const int8 = intFactory("ClickHouseInt8", "Int8");
+const int16 = intFactory("ClickHouseInt16", "Int16");
+const int32 = intFactory("ClickHouseInt32", "Int32");
+const uint8 = intFactory("ClickHouseUInt8", "UInt8");
+const uint16 = intFactory("ClickHouseUInt16", "UInt16");
+const uint32 = intFactory("ClickHouseUInt32", "UInt32");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseInt,
+  ClickHouseIntBuilder,
+  int16,
+  int32,
+  int8,
+  uint16,
+  uint32,
+  uint8
+});
diff --git a/clickhouse-core/columns/int.d.cts b/clickhouse-core/columns/int.d.cts
new file mode 100644
index 0000000..9b4e8d0
--- /dev/null
+++ b/clickhouse-core/columns/int.d.cts
@@ -0,0 +1,57 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+/** ClickHouse integer types that fit losslessly into a JavaScript `number`. */
+export type ClickHouseNumericIntTypeName = 'Int8' | 'Int16' | 'Int32' | 'UInt8' | 'UInt16' | 'UInt32';
+export interface ClickHouseIntConfig {
+    chType: ClickHouseNumericIntTypeName;
+}
+export type ClickHouseIntBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseIntBuilder<{
+    name: TName;
+    dataType: 'number';
+    columnType: TColumnType;
+    data: number;
+    driverParam: number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseIntBuilder<T extends ColumnBuilderBaseConfig<'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseIntConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseNumericIntTypeName);
+}
+export declare class ClickHouseInt<T extends ColumnBaseConfig<'number', string>> extends ClickHouseColumn<T, ClickHouseIntConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseNumericIntTypeName;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: number | string): number;
+}
+/** `Int8` — signed 8-bit integer, range `-128` to `127`. */
+export declare const int8: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt8">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt8">;
+};
+/** `Int16` — signed 16-bit integer, range `-32768` to `32767`. */
+export declare const int16: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt16">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt16">;
+};
+/** `Int32` — signed 32-bit integer, range `-2147483648` to `2147483647`. */
+export declare const int32: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt32">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt32">;
+};
+/** `UInt8` — unsigned 8-bit integer, range `0` to `255`. */
+export declare const uint8: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt8">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt8">;
+};
+/** `UInt16` — unsigned 16-bit integer, range `0` to `65535`. */
+export declare const uint16: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt16">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt16">;
+};
+/** `UInt32` — unsigned 32-bit integer, range `0` to `4294967295`. */
+export declare const uint32: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt32">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt32">;
+};
diff --git a/clickhouse-core/columns/int.d.ts b/clickhouse-core/columns/int.d.ts
new file mode 100644
index 0000000..c39646a
--- /dev/null
+++ b/clickhouse-core/columns/int.d.ts
@@ -0,0 +1,57 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+/** ClickHouse integer types that fit losslessly into a JavaScript `number`. */
+export type ClickHouseNumericIntTypeName = 'Int8' | 'Int16' | 'Int32' | 'UInt8' | 'UInt16' | 'UInt32';
+export interface ClickHouseIntConfig {
+    chType: ClickHouseNumericIntTypeName;
+}
+export type ClickHouseIntBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseIntBuilder<{
+    name: TName;
+    dataType: 'number';
+    columnType: TColumnType;
+    data: number;
+    driverParam: number | string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseIntBuilder<T extends ColumnBuilderBaseConfig<'number', string>> extends ClickHouseColumnBuilder<T, ClickHouseIntConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseNumericIntTypeName);
+}
+export declare class ClickHouseInt<T extends ColumnBaseConfig<'number', string>> extends ClickHouseColumn<T, ClickHouseIntConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseNumericIntTypeName;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: number | string): number;
+}
+/** `Int8` — signed 8-bit integer, range `-128` to `127`. */
+export declare const int8: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt8">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt8">;
+};
+/** `Int16` — signed 16-bit integer, range `-32768` to `32767`. */
+export declare const int16: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt16">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt16">;
+};
+/** `Int32` — signed 32-bit integer, range `-2147483648` to `2147483647`. */
+export declare const int32: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseInt32">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseInt32">;
+};
+/** `UInt8` — unsigned 8-bit integer, range `0` to `255`. */
+export declare const uint8: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt8">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt8">;
+};
+/** `UInt16` — unsigned 16-bit integer, range `0` to `65535`. */
+export declare const uint16: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt16">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt16">;
+};
+/** `UInt32` — unsigned 32-bit integer, range `0` to `4294967295`. */
+export declare const uint32: {
+    (): ClickHouseIntBuilderInitial<"", "ClickHouseUInt32">;
+    <TName extends string>(name: TName): ClickHouseIntBuilderInitial<TName, "ClickHouseUInt32">;
+};
diff --git a/clickhouse-core/columns/int.js b/clickhouse-core/columns/int.js
new file mode 100644
index 0000000..c103fa7
--- /dev/null
+++ b/clickhouse-core/columns/int.js
@@ -0,0 +1,48 @@
+import { entityKind } from "../../entity.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseIntBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseIntBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "number", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseInt(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseInt extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseInt";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapFromDriverValue(value) {
+    return typeof value === "string" ? Number(value) : value;
+  }
+}
+function intFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseIntBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const int8 = intFactory("ClickHouseInt8", "Int8");
+const int16 = intFactory("ClickHouseInt16", "Int16");
+const int32 = intFactory("ClickHouseInt32", "Int32");
+const uint8 = intFactory("ClickHouseUInt8", "UInt8");
+const uint16 = intFactory("ClickHouseUInt16", "UInt16");
+const uint32 = intFactory("ClickHouseUInt32", "UInt32");
+export {
+  ClickHouseInt,
+  ClickHouseIntBuilder,
+  int16,
+  int32,
+  int8,
+  uint16,
+  uint32,
+  uint8
+};
diff --git a/clickhouse-core/columns/ip.cjs b/clickhouse-core/columns/ip.cjs
new file mode 100644
index 0000000..4ac4654
--- /dev/null
+++ b/clickhouse-core/columns/ip.cjs
@@ -0,0 +1,68 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var ip_exports = {};
+__export(ip_exports, {
+  ClickHouseIp: () => ClickHouseIp,
+  ClickHouseIpBuilder: () => ClickHouseIpBuilder,
+  ipv4: () => ipv4,
+  ipv6: () => ipv6
+});
+module.exports = __toCommonJS(ip_exports);
+var import_entity = require("../../entity.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseIpBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseIpBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "string", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseIp(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseIp extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseIp";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapToDriverValue(value) {
+    return (0, import_literals.castFromString)(this.chType === "IPv4" ? "toIPv4" : "toIPv6", value);
+  }
+}
+function ipFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseIpBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const ipv4 = ipFactory("ClickHouseIPv4", "IPv4");
+const ipv6 = ipFactory("ClickHouseIPv6", "IPv6");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseIp,
+  ClickHouseIpBuilder,
+  ipv4,
+  ipv6
+});
diff --git a/clickhouse-core/columns/ip.d.cts b/clickhouse-core/columns/ip.d.cts
new file mode 100644
index 0000000..1032130
--- /dev/null
+++ b/clickhouse-core/columns/ip.d.cts
@@ -0,0 +1,38 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseIpTypeName = 'IPv4' | 'IPv6';
+interface ClickHouseIpRuntimeConfig {
+    chType: ClickHouseIpTypeName;
+}
+export type ClickHouseIpBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseIpBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: TColumnType;
+    data: string;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseIpBuilder<T extends ColumnBuilderBaseConfig<'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseIpRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseIpTypeName);
+}
+export declare class ClickHouseIp<T extends ColumnBaseConfig<'string', string>> extends ClickHouseColumn<T, ClickHouseIpRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseIpTypeName;
+    getBaseSQLType(): string;
+    mapToDriverValue(value: string): SQL;
+}
+/** `IPv4` — a 4-byte IPv4 address, surfaced as its dotted-quad string form. */
+export declare const ipv4: {
+    (): ClickHouseIpBuilderInitial<"", "ClickHouseIPv4">;
+    <TName extends string>(name: TName): ClickHouseIpBuilderInitial<TName, "ClickHouseIPv4">;
+};
+/** `IPv6` — a 16-byte IPv6 address, surfaced as its canonical string form. */
+export declare const ipv6: {
+    (): ClickHouseIpBuilderInitial<"", "ClickHouseIPv6">;
+    <TName extends string>(name: TName): ClickHouseIpBuilderInitial<TName, "ClickHouseIPv6">;
+};
+export {};
diff --git a/clickhouse-core/columns/ip.d.ts b/clickhouse-core/columns/ip.d.ts
new file mode 100644
index 0000000..ee78f16
--- /dev/null
+++ b/clickhouse-core/columns/ip.d.ts
@@ -0,0 +1,38 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseIpTypeName = 'IPv4' | 'IPv6';
+interface ClickHouseIpRuntimeConfig {
+    chType: ClickHouseIpTypeName;
+}
+export type ClickHouseIpBuilderInitial<TName extends string, TColumnType extends string> = ClickHouseIpBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: TColumnType;
+    data: string;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseIpBuilder<T extends ColumnBuilderBaseConfig<'string', string>> extends ClickHouseColumnBuilder<T, ClickHouseIpRuntimeConfig> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], columnType: T['columnType'], chType: ClickHouseIpTypeName);
+}
+export declare class ClickHouseIp<T extends ColumnBaseConfig<'string', string>> extends ClickHouseColumn<T, ClickHouseIpRuntimeConfig> {
+    static readonly [entityKind]: string;
+    readonly chType: ClickHouseIpTypeName;
+    getBaseSQLType(): string;
+    mapToDriverValue(value: string): SQL;
+}
+/** `IPv4` — a 4-byte IPv4 address, surfaced as its dotted-quad string form. */
+export declare const ipv4: {
+    (): ClickHouseIpBuilderInitial<"", "ClickHouseIPv4">;
+    <TName extends string>(name: TName): ClickHouseIpBuilderInitial<TName, "ClickHouseIPv4">;
+};
+/** `IPv6` — a 16-byte IPv6 address, surfaced as its canonical string form. */
+export declare const ipv6: {
+    (): ClickHouseIpBuilderInitial<"", "ClickHouseIPv6">;
+    <TName extends string>(name: TName): ClickHouseIpBuilderInitial<TName, "ClickHouseIPv6">;
+};
+export {};
diff --git a/clickhouse-core/columns/ip.js b/clickhouse-core/columns/ip.js
new file mode 100644
index 0000000..c911857
--- /dev/null
+++ b/clickhouse-core/columns/ip.js
@@ -0,0 +1,41 @@
+import { entityKind } from "../../entity.js";
+import { castFromString } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseIpBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseIpBuilder";
+  constructor(name, columnType, chType) {
+    super(name, "string", columnType);
+    this.config.chType = chType;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseIp(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseIp extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseIp";
+  chType = this.config.chType;
+  getBaseSQLType() {
+    return this.chType;
+  }
+  mapToDriverValue(value) {
+    return castFromString(this.chType === "IPv4" ? "toIPv4" : "toIPv6", value);
+  }
+}
+function ipFactory(columnType, chType) {
+  function column(name) {
+    return new ClickHouseIpBuilder(name ?? "", columnType, chType);
+  }
+  return column;
+}
+const ipv4 = ipFactory("ClickHouseIPv4", "IPv4");
+const ipv6 = ipFactory("ClickHouseIPv6", "IPv6");
+export {
+  ClickHouseIp,
+  ClickHouseIpBuilder,
+  ipv4,
+  ipv6
+};
diff --git a/clickhouse-core/columns/json.cjs b/clickhouse-core/columns/json.cjs
new file mode 100644
index 0000000..1c1e689
--- /dev/null
+++ b/clickhouse-core/columns/json.cjs
@@ -0,0 +1,71 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var json_exports = {};
+__export(json_exports, {
+  ClickHouseJson: () => ClickHouseJson,
+  ClickHouseJsonBuilder: () => ClickHouseJsonBuilder,
+  json: () => json
+});
+module.exports = __toCommonJS(json_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseJsonBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseJsonBuilder";
+  constructor(name) {
+    super(name, "json", "ClickHouseJson");
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseJson(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseJson extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseJson";
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return "JSON";
+  }
+  mapFromDriverValue(value) {
+    if (typeof value !== "string") return value;
+    try {
+      return JSON.parse(value);
+    } catch {
+      return value;
+    }
+  }
+  mapToDriverValue(value) {
+    return import_sql.sql`CAST(${JSON.stringify(value)} AS JSON)`;
+  }
+}
+function json(name) {
+  return new ClickHouseJsonBuilder(name ?? "");
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseJson,
+  ClickHouseJsonBuilder,
+  json
+});
diff --git a/clickhouse-core/columns/json.d.cts b/clickhouse-core/columns/json.d.cts
new file mode 100644
index 0000000..02ff9cb
--- /dev/null
+++ b/clickhouse-core/columns/json.d.cts
@@ -0,0 +1,36 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseJsonBuilderInitial<TName extends string> = ClickHouseJsonBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseJson';
+    data: unknown;
+    driverParam: unknown;
+    enumValues: undefined;
+    notNull: true;
+}>;
+export declare class ClickHouseJsonBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseJson'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+}
+export declare class ClickHouseJson<T extends ColumnBaseConfig<'json', 'ClickHouseJson'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown): unknown;
+    mapToDriverValue(value: unknown): SQL;
+}
+/**
+ * `JSON` — a semi-structured column that ClickHouse shreds into typed sub-columns on write.
+ *
+ * Use `$type` to give it a shape:
+ *
+ * ```ts
+ * payload: json().$type<{ userId: number; tags: string[] }>(),
+ * ```
+ */
+export declare function json(): ClickHouseJsonBuilderInitial<''>;
+export declare function json<TName extends string>(name: TName): ClickHouseJsonBuilderInitial<TName>;
diff --git a/clickhouse-core/columns/json.d.ts b/clickhouse-core/columns/json.d.ts
new file mode 100644
index 0000000..a3e7332
--- /dev/null
+++ b/clickhouse-core/columns/json.d.ts
@@ -0,0 +1,36 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseJsonBuilderInitial<TName extends string> = ClickHouseJsonBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseJson';
+    data: unknown;
+    driverParam: unknown;
+    enumValues: undefined;
+    notNull: true;
+}>;
+export declare class ClickHouseJsonBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseJson'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+}
+export declare class ClickHouseJson<T extends ColumnBaseConfig<'json', 'ClickHouseJson'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown): unknown;
+    mapToDriverValue(value: unknown): SQL;
+}
+/**
+ * `JSON` — a semi-structured column that ClickHouse shreds into typed sub-columns on write.
+ *
+ * Use `$type` to give it a shape:
+ *
+ * ```ts
+ * payload: json().$type<{ userId: number; tags: string[] }>(),
+ * ```
+ */
+export declare function json(): ClickHouseJsonBuilderInitial<''>;
+export declare function json<TName extends string>(name: TName): ClickHouseJsonBuilderInitial<TName>;
diff --git a/clickhouse-core/columns/json.js b/clickhouse-core/columns/json.js
new file mode 100644
index 0000000..4703aad
--- /dev/null
+++ b/clickhouse-core/columns/json.js
@@ -0,0 +1,45 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseJsonBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseJsonBuilder";
+  constructor(name) {
+    super(name, "json", "ClickHouseJson");
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseJson(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseJson extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseJson";
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return "JSON";
+  }
+  mapFromDriverValue(value) {
+    if (typeof value !== "string") return value;
+    try {
+      return JSON.parse(value);
+    } catch {
+      return value;
+    }
+  }
+  mapToDriverValue(value) {
+    return sql`CAST(${JSON.stringify(value)} AS JSON)`;
+  }
+}
+function json(name) {
+  return new ClickHouseJsonBuilder(name ?? "");
+}
+export {
+  ClickHouseJson,
+  ClickHouseJsonBuilder,
+  json
+};
diff --git a/clickhouse-core/columns/low-cardinality.cjs b/clickhouse-core/columns/low-cardinality.cjs
new file mode 100644
index 0000000..0477783
--- /dev/null
+++ b/clickhouse-core/columns/low-cardinality.cjs
@@ -0,0 +1,83 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var low_cardinality_exports = {};
+__export(low_cardinality_exports, {
+  ClickHouseLowCardinality: () => ClickHouseLowCardinality,
+  ClickHouseLowCardinalityBuilder: () => ClickHouseLowCardinalityBuilder,
+  lowCardinality: () => lowCardinality
+});
+module.exports = __toCommonJS(low_cardinality_exports);
+var import_entity = require("../../entity.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseLowCardinalityBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseLowCardinalityBuilder";
+  constructor(name, baseBuilder) {
+    super(name, baseBuilder.getDataType(), "ClickHouseLowCardinality");
+    this.config.baseBuilder = baseBuilder;
+    this.config.enumValues = baseBuilder.getEnumValues();
+  }
+  /** @internal */
+  build(table) {
+    const baseColumn = this.config.baseBuilder.markAsElement().build(table);
+    return new ClickHouseLowCardinality(
+      table,
+      this.config,
+      baseColumn
+    );
+  }
+}
+class ClickHouseLowCardinality extends import_common.ClickHouseColumn {
+  constructor(table, config, baseColumn) {
+    super(table, config);
+    this.baseColumn = baseColumn;
+  }
+  baseColumn;
+  static [import_entity.entityKind] = "ClickHouseLowCardinality";
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return this.baseColumn.getSQLType();
+  }
+  /**
+   * `Nullable` nests *inside* `LowCardinality`, not around it, so the generic
+   * `Nullable(<base>)` wrapping in {@link ClickHouseColumn.getSQLType} would produce an invalid type.
+   */
+  getSQLType() {
+    const inner = this.baseColumn.getSQLType();
+    const innerType = this.notNull || inner.startsWith("Nullable(") ? inner : `Nullable(${inner})`;
+    return `LowCardinality(${innerType})`;
+  }
+  mapFromDriverValue(value) {
+    return this.baseColumn.mapFromDriverValue(value);
+  }
+  mapToDriverValue(value) {
+    return this.baseColumn.mapToDriverValue(value);
+  }
+  mapToRowValue(value) {
+    return this.baseColumn.mapToRowValue(value);
+  }
+}
+function lowCardinality(a, b) {
+  return typeof a === "string" ? new ClickHouseLowCardinalityBuilder(a, b) : new ClickHouseLowCardinalityBuilder("", a);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseLowCardinality,
+  ClickHouseLowCardinalityBuilder,
+  lowCardinality
+});
diff --git a/clickhouse-core/columns/low-cardinality.d.cts b/clickhouse-core/columns/low-cardinality.d.cts
new file mode 100644
index 0000000..c2ac57d
--- /dev/null
+++ b/clickhouse-core/columns/low-cardinality.d.cts
@@ -0,0 +1,52 @@
+import type { AnyClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.cjs';
+export type ClickHouseLowCardinalityBuilderInitial<TName extends string, TBase extends ClickHouseColumnBuilderBase> = ClickHouseLowCardinalityBuilder<{
+    name: TName;
+    dataType: TBase['_']['dataType'];
+    columnType: 'ClickHouseLowCardinality';
+    data: TBase['_'] extends {
+        $type: infer U;
+    } ? U : TBase['_']['data'];
+    driverParam: TBase['_']['driverParam'];
+    enumValues: TBase['_']['enumValues'];
+}, TBase>;
+export declare class ClickHouseLowCardinalityBuilder<T extends ColumnBuilderBaseConfig<any, 'ClickHouseLowCardinality'>, TBase extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    baseBuilder: TBase;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], baseBuilder: TBase);
+}
+export declare class ClickHouseLowCardinality<T extends ColumnBaseConfig<any, 'ClickHouseLowCardinality'>> extends ClickHouseColumn<T, {
+    enumValues: T['enumValues'];
+}> {
+    readonly baseColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, baseColumn: ClickHouseColumn);
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+    /**
+     * `Nullable` nests *inside* `LowCardinality`, not around it, so the generic
+     * `Nullable(<base>)` wrapping in {@link ClickHouseColumn.getSQLType} would produce an invalid type.
+     */
+    getSQLType(): string;
+    mapFromDriverValue(value: unknown): unknown;
+    mapToDriverValue(value: unknown): unknown;
+    mapToRowValue(value: unknown): unknown;
+}
+/**
+ * `LowCardinality(T)` — dictionary-encodes a column whose values repeat heavily. ClickHouse
+ * recommends it below roughly 10 000 distinct values.
+ *
+ * ```ts
+ * country: lowCardinality(string()).notNull(),  // LowCardinality(String)
+ * region: lowCardinality(string()),             // LowCardinality(Nullable(String))
+ * ```
+ */
+export declare function lowCardinality<TBase extends ClickHouseColumnBuilderBase>(baseBuilder: TBase): ClickHouseLowCardinalityBuilderInitial<'', TBase>;
+export declare function lowCardinality<TName extends string, TBase extends ClickHouseColumnBuilderBase>(name: TName, baseBuilder: TBase): ClickHouseLowCardinalityBuilderInitial<TName, TBase>;
diff --git a/clickhouse-core/columns/low-cardinality.d.ts b/clickhouse-core/columns/low-cardinality.d.ts
new file mode 100644
index 0000000..9c1ed6d
--- /dev/null
+++ b/clickhouse-core/columns/low-cardinality.d.ts
@@ -0,0 +1,52 @@
+import type { AnyClickHouseTable } from '../table.js';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.js';
+export type ClickHouseLowCardinalityBuilderInitial<TName extends string, TBase extends ClickHouseColumnBuilderBase> = ClickHouseLowCardinalityBuilder<{
+    name: TName;
+    dataType: TBase['_']['dataType'];
+    columnType: 'ClickHouseLowCardinality';
+    data: TBase['_'] extends {
+        $type: infer U;
+    } ? U : TBase['_']['data'];
+    driverParam: TBase['_']['driverParam'];
+    enumValues: TBase['_']['enumValues'];
+}, TBase>;
+export declare class ClickHouseLowCardinalityBuilder<T extends ColumnBuilderBaseConfig<any, 'ClickHouseLowCardinality'>, TBase extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    baseBuilder: TBase;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], baseBuilder: TBase);
+}
+export declare class ClickHouseLowCardinality<T extends ColumnBaseConfig<any, 'ClickHouseLowCardinality'>> extends ClickHouseColumn<T, {
+    enumValues: T['enumValues'];
+}> {
+    readonly baseColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, baseColumn: ClickHouseColumn);
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+    /**
+     * `Nullable` nests *inside* `LowCardinality`, not around it, so the generic
+     * `Nullable(<base>)` wrapping in {@link ClickHouseColumn.getSQLType} would produce an invalid type.
+     */
+    getSQLType(): string;
+    mapFromDriverValue(value: unknown): unknown;
+    mapToDriverValue(value: unknown): unknown;
+    mapToRowValue(value: unknown): unknown;
+}
+/**
+ * `LowCardinality(T)` — dictionary-encodes a column whose values repeat heavily. ClickHouse
+ * recommends it below roughly 10 000 distinct values.
+ *
+ * ```ts
+ * country: lowCardinality(string()).notNull(),  // LowCardinality(String)
+ * region: lowCardinality(string()),             // LowCardinality(Nullable(String))
+ * ```
+ */
+export declare function lowCardinality<TBase extends ClickHouseColumnBuilderBase>(baseBuilder: TBase): ClickHouseLowCardinalityBuilderInitial<'', TBase>;
+export declare function lowCardinality<TName extends string, TBase extends ClickHouseColumnBuilderBase>(name: TName, baseBuilder: TBase): ClickHouseLowCardinalityBuilderInitial<TName, TBase>;
diff --git a/clickhouse-core/columns/low-cardinality.js b/clickhouse-core/columns/low-cardinality.js
new file mode 100644
index 0000000..30ca819
--- /dev/null
+++ b/clickhouse-core/columns/low-cardinality.js
@@ -0,0 +1,57 @@
+import { entityKind } from "../../entity.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseLowCardinalityBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseLowCardinalityBuilder";
+  constructor(name, baseBuilder) {
+    super(name, baseBuilder.getDataType(), "ClickHouseLowCardinality");
+    this.config.baseBuilder = baseBuilder;
+    this.config.enumValues = baseBuilder.getEnumValues();
+  }
+  /** @internal */
+  build(table) {
+    const baseColumn = this.config.baseBuilder.markAsElement().build(table);
+    return new ClickHouseLowCardinality(
+      table,
+      this.config,
+      baseColumn
+    );
+  }
+}
+class ClickHouseLowCardinality extends ClickHouseColumn {
+  constructor(table, config, baseColumn) {
+    super(table, config);
+    this.baseColumn = baseColumn;
+  }
+  baseColumn;
+  static [entityKind] = "ClickHouseLowCardinality";
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return this.baseColumn.getSQLType();
+  }
+  /**
+   * `Nullable` nests *inside* `LowCardinality`, not around it, so the generic
+   * `Nullable(<base>)` wrapping in {@link ClickHouseColumn.getSQLType} would produce an invalid type.
+   */
+  getSQLType() {
+    const inner = this.baseColumn.getSQLType();
+    const innerType = this.notNull || inner.startsWith("Nullable(") ? inner : `Nullable(${inner})`;
+    return `LowCardinality(${innerType})`;
+  }
+  mapFromDriverValue(value) {
+    return this.baseColumn.mapFromDriverValue(value);
+  }
+  mapToDriverValue(value) {
+    return this.baseColumn.mapToDriverValue(value);
+  }
+  mapToRowValue(value) {
+    return this.baseColumn.mapToRowValue(value);
+  }
+}
+function lowCardinality(a, b) {
+  return typeof a === "string" ? new ClickHouseLowCardinalityBuilder(a, b) : new ClickHouseLowCardinalityBuilder("", a);
+}
+export {
+  ClickHouseLowCardinality,
+  ClickHouseLowCardinalityBuilder,
+  lowCardinality
+};
diff --git a/clickhouse-core/columns/map.cjs b/clickhouse-core/columns/map.cjs
new file mode 100644
index 0000000..99cf27a
--- /dev/null
+++ b/clickhouse-core/columns/map.cjs
@@ -0,0 +1,106 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var map_exports = {};
+__export(map_exports, {
+  ClickHouseMap: () => ClickHouseMap,
+  ClickHouseMapBuilder: () => ClickHouseMapBuilder,
+  map: () => map
+});
+module.exports = __toCommonJS(map_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseMapBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseMapBuilder";
+  constructor(name, keyBuilder, valueBuilder) {
+    super(name, "json", "ClickHouseMap");
+    this.config.keyBuilder = keyBuilder;
+    this.config.valueBuilder = valueBuilder;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const keyColumn = this.config.keyBuilder.markAsElement().build(table);
+    const valueColumn = this.config.valueBuilder.markAsElement().build(table);
+    return new ClickHouseMap(
+      table,
+      this.config,
+      keyColumn,
+      valueColumn
+    );
+  }
+}
+class ClickHouseMap extends import_common.ClickHouseColumn {
+  constructor(table, config, keyColumn, valueColumn) {
+    super(table, config);
+    this.keyColumn = keyColumn;
+    this.valueColumn = valueColumn;
+  }
+  keyColumn;
+  valueColumn;
+  static [import_entity.entityKind] = "ClickHouseMap";
+  /** ClickHouse rejects `Nullable(Map(...))`; use an empty map instead of null. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return `Map(${this.keyColumn.getSQLType()}, ${this.valueColumn.getSQLType()})`;
+  }
+  mapFromDriverValue(value) {
+    const entries = Array.isArray(value) ? value : Object.entries(value);
+    const result = {};
+    for (const [key, entryValue] of entries) {
+      result[this.keyColumn.mapFromDriverValue(key)] = entryValue === null ? null : this.valueColumn.mapFromDriverValue(entryValue);
+    }
+    return result;
+  }
+  /**
+   * A plain object. ClickHouse's JSON formats represent a `Map(K, V)` as one, and JSON object keys
+   * are always strings — so a numeric-keyed map's keys go through the key column's own mapping and
+   * are then stringified, which is exactly what {@link mapFromDriverValue} undoes.
+   */
+  mapToRowValue(value) {
+    const entries = value instanceof Map ? [...value.entries()] : Object.entries(value);
+    const result = {};
+    for (const [key, entryValue] of entries) {
+      result[String(this.keyColumn.mapToRowValue(key))] = entryValue === null ? null : this.valueColumn.mapToRowValue(entryValue);
+    }
+    return result;
+  }
+  mapToDriverValue(value) {
+    const entries = value instanceof Map ? [...value.entries()] : Object.entries(value);
+    if (entries.length === 0) {
+      return import_sql.sql`CAST([], ${import_sql.sql.raw(`'${this.getBaseSQLType()}'`)})`;
+    }
+    const args = entries.flatMap(([key, entryValue]) => [
+      import_sql.sql`${this.keyColumn.mapToDriverValue(key)}`,
+      entryValue === null ? import_sql.sql`NULL` : import_sql.sql`${this.valueColumn.mapToDriverValue(entryValue)}`
+    ]);
+    return import_sql.sql`map(${import_sql.sql.join(args, import_sql.sql`, `)})`;
+  }
+}
+function map(a, b, c) {
+  return typeof a === "string" ? new ClickHouseMapBuilder(a, b, c) : new ClickHouseMapBuilder("", a, b);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseMap,
+  ClickHouseMapBuilder,
+  map
+});
diff --git a/clickhouse-core/columns/map.d.cts b/clickhouse-core/columns/map.d.cts
new file mode 100644
index 0000000..43ad90b
--- /dev/null
+++ b/clickhouse-core/columns/map.d.cts
@@ -0,0 +1,55 @@
+import type { AnyClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.cjs';
+import type { ElementData } from './nullable.cjs';
+/** Keys of a ClickHouse `Map` are always scalars, and arrive as JSON object keys. */
+export type ClickHouseMapKey<TKey extends ClickHouseColumnBuilderBase> = ElementData<TKey> extends infer TData ? TData extends string | number ? TData : string : never;
+export type ClickHouseMapBuilderInitial<TName extends string, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase> = ClickHouseMapBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseMap';
+    data: Record<ClickHouseMapKey<TKey>, ElementData<TValue>>;
+    driverParam: Record<string, unknown>;
+    enumValues: undefined;
+    notNull: true;
+}, TKey, TValue>;
+export declare class ClickHouseMapBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseMap'>, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    keyBuilder: TKey;
+    valueBuilder: TValue;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], keyBuilder: TKey, valueBuilder: TValue);
+}
+export declare class ClickHouseMap<T extends ColumnBaseConfig<'json', 'ClickHouseMap'>> extends ClickHouseColumn<T> {
+    readonly keyColumn: ClickHouseColumn;
+    readonly valueColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, keyColumn: ClickHouseColumn, valueColumn: ClickHouseColumn);
+    /** ClickHouse rejects `Nullable(Map(...))`; use an empty map instead of null. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: Record<string, unknown> | [unknown, unknown][]): Record<string, unknown>;
+    /**
+     * A plain object. ClickHouse's JSON formats represent a `Map(K, V)` as one, and JSON object keys
+     * are always strings — so a numeric-keyed map's keys go through the key column's own mapping and
+     * are then stringified, which is exactly what {@link mapFromDriverValue} undoes.
+     */
+    mapToRowValue(value: Record<string, unknown> | Map<unknown, unknown>): Record<string, unknown>;
+    mapToDriverValue(value: Record<string, unknown> | Map<unknown, unknown>): SQL;
+}
+/**
+ * `Map(K, V)` — an associative array. Keys and values are non-nullable unless wrapped in
+ * {@link nullable}.
+ *
+ * ```ts
+ * labels: map(string(), string()),
+ * counters: map(lowCardinality(string()), uint64()),
+ * ```
+ */
+export declare function map<TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase>(keyBuilder: TKey, valueBuilder: TValue): ClickHouseMapBuilderInitial<'', TKey, TValue>;
+export declare function map<TName extends string, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase>(name: TName, keyBuilder: TKey, valueBuilder: TValue): ClickHouseMapBuilderInitial<TName, TKey, TValue>;
diff --git a/clickhouse-core/columns/map.d.ts b/clickhouse-core/columns/map.d.ts
new file mode 100644
index 0000000..e29719c
--- /dev/null
+++ b/clickhouse-core/columns/map.d.ts
@@ -0,0 +1,55 @@
+import type { AnyClickHouseTable } from '../table.js';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.js';
+import type { ElementData } from './nullable.js';
+/** Keys of a ClickHouse `Map` are always scalars, and arrive as JSON object keys. */
+export type ClickHouseMapKey<TKey extends ClickHouseColumnBuilderBase> = ElementData<TKey> extends infer TData ? TData extends string | number ? TData : string : never;
+export type ClickHouseMapBuilderInitial<TName extends string, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase> = ClickHouseMapBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseMap';
+    data: Record<ClickHouseMapKey<TKey>, ElementData<TValue>>;
+    driverParam: Record<string, unknown>;
+    enumValues: undefined;
+    notNull: true;
+}, TKey, TValue>;
+export declare class ClickHouseMapBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseMap'>, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase> extends ClickHouseColumnBuilder<T, {
+    keyBuilder: TKey;
+    valueBuilder: TValue;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], keyBuilder: TKey, valueBuilder: TValue);
+}
+export declare class ClickHouseMap<T extends ColumnBaseConfig<'json', 'ClickHouseMap'>> extends ClickHouseColumn<T> {
+    readonly keyColumn: ClickHouseColumn;
+    readonly valueColumn: ClickHouseColumn;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, keyColumn: ClickHouseColumn, valueColumn: ClickHouseColumn);
+    /** ClickHouse rejects `Nullable(Map(...))`; use an empty map instead of null. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: Record<string, unknown> | [unknown, unknown][]): Record<string, unknown>;
+    /**
+     * A plain object. ClickHouse's JSON formats represent a `Map(K, V)` as one, and JSON object keys
+     * are always strings — so a numeric-keyed map's keys go through the key column's own mapping and
+     * are then stringified, which is exactly what {@link mapFromDriverValue} undoes.
+     */
+    mapToRowValue(value: Record<string, unknown> | Map<unknown, unknown>): Record<string, unknown>;
+    mapToDriverValue(value: Record<string, unknown> | Map<unknown, unknown>): SQL;
+}
+/**
+ * `Map(K, V)` — an associative array. Keys and values are non-nullable unless wrapped in
+ * {@link nullable}.
+ *
+ * ```ts
+ * labels: map(string(), string()),
+ * counters: map(lowCardinality(string()), uint64()),
+ * ```
+ */
+export declare function map<TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase>(keyBuilder: TKey, valueBuilder: TValue): ClickHouseMapBuilderInitial<'', TKey, TValue>;
+export declare function map<TName extends string, TKey extends ClickHouseColumnBuilderBase, TValue extends ClickHouseColumnBuilderBase>(name: TName, keyBuilder: TKey, valueBuilder: TValue): ClickHouseMapBuilderInitial<TName, TKey, TValue>;
diff --git a/clickhouse-core/columns/map.js b/clickhouse-core/columns/map.js
new file mode 100644
index 0000000..3d46bc8
--- /dev/null
+++ b/clickhouse-core/columns/map.js
@@ -0,0 +1,80 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseMapBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseMapBuilder";
+  constructor(name, keyBuilder, valueBuilder) {
+    super(name, "json", "ClickHouseMap");
+    this.config.keyBuilder = keyBuilder;
+    this.config.valueBuilder = valueBuilder;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const keyColumn = this.config.keyBuilder.markAsElement().build(table);
+    const valueColumn = this.config.valueBuilder.markAsElement().build(table);
+    return new ClickHouseMap(
+      table,
+      this.config,
+      keyColumn,
+      valueColumn
+    );
+  }
+}
+class ClickHouseMap extends ClickHouseColumn {
+  constructor(table, config, keyColumn, valueColumn) {
+    super(table, config);
+    this.keyColumn = keyColumn;
+    this.valueColumn = valueColumn;
+  }
+  keyColumn;
+  valueColumn;
+  static [entityKind] = "ClickHouseMap";
+  /** ClickHouse rejects `Nullable(Map(...))`; use an empty map instead of null. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    return `Map(${this.keyColumn.getSQLType()}, ${this.valueColumn.getSQLType()})`;
+  }
+  mapFromDriverValue(value) {
+    const entries = Array.isArray(value) ? value : Object.entries(value);
+    const result = {};
+    for (const [key, entryValue] of entries) {
+      result[this.keyColumn.mapFromDriverValue(key)] = entryValue === null ? null : this.valueColumn.mapFromDriverValue(entryValue);
+    }
+    return result;
+  }
+  /**
+   * A plain object. ClickHouse's JSON formats represent a `Map(K, V)` as one, and JSON object keys
+   * are always strings — so a numeric-keyed map's keys go through the key column's own mapping and
+   * are then stringified, which is exactly what {@link mapFromDriverValue} undoes.
+   */
+  mapToRowValue(value) {
+    const entries = value instanceof Map ? [...value.entries()] : Object.entries(value);
+    const result = {};
+    for (const [key, entryValue] of entries) {
+      result[String(this.keyColumn.mapToRowValue(key))] = entryValue === null ? null : this.valueColumn.mapToRowValue(entryValue);
+    }
+    return result;
+  }
+  mapToDriverValue(value) {
+    const entries = value instanceof Map ? [...value.entries()] : Object.entries(value);
+    if (entries.length === 0) {
+      return sql`CAST([], ${sql.raw(`'${this.getBaseSQLType()}'`)})`;
+    }
+    const args = entries.flatMap(([key, entryValue]) => [
+      sql`${this.keyColumn.mapToDriverValue(key)}`,
+      entryValue === null ? sql`NULL` : sql`${this.valueColumn.mapToDriverValue(entryValue)}`
+    ]);
+    return sql`map(${sql.join(args, sql`, `)})`;
+  }
+}
+function map(a, b, c) {
+  return typeof a === "string" ? new ClickHouseMapBuilder(a, b, c) : new ClickHouseMapBuilder("", a, b);
+}
+export {
+  ClickHouseMap,
+  ClickHouseMapBuilder,
+  map
+};
diff --git a/clickhouse-core/columns/nullable.cjs b/clickhouse-core/columns/nullable.cjs
new file mode 100644
index 0000000..c7dab43
--- /dev/null
+++ b/clickhouse-core/columns/nullable.cjs
@@ -0,0 +1,30 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var nullable_exports = {};
+__export(nullable_exports, {
+  nullable: () => nullable
+});
+module.exports = __toCommonJS(nullable_exports);
+function nullable(builder) {
+  return builder.markNullable();
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  nullable
+});
diff --git a/clickhouse-core/columns/nullable.d.cts b/clickhouse-core/columns/nullable.d.cts
new file mode 100644
index 0000000..d3a6c70
--- /dev/null
+++ b/clickhouse-core/columns/nullable.d.cts
@@ -0,0 +1,33 @@
+import type { ClickHouseColumnBuilderBase } from './common.cjs';
+/**
+ * The type produced by {@link nullable}.
+ *
+ * Column builders start out with `notNull: boolean` (i.e. "unspecified"), so intersecting `false`
+ * here narrows it rather than colliding with an existing `true`.
+ */
+export type Nullable<TBuilder extends ClickHouseColumnBuilderBase> = TBuilder & {
+    _: {
+        notNull: false;
+    };
+};
+/**
+ * Wraps a type in ClickHouse's `Nullable(T)`.
+ *
+ * Table columns follow Drizzle's usual convention — nullable unless `.notNull()` — so `nullable()` is
+ * mainly for the element types of `Array`, `Map` and `Tuple`, which are non-nullable by default:
+ *
+ * ```ts
+ * tags: array(string()),           // Array(String)
+ * scores: array(nullable(int32())) // Array(Nullable(Int32))
+ * ```
+ */
+export declare function nullable<TBuilder extends ClickHouseColumnBuilderBase>(builder: TBuilder): Nullable<TBuilder>;
+/**
+ * The TypeScript type of a value held by `TBuilder` when used as a composite element — `| null` only
+ * when it was wrapped in {@link nullable}.
+ */
+export type ElementData<TBuilder extends ClickHouseColumnBuilderBase> = TBuilder['_']['notNull'] extends false ? (TBuilder['_'] extends {
+    $type: infer U;
+} ? U : TBuilder['_']['data']) | null : TBuilder['_'] extends {
+    $type: infer U;
+} ? U : TBuilder['_']['data'];
diff --git a/clickhouse-core/columns/nullable.d.ts b/clickhouse-core/columns/nullable.d.ts
new file mode 100644
index 0000000..825c655
--- /dev/null
+++ b/clickhouse-core/columns/nullable.d.ts
@@ -0,0 +1,33 @@
+import type { ClickHouseColumnBuilderBase } from './common.js';
+/**
+ * The type produced by {@link nullable}.
+ *
+ * Column builders start out with `notNull: boolean` (i.e. "unspecified"), so intersecting `false`
+ * here narrows it rather than colliding with an existing `true`.
+ */
+export type Nullable<TBuilder extends ClickHouseColumnBuilderBase> = TBuilder & {
+    _: {
+        notNull: false;
+    };
+};
+/**
+ * Wraps a type in ClickHouse's `Nullable(T)`.
+ *
+ * Table columns follow Drizzle's usual convention — nullable unless `.notNull()` — so `nullable()` is
+ * mainly for the element types of `Array`, `Map` and `Tuple`, which are non-nullable by default:
+ *
+ * ```ts
+ * tags: array(string()),           // Array(String)
+ * scores: array(nullable(int32())) // Array(Nullable(Int32))
+ * ```
+ */
+export declare function nullable<TBuilder extends ClickHouseColumnBuilderBase>(builder: TBuilder): Nullable<TBuilder>;
+/**
+ * The TypeScript type of a value held by `TBuilder` when used as a composite element — `| null` only
+ * when it was wrapped in {@link nullable}.
+ */
+export type ElementData<TBuilder extends ClickHouseColumnBuilderBase> = TBuilder['_']['notNull'] extends false ? (TBuilder['_'] extends {
+    $type: infer U;
+} ? U : TBuilder['_']['data']) | null : TBuilder['_'] extends {
+    $type: infer U;
+} ? U : TBuilder['_']['data'];
diff --git a/clickhouse-core/columns/nullable.js b/clickhouse-core/columns/nullable.js
new file mode 100644
index 0000000..42c7234
--- /dev/null
+++ b/clickhouse-core/columns/nullable.js
@@ -0,0 +1,6 @@
+function nullable(builder) {
+  return builder.markNullable();
+}
+export {
+  nullable
+};
diff --git a/clickhouse-core/columns/string.cjs b/clickhouse-core/columns/string.cjs
new file mode 100644
index 0000000..2ccc735
--- /dev/null
+++ b/clickhouse-core/columns/string.cjs
@@ -0,0 +1,92 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var string_exports = {};
+__export(string_exports, {
+  ClickHouseFixedString: () => ClickHouseFixedString,
+  ClickHouseFixedStringBuilder: () => ClickHouseFixedStringBuilder,
+  ClickHouseString: () => ClickHouseString,
+  ClickHouseStringBuilder: () => ClickHouseStringBuilder,
+  fixedString: () => fixedString,
+  string: () => string
+});
+module.exports = __toCommonJS(string_exports);
+var import_entity = require("../../entity.cjs");
+var import_utils = require("../../utils.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseStringBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseStringBuilder";
+  constructor(name, config) {
+    super(name, "string", "ClickHouseString");
+    this.config.enumValues = config.enum;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseString(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseString extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseString";
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return "String";
+  }
+}
+function string(a, b = {}) {
+  const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+  return new ClickHouseStringBuilder(name, config);
+}
+class ClickHouseFixedStringBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseFixedStringBuilder";
+  constructor(name, config) {
+    super(name, "string", "ClickHouseFixedString");
+    this.config.length = config.length;
+    this.config.enumValues = config.enum;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseFixedString(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseFixedString extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseFixedString";
+  length = this.config.length;
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return `FixedString(${this.length})`;
+  }
+}
+function fixedString(a, b) {
+  const { name, config } = (0, import_utils.getColumnNameAndConfig)(a, b);
+  return new ClickHouseFixedStringBuilder(name, config);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseFixedString,
+  ClickHouseFixedStringBuilder,
+  ClickHouseString,
+  ClickHouseStringBuilder,
+  fixedString,
+  string
+});
diff --git a/clickhouse-core/columns/string.d.cts b/clickhouse-core/columns/string.d.cts
new file mode 100644
index 0000000..87c8f40
--- /dev/null
+++ b/clickhouse-core/columns/string.d.cts
@@ -0,0 +1,77 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { Writable } from '../../utils.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export interface ClickHouseStringConfig<TEnum extends readonly string[] | string[] | undefined = undefined> {
+    /**
+     * Narrows the TypeScript type of the column to a union of string literals.
+     *
+     * This is a type-level constraint only — unlike {@link enum8}/{@link enum16} it is not enforced by
+     * ClickHouse, and the column is still stored as a plain `String`.
+     */
+    enum?: TEnum;
+}
+export type ClickHouseStringBuilderInitial<TName extends string, TEnum extends [string, ...string[]]> = ClickHouseStringBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseString';
+    data: TEnum[number];
+    driverParam: string;
+    enumValues: TEnum;
+}>;
+export declare class ClickHouseStringBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseString'>> extends ClickHouseColumnBuilder<T, {
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], config: ClickHouseStringConfig<T['enumValues']>);
+}
+export declare class ClickHouseString<T extends ColumnBaseConfig<'string', 'ClickHouseString'>> extends ClickHouseColumn<T, {
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `String` — an arbitrary-length byte string. ClickHouse does not enforce a length limit and treats
+ * the value as bytes, so encoding is up to the application.
+ */
+export declare function string(): ClickHouseStringBuilderInitial<'', [string, ...string[]]>;
+export declare function string<U extends string, T extends Readonly<[U, ...U[]]>>(config?: ClickHouseStringConfig<T | Writable<T>>): ClickHouseStringBuilderInitial<'', Writable<T>>;
+export declare function string<TName extends string, U extends string, T extends Readonly<[U, ...U[]]>>(name: TName, config?: ClickHouseStringConfig<T | Writable<T>>): ClickHouseStringBuilderInitial<TName, Writable<T>>;
+export interface ClickHouseFixedStringConfig<TEnum extends readonly string[] | string[] | undefined = undefined> {
+    /** The exact byte length of the column. Shorter values are right-padded with null bytes. */
+    length: number;
+    enum?: TEnum;
+}
+export type ClickHouseFixedStringBuilderInitial<TName extends string, TEnum extends [string, ...string[]]> = ClickHouseFixedStringBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseFixedString';
+    data: TEnum[number];
+    driverParam: string;
+    enumValues: TEnum;
+}>;
+export declare class ClickHouseFixedStringBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseFixedString'>> extends ClickHouseColumnBuilder<T, {
+    length: number;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], config: ClickHouseFixedStringConfig<T['enumValues']>);
+}
+export declare class ClickHouseFixedString<T extends ColumnBaseConfig<'string', 'ClickHouseFixedString'>> extends ClickHouseColumn<T, {
+    length: number;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    readonly length: number;
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `FixedString(N)` — a fixed-width byte string. Values shorter than `N` are padded with null bytes
+ * on write; values longer than `N` are rejected by ClickHouse.
+ */
+export declare function fixedString<U extends string, T extends Readonly<[U, ...U[]]>>(config: ClickHouseFixedStringConfig<T | Writable<T>>): ClickHouseFixedStringBuilderInitial<'', Writable<T>>;
+export declare function fixedString<TName extends string, U extends string, T extends Readonly<[U, ...U[]]>>(name: TName, config: ClickHouseFixedStringConfig<T | Writable<T>>): ClickHouseFixedStringBuilderInitial<TName, Writable<T>>;
diff --git a/clickhouse-core/columns/string.d.ts b/clickhouse-core/columns/string.d.ts
new file mode 100644
index 0000000..f938bff
--- /dev/null
+++ b/clickhouse-core/columns/string.d.ts
@@ -0,0 +1,77 @@
+import type { ColumnBuilderBaseConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { Writable } from '../../utils.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export interface ClickHouseStringConfig<TEnum extends readonly string[] | string[] | undefined = undefined> {
+    /**
+     * Narrows the TypeScript type of the column to a union of string literals.
+     *
+     * This is a type-level constraint only — unlike {@link enum8}/{@link enum16} it is not enforced by
+     * ClickHouse, and the column is still stored as a plain `String`.
+     */
+    enum?: TEnum;
+}
+export type ClickHouseStringBuilderInitial<TName extends string, TEnum extends [string, ...string[]]> = ClickHouseStringBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseString';
+    data: TEnum[number];
+    driverParam: string;
+    enumValues: TEnum;
+}>;
+export declare class ClickHouseStringBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseString'>> extends ClickHouseColumnBuilder<T, {
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], config: ClickHouseStringConfig<T['enumValues']>);
+}
+export declare class ClickHouseString<T extends ColumnBaseConfig<'string', 'ClickHouseString'>> extends ClickHouseColumn<T, {
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `String` — an arbitrary-length byte string. ClickHouse does not enforce a length limit and treats
+ * the value as bytes, so encoding is up to the application.
+ */
+export declare function string(): ClickHouseStringBuilderInitial<'', [string, ...string[]]>;
+export declare function string<U extends string, T extends Readonly<[U, ...U[]]>>(config?: ClickHouseStringConfig<T | Writable<T>>): ClickHouseStringBuilderInitial<'', Writable<T>>;
+export declare function string<TName extends string, U extends string, T extends Readonly<[U, ...U[]]>>(name: TName, config?: ClickHouseStringConfig<T | Writable<T>>): ClickHouseStringBuilderInitial<TName, Writable<T>>;
+export interface ClickHouseFixedStringConfig<TEnum extends readonly string[] | string[] | undefined = undefined> {
+    /** The exact byte length of the column. Shorter values are right-padded with null bytes. */
+    length: number;
+    enum?: TEnum;
+}
+export type ClickHouseFixedStringBuilderInitial<TName extends string, TEnum extends [string, ...string[]]> = ClickHouseFixedStringBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseFixedString';
+    data: TEnum[number];
+    driverParam: string;
+    enumValues: TEnum;
+}>;
+export declare class ClickHouseFixedStringBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseFixedString'>> extends ClickHouseColumnBuilder<T, {
+    length: number;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], config: ClickHouseFixedStringConfig<T['enumValues']>);
+}
+export declare class ClickHouseFixedString<T extends ColumnBaseConfig<'string', 'ClickHouseFixedString'>> extends ClickHouseColumn<T, {
+    length: number;
+    enumValues: T['enumValues'];
+}> {
+    static readonly [entityKind]: string;
+    readonly length: number;
+    readonly enumValues: T["enumValues"];
+    getBaseSQLType(): string;
+}
+/**
+ * `FixedString(N)` — a fixed-width byte string. Values shorter than `N` are padded with null bytes
+ * on write; values longer than `N` are rejected by ClickHouse.
+ */
+export declare function fixedString<U extends string, T extends Readonly<[U, ...U[]]>>(config: ClickHouseFixedStringConfig<T | Writable<T>>): ClickHouseFixedStringBuilderInitial<'', Writable<T>>;
+export declare function fixedString<TName extends string, U extends string, T extends Readonly<[U, ...U[]]>>(name: TName, config: ClickHouseFixedStringConfig<T | Writable<T>>): ClickHouseFixedStringBuilderInitial<TName, Writable<T>>;
diff --git a/clickhouse-core/columns/string.js b/clickhouse-core/columns/string.js
new file mode 100644
index 0000000..867c606
--- /dev/null
+++ b/clickhouse-core/columns/string.js
@@ -0,0 +1,63 @@
+import { entityKind } from "../../entity.js";
+import { getColumnNameAndConfig } from "../../utils.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseStringBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseStringBuilder";
+  constructor(name, config) {
+    super(name, "string", "ClickHouseString");
+    this.config.enumValues = config.enum;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseString(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseString extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseString";
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return "String";
+  }
+}
+function string(a, b = {}) {
+  const { name, config } = getColumnNameAndConfig(a, b);
+  return new ClickHouseStringBuilder(name, config);
+}
+class ClickHouseFixedStringBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseFixedStringBuilder";
+  constructor(name, config) {
+    super(name, "string", "ClickHouseFixedString");
+    this.config.length = config.length;
+    this.config.enumValues = config.enum;
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseFixedString(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseFixedString extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseFixedString";
+  length = this.config.length;
+  enumValues = this.config.enumValues;
+  getBaseSQLType() {
+    return `FixedString(${this.length})`;
+  }
+}
+function fixedString(a, b) {
+  const { name, config } = getColumnNameAndConfig(a, b);
+  return new ClickHouseFixedStringBuilder(name, config);
+}
+export {
+  ClickHouseFixedString,
+  ClickHouseFixedStringBuilder,
+  ClickHouseString,
+  ClickHouseStringBuilder,
+  fixedString,
+  string
+};
diff --git a/clickhouse-core/columns/tuple.cjs b/clickhouse-core/columns/tuple.cjs
new file mode 100644
index 0000000..1afaaed
--- /dev/null
+++ b/clickhouse-core/columns/tuple.cjs
@@ -0,0 +1,121 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var tuple_exports = {};
+__export(tuple_exports, {
+  ClickHouseTuple: () => ClickHouseTuple,
+  ClickHouseTupleBuilder: () => ClickHouseTupleBuilder,
+  tuple: () => tuple
+});
+module.exports = __toCommonJS(tuple_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseTupleBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseTupleBuilder";
+  constructor(name, members) {
+    super(name, "json", "ClickHouseTuple");
+    this.config.members = members;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const members = this.config.members;
+    const isNamed = !Array.isArray(members);
+    const entries = Object.entries(members).map(([key, builder]) => [
+      isNamed ? key : void 0,
+      builder.markAsElement().build(table)
+    ]);
+    return new ClickHouseTuple(
+      table,
+      this.config,
+      entries,
+      isNamed
+    );
+  }
+}
+class ClickHouseTuple extends import_common.ClickHouseColumn {
+  constructor(table, config, members, isNamed) {
+    super(table, config);
+    this.members = members;
+    this.isNamed = isNamed;
+  }
+  members;
+  isNamed;
+  static [import_entity.entityKind] = "ClickHouseTuple";
+  /** ClickHouse rejects `Nullable(Tuple(...))`. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    const members = this.members.map(
+      ([name, column]) => name === void 0 ? column.getSQLType() : `${(0, import_literals.escapeClickHouseIdentifier)(name)} ${column.getSQLType()}`
+    ).join(", ");
+    return `Tuple(${members})`;
+  }
+  mapFromDriverValue(value) {
+    if (this.isNamed && !Array.isArray(value)) {
+      const result = {};
+      for (const [name, column] of this.members) {
+        const raw = value[name];
+        result[name] = raw === null || raw === void 0 ? null : column.mapFromDriverValue(raw);
+      }
+      return result;
+    }
+    const values = Array.isArray(value) ? value : Object.values(value);
+    return this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? null : column.mapFromDriverValue(raw);
+    });
+  }
+  mapToDriverValue(value) {
+    const values = Array.isArray(value) ? value : this.members.map(([name]) => value[name]);
+    const args = this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? import_sql.sql`NULL` : import_sql.sql`${column.mapToDriverValue(raw)}`;
+    });
+    return import_sql.sql`tuple(${import_sql.sql.join(args, import_sql.sql`, `)})`;
+  }
+  /**
+   * An array for a positional tuple, an object for a named one — matching what
+   * {@link mapFromDriverValue} reads back and what ClickHouse's JSON formats use for each.
+   */
+  mapToRowValue(value) {
+    const values = Array.isArray(value) ? value : this.members.map(([name]) => value[name]);
+    const mapped = this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? null : column.mapToRowValue(raw);
+    });
+    if (!this.isNamed) return mapped;
+    const result = {};
+    this.members.forEach(([name], index) => {
+      result[name] = mapped[index];
+    });
+    return result;
+  }
+}
+function tuple(a, b) {
+  return typeof a === "string" ? new ClickHouseTupleBuilder(a, b) : new ClickHouseTupleBuilder("", a);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseTuple,
+  ClickHouseTupleBuilder,
+  tuple
+});
diff --git a/clickhouse-core/columns/tuple.d.cts b/clickhouse-core/columns/tuple.d.cts
new file mode 100644
index 0000000..c50570c
--- /dev/null
+++ b/clickhouse-core/columns/tuple.d.cts
@@ -0,0 +1,61 @@
+import type { AnyClickHouseTable } from '../table.cjs';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.cjs';
+import type { ElementData } from './nullable.cjs';
+/** The members of a tuple: positional when given as an array, named when given as an object. */
+export type ClickHouseTupleMembers = readonly [ClickHouseColumnBuilderBase, ...ClickHouseColumnBuilderBase[]] | Record<string, ClickHouseColumnBuilderBase>;
+export type ClickHouseTupleData<TMembers extends ClickHouseTupleMembers> = {
+    -readonly [K in keyof TMembers]: TMembers[K] extends ClickHouseColumnBuilderBase ? ElementData<TMembers[K]> : never;
+};
+export type ClickHouseTupleBuilderInitial<TName extends string, TMembers extends ClickHouseTupleMembers> = ClickHouseTupleBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseTuple';
+    data: ClickHouseTupleData<TMembers>;
+    driverParam: unknown;
+    enumValues: undefined;
+    notNull: true;
+}, TMembers>;
+export declare class ClickHouseTupleBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseTuple'>, TMembers extends ClickHouseTupleMembers> extends ClickHouseColumnBuilder<T, {
+    members: TMembers;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], members: TMembers);
+}
+export declare class ClickHouseTuple<T extends ColumnBaseConfig<'json', 'ClickHouseTuple'>> extends ClickHouseColumn<T> {
+    /** Members in declaration order; the name is set only for named tuples. */
+    readonly members: [string | undefined, ClickHouseColumn][];
+    readonly isNamed: boolean;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, 
+    /** Members in declaration order; the name is set only for named tuples. */
+    members: [string | undefined, ClickHouseColumn][], isNamed: boolean);
+    /** ClickHouse rejects `Nullable(Tuple(...))`. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown[] | Record<string, unknown>): unknown;
+    mapToDriverValue(value: unknown[] | Record<string, unknown>): SQL;
+    /**
+     * An array for a positional tuple, an object for a named one — matching what
+     * {@link mapFromDriverValue} reads back and what ClickHouse's JSON formats use for each.
+     */
+    mapToRowValue(value: unknown[] | Record<string, unknown>): unknown;
+}
+/**
+ * `Tuple(...)` — a fixed-shape record.
+ *
+ * Positional members are declared with an array and surface as a TypeScript tuple; named members are
+ * declared with an object and surface as an object.
+ *
+ * ```ts
+ * point: tuple([float64(), float64()]),              // Tuple(Float64, Float64)
+ * bounds: tuple({ min: int32(), max: int32() }),     // Tuple(`min` Int32, `max` Int32)
+ * ```
+ */
+export declare function tuple<TMembers extends ClickHouseTupleMembers>(members: TMembers): ClickHouseTupleBuilderInitial<'', TMembers>;
+export declare function tuple<TName extends string, TMembers extends ClickHouseTupleMembers>(name: TName, members: TMembers): ClickHouseTupleBuilderInitial<TName, TMembers>;
diff --git a/clickhouse-core/columns/tuple.d.ts b/clickhouse-core/columns/tuple.d.ts
new file mode 100644
index 0000000..22dbedc
--- /dev/null
+++ b/clickhouse-core/columns/tuple.d.ts
@@ -0,0 +1,61 @@
+import type { AnyClickHouseTable } from '../table.js';
+import type { ColumnBuilderBaseConfig, ColumnBuilderRuntimeConfig } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder, type ClickHouseColumnBuilderBase } from './common.js';
+import type { ElementData } from './nullable.js';
+/** The members of a tuple: positional when given as an array, named when given as an object. */
+export type ClickHouseTupleMembers = readonly [ClickHouseColumnBuilderBase, ...ClickHouseColumnBuilderBase[]] | Record<string, ClickHouseColumnBuilderBase>;
+export type ClickHouseTupleData<TMembers extends ClickHouseTupleMembers> = {
+    -readonly [K in keyof TMembers]: TMembers[K] extends ClickHouseColumnBuilderBase ? ElementData<TMembers[K]> : never;
+};
+export type ClickHouseTupleBuilderInitial<TName extends string, TMembers extends ClickHouseTupleMembers> = ClickHouseTupleBuilder<{
+    name: TName;
+    dataType: 'json';
+    columnType: 'ClickHouseTuple';
+    data: ClickHouseTupleData<TMembers>;
+    driverParam: unknown;
+    enumValues: undefined;
+    notNull: true;
+}, TMembers>;
+export declare class ClickHouseTupleBuilder<T extends ColumnBuilderBaseConfig<'json', 'ClickHouseTuple'>, TMembers extends ClickHouseTupleMembers> extends ClickHouseColumnBuilder<T, {
+    members: TMembers;
+}> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name'], members: TMembers);
+}
+export declare class ClickHouseTuple<T extends ColumnBaseConfig<'json', 'ClickHouseTuple'>> extends ClickHouseColumn<T> {
+    /** Members in declaration order; the name is set only for named tuples. */
+    readonly members: [string | undefined, ClickHouseColumn][];
+    readonly isNamed: boolean;
+    static readonly [entityKind]: string;
+    constructor(table: AnyClickHouseTable<{
+        name: T['tableName'];
+    }>, config: ColumnBuilderRuntimeConfig<any, any>, 
+    /** Members in declaration order; the name is set only for named tuples. */
+    members: [string | undefined, ClickHouseColumn][], isNamed: boolean);
+    /** ClickHouse rejects `Nullable(Tuple(...))`. */
+    get supportsNullable(): boolean;
+    getBaseSQLType(): string;
+    mapFromDriverValue(value: unknown[] | Record<string, unknown>): unknown;
+    mapToDriverValue(value: unknown[] | Record<string, unknown>): SQL;
+    /**
+     * An array for a positional tuple, an object for a named one — matching what
+     * {@link mapFromDriverValue} reads back and what ClickHouse's JSON formats use for each.
+     */
+    mapToRowValue(value: unknown[] | Record<string, unknown>): unknown;
+}
+/**
+ * `Tuple(...)` — a fixed-shape record.
+ *
+ * Positional members are declared with an array and surface as a TypeScript tuple; named members are
+ * declared with an object and surface as an object.
+ *
+ * ```ts
+ * point: tuple([float64(), float64()]),              // Tuple(Float64, Float64)
+ * bounds: tuple({ min: int32(), max: int32() }),     // Tuple(`min` Int32, `max` Int32)
+ * ```
+ */
+export declare function tuple<TMembers extends ClickHouseTupleMembers>(members: TMembers): ClickHouseTupleBuilderInitial<'', TMembers>;
+export declare function tuple<TName extends string, TMembers extends ClickHouseTupleMembers>(name: TName, members: TMembers): ClickHouseTupleBuilderInitial<TName, TMembers>;
diff --git a/clickhouse-core/columns/tuple.js b/clickhouse-core/columns/tuple.js
new file mode 100644
index 0000000..507385b
--- /dev/null
+++ b/clickhouse-core/columns/tuple.js
@@ -0,0 +1,95 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { escapeClickHouseIdentifier } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseTupleBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseTupleBuilder";
+  constructor(name, members) {
+    super(name, "json", "ClickHouseTuple");
+    this.config.members = members;
+    this.config.notNull = true;
+  }
+  /** @internal */
+  build(table) {
+    const members = this.config.members;
+    const isNamed = !Array.isArray(members);
+    const entries = Object.entries(members).map(([key, builder]) => [
+      isNamed ? key : void 0,
+      builder.markAsElement().build(table)
+    ]);
+    return new ClickHouseTuple(
+      table,
+      this.config,
+      entries,
+      isNamed
+    );
+  }
+}
+class ClickHouseTuple extends ClickHouseColumn {
+  constructor(table, config, members, isNamed) {
+    super(table, config);
+    this.members = members;
+    this.isNamed = isNamed;
+  }
+  members;
+  isNamed;
+  static [entityKind] = "ClickHouseTuple";
+  /** ClickHouse rejects `Nullable(Tuple(...))`. */
+  get supportsNullable() {
+    return false;
+  }
+  getBaseSQLType() {
+    const members = this.members.map(
+      ([name, column]) => name === void 0 ? column.getSQLType() : `${escapeClickHouseIdentifier(name)} ${column.getSQLType()}`
+    ).join(", ");
+    return `Tuple(${members})`;
+  }
+  mapFromDriverValue(value) {
+    if (this.isNamed && !Array.isArray(value)) {
+      const result = {};
+      for (const [name, column] of this.members) {
+        const raw = value[name];
+        result[name] = raw === null || raw === void 0 ? null : column.mapFromDriverValue(raw);
+      }
+      return result;
+    }
+    const values = Array.isArray(value) ? value : Object.values(value);
+    return this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? null : column.mapFromDriverValue(raw);
+    });
+  }
+  mapToDriverValue(value) {
+    const values = Array.isArray(value) ? value : this.members.map(([name]) => value[name]);
+    const args = this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? sql`NULL` : sql`${column.mapToDriverValue(raw)}`;
+    });
+    return sql`tuple(${sql.join(args, sql`, `)})`;
+  }
+  /**
+   * An array for a positional tuple, an object for a named one — matching what
+   * {@link mapFromDriverValue} reads back and what ClickHouse's JSON formats use for each.
+   */
+  mapToRowValue(value) {
+    const values = Array.isArray(value) ? value : this.members.map(([name]) => value[name]);
+    const mapped = this.members.map(([, column], index) => {
+      const raw = values[index];
+      return raw === null || raw === void 0 ? null : column.mapToRowValue(raw);
+    });
+    if (!this.isNamed) return mapped;
+    const result = {};
+    this.members.forEach(([name], index) => {
+      result[name] = mapped[index];
+    });
+    return result;
+  }
+}
+function tuple(a, b) {
+  return typeof a === "string" ? new ClickHouseTupleBuilder(a, b) : new ClickHouseTupleBuilder("", a);
+}
+export {
+  ClickHouseTuple,
+  ClickHouseTupleBuilder,
+  tuple
+};
diff --git a/clickhouse-core/columns/uuid.cjs b/clickhouse-core/columns/uuid.cjs
new file mode 100644
index 0000000..3276762
--- /dev/null
+++ b/clickhouse-core/columns/uuid.cjs
@@ -0,0 +1,64 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var uuid_exports = {};
+__export(uuid_exports, {
+  ClickHouseUUID: () => ClickHouseUUID,
+  ClickHouseUUIDBuilder: () => ClickHouseUUIDBuilder,
+  uuid: () => uuid
+});
+module.exports = __toCommonJS(uuid_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_literals = require("../literals.cjs");
+var import_common = require("./common.cjs");
+class ClickHouseUUIDBuilder extends import_common.ClickHouseColumnBuilder {
+  static [import_entity.entityKind] = "ClickHouseUUIDBuilder";
+  constructor(name) {
+    super(name, "string", "ClickHouseUUID");
+  }
+  /** Adds `DEFAULT generateUUIDv4()` to the column definition. */
+  defaultRandom() {
+    return this.default(import_sql.sql`generateUUIDv4()`);
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseUUID(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseUUID extends import_common.ClickHouseColumn {
+  static [import_entity.entityKind] = "ClickHouseUUID";
+  getBaseSQLType() {
+    return "UUID";
+  }
+  mapToDriverValue(value) {
+    return (0, import_literals.castFromString)("toUUID", value);
+  }
+}
+function uuid(name) {
+  return new ClickHouseUUIDBuilder(name ?? "");
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseUUID,
+  ClickHouseUUIDBuilder,
+  uuid
+});
diff --git a/clickhouse-core/columns/uuid.d.cts b/clickhouse-core/columns/uuid.d.cts
new file mode 100644
index 0000000..b84501d
--- /dev/null
+++ b/clickhouse-core/columns/uuid.d.cts
@@ -0,0 +1,27 @@
+import type { ColumnBuilderBaseConfig, HasDefault } from '../../column-builder.cjs';
+import type { ColumnBaseConfig } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { SQL } from '../../sql/sql.cjs';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.cjs';
+export type ClickHouseUUIDBuilderInitial<TName extends string> = ClickHouseUUIDBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseUUID';
+    data: string;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseUUIDBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseUUID'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+    /** Adds `DEFAULT generateUUIDv4()` to the column definition. */
+    defaultRandom(): HasDefault<this>;
+}
+export declare class ClickHouseUUID<T extends ColumnBaseConfig<'string', 'ClickHouseUUID'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    getBaseSQLType(): string;
+    mapToDriverValue(value: string): SQL;
+}
+/** `UUID` — a 16-byte universally unique identifier, surfaced as its canonical string form. */
+export declare function uuid(): ClickHouseUUIDBuilderInitial<''>;
+export declare function uuid<TName extends string>(name: TName): ClickHouseUUIDBuilderInitial<TName>;
diff --git a/clickhouse-core/columns/uuid.d.ts b/clickhouse-core/columns/uuid.d.ts
new file mode 100644
index 0000000..42eb783
--- /dev/null
+++ b/clickhouse-core/columns/uuid.d.ts
@@ -0,0 +1,27 @@
+import type { ColumnBuilderBaseConfig, HasDefault } from '../../column-builder.js';
+import type { ColumnBaseConfig } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import type { SQL } from '../../sql/sql.js';
+import { ClickHouseColumn, ClickHouseColumnBuilder } from './common.js';
+export type ClickHouseUUIDBuilderInitial<TName extends string> = ClickHouseUUIDBuilder<{
+    name: TName;
+    dataType: 'string';
+    columnType: 'ClickHouseUUID';
+    data: string;
+    driverParam: string;
+    enumValues: undefined;
+}>;
+export declare class ClickHouseUUIDBuilder<T extends ColumnBuilderBaseConfig<'string', 'ClickHouseUUID'>> extends ClickHouseColumnBuilder<T> {
+    static readonly [entityKind]: string;
+    constructor(name: T['name']);
+    /** Adds `DEFAULT generateUUIDv4()` to the column definition. */
+    defaultRandom(): HasDefault<this>;
+}
+export declare class ClickHouseUUID<T extends ColumnBaseConfig<'string', 'ClickHouseUUID'>> extends ClickHouseColumn<T> {
+    static readonly [entityKind]: string;
+    getBaseSQLType(): string;
+    mapToDriverValue(value: string): SQL;
+}
+/** `UUID` — a 16-byte universally unique identifier, surfaced as its canonical string form. */
+export declare function uuid(): ClickHouseUUIDBuilderInitial<''>;
+export declare function uuid<TName extends string>(name: TName): ClickHouseUUIDBuilderInitial<TName>;
diff --git a/clickhouse-core/columns/uuid.js b/clickhouse-core/columns/uuid.js
new file mode 100644
index 0000000..c02ea7b
--- /dev/null
+++ b/clickhouse-core/columns/uuid.js
@@ -0,0 +1,38 @@
+import { entityKind } from "../../entity.js";
+import { sql } from "../../sql/sql.js";
+import { castFromString } from "../literals.js";
+import { ClickHouseColumn, ClickHouseColumnBuilder } from "./common.js";
+class ClickHouseUUIDBuilder extends ClickHouseColumnBuilder {
+  static [entityKind] = "ClickHouseUUIDBuilder";
+  constructor(name) {
+    super(name, "string", "ClickHouseUUID");
+  }
+  /** Adds `DEFAULT generateUUIDv4()` to the column definition. */
+  defaultRandom() {
+    return this.default(sql`generateUUIDv4()`);
+  }
+  /** @internal */
+  build(table) {
+    return new ClickHouseUUID(
+      table,
+      this.config
+    );
+  }
+}
+class ClickHouseUUID extends ClickHouseColumn {
+  static [entityKind] = "ClickHouseUUID";
+  getBaseSQLType() {
+    return "UUID";
+  }
+  mapToDriverValue(value) {
+    return castFromString("toUUID", value);
+  }
+}
+function uuid(name) {
+  return new ClickHouseUUIDBuilder(name ?? "");
+}
+export {
+  ClickHouseUUID,
+  ClickHouseUUIDBuilder,
+  uuid
+};
diff --git a/clickhouse-core/db.cjs b/clickhouse-core/db.cjs
new file mode 100644
index 0000000..6a4d299
--- /dev/null
+++ b/clickhouse-core/db.cjs
@@ -0,0 +1,187 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var db_exports = {};
+__export(db_exports, {
+  ClickHouseDatabase: () => ClickHouseDatabase,
+  withReplicas: () => withReplicas
+});
+module.exports = __toCommonJS(db_exports);
+var import_entity = require("../entity.cjs");
+var import_selection_proxy = require("../selection-proxy.cjs");
+var import_sql = require("../sql/sql.cjs");
+var import_subquery = require("../subquery.cjs");
+var import_count = require("./query-builders/count.cjs");
+var import_query_builders = require("./query-builders/index.cjs");
+class ClickHouseDatabase {
+  constructor(dialect, session, schema) {
+    this.dialect = dialect;
+    this.session = session;
+    this._ = schema ? {
+      schema: schema.schema,
+      fullSchema: schema.fullSchema,
+      tableNamesMap: schema.tableNamesMap
+    } : {
+      schema: void 0,
+      fullSchema: {},
+      tableNamesMap: {}
+    };
+    this.$cache = { invalidate: async (_params) => {
+    } };
+  }
+  dialect;
+  session;
+  static [import_entity.entityKind] = "ClickHouseDatabase";
+  /**
+   * Creates a subquery that defines a temporary named result set as a CTE.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+   *
+   * @param alias The alias for the subquery.
+   */
+  $with = (alias, selection) => {
+    const self = this;
+    const as = (qb) => {
+      if (typeof qb === "function") {
+        qb = qb(new import_query_builders.QueryBuilder(self.dialect));
+      }
+      return new Proxy(
+        new import_subquery.WithSubquery(
+          qb.getSQL(),
+          selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
+          alias,
+          true
+        ),
+        new import_selection_proxy.SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+      );
+    };
+    return { as };
+  };
+  $count(source, filters) {
+    return new import_count.ClickHouseCountBuilder({ source, filters, session: this.session });
+  }
+  /**
+   * Incorporates a previously defined CTE (using `$with`) into the main query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+   */
+  with(...queries) {
+    const self = this;
+    function select(fields) {
+      return new import_query_builders.ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: self.session,
+        dialect: self.dialect,
+        withList: queries
+      });
+    }
+    function selectDistinct(fields) {
+      return new import_query_builders.ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: self.session,
+        dialect: self.dialect,
+        withList: queries,
+        distinct: true
+      });
+    }
+    return { select, selectDistinct };
+  }
+  select(fields) {
+    return new import_query_builders.ClickHouseSelectBuilder({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
+  }
+  selectDistinct(fields) {
+    return new import_query_builders.ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: this.session,
+      dialect: this.dialect,
+      distinct: true
+    });
+  }
+  /**
+   * Creates an `ALTER TABLE … UPDATE` mutation.
+   *
+   * ClickHouse has no in-place `UPDATE`. The statement returns as soon as the mutation is accepted
+   * and the rewrite happens asynchronously — pass `.settings({ mutations_sync: 2 })` to wait for it.
+   * Updates are expensive; prefer modelling changes as new rows with a `ReplacingMergeTree`.
+   *
+   * @example
+   * ```ts
+   * await db.update(users).set({ name: 'Ada' }).where(eq(users.id, 1));
+   * ```
+   */
+  update(table) {
+    return new import_query_builders.ClickHouseUpdateBuilder(table, this.session, this.dialect);
+  }
+  /**
+   * Creates an insert query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/insert}
+   *
+   * @example
+   * ```ts
+   * await db.insert(events).values({ id: 1n, url: '/' });
+   * await db.insert(events).values([{ id: 1n, url: '/' }, { id: 2n, url: '/about' }]);
+   * ```
+   */
+  insert(table) {
+    return new import_query_builders.ClickHouseInsertBuilder(table, this.session, this.dialect);
+  }
+  /**
+   * Creates a delete query.
+   *
+   * Emits a lightweight `DELETE FROM … WHERE`; call `.mutation()` for `ALTER TABLE … DELETE`.
+   * Calling this without `.where()` deletes every row.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/delete}
+   */
+  delete(table) {
+    return new import_query_builders.ClickHouseDeleteBase(table, this.session, this.dialect);
+  }
+  execute(query) {
+    return this.session.execute(typeof query === "string" ? import_sql.sql.raw(query) : query.getSQL());
+  }
+  $cache;
+}
+const withReplicas = (primary, replicas, getReplica = () => replicas[Math.floor(Math.random() * replicas.length)]) => {
+  const select = (...args) => getReplica(replicas).select(...args);
+  const selectDistinct = (...args) => getReplica(replicas).selectDistinct(...args);
+  const $count = (...args) => getReplica(replicas).$count(...args);
+  const $with = (...args) => getReplica(replicas).with(...args);
+  const update = (...args) => primary.update(...args);
+  const insert = (...args) => primary.insert(...args);
+  const $delete = (...args) => primary.delete(...args);
+  const execute = (...args) => primary.execute(...args);
+  return {
+    ...primary,
+    update,
+    insert,
+    delete: $delete,
+    execute,
+    $primary: primary,
+    $replicas: replicas,
+    select,
+    selectDistinct,
+    $count,
+    with: $with
+  };
+};
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDatabase,
+  withReplicas
+});
diff --git a/clickhouse-core/db.d.cts b/clickhouse-core/db.d.cts
new file mode 100644
index 0000000..7a7f8ea
--- /dev/null
+++ b/clickhouse-core/db.d.cts
@@ -0,0 +1,125 @@
+import type { Cache } from '../cache/core/cache.cjs';
+import { entityKind } from '../entity.cjs';
+import type { ExtractTablesWithRelations, RelationalSchemaConfig, TablesRelationalConfig } from '../relations.cjs';
+import { type SQL, type SQLWrapper } from '../sql/sql.cjs';
+import { WithSubquery } from '../subquery.cjs';
+import type { ClickHouseDialect } from './dialect.cjs';
+import { ClickHouseCountBuilder } from './query-builders/count.cjs';
+import { ClickHouseDeleteBase, ClickHouseInsertBuilder, ClickHouseSelectBuilder, ClickHouseUpdateBuilder } from './query-builders/index.cjs';
+import type { SelectedFields } from './query-builders/select.types.cjs';
+import type { ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase } from './session.cjs';
+import type { WithBuilder } from './subquery.cjs';
+import type { ClickHouseTable } from './table.cjs';
+/**
+ * A ClickHouse database handle.
+ *
+ * Two things are deliberately absent compared with the row-store dialects:
+ *
+ * - `transaction()` — ClickHouse's transactions are experimental and single-node, so Drizzle does not
+ *   expose an API that would imply atomicity it cannot provide.
+ * - `db.query` — the relational query API is built on foreign keys and correlated lateral joins,
+ *   neither of which ClickHouse has. Model relations with explicit joins instead.
+ */
+export declare class ClickHouseDatabase<TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TFullSchema extends Record<string, unknown> = {}, TSchema extends TablesRelationalConfig = ExtractTablesWithRelations<TFullSchema>> {
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly schema: TSchema | undefined;
+        readonly fullSchema: TFullSchema;
+        readonly tableNamesMap: Record<string, string>;
+    };
+    constructor(
+    /** @internal */
+    dialect: ClickHouseDialect, 
+    /** @internal */
+    session: ClickHouseSession<any, any>, schema: RelationalSchemaConfig<TSchema> | undefined);
+    /**
+     * Creates a subquery that defines a temporary named result set as a CTE.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+     *
+     * @param alias The alias for the subquery.
+     */
+    $with: WithBuilder;
+    $count(source: ClickHouseTable | SQL | SQLWrapper, filters?: SQL<unknown>): ClickHouseCountBuilder<ClickHouseSession<any, any>>;
+    /**
+     * Incorporates a previously defined CTE (using `$with`) into the main query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+     */
+    with(...queries: WithSubquery[]): {
+        select: {
+            (): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+        };
+        selectDistinct: {
+            (): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+        };
+    };
+    /**
+     * Creates a select query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select}
+     *
+     * @example
+     * ```ts
+     * const allEvents = await db.select().from(events);
+     *
+     * const urls = await db.select({ url: events.url }).from(events);
+     * ```
+     */
+    select(): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+    select<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+    /**
+     * Adds `distinct` to a select query, returning only unique rows.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#distinct}
+     */
+    selectDistinct(): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+    selectDistinct<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+    /**
+     * Creates an `ALTER TABLE … UPDATE` mutation.
+     *
+     * ClickHouse has no in-place `UPDATE`. The statement returns as soon as the mutation is accepted
+     * and the rewrite happens asynchronously — pass `.settings({ mutations_sync: 2 })` to wait for it.
+     * Updates are expensive; prefer modelling changes as new rows with a `ReplacingMergeTree`.
+     *
+     * @example
+     * ```ts
+     * await db.update(users).set({ name: 'Ada' }).where(eq(users.id, 1));
+     * ```
+     */
+    update<TTable extends ClickHouseTable>(table: TTable): ClickHouseUpdateBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Creates an insert query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/insert}
+     *
+     * @example
+     * ```ts
+     * await db.insert(events).values({ id: 1n, url: '/' });
+     * await db.insert(events).values([{ id: 1n, url: '/' }, { id: 2n, url: '/about' }]);
+     * ```
+     */
+    insert<TTable extends ClickHouseTable>(table: TTable): ClickHouseInsertBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Creates a delete query.
+     *
+     * Emits a lightweight `DELETE FROM … WHERE`; call `.mutation()` for `ALTER TABLE … DELETE`.
+     * Calling this without `.where()` deletes every row.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/delete}
+     */
+    delete<TTable extends ClickHouseTable>(table: TTable): ClickHouseDeleteBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    execute<T extends {
+        [column: string]: any;
+    } = Record<string, unknown>>(query: SQLWrapper | string): Promise<ClickHouseQueryResultKind<TQueryResult, T>>;
+    $cache: {
+        invalidate: Cache['onMutate'];
+    };
+}
+export type ClickHouseWithReplicas<Q> = Q & {
+    $primary: Q;
+    $replicas: Q[];
+};
+export declare const withReplicas: <Q extends ClickHouseDatabase<any, any, any, any>>(primary: Q, replicas: [Q, ...Q[]], getReplica?: (replicas: Q[]) => Q) => ClickHouseWithReplicas<Q>;
diff --git a/clickhouse-core/db.d.ts b/clickhouse-core/db.d.ts
new file mode 100644
index 0000000..b4430ee
--- /dev/null
+++ b/clickhouse-core/db.d.ts
@@ -0,0 +1,125 @@
+import type { Cache } from '../cache/core/cache.js';
+import { entityKind } from '../entity.js';
+import type { ExtractTablesWithRelations, RelationalSchemaConfig, TablesRelationalConfig } from '../relations.js';
+import { type SQL, type SQLWrapper } from '../sql/sql.js';
+import { WithSubquery } from '../subquery.js';
+import type { ClickHouseDialect } from './dialect.js';
+import { ClickHouseCountBuilder } from './query-builders/count.js';
+import { ClickHouseDeleteBase, ClickHouseInsertBuilder, ClickHouseSelectBuilder, ClickHouseUpdateBuilder } from './query-builders/index.js';
+import type { SelectedFields } from './query-builders/select.types.js';
+import type { ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase } from './session.js';
+import type { WithBuilder } from './subquery.js';
+import type { ClickHouseTable } from './table.js';
+/**
+ * A ClickHouse database handle.
+ *
+ * Two things are deliberately absent compared with the row-store dialects:
+ *
+ * - `transaction()` — ClickHouse's transactions are experimental and single-node, so Drizzle does not
+ *   expose an API that would imply atomicity it cannot provide.
+ * - `db.query` — the relational query API is built on foreign keys and correlated lateral joins,
+ *   neither of which ClickHouse has. Model relations with explicit joins instead.
+ */
+export declare class ClickHouseDatabase<TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TFullSchema extends Record<string, unknown> = {}, TSchema extends TablesRelationalConfig = ExtractTablesWithRelations<TFullSchema>> {
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly schema: TSchema | undefined;
+        readonly fullSchema: TFullSchema;
+        readonly tableNamesMap: Record<string, string>;
+    };
+    constructor(
+    /** @internal */
+    dialect: ClickHouseDialect, 
+    /** @internal */
+    session: ClickHouseSession<any, any>, schema: RelationalSchemaConfig<TSchema> | undefined);
+    /**
+     * Creates a subquery that defines a temporary named result set as a CTE.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+     *
+     * @param alias The alias for the subquery.
+     */
+    $with: WithBuilder;
+    $count(source: ClickHouseTable | SQL | SQLWrapper, filters?: SQL<unknown>): ClickHouseCountBuilder<ClickHouseSession<any, any>>;
+    /**
+     * Incorporates a previously defined CTE (using `$with`) into the main query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+     */
+    with(...queries: WithSubquery[]): {
+        select: {
+            (): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+        };
+        selectDistinct: {
+            (): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+        };
+    };
+    /**
+     * Creates a select query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select}
+     *
+     * @example
+     * ```ts
+     * const allEvents = await db.select().from(events);
+     *
+     * const urls = await db.select({ url: events.url }).from(events);
+     * ```
+     */
+    select(): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+    select<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+    /**
+     * Adds `distinct` to a select query, returning only unique rows.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#distinct}
+     */
+    selectDistinct(): ClickHouseSelectBuilder<undefined, TPreparedQueryHKT>;
+    selectDistinct<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, TPreparedQueryHKT>;
+    /**
+     * Creates an `ALTER TABLE … UPDATE` mutation.
+     *
+     * ClickHouse has no in-place `UPDATE`. The statement returns as soon as the mutation is accepted
+     * and the rewrite happens asynchronously — pass `.settings({ mutations_sync: 2 })` to wait for it.
+     * Updates are expensive; prefer modelling changes as new rows with a `ReplacingMergeTree`.
+     *
+     * @example
+     * ```ts
+     * await db.update(users).set({ name: 'Ada' }).where(eq(users.id, 1));
+     * ```
+     */
+    update<TTable extends ClickHouseTable>(table: TTable): ClickHouseUpdateBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Creates an insert query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/insert}
+     *
+     * @example
+     * ```ts
+     * await db.insert(events).values({ id: 1n, url: '/' });
+     * await db.insert(events).values([{ id: 1n, url: '/' }, { id: 2n, url: '/about' }]);
+     * ```
+     */
+    insert<TTable extends ClickHouseTable>(table: TTable): ClickHouseInsertBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Creates a delete query.
+     *
+     * Emits a lightweight `DELETE FROM … WHERE`; call `.mutation()` for `ALTER TABLE … DELETE`.
+     * Calling this without `.where()` deletes every row.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/delete}
+     */
+    delete<TTable extends ClickHouseTable>(table: TTable): ClickHouseDeleteBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    execute<T extends {
+        [column: string]: any;
+    } = Record<string, unknown>>(query: SQLWrapper | string): Promise<ClickHouseQueryResultKind<TQueryResult, T>>;
+    $cache: {
+        invalidate: Cache['onMutate'];
+    };
+}
+export type ClickHouseWithReplicas<Q> = Q & {
+    $primary: Q;
+    $replicas: Q[];
+};
+export declare const withReplicas: <Q extends ClickHouseDatabase<any, any, any, any>>(primary: Q, replicas: [Q, ...Q[]], getReplica?: (replicas: Q[]) => Q) => ClickHouseWithReplicas<Q>;
diff --git a/clickhouse-core/db.js b/clickhouse-core/db.js
new file mode 100644
index 0000000..ae6b6cd
--- /dev/null
+++ b/clickhouse-core/db.js
@@ -0,0 +1,168 @@
+import { entityKind } from "../entity.js";
+import { SelectionProxyHandler } from "../selection-proxy.js";
+import { sql } from "../sql/sql.js";
+import { WithSubquery } from "../subquery.js";
+import { ClickHouseCountBuilder } from "./query-builders/count.js";
+import {
+  ClickHouseDeleteBase,
+  ClickHouseInsertBuilder,
+  ClickHouseSelectBuilder,
+  ClickHouseUpdateBuilder,
+  QueryBuilder
+} from "./query-builders/index.js";
+class ClickHouseDatabase {
+  constructor(dialect, session, schema) {
+    this.dialect = dialect;
+    this.session = session;
+    this._ = schema ? {
+      schema: schema.schema,
+      fullSchema: schema.fullSchema,
+      tableNamesMap: schema.tableNamesMap
+    } : {
+      schema: void 0,
+      fullSchema: {},
+      tableNamesMap: {}
+    };
+    this.$cache = { invalidate: async (_params) => {
+    } };
+  }
+  dialect;
+  session;
+  static [entityKind] = "ClickHouseDatabase";
+  /**
+   * Creates a subquery that defines a temporary named result set as a CTE.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+   *
+   * @param alias The alias for the subquery.
+   */
+  $with = (alias, selection) => {
+    const self = this;
+    const as = (qb) => {
+      if (typeof qb === "function") {
+        qb = qb(new QueryBuilder(self.dialect));
+      }
+      return new Proxy(
+        new WithSubquery(
+          qb.getSQL(),
+          selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
+          alias,
+          true
+        ),
+        new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+      );
+    };
+    return { as };
+  };
+  $count(source, filters) {
+    return new ClickHouseCountBuilder({ source, filters, session: this.session });
+  }
+  /**
+   * Incorporates a previously defined CTE (using `$with`) into the main query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#with-clause}
+   */
+  with(...queries) {
+    const self = this;
+    function select(fields) {
+      return new ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: self.session,
+        dialect: self.dialect,
+        withList: queries
+      });
+    }
+    function selectDistinct(fields) {
+      return new ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: self.session,
+        dialect: self.dialect,
+        withList: queries,
+        distinct: true
+      });
+    }
+    return { select, selectDistinct };
+  }
+  select(fields) {
+    return new ClickHouseSelectBuilder({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
+  }
+  selectDistinct(fields) {
+    return new ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: this.session,
+      dialect: this.dialect,
+      distinct: true
+    });
+  }
+  /**
+   * Creates an `ALTER TABLE … UPDATE` mutation.
+   *
+   * ClickHouse has no in-place `UPDATE`. The statement returns as soon as the mutation is accepted
+   * and the rewrite happens asynchronously — pass `.settings({ mutations_sync: 2 })` to wait for it.
+   * Updates are expensive; prefer modelling changes as new rows with a `ReplacingMergeTree`.
+   *
+   * @example
+   * ```ts
+   * await db.update(users).set({ name: 'Ada' }).where(eq(users.id, 1));
+   * ```
+   */
+  update(table) {
+    return new ClickHouseUpdateBuilder(table, this.session, this.dialect);
+  }
+  /**
+   * Creates an insert query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/insert}
+   *
+   * @example
+   * ```ts
+   * await db.insert(events).values({ id: 1n, url: '/' });
+   * await db.insert(events).values([{ id: 1n, url: '/' }, { id: 2n, url: '/about' }]);
+   * ```
+   */
+  insert(table) {
+    return new ClickHouseInsertBuilder(table, this.session, this.dialect);
+  }
+  /**
+   * Creates a delete query.
+   *
+   * Emits a lightweight `DELETE FROM … WHERE`; call `.mutation()` for `ALTER TABLE … DELETE`.
+   * Calling this without `.where()` deletes every row.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/delete}
+   */
+  delete(table) {
+    return new ClickHouseDeleteBase(table, this.session, this.dialect);
+  }
+  execute(query) {
+    return this.session.execute(typeof query === "string" ? sql.raw(query) : query.getSQL());
+  }
+  $cache;
+}
+const withReplicas = (primary, replicas, getReplica = () => replicas[Math.floor(Math.random() * replicas.length)]) => {
+  const select = (...args) => getReplica(replicas).select(...args);
+  const selectDistinct = (...args) => getReplica(replicas).selectDistinct(...args);
+  const $count = (...args) => getReplica(replicas).$count(...args);
+  const $with = (...args) => getReplica(replicas).with(...args);
+  const update = (...args) => primary.update(...args);
+  const insert = (...args) => primary.insert(...args);
+  const $delete = (...args) => primary.delete(...args);
+  const execute = (...args) => primary.execute(...args);
+  return {
+    ...primary,
+    update,
+    insert,
+    delete: $delete,
+    execute,
+    $primary: primary,
+    $replicas: replicas,
+    select,
+    selectDistinct,
+    $count,
+    with: $with
+  };
+};
+export {
+  ClickHouseDatabase,
+  withReplicas
+};
diff --git a/clickhouse-core/ddl.cjs b/clickhouse-core/ddl.cjs
new file mode 100644
index 0000000..ed0a0bc
--- /dev/null
+++ b/clickhouse-core/ddl.cjs
@@ -0,0 +1,38 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var ddl_exports = {};
+__export(ddl_exports, {
+  createTableSQL: () => createTableSQL,
+  dropTableSQL: () => dropTableSQL
+});
+module.exports = __toCommonJS(ddl_exports);
+var import_dialect = require("./dialect.cjs");
+function createTableSQL(table, options = {}) {
+  const dialect = new import_dialect.ClickHouseDialect({ casing: options.casing });
+  return dialect.sqlToQuery(dialect.buildCreateTableQuery(table, options), "indexes").sql;
+}
+function dropTableSQL(table, options = {}) {
+  const dialect = new import_dialect.ClickHouseDialect({ casing: options.casing });
+  return dialect.sqlToQuery(dialect.buildDropTableQuery(table, options), "indexes").sql;
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  createTableSQL,
+  dropTableSQL
+});
diff --git a/clickhouse-core/ddl.d.cts b/clickhouse-core/ddl.d.cts
new file mode 100644
index 0000000..92d889f
--- /dev/null
+++ b/clickhouse-core/ddl.d.cts
@@ -0,0 +1,32 @@
+import type { Casing } from '../utils.cjs';
+import type { ClickHouseTable } from './table.cjs';
+export interface ClickHouseDDLOptions {
+    /** Column-name casing, matching the `casing` option passed to `drizzle()`. */
+    casing?: Casing;
+    /** Emits `ON CLUSTER <name>`, so the statement is replicated across a cluster. */
+    onCluster?: string;
+}
+export interface CreateTableOptions extends ClickHouseDDLOptions {
+    ifNotExists?: boolean;
+}
+export interface DropTableOptions extends ClickHouseDDLOptions {
+    ifExists?: boolean;
+    /** Waits for the data to actually be removed instead of returning as soon as the drop is queued. */
+    sync?: boolean;
+}
+/**
+ * Renders the `CREATE TABLE` statement for a table declared with `clickhouseTable`.
+ *
+ * ```ts
+ * await db.execute(createTableSQL(events, { ifNotExists: true }));
+ * ```
+ */
+export declare function createTableSQL(table: ClickHouseTable, options?: CreateTableOptions): string;
+/**
+ * Renders the `DROP TABLE` statement for a table declared with `clickhouseTable`.
+ *
+ * ```ts
+ * await db.execute(dropTableSQL(events, { ifExists: true }));
+ * ```
+ */
+export declare function dropTableSQL(table: ClickHouseTable, options?: DropTableOptions): string;
diff --git a/clickhouse-core/ddl.d.ts b/clickhouse-core/ddl.d.ts
new file mode 100644
index 0000000..e8bd631
--- /dev/null
+++ b/clickhouse-core/ddl.d.ts
@@ -0,0 +1,32 @@
+import type { Casing } from '../utils.js';
+import type { ClickHouseTable } from './table.js';
+export interface ClickHouseDDLOptions {
+    /** Column-name casing, matching the `casing` option passed to `drizzle()`. */
+    casing?: Casing;
+    /** Emits `ON CLUSTER <name>`, so the statement is replicated across a cluster. */
+    onCluster?: string;
+}
+export interface CreateTableOptions extends ClickHouseDDLOptions {
+    ifNotExists?: boolean;
+}
+export interface DropTableOptions extends ClickHouseDDLOptions {
+    ifExists?: boolean;
+    /** Waits for the data to actually be removed instead of returning as soon as the drop is queued. */
+    sync?: boolean;
+}
+/**
+ * Renders the `CREATE TABLE` statement for a table declared with `clickhouseTable`.
+ *
+ * ```ts
+ * await db.execute(createTableSQL(events, { ifNotExists: true }));
+ * ```
+ */
+export declare function createTableSQL(table: ClickHouseTable, options?: CreateTableOptions): string;
+/**
+ * Renders the `DROP TABLE` statement for a table declared with `clickhouseTable`.
+ *
+ * ```ts
+ * await db.execute(dropTableSQL(events, { ifExists: true }));
+ * ```
+ */
+export declare function dropTableSQL(table: ClickHouseTable, options?: DropTableOptions): string;
diff --git a/clickhouse-core/ddl.js b/clickhouse-core/ddl.js
new file mode 100644
index 0000000..3f50712
--- /dev/null
+++ b/clickhouse-core/ddl.js
@@ -0,0 +1,13 @@
+import { ClickHouseDialect } from "./dialect.js";
+function createTableSQL(table, options = {}) {
+  const dialect = new ClickHouseDialect({ casing: options.casing });
+  return dialect.sqlToQuery(dialect.buildCreateTableQuery(table, options), "indexes").sql;
+}
+function dropTableSQL(table, options = {}) {
+  const dialect = new ClickHouseDialect({ casing: options.casing });
+  return dialect.sqlToQuery(dialect.buildDropTableQuery(table, options), "indexes").sql;
+}
+export {
+  createTableSQL,
+  dropTableSQL
+};
diff --git a/clickhouse-core/dialect.cjs b/clickhouse-core/dialect.cjs
new file mode 100644
index 0000000..038560c
--- /dev/null
+++ b/clickhouse-core/dialect.cjs
@@ -0,0 +1,563 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var dialect_exports = {};
+__export(dialect_exports, {
+  ClickHouseDialect: () => ClickHouseDialect
+});
+module.exports = __toCommonJS(dialect_exports);
+var import_casing = require("../casing.cjs");
+var import_column = require("../column.cjs");
+var import_entity = require("../entity.cjs");
+var import_sql = require("../sql/sql.cjs");
+var import_subquery = require("../subquery.cjs");
+var import_table = require("../table.cjs");
+var import_utils = require("../utils.cjs");
+var import_view_common = require("../view-common.cjs");
+var import_common = require("./columns/common.cjs");
+var import_engines = require("./engines.cjs");
+var import_literals = require("./literals.cjs");
+var import_errors2 = require("./errors.cjs");
+var import_table2 = require("./table.cjs");
+var import_utils2 = require("./utils.cjs");
+class ClickHouseDialect {
+  static [import_entity.entityKind] = "ClickHouseDialect";
+  /** @internal */
+  casing;
+  constructor(config) {
+    this.casing = new import_casing.CasingCache(config?.casing);
+  }
+  /**
+   * Applies pending migrations.
+   *
+   * Unlike the other dialects this does not wrap the run in a transaction — ClickHouse has no usable
+   * one. Each statement is applied on its own, and the bookkeeping row is written only after a
+   * migration's statements have all succeeded, so a failure part-way leaves that migration marked as
+   * unapplied and it will be retried.
+   */
+  async migrate(migrations, session, config) {
+    const migrationsTable = config.migrationsTable ?? "__drizzle_migrations";
+    const migrationTableCreate = import_sql.sql`
+			create table if not exists ${import_sql.sql.identifier(migrationsTable)} (
+				hash String,
+				created_at Int64
+			)
+			engine = MergeTree()
+			order by created_at
+		`;
+    await session.execute(migrationTableCreate);
+    const dbMigrations = await session.all(
+      import_sql.sql`select hash, created_at from ${import_sql.sql.identifier(migrationsTable)} order by created_at desc limit 1`
+    );
+    const lastDbMigration = dbMigrations[0];
+    for (const migration of migrations) {
+      if (lastDbMigration && Number(lastDbMigration.created_at) >= migration.folderMillis) {
+        continue;
+      }
+      for (const stmt of migration.sql) {
+        await session.execute(import_sql.sql.raw(stmt));
+      }
+      await session.execute(
+        import_sql.sql`insert into ${import_sql.sql.identifier(migrationsTable)} (hash, created_at) values (${migration.hash}, ${import_sql.sql.raw(String(migration.folderMillis))})`
+      );
+    }
+  }
+  escapeName(name) {
+    return (0, import_literals.escapeClickHouseIdentifier)(name);
+  }
+  /**
+   * ClickHouse has no bind-parameter protocol, so every value is rendered inline as a literal —
+   * see `literals.ts` for why. `escapeParam` is therefore never reached: `sqlToQuery` always builds
+   * queries with `inlineParams` set.
+   */
+  escapeParam(_num, _value) {
+    throw new Error("ClickHouse renders parameters inline; escapeParam should not be reached");
+  }
+  escapeString(str) {
+    return (0, import_literals.escapeClickHouseString)(str);
+  }
+  buildWithCTE(queries) {
+    if (!queries?.length) return void 0;
+    const withSqlChunks = [import_sql.sql`with `];
+    for (const [i, w] of queries.entries()) {
+      withSqlChunks.push(import_sql.sql`${import_sql.sql.identifier(w._.alias)} as (${w._.sql})`);
+      if (i < queries.length - 1) {
+        withSqlChunks.push(import_sql.sql`, `);
+      }
+    }
+    withSqlChunks.push(import_sql.sql` `);
+    return import_sql.sql.join(withSqlChunks);
+  }
+  buildSettings(settings) {
+    return settings && Object.keys(settings).length > 0 ? import_sql.sql` settings ${(0, import_engines.settingsSQL)(settings)}` : void 0;
+  }
+  /**
+   * Rewrites qualified `` `table`.`column` `` references to bare `` `column` `` ones.
+   *
+   * `ALTER TABLE … UPDATE` and lightweight `DELETE FROM` are rewritten by ClickHouse into
+   * expressions evaluated against the table's columns directly, with no table name in scope — a
+   * qualified reference there fails with `UNKNOWN_IDENTIFIER`. Both forms are single-table, so
+   * dropping the qualifier is unambiguous.
+   */
+  unqualifyColumns(value) {
+    const rewrite = (chunk) => {
+      if ((0, import_entity.is)(chunk, import_common.ClickHouseColumn)) {
+        return import_sql.sql.identifier(this.casing.getColumnCasing(chunk));
+      }
+      if ((0, import_entity.is)(chunk, import_sql.SQL)) {
+        return this.unqualifyColumns(chunk);
+      }
+      if (Array.isArray(chunk)) {
+        return chunk.map((inner) => rewrite(inner));
+      }
+      return chunk;
+    };
+    return new import_sql.SQL(value.queryChunks.map((chunk) => rewrite(chunk)));
+  }
+  /**
+   * Builds a `DELETE`.
+   *
+   * The default is a lightweight `DELETE FROM … WHERE`, which ClickHouse supports from 22.8 onwards.
+   * `.mutation()` switches to `ALTER TABLE … DELETE WHERE`, which works on older servers and on
+   * engines where lightweight deletes are unavailable.
+   *
+   * Either form requires a `WHERE`, so a delete with no filter becomes `WHERE 1` — a deliberate
+   * "delete everything" rather than a syntax error.
+   */
+  buildDeleteQuery({ table, where, mutation, settings }) {
+    const whereSql = import_sql.sql` where ${where ? this.unqualifyColumns(where) : import_sql.sql`1`}`;
+    const settingsSql = this.buildSettings(settings);
+    return mutation ? import_sql.sql`alter table ${table} delete${whereSql}${settingsSql}` : import_sql.sql`delete from ${table}${whereSql}${settingsSql}`;
+  }
+  buildUpdateSet(table, set) {
+    const tableColumns = table[import_table.Table.Symbol.Columns];
+    const columnNames = Object.keys(tableColumns).filter(
+      (colName) => set[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0
+    );
+    const setSize = columnNames.length;
+    return import_sql.sql.join(
+      columnNames.flatMap((colName, i) => {
+        const col = tableColumns[colName];
+        const onUpdateFnResult = col.onUpdateFn?.();
+        const value = set[colName] ?? ((0, import_entity.is)(onUpdateFnResult, import_sql.SQL) ? onUpdateFnResult : import_sql.sql.param(onUpdateFnResult, col));
+        const res = import_sql.sql`${import_sql.sql.identifier(this.casing.getColumnCasing(col))} = ${value}`;
+        if (i < setSize - 1) {
+          return [res, import_sql.sql.raw(", ")];
+        }
+        return [res];
+      })
+    );
+  }
+  /**
+   * Builds an `ALTER TABLE … UPDATE`, ClickHouse's mutation form of `UPDATE`.
+   *
+   * Mutations are asynchronous: the statement returns once accepted, and the rewrite happens in the
+   * background. Pass `.settings({ mutations_sync: 2 })` to block until it has finished everywhere.
+   */
+  buildUpdateQuery({ table, set, where, settings }) {
+    const setSql = this.unqualifyColumns(this.buildUpdateSet(table, set));
+    const whereSql = import_sql.sql` where ${where ? this.unqualifyColumns(where) : import_sql.sql`1`}`;
+    const settingsSql = this.buildSettings(settings);
+    return import_sql.sql`alter table ${table} update ${setSql}${whereSql}${settingsSql}`;
+  }
+  /**
+   * Builds selection SQL with provided fields/expressions
+   *
+   * If `isSingleTable` is true, then columns won't be prefixed with table name
+   */
+  buildSelection(fields, { isSingleTable = false } = {}) {
+    const columnsLen = fields.length;
+    const chunks = fields.flatMap(({ field }, i) => {
+      const chunk = [];
+      if ((0, import_entity.is)(field, import_sql.SQL.Aliased) && field.isSelectionField) {
+        chunk.push(import_sql.sql.identifier(field.fieldAlias));
+      } else if ((0, import_entity.is)(field, import_sql.SQL.Aliased) || (0, import_entity.is)(field, import_sql.SQL)) {
+        const query = (0, import_entity.is)(field, import_sql.SQL.Aliased) ? field.sql : field;
+        if (isSingleTable) {
+          chunk.push(
+            new import_sql.SQL(
+              query.queryChunks.map((c) => {
+                if ((0, import_entity.is)(c, import_common.ClickHouseColumn)) {
+                  return import_sql.sql.identifier(this.casing.getColumnCasing(c));
+                }
+                return c;
+              })
+            )
+          );
+        } else {
+          chunk.push(query);
+        }
+        if ((0, import_entity.is)(field, import_sql.SQL.Aliased)) {
+          chunk.push(import_sql.sql` as ${import_sql.sql.identifier(field.fieldAlias)}`);
+        }
+      } else if ((0, import_entity.is)(field, import_column.Column)) {
+        if (isSingleTable) {
+          chunk.push(import_sql.sql.identifier(this.casing.getColumnCasing(field)));
+        } else {
+          chunk.push(field);
+        }
+      } else if ((0, import_entity.is)(field, import_subquery.Subquery)) {
+        const entries = Object.entries(field._.selectedFields);
+        if (entries.length === 1) {
+          const entry = entries[0][1];
+          const fieldDecoder = (0, import_entity.is)(entry, import_sql.SQL) ? entry.decoder : (0, import_entity.is)(entry, import_column.Column) ? { mapFromDriverValue: (v) => entry.mapFromDriverValue(v) } : entry.sql.decoder;
+          if (fieldDecoder) {
+            field._.sql.decoder = fieldDecoder;
+          }
+        }
+        chunk.push(field);
+      }
+      if (i < columnsLen - 1) {
+        chunk.push(import_sql.sql`, `);
+      }
+      return chunk;
+    });
+    return import_sql.sql.join(chunks);
+  }
+  buildLimit(limit) {
+    return typeof limit === "object" || typeof limit === "number" && limit >= 0 ? import_sql.sql` limit ${limit}` : void 0;
+  }
+  buildOrderBy(orderBy) {
+    return orderBy && orderBy.length > 0 ? import_sql.sql` order by ${import_sql.sql.join(orderBy, import_sql.sql`, `)}` : void 0;
+  }
+  buildSelectQuery({
+    withList,
+    fields,
+    fieldsFlat,
+    where,
+    having,
+    table,
+    joins,
+    orderBy,
+    groupBy,
+    limit,
+    offset,
+    distinct,
+    setOperators,
+    final,
+    prewhere,
+    sample,
+    arrayJoins,
+    limitBy,
+    withTotals,
+    settings
+  }) {
+    const fieldsList = fieldsFlat ?? (0, import_utils.orderSelectedFields)(fields);
+    for (const f of fieldsList) {
+      if ((0, import_entity.is)(f.field, import_column.Column) && (0, import_table.getTableName)(f.field.table) !== ((0, import_entity.is)(table, import_subquery.Subquery) ? table._.alias : (0, import_entity.is)(table, import_sql.SQL) ? void 0 : (0, import_table.getTableName)(table)) && !((table2) => joins?.some(
+        ({ alias }) => alias === (table2[import_table.Table.Symbol.IsAlias] ? (0, import_table.getTableName)(table2) : table2[import_table.Table.Symbol.BaseName])
+      ))(f.field.table)) {
+        const tableName = (0, import_table.getTableName)(f.field.table);
+        throw new Error(
+          `Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`
+        );
+      }
+    }
+    const isSingleTable = !joins || joins.length === 0;
+    const withSql = this.buildWithCTE(withList);
+    const distinctSql = distinct ? import_sql.sql` distinct` : void 0;
+    const selection = this.buildSelection(fieldsList, { isSingleTable });
+    const tableSql = (() => {
+      if ((0, import_entity.is)(table, import_table.Table) && table[import_table.Table.Symbol.IsAlias]) {
+        return import_sql.sql`${import_sql.sql`${import_sql.sql.identifier(table[import_table.Table.Symbol.Schema] ?? "")}.`.if(table[import_table.Table.Symbol.Schema])}${import_sql.sql.identifier(table[import_table.Table.Symbol.OriginalName])} ${import_sql.sql.identifier(table[import_table.Table.Symbol.Name])}`;
+      }
+      return table;
+    })();
+    const finalSql = final ? import_sql.sql` final` : void 0;
+    const sampleSql = sample ? import_sql.sql` sample ${import_sql.sql.raw(String(sample.value))}${sample.offset === void 0 ? void 0 : import_sql.sql` offset ${import_sql.sql.raw(String(sample.offset))}`}` : void 0;
+    const joinsSql = import_sql.sql.join(this.buildJoins(joins));
+    const arrayJoinsSql = arrayJoins?.length ? import_sql.sql.join(
+      arrayJoins.map(
+        (arrayJoin) => import_sql.sql` ${import_sql.sql.raw(arrayJoin.left ? "left array join" : "array join")} ${import_sql.sql.join(arrayJoin.expressions, import_sql.sql`, `)}`
+      )
+    ) : void 0;
+    const prewhereSql = prewhere ? import_sql.sql` prewhere ${prewhere}` : void 0;
+    const whereSql = where ? import_sql.sql` where ${where}` : void 0;
+    const groupBySql = groupBy && groupBy.length > 0 ? import_sql.sql` group by ${import_sql.sql.join(groupBy, import_sql.sql`, `)}${withTotals ? import_sql.sql` with totals` : void 0}` : void 0;
+    const havingSql = having ? import_sql.sql` having ${having}` : void 0;
+    const orderBySql = this.buildOrderBy(orderBy);
+    const limitBySql = limitBy ? import_sql.sql` limit ${limitBy.limit}${limitBy.offset === void 0 ? void 0 : import_sql.sql` offset ${limitBy.offset}`} by ${import_sql.sql.join(limitBy.expressions, import_sql.sql`, `)}` : void 0;
+    const limitSql = this.buildLimit(limit);
+    const offsetSql = offset ? import_sql.sql` offset ${offset}` : void 0;
+    const settingsSql = this.buildSettings(settings);
+    const finalQuery = import_sql.sql`${withSql}select${distinctSql} ${selection} from ${tableSql}${finalSql}${sampleSql}${joinsSql}${arrayJoinsSql}${prewhereSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitBySql}${limitSql}${offsetSql}${settingsSql}`;
+    if (setOperators.length > 0) {
+      return this.buildSetOperations(finalQuery, setOperators);
+    }
+    return finalQuery;
+  }
+  buildJoins(joins) {
+    const joinsArray = [];
+    if (!joins) return joinsArray;
+    for (const [index, joinMeta] of joins.entries()) {
+      if (index === 0) {
+        joinsArray.push(import_sql.sql` `);
+      }
+      const table = joinMeta.table;
+      const globalSql = joinMeta.global ? import_sql.sql`global ` : void 0;
+      const strictnessSql = joinMeta.strictness ? import_sql.sql`${import_sql.sql.raw(joinMeta.strictness)} ` : void 0;
+      const onSql = joinMeta.on ? import_sql.sql` on ${joinMeta.on}` : void 0;
+      const joinPrefix = joinMeta.joinType === "cross" ? import_sql.sql`${globalSql}cross` : import_sql.sql`${globalSql}${strictnessSql}${import_sql.sql.raw(joinMeta.joinType)}`;
+      if ((0, import_entity.is)(table, import_table2.ClickHouseTable)) {
+        const tableName = table[import_table2.ClickHouseTable.Symbol.Name];
+        const tableSchema = table[import_table2.ClickHouseTable.Symbol.Schema];
+        const origTableName = table[import_table2.ClickHouseTable.Symbol.OriginalName];
+        const alias = tableName === origTableName ? void 0 : joinMeta.alias;
+        joinsArray.push(
+          import_sql.sql`${joinPrefix} join ${tableSchema ? import_sql.sql`${import_sql.sql.identifier(tableSchema)}.` : void 0}${import_sql.sql.identifier(origTableName)}${alias && import_sql.sql` ${import_sql.sql.identifier(alias)}`}${onSql}`
+        );
+      } else if ((0, import_entity.is)(table, import_sql.View)) {
+        const viewName = table[import_view_common.ViewBaseConfig].name;
+        const viewSchema = table[import_view_common.ViewBaseConfig].schema;
+        const origViewName = table[import_view_common.ViewBaseConfig].originalName;
+        const alias = viewName === origViewName ? void 0 : joinMeta.alias;
+        joinsArray.push(
+          import_sql.sql`${joinPrefix} join ${viewSchema ? import_sql.sql`${import_sql.sql.identifier(viewSchema)}.` : void 0}${import_sql.sql.identifier(origViewName)}${alias && import_sql.sql` ${import_sql.sql.identifier(alias)}`}${onSql}`
+        );
+      } else {
+        joinsArray.push(import_sql.sql`${joinPrefix} join ${table}${onSql}`);
+      }
+      if (index < joins.length - 1) {
+        joinsArray.push(import_sql.sql` `);
+      }
+    }
+    return joinsArray;
+  }
+  buildSetOperations(leftSelect, setOperators) {
+    const [setOperator, ...rest] = setOperators;
+    if (!setOperator) {
+      throw new Error("Cannot pass undefined values to any set operator");
+    }
+    if (rest.length === 0) {
+      return this.buildSetOperationQuery({ leftSelect, setOperator });
+    }
+    return this.buildSetOperations(
+      this.buildSetOperationQuery({ leftSelect, setOperator }),
+      rest
+    );
+  }
+  buildSetOperationQuery({
+    leftSelect,
+    setOperator: { type, isAll, rightSelect, limit, orderBy, offset }
+  }) {
+    const leftChunk = import_sql.sql`(${leftSelect.getSQL()}) `;
+    const rightChunk = import_sql.sql`(${rightSelect.getSQL()})`;
+    let orderBySql;
+    if (orderBy && orderBy.length > 0) {
+      const orderByValues = [];
+      for (const orderByUnit of orderBy) {
+        if ((0, import_entity.is)(orderByUnit, import_common.ClickHouseColumn)) {
+          orderByValues.push(import_sql.sql.identifier(this.casing.getColumnCasing(orderByUnit)));
+        } else if ((0, import_entity.is)(orderByUnit, import_sql.SQL)) {
+          for (let i = 0; i < orderByUnit.queryChunks.length; i++) {
+            const chunk = orderByUnit.queryChunks[i];
+            if ((0, import_entity.is)(chunk, import_common.ClickHouseColumn)) {
+              orderByUnit.queryChunks[i] = import_sql.sql.identifier(this.casing.getColumnCasing(chunk));
+            }
+          }
+          orderByValues.push(import_sql.sql`${orderByUnit}`);
+        } else {
+          orderByValues.push(import_sql.sql`${orderByUnit}`);
+        }
+      }
+      orderBySql = import_sql.sql` order by ${import_sql.sql.join(orderByValues, import_sql.sql`, `)} `;
+    }
+    const limitSql = typeof limit === "object" || typeof limit === "number" && limit >= 0 ? import_sql.sql` limit ${limit}` : void 0;
+    const operatorChunk = import_sql.sql.raw(`${type} ${isAll ? "all " : "distinct "}`);
+    const offsetSql = offset ? import_sql.sql` offset ${offset}` : void 0;
+    return import_sql.sql`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
+  }
+  /**
+   * Builds an `INSERT`.
+   *
+   * Columns are always listed explicitly so that the row order in `values()` does not have to match
+   * the table's column order. Computed (`MATERIALIZED`/`ALIAS`) columns are excluded, since
+   * ClickHouse rejects writes to them.
+   */
+  buildInsertQuery({ table, values, select, settings }) {
+    const settingsSql = this.buildSettings(settings);
+    const columns = table[import_table.Table.Symbol.Columns];
+    const colEntries = Object.entries(columns).filter(([, col]) => !col.shouldDisableInsert());
+    const insertOrder = colEntries.map(([, column]) => import_sql.sql.identifier(this.casing.getColumnCasing(column)));
+    if (select) {
+      return import_sql.sql`insert into ${table} ${select}${settingsSql}`;
+    }
+    if (!values) {
+      throw new Error("An insert must be given either values() or select()");
+    }
+    const valuesSqlList = [];
+    for (const [valueIndex, value] of values.entries()) {
+      const valueList = [];
+      for (const [fieldName, col] of colEntries) {
+        const colValue = value[fieldName];
+        if (colValue === void 0 || (0, import_entity.is)(colValue, import_sql.Param) && colValue.value === void 0) {
+          if (col.defaultFn !== void 0) {
+            const defaultFnResult = col.defaultFn();
+            valueList.push((0, import_entity.is)(defaultFnResult, import_sql.SQL) ? defaultFnResult : import_sql.sql.param(defaultFnResult, col));
+          } else if (!col.default && col.onUpdateFn !== void 0) {
+            const onUpdateFnResult = col.onUpdateFn();
+            valueList.push((0, import_entity.is)(onUpdateFnResult, import_sql.SQL) ? onUpdateFnResult : import_sql.sql.param(onUpdateFnResult, col));
+          } else {
+            valueList.push(import_sql.sql`default`);
+          }
+        } else {
+          valueList.push(colValue);
+        }
+      }
+      valuesSqlList.push(valueList);
+      if (valueIndex < values.length - 1) {
+        valuesSqlList.push(import_sql.sql`, `);
+      }
+    }
+    const valuesSql = import_sql.sql.join(valuesSqlList);
+    return import_sql.sql`insert into ${table} ${insertOrder} values ${valuesSql}${settingsSql}`;
+  }
+  /**
+   * The insert target as ClickHouse should see it — quoted, and schema-qualified when the table
+   * declares one. The driver's row-format insert takes a table *name*, not an expression, so it
+   * cannot go through the ordinary table rendering.
+   */
+  insertTargetName(table) {
+    return this.sqlToQuery(import_sql.sql`${table}`).sql;
+  }
+  /** The statement that precedes a row-format body: `insert into t format JSONEachRow`. */
+  buildInsertRowsQuery(table, format) {
+    return import_sql.sql`insert into ${table} format ${import_sql.sql.raw(format)}`;
+  }
+  /**
+   * One row as a row format carries it: ClickHouse column names, values through
+   * {@link ClickHouseColumn.mapToRowValue}.
+   *
+   * A column the row does not mention is **omitted rather than defaulted here**, because
+   * `JSONEachRow` matches by name and the server applies that column's own `DEFAULT` — which is the
+   * one the table declares, and therefore the one a `CREATE TABLE` and an `ALTER … ADD COLUMN` agree
+   * on. A Drizzle-side `$defaultFn` is still evaluated, since nothing server-side knows about it.
+   *
+   * Throws on a value there is no way to send: a body has nowhere to put an expression, so
+   * `sql\`now()\`` and placeholders belong to the statement path. The insert builder routes rows
+   * carrying either there automatically, so reaching this is either a stream that yields one or a
+   * caller that forced the format.
+   */
+  mapRowForInsert(table, entry) {
+    const columns = table[import_table.Table.Symbol.Columns];
+    for (const key of Object.keys(entry)) {
+      if (!(key in columns)) {
+        throw new import_errors2.ClickHouseInsertValueError(
+          `Column "${key}" does not exist on table "${(0, import_table.getTableName)(table)}". A row format matches by name, so an unrecognised key would be dropped by the server rather than rejected.`
+        );
+      }
+    }
+    const row = {};
+    for (const [fieldName, column] of Object.entries(columns)) {
+      if (column.shouldDisableInsert()) continue;
+      let value = entry[fieldName];
+      if (value === void 0 && column.defaultFn !== void 0) {
+        value = column.defaultFn();
+      }
+      if (value === void 0) continue;
+      if ((0, import_entity.is)(value, import_sql.SQL) || (0, import_entity.is)(value, import_sql.Param) || (0, import_entity.is)(value, import_sql.Placeholder)) {
+        throw new import_errors2.ClickHouseInsertValueError(
+          `Column "${fieldName}" of "${(0, import_table.getTableName)(table)}" was given a SQL expression, which a row format cannot carry. Insert this row through the statement path instead.`
+        );
+      }
+      row[this.casing.getColumnCasing(column)] = value === null ? null : column.mapToRowValue(value);
+    }
+    return row;
+  }
+  /**
+   * Renders one column's definition as it appears inside `CREATE TABLE`.
+   *
+   * Order matters to ClickHouse: `name Type [DEFAULT|MATERIALIZED|ALIAS|EPHEMERAL expr] [COMMENT]
+   * [CODEC] [TTL]`.
+   */
+  buildColumnDefinition(column) {
+    const chunks = [
+      import_sql.sql`${import_sql.sql.identifier(this.casing.getColumnCasing(column))} ${import_sql.sql.raw(column.getSQLType())}`
+    ];
+    const computedKeyword = column.computedKeyword;
+    if (computedKeyword !== void 0) {
+      const generated = column.generated;
+      const expression = typeof generated.as === "function" ? generated.as() : generated.as;
+      chunks.push(import_sql.sql`${import_sql.sql.raw(computedKeyword)} ${expression}`);
+    } else if (column.isEphemeral) {
+      chunks.push(
+        column.default === void 0 ? import_sql.sql`EPHEMERAL` : import_sql.sql`EPHEMERAL ${(0, import_entity.is)(column.default, import_sql.SQL) ? column.default : import_sql.sql.param(column.default, column)}`
+      );
+    } else if (column.default !== void 0) {
+      chunks.push(import_sql.sql`DEFAULT ${(0, import_entity.is)(column.default, import_sql.SQL) ? column.default : import_sql.sql.param(column.default, column)}`);
+    }
+    if (column.comment !== void 0) {
+      chunks.push(import_sql.sql`COMMENT ${import_sql.sql.raw((0, import_literals.escapeClickHouseString)(column.comment))}`);
+    }
+    const codec = column.codec;
+    if (codec?.length) {
+      chunks.push(
+        import_sql.sql`CODEC(${import_sql.sql.join(codec.map((c) => typeof c === "string" ? import_sql.sql.raw(c) : c), import_sql.sql`, `)})`
+      );
+    }
+    if (column.ttl !== void 0) {
+      chunks.push(import_sql.sql`TTL ${column.ttl}`);
+    }
+    return import_sql.sql.join(chunks, import_sql.sql` `);
+  }
+  /**
+   * Builds a `CREATE TABLE` statement for a table declared with {@link clickhouseTable}.
+   *
+   * Render it with `sqlToQuery(stmt, 'indexes')`: inside DDL, column references must be bare names
+   * rather than the `table`.`column` form used in queries.
+   */
+  buildCreateTableQuery(table, options = {}) {
+    const { columns, engine, indexes, projections } = (0, import_utils2.getTableConfig)(table);
+    if (!engine) {
+      throw new Error(
+        `Table "${(0, import_table.getTableName)(table)}" does not declare an engine. Add one to the table's extra config, e.g. \`(t) => [mergeTree({ orderBy: t.id })]\`.`
+      );
+    }
+    const entries = [
+      ...columns.map((column) => this.buildColumnDefinition(column)),
+      ...indexes.map((index) => index.getSQL()),
+      ...projections.map((projection) => projection.getSQL())
+    ];
+    const ifNotExistsSql = options.ifNotExists ? import_sql.sql`if not exists ` : void 0;
+    const onClusterSql = options.onCluster ? import_sql.sql` on cluster ${import_sql.sql.identifier(options.onCluster)}` : void 0;
+    const clausesSql = engine.getClausesSQL();
+    return import_sql.sql`create table ${ifNotExistsSql}${table}${onClusterSql} (${import_sql.sql.join(entries, import_sql.sql`, `)}) ${engine.getEngineSQL()}${clausesSql ? import_sql.sql` ${clausesSql}` : void 0}`;
+  }
+  /** Builds a `DROP TABLE` statement. */
+  buildDropTableQuery(table, options = {}) {
+    const ifExistsSql = options.ifExists ? import_sql.sql`if exists ` : void 0;
+    const onClusterSql = options.onCluster ? import_sql.sql` on cluster ${import_sql.sql.identifier(options.onCluster)}` : void 0;
+    const syncSql = options.sync ? import_sql.sql` sync` : void 0;
+    return import_sql.sql`drop table ${ifExistsSql}${table}${onClusterSql}${syncSql}`;
+  }
+  sqlToQuery(sql2, invokeSource) {
+    return sql2.toQuery({
+      casing: this.casing,
+      escapeName: this.escapeName,
+      escapeParam: this.escapeParam,
+      escapeString: this.escapeString,
+      // ClickHouse has no bind parameters; values are rendered as escaped literals instead.
+      inlineParams: true,
+      invokeSource
+    });
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDialect
+});
diff --git a/clickhouse-core/dialect.d.cts b/clickhouse-core/dialect.d.cts
new file mode 100644
index 0000000..195dc86
--- /dev/null
+++ b/clickhouse-core/dialect.d.cts
@@ -0,0 +1,136 @@
+import { entityKind } from '../entity.cjs';
+import type { MigrationConfig, MigrationMeta } from '../migrator.cjs';
+import type { QueryWithTypings } from '../sql/sql.cjs';
+import { SQL } from '../sql/sql.cjs';
+import { type Casing, type UpdateSet } from '../utils.cjs';
+import { ClickHouseColumn } from './columns/common.cjs';
+import type { ClickHouseDeleteConfig } from './query-builders/delete.cjs';
+import type { ClickHouseInsertConfig } from './query-builders/insert.cjs';
+import type { ClickHouseSelectConfig } from './query-builders/select.types.cjs';
+import type { ClickHouseUpdateConfig } from './query-builders/update.cjs';
+import type { ClickHouseSession } from './session.cjs';
+import { ClickHouseTable } from './table.cjs';
+export interface ClickHouseDialectConfig {
+    casing?: Casing;
+}
+export declare class ClickHouseDialect {
+    static readonly [entityKind]: string;
+    constructor(config?: ClickHouseDialectConfig);
+    /**
+     * Applies pending migrations.
+     *
+     * Unlike the other dialects this does not wrap the run in a transaction — ClickHouse has no usable
+     * one. Each statement is applied on its own, and the bookkeeping row is written only after a
+     * migration's statements have all succeeded, so a failure part-way leaves that migration marked as
+     * unapplied and it will be retried.
+     */
+    migrate(migrations: MigrationMeta[], session: ClickHouseSession, config: Omit<MigrationConfig, 'migrationsSchema'>): Promise<void>;
+    escapeName(name: string): string;
+    /**
+     * ClickHouse has no bind-parameter protocol, so every value is rendered inline as a literal —
+     * see `literals.ts` for why. `escapeParam` is therefore never reached: `sqlToQuery` always builds
+     * queries with `inlineParams` set.
+     */
+    escapeParam(_num: number, _value: unknown): string;
+    escapeString(str: string): string;
+    private buildWithCTE;
+    private buildSettings;
+    /**
+     * Rewrites qualified `` `table`.`column` `` references to bare `` `column` `` ones.
+     *
+     * `ALTER TABLE … UPDATE` and lightweight `DELETE FROM` are rewritten by ClickHouse into
+     * expressions evaluated against the table's columns directly, with no table name in scope — a
+     * qualified reference there fails with `UNKNOWN_IDENTIFIER`. Both forms are single-table, so
+     * dropping the qualifier is unambiguous.
+     */
+    private unqualifyColumns;
+    /**
+     * Builds a `DELETE`.
+     *
+     * The default is a lightweight `DELETE FROM … WHERE`, which ClickHouse supports from 22.8 onwards.
+     * `.mutation()` switches to `ALTER TABLE … DELETE WHERE`, which works on older servers and on
+     * engines where lightweight deletes are unavailable.
+     *
+     * Either form requires a `WHERE`, so a delete with no filter becomes `WHERE 1` — a deliberate
+     * "delete everything" rather than a syntax error.
+     */
+    buildDeleteQuery({ table, where, mutation, settings }: ClickHouseDeleteConfig): SQL;
+    buildUpdateSet(table: ClickHouseTable, set: UpdateSet): SQL;
+    /**
+     * Builds an `ALTER TABLE … UPDATE`, ClickHouse's mutation form of `UPDATE`.
+     *
+     * Mutations are asynchronous: the statement returns once accepted, and the rewrite happens in the
+     * background. Pass `.settings({ mutations_sync: 2 })` to block until it has finished everywhere.
+     */
+    buildUpdateQuery({ table, set, where, settings }: ClickHouseUpdateConfig): SQL;
+    /**
+     * Builds selection SQL with provided fields/expressions
+     *
+     * If `isSingleTable` is true, then columns won't be prefixed with table name
+     */
+    private buildSelection;
+    private buildLimit;
+    private buildOrderBy;
+    buildSelectQuery({ withList, fields, fieldsFlat, where, having, table, joins, orderBy, groupBy, limit, offset, distinct, setOperators, final, prewhere, sample, arrayJoins, limitBy, withTotals, settings, }: ClickHouseSelectConfig): SQL;
+    private buildJoins;
+    buildSetOperations(leftSelect: SQL, setOperators: ClickHouseSelectConfig['setOperators']): SQL;
+    buildSetOperationQuery({ leftSelect, setOperator: { type, isAll, rightSelect, limit, orderBy, offset }, }: {
+        leftSelect: SQL;
+        setOperator: ClickHouseSelectConfig['setOperators'][number];
+    }): SQL;
+    /**
+     * Builds an `INSERT`.
+     *
+     * Columns are always listed explicitly so that the row order in `values()` does not have to match
+     * the table's column order. Computed (`MATERIALIZED`/`ALIAS`) columns are excluded, since
+     * ClickHouse rejects writes to them.
+     */
+    buildInsertQuery({ table, values, select, settings }: ClickHouseInsertConfig): SQL;
+    /**
+     * The insert target as ClickHouse should see it — quoted, and schema-qualified when the table
+     * declares one. The driver's row-format insert takes a table *name*, not an expression, so it
+     * cannot go through the ordinary table rendering.
+     */
+    insertTargetName(table: ClickHouseTable): string;
+    /** The statement that precedes a row-format body: `insert into t format JSONEachRow`. */
+    buildInsertRowsQuery(table: ClickHouseTable, format: string): SQL;
+    /**
+     * One row as a row format carries it: ClickHouse column names, values through
+     * {@link ClickHouseColumn.mapToRowValue}.
+     *
+     * A column the row does not mention is **omitted rather than defaulted here**, because
+     * `JSONEachRow` matches by name and the server applies that column's own `DEFAULT` — which is the
+     * one the table declares, and therefore the one a `CREATE TABLE` and an `ALTER … ADD COLUMN` agree
+     * on. A Drizzle-side `$defaultFn` is still evaluated, since nothing server-side knows about it.
+     *
+     * Throws on a value there is no way to send: a body has nowhere to put an expression, so
+     * `sql\`now()\`` and placeholders belong to the statement path. The insert builder routes rows
+     * carrying either there automatically, so reaching this is either a stream that yields one or a
+     * caller that forced the format.
+     */
+    mapRowForInsert(table: ClickHouseTable, entry: Record<string, unknown>): Record<string, unknown>;
+    /**
+     * Renders one column's definition as it appears inside `CREATE TABLE`.
+     *
+     * Order matters to ClickHouse: `name Type [DEFAULT|MATERIALIZED|ALIAS|EPHEMERAL expr] [COMMENT]
+     * [CODEC] [TTL]`.
+     */
+    buildColumnDefinition(column: ClickHouseColumn): SQL;
+    /**
+     * Builds a `CREATE TABLE` statement for a table declared with {@link clickhouseTable}.
+     *
+     * Render it with `sqlToQuery(stmt, 'indexes')`: inside DDL, column references must be bare names
+     * rather than the `table`.`column` form used in queries.
+     */
+    buildCreateTableQuery(table: ClickHouseTable, options?: {
+        ifNotExists?: boolean;
+        onCluster?: string;
+    }): SQL;
+    /** Builds a `DROP TABLE` statement. */
+    buildDropTableQuery(table: ClickHouseTable, options?: {
+        ifExists?: boolean;
+        onCluster?: string;
+        sync?: boolean;
+    }): SQL;
+    sqlToQuery(sql: SQL, invokeSource?: 'indexes' | undefined): QueryWithTypings;
+}
diff --git a/clickhouse-core/dialect.d.ts b/clickhouse-core/dialect.d.ts
new file mode 100644
index 0000000..eaa324b
--- /dev/null
+++ b/clickhouse-core/dialect.d.ts
@@ -0,0 +1,136 @@
+import { entityKind } from '../entity.js';
+import type { MigrationConfig, MigrationMeta } from '../migrator.js';
+import type { QueryWithTypings } from '../sql/sql.js';
+import { SQL } from '../sql/sql.js';
+import { type Casing, type UpdateSet } from '../utils.js';
+import { ClickHouseColumn } from './columns/common.js';
+import type { ClickHouseDeleteConfig } from './query-builders/delete.js';
+import type { ClickHouseInsertConfig } from './query-builders/insert.js';
+import type { ClickHouseSelectConfig } from './query-builders/select.types.js';
+import type { ClickHouseUpdateConfig } from './query-builders/update.js';
+import type { ClickHouseSession } from './session.js';
+import { ClickHouseTable } from './table.js';
+export interface ClickHouseDialectConfig {
+    casing?: Casing;
+}
+export declare class ClickHouseDialect {
+    static readonly [entityKind]: string;
+    constructor(config?: ClickHouseDialectConfig);
+    /**
+     * Applies pending migrations.
+     *
+     * Unlike the other dialects this does not wrap the run in a transaction — ClickHouse has no usable
+     * one. Each statement is applied on its own, and the bookkeeping row is written only after a
+     * migration's statements have all succeeded, so a failure part-way leaves that migration marked as
+     * unapplied and it will be retried.
+     */
+    migrate(migrations: MigrationMeta[], session: ClickHouseSession, config: Omit<MigrationConfig, 'migrationsSchema'>): Promise<void>;
+    escapeName(name: string): string;
+    /**
+     * ClickHouse has no bind-parameter protocol, so every value is rendered inline as a literal —
+     * see `literals.ts` for why. `escapeParam` is therefore never reached: `sqlToQuery` always builds
+     * queries with `inlineParams` set.
+     */
+    escapeParam(_num: number, _value: unknown): string;
+    escapeString(str: string): string;
+    private buildWithCTE;
+    private buildSettings;
+    /**
+     * Rewrites qualified `` `table`.`column` `` references to bare `` `column` `` ones.
+     *
+     * `ALTER TABLE … UPDATE` and lightweight `DELETE FROM` are rewritten by ClickHouse into
+     * expressions evaluated against the table's columns directly, with no table name in scope — a
+     * qualified reference there fails with `UNKNOWN_IDENTIFIER`. Both forms are single-table, so
+     * dropping the qualifier is unambiguous.
+     */
+    private unqualifyColumns;
+    /**
+     * Builds a `DELETE`.
+     *
+     * The default is a lightweight `DELETE FROM … WHERE`, which ClickHouse supports from 22.8 onwards.
+     * `.mutation()` switches to `ALTER TABLE … DELETE WHERE`, which works on older servers and on
+     * engines where lightweight deletes are unavailable.
+     *
+     * Either form requires a `WHERE`, so a delete with no filter becomes `WHERE 1` — a deliberate
+     * "delete everything" rather than a syntax error.
+     */
+    buildDeleteQuery({ table, where, mutation, settings }: ClickHouseDeleteConfig): SQL;
+    buildUpdateSet(table: ClickHouseTable, set: UpdateSet): SQL;
+    /**
+     * Builds an `ALTER TABLE … UPDATE`, ClickHouse's mutation form of `UPDATE`.
+     *
+     * Mutations are asynchronous: the statement returns once accepted, and the rewrite happens in the
+     * background. Pass `.settings({ mutations_sync: 2 })` to block until it has finished everywhere.
+     */
+    buildUpdateQuery({ table, set, where, settings }: ClickHouseUpdateConfig): SQL;
+    /**
+     * Builds selection SQL with provided fields/expressions
+     *
+     * If `isSingleTable` is true, then columns won't be prefixed with table name
+     */
+    private buildSelection;
+    private buildLimit;
+    private buildOrderBy;
+    buildSelectQuery({ withList, fields, fieldsFlat, where, having, table, joins, orderBy, groupBy, limit, offset, distinct, setOperators, final, prewhere, sample, arrayJoins, limitBy, withTotals, settings, }: ClickHouseSelectConfig): SQL;
+    private buildJoins;
+    buildSetOperations(leftSelect: SQL, setOperators: ClickHouseSelectConfig['setOperators']): SQL;
+    buildSetOperationQuery({ leftSelect, setOperator: { type, isAll, rightSelect, limit, orderBy, offset }, }: {
+        leftSelect: SQL;
+        setOperator: ClickHouseSelectConfig['setOperators'][number];
+    }): SQL;
+    /**
+     * Builds an `INSERT`.
+     *
+     * Columns are always listed explicitly so that the row order in `values()` does not have to match
+     * the table's column order. Computed (`MATERIALIZED`/`ALIAS`) columns are excluded, since
+     * ClickHouse rejects writes to them.
+     */
+    buildInsertQuery({ table, values, select, settings }: ClickHouseInsertConfig): SQL;
+    /**
+     * The insert target as ClickHouse should see it — quoted, and schema-qualified when the table
+     * declares one. The driver's row-format insert takes a table *name*, not an expression, so it
+     * cannot go through the ordinary table rendering.
+     */
+    insertTargetName(table: ClickHouseTable): string;
+    /** The statement that precedes a row-format body: `insert into t format JSONEachRow`. */
+    buildInsertRowsQuery(table: ClickHouseTable, format: string): SQL;
+    /**
+     * One row as a row format carries it: ClickHouse column names, values through
+     * {@link ClickHouseColumn.mapToRowValue}.
+     *
+     * A column the row does not mention is **omitted rather than defaulted here**, because
+     * `JSONEachRow` matches by name and the server applies that column's own `DEFAULT` — which is the
+     * one the table declares, and therefore the one a `CREATE TABLE` and an `ALTER … ADD COLUMN` agree
+     * on. A Drizzle-side `$defaultFn` is still evaluated, since nothing server-side knows about it.
+     *
+     * Throws on a value there is no way to send: a body has nowhere to put an expression, so
+     * `sql\`now()\`` and placeholders belong to the statement path. The insert builder routes rows
+     * carrying either there automatically, so reaching this is either a stream that yields one or a
+     * caller that forced the format.
+     */
+    mapRowForInsert(table: ClickHouseTable, entry: Record<string, unknown>): Record<string, unknown>;
+    /**
+     * Renders one column's definition as it appears inside `CREATE TABLE`.
+     *
+     * Order matters to ClickHouse: `name Type [DEFAULT|MATERIALIZED|ALIAS|EPHEMERAL expr] [COMMENT]
+     * [CODEC] [TTL]`.
+     */
+    buildColumnDefinition(column: ClickHouseColumn): SQL;
+    /**
+     * Builds a `CREATE TABLE` statement for a table declared with {@link clickhouseTable}.
+     *
+     * Render it with `sqlToQuery(stmt, 'indexes')`: inside DDL, column references must be bare names
+     * rather than the `table`.`column` form used in queries.
+     */
+    buildCreateTableQuery(table: ClickHouseTable, options?: {
+        ifNotExists?: boolean;
+        onCluster?: string;
+    }): SQL;
+    /** Builds a `DROP TABLE` statement. */
+    buildDropTableQuery(table: ClickHouseTable, options?: {
+        ifExists?: boolean;
+        onCluster?: string;
+        sync?: boolean;
+    }): SQL;
+    sqlToQuery(sql: SQL, invokeSource?: 'indexes' | undefined): QueryWithTypings;
+}
diff --git a/clickhouse-core/dialect.js b/clickhouse-core/dialect.js
new file mode 100644
index 0000000..b391e22
--- /dev/null
+++ b/clickhouse-core/dialect.js
@@ -0,0 +1,539 @@
+import { CasingCache } from "../casing.js";
+import { Column } from "../column.js";
+import { entityKind, is } from "../entity.js";
+import { Param, Placeholder, SQL, sql, View } from "../sql/sql.js";
+import { Subquery } from "../subquery.js";
+import { getTableName, Table } from "../table.js";
+import { orderSelectedFields } from "../utils.js";
+import { ViewBaseConfig } from "../view-common.js";
+import { ClickHouseColumn } from "./columns/common.js";
+import { settingsSQL } from "./engines.js";
+import { escapeClickHouseIdentifier, escapeClickHouseString } from "./literals.js";
+import { ClickHouseInsertValueError } from "./errors.js";
+import { ClickHouseTable } from "./table.js";
+import { getTableConfig } from "./utils.js";
+class ClickHouseDialect {
+  static [entityKind] = "ClickHouseDialect";
+  /** @internal */
+  casing;
+  constructor(config) {
+    this.casing = new CasingCache(config?.casing);
+  }
+  /**
+   * Applies pending migrations.
+   *
+   * Unlike the other dialects this does not wrap the run in a transaction — ClickHouse has no usable
+   * one. Each statement is applied on its own, and the bookkeeping row is written only after a
+   * migration's statements have all succeeded, so a failure part-way leaves that migration marked as
+   * unapplied and it will be retried.
+   */
+  async migrate(migrations, session, config) {
+    const migrationsTable = config.migrationsTable ?? "__drizzle_migrations";
+    const migrationTableCreate = sql`
+			create table if not exists ${sql.identifier(migrationsTable)} (
+				hash String,
+				created_at Int64
+			)
+			engine = MergeTree()
+			order by created_at
+		`;
+    await session.execute(migrationTableCreate);
+    const dbMigrations = await session.all(
+      sql`select hash, created_at from ${sql.identifier(migrationsTable)} order by created_at desc limit 1`
+    );
+    const lastDbMigration = dbMigrations[0];
+    for (const migration of migrations) {
+      if (lastDbMigration && Number(lastDbMigration.created_at) >= migration.folderMillis) {
+        continue;
+      }
+      for (const stmt of migration.sql) {
+        await session.execute(sql.raw(stmt));
+      }
+      await session.execute(
+        sql`insert into ${sql.identifier(migrationsTable)} (hash, created_at) values (${migration.hash}, ${sql.raw(String(migration.folderMillis))})`
+      );
+    }
+  }
+  escapeName(name) {
+    return escapeClickHouseIdentifier(name);
+  }
+  /**
+   * ClickHouse has no bind-parameter protocol, so every value is rendered inline as a literal —
+   * see `literals.ts` for why. `escapeParam` is therefore never reached: `sqlToQuery` always builds
+   * queries with `inlineParams` set.
+   */
+  escapeParam(_num, _value) {
+    throw new Error("ClickHouse renders parameters inline; escapeParam should not be reached");
+  }
+  escapeString(str) {
+    return escapeClickHouseString(str);
+  }
+  buildWithCTE(queries) {
+    if (!queries?.length) return void 0;
+    const withSqlChunks = [sql`with `];
+    for (const [i, w] of queries.entries()) {
+      withSqlChunks.push(sql`${sql.identifier(w._.alias)} as (${w._.sql})`);
+      if (i < queries.length - 1) {
+        withSqlChunks.push(sql`, `);
+      }
+    }
+    withSqlChunks.push(sql` `);
+    return sql.join(withSqlChunks);
+  }
+  buildSettings(settings) {
+    return settings && Object.keys(settings).length > 0 ? sql` settings ${settingsSQL(settings)}` : void 0;
+  }
+  /**
+   * Rewrites qualified `` `table`.`column` `` references to bare `` `column` `` ones.
+   *
+   * `ALTER TABLE … UPDATE` and lightweight `DELETE FROM` are rewritten by ClickHouse into
+   * expressions evaluated against the table's columns directly, with no table name in scope — a
+   * qualified reference there fails with `UNKNOWN_IDENTIFIER`. Both forms are single-table, so
+   * dropping the qualifier is unambiguous.
+   */
+  unqualifyColumns(value) {
+    const rewrite = (chunk) => {
+      if (is(chunk, ClickHouseColumn)) {
+        return sql.identifier(this.casing.getColumnCasing(chunk));
+      }
+      if (is(chunk, SQL)) {
+        return this.unqualifyColumns(chunk);
+      }
+      if (Array.isArray(chunk)) {
+        return chunk.map((inner) => rewrite(inner));
+      }
+      return chunk;
+    };
+    return new SQL(value.queryChunks.map((chunk) => rewrite(chunk)));
+  }
+  /**
+   * Builds a `DELETE`.
+   *
+   * The default is a lightweight `DELETE FROM … WHERE`, which ClickHouse supports from 22.8 onwards.
+   * `.mutation()` switches to `ALTER TABLE … DELETE WHERE`, which works on older servers and on
+   * engines where lightweight deletes are unavailable.
+   *
+   * Either form requires a `WHERE`, so a delete with no filter becomes `WHERE 1` — a deliberate
+   * "delete everything" rather than a syntax error.
+   */
+  buildDeleteQuery({ table, where, mutation, settings }) {
+    const whereSql = sql` where ${where ? this.unqualifyColumns(where) : sql`1`}`;
+    const settingsSql = this.buildSettings(settings);
+    return mutation ? sql`alter table ${table} delete${whereSql}${settingsSql}` : sql`delete from ${table}${whereSql}${settingsSql}`;
+  }
+  buildUpdateSet(table, set) {
+    const tableColumns = table[Table.Symbol.Columns];
+    const columnNames = Object.keys(tableColumns).filter(
+      (colName) => set[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0
+    );
+    const setSize = columnNames.length;
+    return sql.join(
+      columnNames.flatMap((colName, i) => {
+        const col = tableColumns[colName];
+        const onUpdateFnResult = col.onUpdateFn?.();
+        const value = set[colName] ?? (is(onUpdateFnResult, SQL) ? onUpdateFnResult : sql.param(onUpdateFnResult, col));
+        const res = sql`${sql.identifier(this.casing.getColumnCasing(col))} = ${value}`;
+        if (i < setSize - 1) {
+          return [res, sql.raw(", ")];
+        }
+        return [res];
+      })
+    );
+  }
+  /**
+   * Builds an `ALTER TABLE … UPDATE`, ClickHouse's mutation form of `UPDATE`.
+   *
+   * Mutations are asynchronous: the statement returns once accepted, and the rewrite happens in the
+   * background. Pass `.settings({ mutations_sync: 2 })` to block until it has finished everywhere.
+   */
+  buildUpdateQuery({ table, set, where, settings }) {
+    const setSql = this.unqualifyColumns(this.buildUpdateSet(table, set));
+    const whereSql = sql` where ${where ? this.unqualifyColumns(where) : sql`1`}`;
+    const settingsSql = this.buildSettings(settings);
+    return sql`alter table ${table} update ${setSql}${whereSql}${settingsSql}`;
+  }
+  /**
+   * Builds selection SQL with provided fields/expressions
+   *
+   * If `isSingleTable` is true, then columns won't be prefixed with table name
+   */
+  buildSelection(fields, { isSingleTable = false } = {}) {
+    const columnsLen = fields.length;
+    const chunks = fields.flatMap(({ field }, i) => {
+      const chunk = [];
+      if (is(field, SQL.Aliased) && field.isSelectionField) {
+        chunk.push(sql.identifier(field.fieldAlias));
+      } else if (is(field, SQL.Aliased) || is(field, SQL)) {
+        const query = is(field, SQL.Aliased) ? field.sql : field;
+        if (isSingleTable) {
+          chunk.push(
+            new SQL(
+              query.queryChunks.map((c) => {
+                if (is(c, ClickHouseColumn)) {
+                  return sql.identifier(this.casing.getColumnCasing(c));
+                }
+                return c;
+              })
+            )
+          );
+        } else {
+          chunk.push(query);
+        }
+        if (is(field, SQL.Aliased)) {
+          chunk.push(sql` as ${sql.identifier(field.fieldAlias)}`);
+        }
+      } else if (is(field, Column)) {
+        if (isSingleTable) {
+          chunk.push(sql.identifier(this.casing.getColumnCasing(field)));
+        } else {
+          chunk.push(field);
+        }
+      } else if (is(field, Subquery)) {
+        const entries = Object.entries(field._.selectedFields);
+        if (entries.length === 1) {
+          const entry = entries[0][1];
+          const fieldDecoder = is(entry, SQL) ? entry.decoder : is(entry, Column) ? { mapFromDriverValue: (v) => entry.mapFromDriverValue(v) } : entry.sql.decoder;
+          if (fieldDecoder) {
+            field._.sql.decoder = fieldDecoder;
+          }
+        }
+        chunk.push(field);
+      }
+      if (i < columnsLen - 1) {
+        chunk.push(sql`, `);
+      }
+      return chunk;
+    });
+    return sql.join(chunks);
+  }
+  buildLimit(limit) {
+    return typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql` limit ${limit}` : void 0;
+  }
+  buildOrderBy(orderBy) {
+    return orderBy && orderBy.length > 0 ? sql` order by ${sql.join(orderBy, sql`, `)}` : void 0;
+  }
+  buildSelectQuery({
+    withList,
+    fields,
+    fieldsFlat,
+    where,
+    having,
+    table,
+    joins,
+    orderBy,
+    groupBy,
+    limit,
+    offset,
+    distinct,
+    setOperators,
+    final,
+    prewhere,
+    sample,
+    arrayJoins,
+    limitBy,
+    withTotals,
+    settings
+  }) {
+    const fieldsList = fieldsFlat ?? orderSelectedFields(fields);
+    for (const f of fieldsList) {
+      if (is(f.field, Column) && getTableName(f.field.table) !== (is(table, Subquery) ? table._.alias : is(table, SQL) ? void 0 : getTableName(table)) && !((table2) => joins?.some(
+        ({ alias }) => alias === (table2[Table.Symbol.IsAlias] ? getTableName(table2) : table2[Table.Symbol.BaseName])
+      ))(f.field.table)) {
+        const tableName = getTableName(f.field.table);
+        throw new Error(
+          `Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`
+        );
+      }
+    }
+    const isSingleTable = !joins || joins.length === 0;
+    const withSql = this.buildWithCTE(withList);
+    const distinctSql = distinct ? sql` distinct` : void 0;
+    const selection = this.buildSelection(fieldsList, { isSingleTable });
+    const tableSql = (() => {
+      if (is(table, Table) && table[Table.Symbol.IsAlias]) {
+        return sql`${sql`${sql.identifier(table[Table.Symbol.Schema] ?? "")}.`.if(table[Table.Symbol.Schema])}${sql.identifier(table[Table.Symbol.OriginalName])} ${sql.identifier(table[Table.Symbol.Name])}`;
+      }
+      return table;
+    })();
+    const finalSql = final ? sql` final` : void 0;
+    const sampleSql = sample ? sql` sample ${sql.raw(String(sample.value))}${sample.offset === void 0 ? void 0 : sql` offset ${sql.raw(String(sample.offset))}`}` : void 0;
+    const joinsSql = sql.join(this.buildJoins(joins));
+    const arrayJoinsSql = arrayJoins?.length ? sql.join(
+      arrayJoins.map(
+        (arrayJoin) => sql` ${sql.raw(arrayJoin.left ? "left array join" : "array join")} ${sql.join(arrayJoin.expressions, sql`, `)}`
+      )
+    ) : void 0;
+    const prewhereSql = prewhere ? sql` prewhere ${prewhere}` : void 0;
+    const whereSql = where ? sql` where ${where}` : void 0;
+    const groupBySql = groupBy && groupBy.length > 0 ? sql` group by ${sql.join(groupBy, sql`, `)}${withTotals ? sql` with totals` : void 0}` : void 0;
+    const havingSql = having ? sql` having ${having}` : void 0;
+    const orderBySql = this.buildOrderBy(orderBy);
+    const limitBySql = limitBy ? sql` limit ${limitBy.limit}${limitBy.offset === void 0 ? void 0 : sql` offset ${limitBy.offset}`} by ${sql.join(limitBy.expressions, sql`, `)}` : void 0;
+    const limitSql = this.buildLimit(limit);
+    const offsetSql = offset ? sql` offset ${offset}` : void 0;
+    const settingsSql = this.buildSettings(settings);
+    const finalQuery = sql`${withSql}select${distinctSql} ${selection} from ${tableSql}${finalSql}${sampleSql}${joinsSql}${arrayJoinsSql}${prewhereSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitBySql}${limitSql}${offsetSql}${settingsSql}`;
+    if (setOperators.length > 0) {
+      return this.buildSetOperations(finalQuery, setOperators);
+    }
+    return finalQuery;
+  }
+  buildJoins(joins) {
+    const joinsArray = [];
+    if (!joins) return joinsArray;
+    for (const [index, joinMeta] of joins.entries()) {
+      if (index === 0) {
+        joinsArray.push(sql` `);
+      }
+      const table = joinMeta.table;
+      const globalSql = joinMeta.global ? sql`global ` : void 0;
+      const strictnessSql = joinMeta.strictness ? sql`${sql.raw(joinMeta.strictness)} ` : void 0;
+      const onSql = joinMeta.on ? sql` on ${joinMeta.on}` : void 0;
+      const joinPrefix = joinMeta.joinType === "cross" ? sql`${globalSql}cross` : sql`${globalSql}${strictnessSql}${sql.raw(joinMeta.joinType)}`;
+      if (is(table, ClickHouseTable)) {
+        const tableName = table[ClickHouseTable.Symbol.Name];
+        const tableSchema = table[ClickHouseTable.Symbol.Schema];
+        const origTableName = table[ClickHouseTable.Symbol.OriginalName];
+        const alias = tableName === origTableName ? void 0 : joinMeta.alias;
+        joinsArray.push(
+          sql`${joinPrefix} join ${tableSchema ? sql`${sql.identifier(tableSchema)}.` : void 0}${sql.identifier(origTableName)}${alias && sql` ${sql.identifier(alias)}`}${onSql}`
+        );
+      } else if (is(table, View)) {
+        const viewName = table[ViewBaseConfig].name;
+        const viewSchema = table[ViewBaseConfig].schema;
+        const origViewName = table[ViewBaseConfig].originalName;
+        const alias = viewName === origViewName ? void 0 : joinMeta.alias;
+        joinsArray.push(
+          sql`${joinPrefix} join ${viewSchema ? sql`${sql.identifier(viewSchema)}.` : void 0}${sql.identifier(origViewName)}${alias && sql` ${sql.identifier(alias)}`}${onSql}`
+        );
+      } else {
+        joinsArray.push(sql`${joinPrefix} join ${table}${onSql}`);
+      }
+      if (index < joins.length - 1) {
+        joinsArray.push(sql` `);
+      }
+    }
+    return joinsArray;
+  }
+  buildSetOperations(leftSelect, setOperators) {
+    const [setOperator, ...rest] = setOperators;
+    if (!setOperator) {
+      throw new Error("Cannot pass undefined values to any set operator");
+    }
+    if (rest.length === 0) {
+      return this.buildSetOperationQuery({ leftSelect, setOperator });
+    }
+    return this.buildSetOperations(
+      this.buildSetOperationQuery({ leftSelect, setOperator }),
+      rest
+    );
+  }
+  buildSetOperationQuery({
+    leftSelect,
+    setOperator: { type, isAll, rightSelect, limit, orderBy, offset }
+  }) {
+    const leftChunk = sql`(${leftSelect.getSQL()}) `;
+    const rightChunk = sql`(${rightSelect.getSQL()})`;
+    let orderBySql;
+    if (orderBy && orderBy.length > 0) {
+      const orderByValues = [];
+      for (const orderByUnit of orderBy) {
+        if (is(orderByUnit, ClickHouseColumn)) {
+          orderByValues.push(sql.identifier(this.casing.getColumnCasing(orderByUnit)));
+        } else if (is(orderByUnit, SQL)) {
+          for (let i = 0; i < orderByUnit.queryChunks.length; i++) {
+            const chunk = orderByUnit.queryChunks[i];
+            if (is(chunk, ClickHouseColumn)) {
+              orderByUnit.queryChunks[i] = sql.identifier(this.casing.getColumnCasing(chunk));
+            }
+          }
+          orderByValues.push(sql`${orderByUnit}`);
+        } else {
+          orderByValues.push(sql`${orderByUnit}`);
+        }
+      }
+      orderBySql = sql` order by ${sql.join(orderByValues, sql`, `)} `;
+    }
+    const limitSql = typeof limit === "object" || typeof limit === "number" && limit >= 0 ? sql` limit ${limit}` : void 0;
+    const operatorChunk = sql.raw(`${type} ${isAll ? "all " : "distinct "}`);
+    const offsetSql = offset ? sql` offset ${offset}` : void 0;
+    return sql`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
+  }
+  /**
+   * Builds an `INSERT`.
+   *
+   * Columns are always listed explicitly so that the row order in `values()` does not have to match
+   * the table's column order. Computed (`MATERIALIZED`/`ALIAS`) columns are excluded, since
+   * ClickHouse rejects writes to them.
+   */
+  buildInsertQuery({ table, values, select, settings }) {
+    const settingsSql = this.buildSettings(settings);
+    const columns = table[Table.Symbol.Columns];
+    const colEntries = Object.entries(columns).filter(([, col]) => !col.shouldDisableInsert());
+    const insertOrder = colEntries.map(([, column]) => sql.identifier(this.casing.getColumnCasing(column)));
+    if (select) {
+      return sql`insert into ${table} ${select}${settingsSql}`;
+    }
+    if (!values) {
+      throw new Error("An insert must be given either values() or select()");
+    }
+    const valuesSqlList = [];
+    for (const [valueIndex, value] of values.entries()) {
+      const valueList = [];
+      for (const [fieldName, col] of colEntries) {
+        const colValue = value[fieldName];
+        if (colValue === void 0 || is(colValue, Param) && colValue.value === void 0) {
+          if (col.defaultFn !== void 0) {
+            const defaultFnResult = col.defaultFn();
+            valueList.push(is(defaultFnResult, SQL) ? defaultFnResult : sql.param(defaultFnResult, col));
+          } else if (!col.default && col.onUpdateFn !== void 0) {
+            const onUpdateFnResult = col.onUpdateFn();
+            valueList.push(is(onUpdateFnResult, SQL) ? onUpdateFnResult : sql.param(onUpdateFnResult, col));
+          } else {
+            valueList.push(sql`default`);
+          }
+        } else {
+          valueList.push(colValue);
+        }
+      }
+      valuesSqlList.push(valueList);
+      if (valueIndex < values.length - 1) {
+        valuesSqlList.push(sql`, `);
+      }
+    }
+    const valuesSql = sql.join(valuesSqlList);
+    return sql`insert into ${table} ${insertOrder} values ${valuesSql}${settingsSql}`;
+  }
+  /**
+   * The insert target as ClickHouse should see it — quoted, and schema-qualified when the table
+   * declares one. The driver's row-format insert takes a table *name*, not an expression, so it
+   * cannot go through the ordinary table rendering.
+   */
+  insertTargetName(table) {
+    return this.sqlToQuery(sql`${table}`).sql;
+  }
+  /** The statement that precedes a row-format body: `insert into t format JSONEachRow`. */
+  buildInsertRowsQuery(table, format) {
+    return sql`insert into ${table} format ${sql.raw(format)}`;
+  }
+  /**
+   * One row as a row format carries it: ClickHouse column names, values through
+   * {@link ClickHouseColumn.mapToRowValue}.
+   *
+   * A column the row does not mention is **omitted rather than defaulted here**, because
+   * `JSONEachRow` matches by name and the server applies that column's own `DEFAULT` — which is the
+   * one the table declares, and therefore the one a `CREATE TABLE` and an `ALTER … ADD COLUMN` agree
+   * on. A Drizzle-side `$defaultFn` is still evaluated, since nothing server-side knows about it.
+   *
+   * Throws on a value there is no way to send: a body has nowhere to put an expression, so
+   * `sql\`now()\`` and placeholders belong to the statement path. The insert builder routes rows
+   * carrying either there automatically, so reaching this is either a stream that yields one or a
+   * caller that forced the format.
+   */
+  mapRowForInsert(table, entry) {
+    const columns = table[Table.Symbol.Columns];
+    for (const key of Object.keys(entry)) {
+      if (!(key in columns)) {
+        throw new ClickHouseInsertValueError(
+          `Column "${key}" does not exist on table "${getTableName(table)}". A row format matches by name, so an unrecognised key would be dropped by the server rather than rejected.`
+        );
+      }
+    }
+    const row = {};
+    for (const [fieldName, column] of Object.entries(columns)) {
+      if (column.shouldDisableInsert()) continue;
+      let value = entry[fieldName];
+      if (value === void 0 && column.defaultFn !== void 0) {
+        value = column.defaultFn();
+      }
+      if (value === void 0) continue;
+      if (is(value, SQL) || is(value, Param) || is(value, Placeholder)) {
+        throw new ClickHouseInsertValueError(
+          `Column "${fieldName}" of "${getTableName(table)}" was given a SQL expression, which a row format cannot carry. Insert this row through the statement path instead.`
+        );
+      }
+      row[this.casing.getColumnCasing(column)] = value === null ? null : column.mapToRowValue(value);
+    }
+    return row;
+  }
+  /**
+   * Renders one column's definition as it appears inside `CREATE TABLE`.
+   *
+   * Order matters to ClickHouse: `name Type [DEFAULT|MATERIALIZED|ALIAS|EPHEMERAL expr] [COMMENT]
+   * [CODEC] [TTL]`.
+   */
+  buildColumnDefinition(column) {
+    const chunks = [
+      sql`${sql.identifier(this.casing.getColumnCasing(column))} ${sql.raw(column.getSQLType())}`
+    ];
+    const computedKeyword = column.computedKeyword;
+    if (computedKeyword !== void 0) {
+      const generated = column.generated;
+      const expression = typeof generated.as === "function" ? generated.as() : generated.as;
+      chunks.push(sql`${sql.raw(computedKeyword)} ${expression}`);
+    } else if (column.isEphemeral) {
+      chunks.push(
+        column.default === void 0 ? sql`EPHEMERAL` : sql`EPHEMERAL ${is(column.default, SQL) ? column.default : sql.param(column.default, column)}`
+      );
+    } else if (column.default !== void 0) {
+      chunks.push(sql`DEFAULT ${is(column.default, SQL) ? column.default : sql.param(column.default, column)}`);
+    }
+    if (column.comment !== void 0) {
+      chunks.push(sql`COMMENT ${sql.raw(escapeClickHouseString(column.comment))}`);
+    }
+    const codec = column.codec;
+    if (codec?.length) {
+      chunks.push(
+        sql`CODEC(${sql.join(codec.map((c) => typeof c === "string" ? sql.raw(c) : c), sql`, `)})`
+      );
+    }
+    if (column.ttl !== void 0) {
+      chunks.push(sql`TTL ${column.ttl}`);
+    }
+    return sql.join(chunks, sql` `);
+  }
+  /**
+   * Builds a `CREATE TABLE` statement for a table declared with {@link clickhouseTable}.
+   *
+   * Render it with `sqlToQuery(stmt, 'indexes')`: inside DDL, column references must be bare names
+   * rather than the `table`.`column` form used in queries.
+   */
+  buildCreateTableQuery(table, options = {}) {
+    const { columns, engine, indexes, projections } = getTableConfig(table);
+    if (!engine) {
+      throw new Error(
+        `Table "${getTableName(table)}" does not declare an engine. Add one to the table's extra config, e.g. \`(t) => [mergeTree({ orderBy: t.id })]\`.`
+      );
+    }
+    const entries = [
+      ...columns.map((column) => this.buildColumnDefinition(column)),
+      ...indexes.map((index) => index.getSQL()),
+      ...projections.map((projection) => projection.getSQL())
+    ];
+    const ifNotExistsSql = options.ifNotExists ? sql`if not exists ` : void 0;
+    const onClusterSql = options.onCluster ? sql` on cluster ${sql.identifier(options.onCluster)}` : void 0;
+    const clausesSql = engine.getClausesSQL();
+    return sql`create table ${ifNotExistsSql}${table}${onClusterSql} (${sql.join(entries, sql`, `)}) ${engine.getEngineSQL()}${clausesSql ? sql` ${clausesSql}` : void 0}`;
+  }
+  /** Builds a `DROP TABLE` statement. */
+  buildDropTableQuery(table, options = {}) {
+    const ifExistsSql = options.ifExists ? sql`if exists ` : void 0;
+    const onClusterSql = options.onCluster ? sql` on cluster ${sql.identifier(options.onCluster)}` : void 0;
+    const syncSql = options.sync ? sql` sync` : void 0;
+    return sql`drop table ${ifExistsSql}${table}${onClusterSql}${syncSql}`;
+  }
+  sqlToQuery(sql2, invokeSource) {
+    return sql2.toQuery({
+      casing: this.casing,
+      escapeName: this.escapeName,
+      escapeParam: this.escapeParam,
+      escapeString: this.escapeString,
+      // ClickHouse has no bind parameters; values are rendered as escaped literals instead.
+      inlineParams: true,
+      invokeSource
+    });
+  }
+}
+export {
+  ClickHouseDialect
+};
diff --git a/clickhouse-core/engines.cjs b/clickhouse-core/engines.cjs
new file mode 100644
index 0000000..a4caaab
--- /dev/null
+++ b/clickhouse-core/engines.cjs
@@ -0,0 +1,221 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var engines_exports = {};
+__export(engines_exports, {
+  ClickHouseTableEngine: () => ClickHouseTableEngine,
+  aggregatingMergeTree: () => aggregatingMergeTree,
+  collapsingMergeTree: () => collapsingMergeTree,
+  customEngine: () => customEngine,
+  distributed: () => distributed,
+  keySQL: () => keySQL,
+  log: () => log,
+  memory: () => memory,
+  mergeTree: () => mergeTree,
+  nullEngine: () => nullEngine,
+  replacingMergeTree: () => replacingMergeTree,
+  setEngine: () => setEngine,
+  settingsSQL: () => settingsSQL,
+  stripeLog: () => stripeLog,
+  summingMergeTree: () => summingMergeTree,
+  tinyLog: () => tinyLog,
+  versionedCollapsingMergeTree: () => versionedCollapsingMergeTree
+});
+module.exports = __toCommonJS(engines_exports);
+var import_entity = require("../entity.cjs");
+var import_sql = require("../sql/sql.cjs");
+function toExpressionList(key) {
+  if (key === void 0) return void 0;
+  return Array.isArray(key) ? key : [key];
+}
+class ClickHouseTableEngine {
+  constructor(name, args = [], options = {}) {
+    this.name = name;
+    this.args = args;
+    this.orderBy = toExpressionList(options.orderBy);
+    this.partitionBy = toExpressionList(options.partitionBy);
+    this.primaryKey = toExpressionList(options.primaryKey);
+    this.sampleBy = toExpressionList(options.sampleBy);
+    this.ttl = options.ttl;
+    this.settings = options.settings;
+  }
+  name;
+  args;
+  static [import_entity.entityKind] = "ClickHouseTableEngine";
+  orderBy;
+  partitionBy;
+  primaryKey;
+  sampleBy;
+  ttl;
+  settings;
+  /** Whether this engine belongs to the MergeTree family and therefore takes a sorting key. */
+  get isMergeTree() {
+    return this.name.endsWith("MergeTree");
+  }
+  /** Renders `ENGINE = <name>(<args>)`. */
+  getEngineSQL() {
+    const engine = import_sql.sql.raw(this.name);
+    return this.args.length > 0 ? import_sql.sql`ENGINE = ${engine}(${import_sql.sql.join(this.args, import_sql.sql`, `)})` : import_sql.sql`ENGINE = ${engine}`;
+  }
+  /**
+   * Renders the clauses that follow the engine, in the order ClickHouse expects them.
+   *
+   * Returns `undefined` when the engine takes no clauses at all — `Memory`, the Log family and the
+   * integration engines.
+   */
+  getClausesSQL() {
+    const chunks = [];
+    if (this.partitionBy) {
+      chunks.push(import_sql.sql`PARTITION BY ${keySQL(this.partitionBy)}`);
+    }
+    if (this.isMergeTree) {
+      chunks.push(import_sql.sql`ORDER BY ${this.orderBy ? keySQL(this.orderBy) : import_sql.sql`tuple()`}`);
+    } else if (this.orderBy) {
+      chunks.push(import_sql.sql`ORDER BY ${keySQL(this.orderBy)}`);
+    }
+    if (this.primaryKey) {
+      chunks.push(import_sql.sql`PRIMARY KEY ${keySQL(this.primaryKey)}`);
+    }
+    if (this.sampleBy) {
+      chunks.push(import_sql.sql`SAMPLE BY ${keySQL(this.sampleBy)}`);
+    }
+    if (this.ttl) {
+      chunks.push(import_sql.sql`TTL ${this.ttl}`);
+    }
+    if (this.settings && Object.keys(this.settings).length > 0) {
+      chunks.push(import_sql.sql`SETTINGS ${settingsSQL(this.settings)}`);
+    }
+    return chunks.length > 0 ? import_sql.sql.join(chunks, import_sql.sql` `) : void 0;
+  }
+}
+function keySQL(expressions) {
+  return expressions.length === 1 ? import_sql.sql`${expressions[0]}` : import_sql.sql`(${import_sql.sql.join(expressions, import_sql.sql`, `)})`;
+}
+function settingsSQL(settings) {
+  return import_sql.sql.join(
+    Object.entries(settings).map(
+      ([name, value]) => import_sql.sql`${import_sql.sql.raw(name)} = ${typeof value === "boolean" ? import_sql.sql.raw(value ? "1" : "0") : value}`
+    ),
+    import_sql.sql`, `
+  );
+}
+function mergeTreeFactory(baseName) {
+  return (options = {}, args = []) => {
+    const { replicated } = options;
+    if (!replicated) {
+      return new ClickHouseTableEngine(baseName, args, options);
+    }
+    const replicationArgs = [];
+    if (replicated.zooPath !== void 0) {
+      replicationArgs.push(import_sql.sql`${replicated.zooPath}`);
+    }
+    if (replicated.replicaName !== void 0) {
+      replicationArgs.push(import_sql.sql`${replicated.replicaName}`);
+    }
+    return new ClickHouseTableEngine(`Replicated${baseName}`, [...replicationArgs, ...args], options);
+  };
+}
+const buildMergeTree = mergeTreeFactory("MergeTree");
+const buildReplacingMergeTree = mergeTreeFactory("ReplacingMergeTree");
+const buildSummingMergeTree = mergeTreeFactory("SummingMergeTree");
+const buildAggregatingMergeTree = mergeTreeFactory("AggregatingMergeTree");
+const buildCollapsingMergeTree = mergeTreeFactory("CollapsingMergeTree");
+const buildVersionedCollapsingMergeTree = mergeTreeFactory("VersionedCollapsingMergeTree");
+function mergeTree(options = {}) {
+  return buildMergeTree(options);
+}
+function replacingMergeTree(options = {}) {
+  const args = [];
+  if (options.version !== void 0) args.push(options.version);
+  if (options.isDeleted !== void 0) {
+    if (options.version === void 0) {
+      throw new Error("ReplacingMergeTree: `isDeleted` requires `version` to be set as well");
+    }
+    args.push(options.isDeleted);
+  }
+  return buildReplacingMergeTree(options, args);
+}
+function summingMergeTree(options = {}) {
+  const args = options.columns?.length ? [keySQL(options.columns)] : [];
+  return buildSummingMergeTree(options, args);
+}
+function aggregatingMergeTree(options = {}) {
+  return buildAggregatingMergeTree(options);
+}
+function collapsingMergeTree(options) {
+  return buildCollapsingMergeTree(options, [options.sign]);
+}
+function versionedCollapsingMergeTree(options) {
+  return buildVersionedCollapsingMergeTree(options, [options.sign, options.version]);
+}
+function memory() {
+  return new ClickHouseTableEngine("Memory");
+}
+function log() {
+  return new ClickHouseTableEngine("Log");
+}
+function tinyLog() {
+  return new ClickHouseTableEngine("TinyLog");
+}
+function stripeLog() {
+  return new ClickHouseTableEngine("StripeLog");
+}
+function nullEngine() {
+  return new ClickHouseTableEngine("Null");
+}
+function setEngine() {
+  return new ClickHouseTableEngine("Set");
+}
+function distributed(options) {
+  const args = [
+    import_sql.sql`${options.cluster}`,
+    import_sql.sql`${options.database}`,
+    import_sql.sql`${options.table}`
+  ];
+  if (options.shardingKey !== void 0) args.push(options.shardingKey);
+  if (options.policyName !== void 0) {
+    if (options.shardingKey === void 0) {
+      throw new Error("Distributed: `policyName` requires `shardingKey` to be set as well");
+    }
+    args.push(import_sql.sql`${options.policyName}`);
+  }
+  return new ClickHouseTableEngine("Distributed", args);
+}
+function customEngine(name, args = [], options = {}) {
+  return new ClickHouseTableEngine(name, args, options);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseTableEngine,
+  aggregatingMergeTree,
+  collapsingMergeTree,
+  customEngine,
+  distributed,
+  keySQL,
+  log,
+  memory,
+  mergeTree,
+  nullEngine,
+  replacingMergeTree,
+  setEngine,
+  settingsSQL,
+  stripeLog,
+  summingMergeTree,
+  tinyLog,
+  versionedCollapsingMergeTree
+});
diff --git a/clickhouse-core/engines.d.cts b/clickhouse-core/engines.d.cts
new file mode 100644
index 0000000..ef7fb3b
--- /dev/null
+++ b/clickhouse-core/engines.d.cts
@@ -0,0 +1,172 @@
+import { entityKind } from '../entity.cjs';
+import type { SQL } from '../sql/sql.cjs';
+import type { ClickHouseColumn } from './columns/common.cjs';
+/** An expression usable in `ORDER BY`, `PARTITION BY`, `PRIMARY KEY` or `SAMPLE BY`. */
+export type ClickHouseExpression = ClickHouseColumn | SQL;
+/** One or more key expressions. A bare value is treated as a single-element key. */
+export type ClickHouseKey = ClickHouseExpression | ClickHouseExpression[];
+/** Values accepted in a table's `SETTINGS` clause. */
+export type ClickHouseSettings = Record<string, string | number | boolean>;
+/**
+ * The table-level clauses the MergeTree family accepts after `ENGINE = …`.
+ *
+ * Log-family, `Memory` and integration engines accept none of these, which is why they are typed onto
+ * the MergeTree factories rather than onto the engine base.
+ */
+export interface MergeTreeOptions {
+    /**
+     * `ORDER BY` — the sorting key, which determines on-disk order and the granule index.
+     *
+     * MergeTree requires one. Omitting it emits `ORDER BY tuple()`, ClickHouse's way of spelling "no
+     * sorting key", which is only appropriate for tables you never filter on a key.
+     */
+    orderBy?: ClickHouseKey;
+    /** `PARTITION BY` — the partitioning key. Keep the cardinality low; monthly is a common choice. */
+    partitionBy?: ClickHouseKey;
+    /**
+     * `PRIMARY KEY` — the sparse index key.
+     *
+     * Defaults to the sorting key, and only needs to be given when it should be a *prefix* of
+     * `ORDER BY` rather than equal to it.
+     */
+    primaryKey?: ClickHouseKey;
+    /** `SAMPLE BY` — the sampling key. Must be present in the primary key. */
+    sampleBy?: ClickHouseKey;
+    /** `TTL` — when rows expire, e.g. ``sql`createdAt + INTERVAL 90 DAY` ``. */
+    ttl?: SQL;
+    /** `SETTINGS` — engine settings such as `index_granularity`. */
+    settings?: ClickHouseSettings;
+    /**
+     * Wraps the engine in its `Replicated…` variant.
+     *
+     * On ClickHouse Cloud and on servers with a configured default replication path both fields can be
+     * omitted, and `ENGINE = ReplicatedMergeTree` is emitted without arguments.
+     */
+    replicated?: {
+        /** The ZooKeeper/Keeper path, e.g. `'/clickhouse/tables/{shard}/events'`. */
+        zooPath?: string;
+        /** The replica name, e.g. `'{replica}'`. */
+        replicaName?: string;
+    };
+}
+/**
+ * A ClickHouse table engine together with the table-level clauses that follow it.
+ *
+ * Instances are produced by {@link mergeTree} and friends and are declared in a table's extra-config
+ * array:
+ *
+ * ```ts
+ * export const events = clickhouseTable('events', {
+ * 	id: uint64().notNull(),
+ * 	ts: dateTime({ timezone: 'UTC' }).notNull(),
+ * }, (t) => [
+ * 	mergeTree({ orderBy: [t.ts, t.id], partitionBy: sql`toYYYYMM(${t.ts})` }),
+ * ]);
+ * ```
+ */
+export declare class ClickHouseTableEngine {
+    /** The engine name as it appears after `ENGINE = `, e.g. `ReplacingMergeTree`. */
+    readonly name: string;
+    /** Positional engine arguments, e.g. the version column of `ReplacingMergeTree`. */
+    readonly args: ClickHouseExpression[];
+    static readonly [entityKind]: string;
+    readonly orderBy: ClickHouseExpression[] | undefined;
+    readonly partitionBy: ClickHouseExpression[] | undefined;
+    readonly primaryKey: ClickHouseExpression[] | undefined;
+    readonly sampleBy: ClickHouseExpression[] | undefined;
+    readonly ttl: SQL | undefined;
+    readonly settings: ClickHouseSettings | undefined;
+    constructor(
+    /** The engine name as it appears after `ENGINE = `, e.g. `ReplacingMergeTree`. */
+    name: string, 
+    /** Positional engine arguments, e.g. the version column of `ReplacingMergeTree`. */
+    args?: ClickHouseExpression[], options?: MergeTreeOptions);
+    /** Whether this engine belongs to the MergeTree family and therefore takes a sorting key. */
+    get isMergeTree(): boolean;
+    /** Renders `ENGINE = <name>(<args>)`. */
+    getEngineSQL(): SQL;
+    /**
+     * Renders the clauses that follow the engine, in the order ClickHouse expects them.
+     *
+     * Returns `undefined` when the engine takes no clauses at all — `Memory`, the Log family and the
+     * integration engines.
+     */
+    getClausesSQL(): SQL | undefined;
+}
+/** Renders a key as a bare expression when it has one element, or a tuple when it has several. */
+export declare function keySQL(expressions: ClickHouseExpression[]): SQL;
+/** Renders `name = value` pairs for a `SETTINGS` clause. */
+export declare function settingsSQL(settings: ClickHouseSettings): SQL;
+/**
+ * `MergeTree` — the default engine, and the right choice for almost every analytical table.
+ */
+export declare function mergeTree(options?: MergeTreeOptions): ClickHouseTableEngine;
+export interface ReplacingMergeTreeOptions extends MergeTreeOptions {
+    /**
+     * The column that decides which of two rows with the same sorting key wins — the largest value is
+     * kept. Defaults to "the last row inserted".
+     */
+    version?: ClickHouseExpression;
+    /** A `UInt8` column marking a row as deleted; requires `version`. */
+    isDeleted?: ClickHouseExpression;
+}
+/**
+ * `ReplacingMergeTree` — deduplicates rows sharing a sorting key during background merges.
+ *
+ * Deduplication is eventual, so reads either need `FINAL` or have to tolerate duplicates.
+ */
+export declare function replacingMergeTree(options?: ReplacingMergeTreeOptions): ClickHouseTableEngine;
+export interface SummingMergeTreeOptions extends MergeTreeOptions {
+    /** The numeric columns to sum. Defaults to every numeric column outside the sorting key. */
+    columns?: ClickHouseExpression[];
+}
+/** `SummingMergeTree` — collapses rows sharing a sorting key by summing their numeric columns. */
+export declare function summingMergeTree(options?: SummingMergeTreeOptions): ClickHouseTableEngine;
+/** `AggregatingMergeTree` — collapses rows sharing a sorting key using `AggregateFunction` states. */
+export declare function aggregatingMergeTree(options?: MergeTreeOptions): ClickHouseTableEngine;
+export interface CollapsingMergeTreeOptions extends MergeTreeOptions {
+    /** The `Int8` sign column: `1` marks a state row, `-1` marks its cancellation. */
+    sign: ClickHouseExpression;
+}
+/** `CollapsingMergeTree` — cancels out pairs of rows with opposite `sign` values during merges. */
+export declare function collapsingMergeTree(options: CollapsingMergeTreeOptions): ClickHouseTableEngine;
+export interface VersionedCollapsingMergeTreeOptions extends CollapsingMergeTreeOptions {
+    /** The version column, so that rows collapse correctly even when inserted out of order. */
+    version: ClickHouseExpression;
+}
+/** `VersionedCollapsingMergeTree` — `CollapsingMergeTree` that tolerates out-of-order inserts. */
+export declare function versionedCollapsingMergeTree(options: VersionedCollapsingMergeTreeOptions): ClickHouseTableEngine;
+/** `Memory` — rows live in RAM only and are lost on restart. Useful for tests and staging data. */
+export declare function memory(): ClickHouseTableEngine;
+/** `Log` — a simple append-only engine for small tables written once and read whole. */
+export declare function log(): ClickHouseTableEngine;
+/** `TinyLog` — the simplest engine; one file per column, no concurrent reads. */
+export declare function tinyLog(): ClickHouseTableEngine;
+/** `StripeLog` — a log engine that stores all columns in a single file. */
+export declare function stripeLog(): ClickHouseTableEngine;
+/** `Null` — discards everything written to it. Useful as the source of a materialized view. */
+export declare function nullEngine(): ClickHouseTableEngine;
+/** `Set` — an in-memory set, usable only on the right-hand side of `IN`. */
+export declare function setEngine(): ClickHouseTableEngine;
+export interface DistributedOptions {
+    /** The cluster name as defined in the server's `remote_servers` config. */
+    cluster: string;
+    /** The database holding the local shard tables. */
+    database: string;
+    /** The local table on each shard. */
+    table: string;
+    /** How rows are routed to shards, e.g. ``sql`rand()` ``. */
+    shardingKey?: ClickHouseExpression;
+    /** The policy name for storing temporary files during asynchronous sends. */
+    policyName?: string;
+}
+/** `Distributed` — a view over shards of the same table on every node of a cluster. */
+export declare function distributed(options: DistributedOptions): ClickHouseTableEngine;
+/**
+ * Any other engine, spelled out by hand.
+ *
+ * ```ts
+ * customEngine('S3', [sql`'https://bucket.s3.amazonaws.com/data/*.csv'`, sql`'CSV'`])
+ * ```
+ */
+export declare function customEngine(name: string, args?: ClickHouseExpression[], options?: MergeTreeOptions): ClickHouseTableEngine;
diff --git a/clickhouse-core/engines.d.ts b/clickhouse-core/engines.d.ts
new file mode 100644
index 0000000..31dc400
--- /dev/null
+++ b/clickhouse-core/engines.d.ts
@@ -0,0 +1,172 @@
+import { entityKind } from '../entity.js';
+import type { SQL } from '../sql/sql.js';
+import type { ClickHouseColumn } from './columns/common.js';
+/** An expression usable in `ORDER BY`, `PARTITION BY`, `PRIMARY KEY` or `SAMPLE BY`. */
+export type ClickHouseExpression = ClickHouseColumn | SQL;
+/** One or more key expressions. A bare value is treated as a single-element key. */
+export type ClickHouseKey = ClickHouseExpression | ClickHouseExpression[];
+/** Values accepted in a table's `SETTINGS` clause. */
+export type ClickHouseSettings = Record<string, string | number | boolean>;
+/**
+ * The table-level clauses the MergeTree family accepts after `ENGINE = …`.
+ *
+ * Log-family, `Memory` and integration engines accept none of these, which is why they are typed onto
+ * the MergeTree factories rather than onto the engine base.
+ */
+export interface MergeTreeOptions {
+    /**
+     * `ORDER BY` — the sorting key, which determines on-disk order and the granule index.
+     *
+     * MergeTree requires one. Omitting it emits `ORDER BY tuple()`, ClickHouse's way of spelling "no
+     * sorting key", which is only appropriate for tables you never filter on a key.
+     */
+    orderBy?: ClickHouseKey;
+    /** `PARTITION BY` — the partitioning key. Keep the cardinality low; monthly is a common choice. */
+    partitionBy?: ClickHouseKey;
+    /**
+     * `PRIMARY KEY` — the sparse index key.
+     *
+     * Defaults to the sorting key, and only needs to be given when it should be a *prefix* of
+     * `ORDER BY` rather than equal to it.
+     */
+    primaryKey?: ClickHouseKey;
+    /** `SAMPLE BY` — the sampling key. Must be present in the primary key. */
+    sampleBy?: ClickHouseKey;
+    /** `TTL` — when rows expire, e.g. ``sql`createdAt + INTERVAL 90 DAY` ``. */
+    ttl?: SQL;
+    /** `SETTINGS` — engine settings such as `index_granularity`. */
+    settings?: ClickHouseSettings;
+    /**
+     * Wraps the engine in its `Replicated…` variant.
+     *
+     * On ClickHouse Cloud and on servers with a configured default replication path both fields can be
+     * omitted, and `ENGINE = ReplicatedMergeTree` is emitted without arguments.
+     */
+    replicated?: {
+        /** The ZooKeeper/Keeper path, e.g. `'/clickhouse/tables/{shard}/events'`. */
+        zooPath?: string;
+        /** The replica name, e.g. `'{replica}'`. */
+        replicaName?: string;
+    };
+}
+/**
+ * A ClickHouse table engine together with the table-level clauses that follow it.
+ *
+ * Instances are produced by {@link mergeTree} and friends and are declared in a table's extra-config
+ * array:
+ *
+ * ```ts
+ * export const events = clickhouseTable('events', {
+ * 	id: uint64().notNull(),
+ * 	ts: dateTime({ timezone: 'UTC' }).notNull(),
+ * }, (t) => [
+ * 	mergeTree({ orderBy: [t.ts, t.id], partitionBy: sql`toYYYYMM(${t.ts})` }),
+ * ]);
+ * ```
+ */
+export declare class ClickHouseTableEngine {
+    /** The engine name as it appears after `ENGINE = `, e.g. `ReplacingMergeTree`. */
+    readonly name: string;
+    /** Positional engine arguments, e.g. the version column of `ReplacingMergeTree`. */
+    readonly args: ClickHouseExpression[];
+    static readonly [entityKind]: string;
+    readonly orderBy: ClickHouseExpression[] | undefined;
+    readonly partitionBy: ClickHouseExpression[] | undefined;
+    readonly primaryKey: ClickHouseExpression[] | undefined;
+    readonly sampleBy: ClickHouseExpression[] | undefined;
+    readonly ttl: SQL | undefined;
+    readonly settings: ClickHouseSettings | undefined;
+    constructor(
+    /** The engine name as it appears after `ENGINE = `, e.g. `ReplacingMergeTree`. */
+    name: string, 
+    /** Positional engine arguments, e.g. the version column of `ReplacingMergeTree`. */
+    args?: ClickHouseExpression[], options?: MergeTreeOptions);
+    /** Whether this engine belongs to the MergeTree family and therefore takes a sorting key. */
+    get isMergeTree(): boolean;
+    /** Renders `ENGINE = <name>(<args>)`. */
+    getEngineSQL(): SQL;
+    /**
+     * Renders the clauses that follow the engine, in the order ClickHouse expects them.
+     *
+     * Returns `undefined` when the engine takes no clauses at all — `Memory`, the Log family and the
+     * integration engines.
+     */
+    getClausesSQL(): SQL | undefined;
+}
+/** Renders a key as a bare expression when it has one element, or a tuple when it has several. */
+export declare function keySQL(expressions: ClickHouseExpression[]): SQL;
+/** Renders `name = value` pairs for a `SETTINGS` clause. */
+export declare function settingsSQL(settings: ClickHouseSettings): SQL;
+/**
+ * `MergeTree` — the default engine, and the right choice for almost every analytical table.
+ */
+export declare function mergeTree(options?: MergeTreeOptions): ClickHouseTableEngine;
+export interface ReplacingMergeTreeOptions extends MergeTreeOptions {
+    /**
+     * The column that decides which of two rows with the same sorting key wins — the largest value is
+     * kept. Defaults to "the last row inserted".
+     */
+    version?: ClickHouseExpression;
+    /** A `UInt8` column marking a row as deleted; requires `version`. */
+    isDeleted?: ClickHouseExpression;
+}
+/**
+ * `ReplacingMergeTree` — deduplicates rows sharing a sorting key during background merges.
+ *
+ * Deduplication is eventual, so reads either need `FINAL` or have to tolerate duplicates.
+ */
+export declare function replacingMergeTree(options?: ReplacingMergeTreeOptions): ClickHouseTableEngine;
+export interface SummingMergeTreeOptions extends MergeTreeOptions {
+    /** The numeric columns to sum. Defaults to every numeric column outside the sorting key. */
+    columns?: ClickHouseExpression[];
+}
+/** `SummingMergeTree` — collapses rows sharing a sorting key by summing their numeric columns. */
+export declare function summingMergeTree(options?: SummingMergeTreeOptions): ClickHouseTableEngine;
+/** `AggregatingMergeTree` — collapses rows sharing a sorting key using `AggregateFunction` states. */
+export declare function aggregatingMergeTree(options?: MergeTreeOptions): ClickHouseTableEngine;
+export interface CollapsingMergeTreeOptions extends MergeTreeOptions {
+    /** The `Int8` sign column: `1` marks a state row, `-1` marks its cancellation. */
+    sign: ClickHouseExpression;
+}
+/** `CollapsingMergeTree` — cancels out pairs of rows with opposite `sign` values during merges. */
+export declare function collapsingMergeTree(options: CollapsingMergeTreeOptions): ClickHouseTableEngine;
+export interface VersionedCollapsingMergeTreeOptions extends CollapsingMergeTreeOptions {
+    /** The version column, so that rows collapse correctly even when inserted out of order. */
+    version: ClickHouseExpression;
+}
+/** `VersionedCollapsingMergeTree` — `CollapsingMergeTree` that tolerates out-of-order inserts. */
+export declare function versionedCollapsingMergeTree(options: VersionedCollapsingMergeTreeOptions): ClickHouseTableEngine;
+/** `Memory` — rows live in RAM only and are lost on restart. Useful for tests and staging data. */
+export declare function memory(): ClickHouseTableEngine;
+/** `Log` — a simple append-only engine for small tables written once and read whole. */
+export declare function log(): ClickHouseTableEngine;
+/** `TinyLog` — the simplest engine; one file per column, no concurrent reads. */
+export declare function tinyLog(): ClickHouseTableEngine;
+/** `StripeLog` — a log engine that stores all columns in a single file. */
+export declare function stripeLog(): ClickHouseTableEngine;
+/** `Null` — discards everything written to it. Useful as the source of a materialized view. */
+export declare function nullEngine(): ClickHouseTableEngine;
+/** `Set` — an in-memory set, usable only on the right-hand side of `IN`. */
+export declare function setEngine(): ClickHouseTableEngine;
+export interface DistributedOptions {
+    /** The cluster name as defined in the server's `remote_servers` config. */
+    cluster: string;
+    /** The database holding the local shard tables. */
+    database: string;
+    /** The local table on each shard. */
+    table: string;
+    /** How rows are routed to shards, e.g. ``sql`rand()` ``. */
+    shardingKey?: ClickHouseExpression;
+    /** The policy name for storing temporary files during asynchronous sends. */
+    policyName?: string;
+}
+/** `Distributed` — a view over shards of the same table on every node of a cluster. */
+export declare function distributed(options: DistributedOptions): ClickHouseTableEngine;
+/**
+ * Any other engine, spelled out by hand.
+ *
+ * ```ts
+ * customEngine('S3', [sql`'https://bucket.s3.amazonaws.com/data/*.csv'`, sql`'CSV'`])
+ * ```
+ */
+export declare function customEngine(name: string, args?: ClickHouseExpression[], options?: MergeTreeOptions): ClickHouseTableEngine;
diff --git a/clickhouse-core/engines.js b/clickhouse-core/engines.js
new file mode 100644
index 0000000..2d13e8c
--- /dev/null
+++ b/clickhouse-core/engines.js
@@ -0,0 +1,181 @@
+import { entityKind } from "../entity.js";
+import { sql } from "../sql/sql.js";
+function toExpressionList(key) {
+  if (key === void 0) return void 0;
+  return Array.isArray(key) ? key : [key];
+}
+class ClickHouseTableEngine {
+  constructor(name, args = [], options = {}) {
+    this.name = name;
+    this.args = args;
+    this.orderBy = toExpressionList(options.orderBy);
+    this.partitionBy = toExpressionList(options.partitionBy);
+    this.primaryKey = toExpressionList(options.primaryKey);
+    this.sampleBy = toExpressionList(options.sampleBy);
+    this.ttl = options.ttl;
+    this.settings = options.settings;
+  }
+  name;
+  args;
+  static [entityKind] = "ClickHouseTableEngine";
+  orderBy;
+  partitionBy;
+  primaryKey;
+  sampleBy;
+  ttl;
+  settings;
+  /** Whether this engine belongs to the MergeTree family and therefore takes a sorting key. */
+  get isMergeTree() {
+    return this.name.endsWith("MergeTree");
+  }
+  /** Renders `ENGINE = <name>(<args>)`. */
+  getEngineSQL() {
+    const engine = sql.raw(this.name);
+    return this.args.length > 0 ? sql`ENGINE = ${engine}(${sql.join(this.args, sql`, `)})` : sql`ENGINE = ${engine}`;
+  }
+  /**
+   * Renders the clauses that follow the engine, in the order ClickHouse expects them.
+   *
+   * Returns `undefined` when the engine takes no clauses at all — `Memory`, the Log family and the
+   * integration engines.
+   */
+  getClausesSQL() {
+    const chunks = [];
+    if (this.partitionBy) {
+      chunks.push(sql`PARTITION BY ${keySQL(this.partitionBy)}`);
+    }
+    if (this.isMergeTree) {
+      chunks.push(sql`ORDER BY ${this.orderBy ? keySQL(this.orderBy) : sql`tuple()`}`);
+    } else if (this.orderBy) {
+      chunks.push(sql`ORDER BY ${keySQL(this.orderBy)}`);
+    }
+    if (this.primaryKey) {
+      chunks.push(sql`PRIMARY KEY ${keySQL(this.primaryKey)}`);
+    }
+    if (this.sampleBy) {
+      chunks.push(sql`SAMPLE BY ${keySQL(this.sampleBy)}`);
+    }
+    if (this.ttl) {
+      chunks.push(sql`TTL ${this.ttl}`);
+    }
+    if (this.settings && Object.keys(this.settings).length > 0) {
+      chunks.push(sql`SETTINGS ${settingsSQL(this.settings)}`);
+    }
+    return chunks.length > 0 ? sql.join(chunks, sql` `) : void 0;
+  }
+}
+function keySQL(expressions) {
+  return expressions.length === 1 ? sql`${expressions[0]}` : sql`(${sql.join(expressions, sql`, `)})`;
+}
+function settingsSQL(settings) {
+  return sql.join(
+    Object.entries(settings).map(
+      ([name, value]) => sql`${sql.raw(name)} = ${typeof value === "boolean" ? sql.raw(value ? "1" : "0") : value}`
+    ),
+    sql`, `
+  );
+}
+function mergeTreeFactory(baseName) {
+  return (options = {}, args = []) => {
+    const { replicated } = options;
+    if (!replicated) {
+      return new ClickHouseTableEngine(baseName, args, options);
+    }
+    const replicationArgs = [];
+    if (replicated.zooPath !== void 0) {
+      replicationArgs.push(sql`${replicated.zooPath}`);
+    }
+    if (replicated.replicaName !== void 0) {
+      replicationArgs.push(sql`${replicated.replicaName}`);
+    }
+    return new ClickHouseTableEngine(`Replicated${baseName}`, [...replicationArgs, ...args], options);
+  };
+}
+const buildMergeTree = mergeTreeFactory("MergeTree");
+const buildReplacingMergeTree = mergeTreeFactory("ReplacingMergeTree");
+const buildSummingMergeTree = mergeTreeFactory("SummingMergeTree");
+const buildAggregatingMergeTree = mergeTreeFactory("AggregatingMergeTree");
+const buildCollapsingMergeTree = mergeTreeFactory("CollapsingMergeTree");
+const buildVersionedCollapsingMergeTree = mergeTreeFactory("VersionedCollapsingMergeTree");
+function mergeTree(options = {}) {
+  return buildMergeTree(options);
+}
+function replacingMergeTree(options = {}) {
+  const args = [];
+  if (options.version !== void 0) args.push(options.version);
+  if (options.isDeleted !== void 0) {
+    if (options.version === void 0) {
+      throw new Error("ReplacingMergeTree: `isDeleted` requires `version` to be set as well");
+    }
+    args.push(options.isDeleted);
+  }
+  return buildReplacingMergeTree(options, args);
+}
+function summingMergeTree(options = {}) {
+  const args = options.columns?.length ? [keySQL(options.columns)] : [];
+  return buildSummingMergeTree(options, args);
+}
+function aggregatingMergeTree(options = {}) {
+  return buildAggregatingMergeTree(options);
+}
+function collapsingMergeTree(options) {
+  return buildCollapsingMergeTree(options, [options.sign]);
+}
+function versionedCollapsingMergeTree(options) {
+  return buildVersionedCollapsingMergeTree(options, [options.sign, options.version]);
+}
+function memory() {
+  return new ClickHouseTableEngine("Memory");
+}
+function log() {
+  return new ClickHouseTableEngine("Log");
+}
+function tinyLog() {
+  return new ClickHouseTableEngine("TinyLog");
+}
+function stripeLog() {
+  return new ClickHouseTableEngine("StripeLog");
+}
+function nullEngine() {
+  return new ClickHouseTableEngine("Null");
+}
+function setEngine() {
+  return new ClickHouseTableEngine("Set");
+}
+function distributed(options) {
+  const args = [
+    sql`${options.cluster}`,
+    sql`${options.database}`,
+    sql`${options.table}`
+  ];
+  if (options.shardingKey !== void 0) args.push(options.shardingKey);
+  if (options.policyName !== void 0) {
+    if (options.shardingKey === void 0) {
+      throw new Error("Distributed: `policyName` requires `shardingKey` to be set as well");
+    }
+    args.push(sql`${options.policyName}`);
+  }
+  return new ClickHouseTableEngine("Distributed", args);
+}
+function customEngine(name, args = [], options = {}) {
+  return new ClickHouseTableEngine(name, args, options);
+}
+export {
+  ClickHouseTableEngine,
+  aggregatingMergeTree,
+  collapsingMergeTree,
+  customEngine,
+  distributed,
+  keySQL,
+  log,
+  memory,
+  mergeTree,
+  nullEngine,
+  replacingMergeTree,
+  setEngine,
+  settingsSQL,
+  stripeLog,
+  summingMergeTree,
+  tinyLog,
+  versionedCollapsingMergeTree
+};
diff --git a/clickhouse-core/errors.cjs b/clickhouse-core/errors.cjs
new file mode 100644
index 0000000..737f1fe
--- /dev/null
+++ b/clickhouse-core/errors.cjs
@@ -0,0 +1,35 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var errors_exports = {};
+__export(errors_exports, {
+  ClickHouseInsertValueError: () => ClickHouseInsertValueError
+});
+module.exports = __toCommonJS(errors_exports);
+var import_entity = require("../entity.cjs");
+class ClickHouseInsertValueError extends Error {
+  static [import_entity.entityKind] = "ClickHouseInsertValueError";
+  constructor(message) {
+    super(message);
+    this.name = "ClickHouseInsertValueError";
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseInsertValueError
+});
diff --git a/clickhouse-core/errors.d.cts b/clickhouse-core/errors.d.cts
new file mode 100644
index 0000000..b519d81
--- /dev/null
+++ b/clickhouse-core/errors.d.cts
@@ -0,0 +1,16 @@
+import { entityKind } from '../entity.cjs';
+/**
+ * A row that cannot be sent as a body — an unknown column, or a value that is a SQL expression.
+ *
+ * Distinct from a query failure because it is one: the statement never reached the server. Rows are
+ * mapped lazily so a stream can be larger than memory, which means these surface from inside the
+ * driver's insert, and without a type to recognise them by they would be wrapped as
+ * `DrizzleQueryError` and read as "ClickHouse rejected this".
+ *
+ * Its own module rather than `query-builders/insert.ts` so the dialect can throw it without taking a
+ * runtime dependency on the builder that consumes the dialect.
+ */
+export declare class ClickHouseInsertValueError extends Error {
+    static readonly [entityKind]: string;
+    constructor(message: string);
+}
diff --git a/clickhouse-core/errors.d.ts b/clickhouse-core/errors.d.ts
new file mode 100644
index 0000000..460e7d6
--- /dev/null
+++ b/clickhouse-core/errors.d.ts
@@ -0,0 +1,16 @@
+import { entityKind } from '../entity.js';
+/**
+ * A row that cannot be sent as a body — an unknown column, or a value that is a SQL expression.
+ *
+ * Distinct from a query failure because it is one: the statement never reached the server. Rows are
+ * mapped lazily so a stream can be larger than memory, which means these surface from inside the
+ * driver's insert, and without a type to recognise them by they would be wrapped as
+ * `DrizzleQueryError` and read as "ClickHouse rejected this".
+ *
+ * Its own module rather than `query-builders/insert.ts` so the dialect can throw it without taking a
+ * runtime dependency on the builder that consumes the dialect.
+ */
+export declare class ClickHouseInsertValueError extends Error {
+    static readonly [entityKind]: string;
+    constructor(message: string);
+}
diff --git a/clickhouse-core/errors.js b/clickhouse-core/errors.js
new file mode 100644
index 0000000..de43635
--- /dev/null
+++ b/clickhouse-core/errors.js
@@ -0,0 +1,11 @@
+import { entityKind } from "../entity.js";
+class ClickHouseInsertValueError extends Error {
+  static [entityKind] = "ClickHouseInsertValueError";
+  constructor(message) {
+    super(message);
+    this.name = "ClickHouseInsertValueError";
+  }
+}
+export {
+  ClickHouseInsertValueError
+};
diff --git a/clickhouse-core/expressions.cjs b/clickhouse-core/expressions.cjs
new file mode 100644
index 0000000..584a0d4
--- /dev/null
+++ b/clickhouse-core/expressions.cjs
@@ -0,0 +1,68 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var expressions_exports = {};
+__export(expressions_exports, {
+  arrayContains: () => arrayContains,
+  arrayLength: () => arrayLength,
+  concat: () => concat,
+  globalIn: () => globalIn,
+  mapContains: () => mapContains,
+  substring: () => substring
+});
+module.exports = __toCommonJS(expressions_exports);
+var import_expressions = require("../sql/expressions/index.cjs");
+var import_sql = require("../sql/sql.cjs");
+__reExport(expressions_exports, require("../sql/expressions/index.cjs"), module.exports);
+function concat(column, value) {
+  return import_sql.sql`concat(${column}, ${(0, import_expressions.bindIfParam)(value, column)})`;
+}
+function substring(column, { from, for: _for }) {
+  const chunks = [import_sql.sql`substring(`, column];
+  if (from !== void 0) {
+    chunks.push(import_sql.sql`, `, (0, import_expressions.bindIfParam)(from, column));
+  }
+  if (_for !== void 0) {
+    chunks.push(import_sql.sql`, `, (0, import_expressions.bindIfParam)(_for, column));
+  }
+  chunks.push(import_sql.sql`)`);
+  return import_sql.sql.join(chunks);
+}
+function arrayContains(column, value) {
+  return import_sql.sql`has(${column}, ${value})`;
+}
+function arrayLength(column) {
+  return import_sql.sql`length(${column})`;
+}
+function mapContains(column, key) {
+  return import_sql.sql`mapContains(${column}, ${key})`;
+}
+function globalIn(column, values) {
+  return import_sql.sql`${column} global in ${values}`;
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  arrayContains,
+  arrayLength,
+  concat,
+  globalIn,
+  mapContains,
+  substring,
+  ...require("../sql/expressions/index.cjs")
+});
diff --git a/clickhouse-core/expressions.d.cts b/clickhouse-core/expressions.d.cts
new file mode 100644
index 0000000..cc98ec0
--- /dev/null
+++ b/clickhouse-core/expressions.d.cts
@@ -0,0 +1,22 @@
+import type { Placeholder, SQL, SQLWrapper } from '../sql/sql.cjs';
+import type { ClickHouseColumn } from './columns/index.cjs';
+export * from '../sql/expressions/index.cjs';
+/** `concat(a, b)` — ClickHouse's `||` is only defined for strings, so the function form is used. */
+export declare function concat(column: ClickHouseColumn | SQL.Aliased, value: string | Placeholder | SQLWrapper): SQL;
+export declare function substring(column: ClickHouseColumn | SQL.Aliased, { from, for: _for }: {
+    from?: number | Placeholder | SQLWrapper;
+    for?: number | Placeholder | SQLWrapper;
+}): SQL;
+/** `has(arr, value)` — whether an `Array(T)` column contains a value. */
+export declare function arrayContains(column: ClickHouseColumn | SQL.Aliased, value: unknown | SQLWrapper): SQL;
+/** `length(arr)` — the number of elements in an `Array(T)` column. */
+export declare function arrayLength(column: ClickHouseColumn | SQL.Aliased): SQL<number>;
+/** `mapContains(map, key)` — whether a `Map(K, V)` column has the given key. */
+export declare function mapContains(column: ClickHouseColumn | SQL.Aliased, key: unknown | SQLWrapper): SQL;
+/**
+ * `<column> IN <subquery or values>` restricted to the current shard.
+ *
+ * ClickHouse's `GLOBAL IN` broadcasts the right-hand side to every shard; plain `IN` evaluates it
+ * per shard. This is the explicit `GLOBAL` form for distributed queries.
+ */
+export declare function globalIn(column: ClickHouseColumn | SQL.Aliased, values: SQLWrapper): SQL;
diff --git a/clickhouse-core/expressions.d.ts b/clickhouse-core/expressions.d.ts
new file mode 100644
index 0000000..35bb242
--- /dev/null
+++ b/clickhouse-core/expressions.d.ts
@@ -0,0 +1,22 @@
+import type { Placeholder, SQL, SQLWrapper } from '../sql/sql.js';
+import type { ClickHouseColumn } from './columns/index.js';
+export * from '../sql/expressions/index.js';
+/** `concat(a, b)` — ClickHouse's `||` is only defined for strings, so the function form is used. */
+export declare function concat(column: ClickHouseColumn | SQL.Aliased, value: string | Placeholder | SQLWrapper): SQL;
+export declare function substring(column: ClickHouseColumn | SQL.Aliased, { from, for: _for }: {
+    from?: number | Placeholder | SQLWrapper;
+    for?: number | Placeholder | SQLWrapper;
+}): SQL;
+/** `has(arr, value)` — whether an `Array(T)` column contains a value. */
+export declare function arrayContains(column: ClickHouseColumn | SQL.Aliased, value: unknown | SQLWrapper): SQL;
+/** `length(arr)` — the number of elements in an `Array(T)` column. */
+export declare function arrayLength(column: ClickHouseColumn | SQL.Aliased): SQL<number>;
+/** `mapContains(map, key)` — whether a `Map(K, V)` column has the given key. */
+export declare function mapContains(column: ClickHouseColumn | SQL.Aliased, key: unknown | SQLWrapper): SQL;
+/**
+ * `<column> IN <subquery or values>` restricted to the current shard.
+ *
+ * ClickHouse's `GLOBAL IN` broadcasts the right-hand side to every shard; plain `IN` evaluates it
+ * per shard. This is the explicit `GLOBAL` form for distributed queries.
+ */
+export declare function globalIn(column: ClickHouseColumn | SQL.Aliased, values: SQLWrapper): SQL;
diff --git a/clickhouse-core/expressions.js b/clickhouse-core/expressions.js
new file mode 100644
index 0000000..431fa83
--- /dev/null
+++ b/clickhouse-core/expressions.js
@@ -0,0 +1,37 @@
+import { bindIfParam } from "../sql/expressions/index.js";
+import { sql } from "../sql/sql.js";
+export * from "../sql/expressions/index.js";
+function concat(column, value) {
+  return sql`concat(${column}, ${bindIfParam(value, column)})`;
+}
+function substring(column, { from, for: _for }) {
+  const chunks = [sql`substring(`, column];
+  if (from !== void 0) {
+    chunks.push(sql`, `, bindIfParam(from, column));
+  }
+  if (_for !== void 0) {
+    chunks.push(sql`, `, bindIfParam(_for, column));
+  }
+  chunks.push(sql`)`);
+  return sql.join(chunks);
+}
+function arrayContains(column, value) {
+  return sql`has(${column}, ${value})`;
+}
+function arrayLength(column) {
+  return sql`length(${column})`;
+}
+function mapContains(column, key) {
+  return sql`mapContains(${column}, ${key})`;
+}
+function globalIn(column, values) {
+  return sql`${column} global in ${values}`;
+}
+export {
+  arrayContains,
+  arrayLength,
+  concat,
+  globalIn,
+  mapContains,
+  substring
+};
diff --git a/clickhouse-core/index.cjs b/clickhouse-core/index.cjs
new file mode 100644
index 0000000..df38bf8
--- /dev/null
+++ b/clickhouse-core/index.cjs
@@ -0,0 +1,50 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var clickhouse_core_exports = {};
+module.exports = __toCommonJS(clickhouse_core_exports);
+__reExport(clickhouse_core_exports, require("./alias.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./columns/index.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./db.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./ddl.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./dialect.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./engines.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./errors.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./indexes.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./literals.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./query-builders/index.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./schema.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./session.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./subquery.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./table.cjs"), module.exports);
+__reExport(clickhouse_core_exports, require("./utils.cjs"), module.exports);
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ...require("./alias.cjs"),
+  ...require("./columns/index.cjs"),
+  ...require("./db.cjs"),
+  ...require("./ddl.cjs"),
+  ...require("./dialect.cjs"),
+  ...require("./engines.cjs"),
+  ...require("./errors.cjs"),
+  ...require("./indexes.cjs"),
+  ...require("./literals.cjs"),
+  ...require("./query-builders/index.cjs"),
+  ...require("./schema.cjs"),
+  ...require("./session.cjs"),
+  ...require("./subquery.cjs"),
+  ...require("./table.cjs"),
+  ...require("./utils.cjs")
+});
diff --git a/clickhouse-core/index.d.cts b/clickhouse-core/index.d.cts
new file mode 100644
index 0000000..039bc48
--- /dev/null
+++ b/clickhouse-core/index.d.cts
@@ -0,0 +1,15 @@
+export * from './alias.cjs';
+export * from './columns/index.cjs';
+export * from './db.cjs';
+export * from './ddl.cjs';
+export * from './dialect.cjs';
+export * from './engines.cjs';
+export * from './errors.cjs';
+export * from './indexes.cjs';
+export * from './literals.cjs';
+export * from './query-builders/index.cjs';
+export * from './schema.cjs';
+export * from './session.cjs';
+export * from './subquery.cjs';
+export * from './table.cjs';
+export * from './utils.cjs';
diff --git a/clickhouse-core/index.d.ts b/clickhouse-core/index.d.ts
new file mode 100644
index 0000000..13ceff8
--- /dev/null
+++ b/clickhouse-core/index.d.ts
@@ -0,0 +1,15 @@
+export * from './alias.js';
+export * from './columns/index.js';
+export * from './db.js';
+export * from './ddl.js';
+export * from './dialect.js';
+export * from './engines.js';
+export * from './errors.js';
+export * from './indexes.js';
+export * from './literals.js';
+export * from './query-builders/index.js';
+export * from './schema.js';
+export * from './session.js';
+export * from './subquery.js';
+export * from './table.js';
+export * from './utils.js';
diff --git a/clickhouse-core/index.js b/clickhouse-core/index.js
new file mode 100644
index 0000000..0d78576
--- /dev/null
+++ b/clickhouse-core/index.js
@@ -0,0 +1,15 @@
+export * from "./alias.js";
+export * from "./columns/index.js";
+export * from "./db.js";
+export * from "./ddl.js";
+export * from "./dialect.js";
+export * from "./engines.js";
+export * from "./errors.js";
+export * from "./indexes.js";
+export * from "./literals.js";
+export * from "./query-builders/index.js";
+export * from "./schema.js";
+export * from "./session.js";
+export * from "./subquery.js";
+export * from "./table.js";
+export * from "./utils.js";
diff --git a/clickhouse-core/indexes.cjs b/clickhouse-core/indexes.cjs
new file mode 100644
index 0000000..16d9c90
--- /dev/null
+++ b/clickhouse-core/indexes.cjs
@@ -0,0 +1,170 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var indexes_exports = {};
+__export(indexes_exports, {
+  Index: () => Index,
+  IndexBuilder: () => IndexBuilder,
+  IndexBuilderOn: () => IndexBuilderOn,
+  Projection: () => Projection,
+  ProjectionBuilder: () => ProjectionBuilder,
+  index: () => index,
+  projection: () => projection
+});
+module.exports = __toCommonJS(indexes_exports);
+var import_entity = require("../entity.cjs");
+var import_sql = require("../sql/sql.cjs");
+class IndexBuilderOn {
+  constructor(name) {
+    this.name = name;
+  }
+  name;
+  static [import_entity.entityKind] = "ClickHouseIndexBuilderOn";
+  /** The column or expression to index. */
+  on(...expressions) {
+    if (expressions.length === 0) {
+      throw new Error(`Index "${this.name}" must be declared on at least one column or expression`);
+    }
+    return new IndexBuilder(this.name, expressions);
+  }
+}
+class IndexBuilder {
+  static [import_entity.entityKind] = "ClickHouseIndexBuilder";
+  /** @internal */
+  config;
+  constructor(name, expressions) {
+    this.config = {
+      name,
+      expressions,
+      // `minmax` is the cheapest index and the one that helps most often, so it is the default.
+      type: "minmax",
+      typeArgs: [],
+      granularity: void 0
+    };
+  }
+  /** `TYPE minmax` — stores the extremes of the expression per granule. */
+  minmax() {
+    this.config.type = "minmax";
+    this.config.typeArgs = [];
+    return this;
+  }
+  /**
+   * `TYPE set(max_rows)` — stores up to `maxRows` distinct values per granule, or all of them when
+   * `maxRows` is `0`.
+   */
+  set(maxRows) {
+    this.config.type = "set";
+    this.config.typeArgs = [maxRows];
+    return this;
+  }
+  /** `TYPE bloom_filter([false_positive_rate])` — a Bloom filter over the expression's values. */
+  bloomFilter(falsePositiveRate) {
+    this.config.type = "bloom_filter";
+    this.config.typeArgs = falsePositiveRate === void 0 ? [] : [falsePositiveRate];
+    return this;
+  }
+  /**
+   * `TYPE ngrambf_v1(n, size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — a
+   * Bloom filter over character n-grams, which speeds up `LIKE` and substring searches.
+   */
+  ngrambf(n, sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed) {
+    this.config.type = "ngrambf_v1";
+    this.config.typeArgs = [n, sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed];
+    return this;
+  }
+  /**
+   * `TYPE tokenbf_v1(size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — like
+   * {@link ngrambf} but tokenised on non-alphanumeric characters.
+   */
+  tokenbf(sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed) {
+    this.config.type = "tokenbf_v1";
+    this.config.typeArgs = [sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed];
+    return this;
+  }
+  /** `GRANULARITY n` — how many table granules each index entry covers. Defaults to `1`. */
+  granularity(value) {
+    this.config.granularity = value;
+    return this;
+  }
+  /** @internal */
+  build(table) {
+    return new Index(this.config, table);
+  }
+}
+class Index {
+  static [import_entity.entityKind] = "ClickHouseIndex";
+  config;
+  constructor(config, table) {
+    this.config = { ...config, table };
+  }
+  /** Renders the `INDEX …` entry as it appears inside `CREATE TABLE`. */
+  getSQL() {
+    const { name, expressions, type, typeArgs, granularity } = this.config;
+    const expression = expressions.length === 1 ? import_sql.sql`${expressions[0]}` : import_sql.sql`(${import_sql.sql.join(expressions, import_sql.sql`, `)})`;
+    const typeSQL = typeArgs.length > 0 ? import_sql.sql`${import_sql.sql.raw(type)}(${import_sql.sql.raw(typeArgs.join(", "))})` : import_sql.sql`${import_sql.sql.raw(type)}`;
+    return import_sql.sql`INDEX ${import_sql.sql.identifier(name)} ${expression} TYPE ${typeSQL} GRANULARITY ${import_sql.sql.raw(String(granularity ?? 1))}`;
+  }
+}
+function index(name) {
+  return new IndexBuilderOn(name);
+}
+class ProjectionBuilder {
+  constructor(name) {
+    this.name = name;
+  }
+  name;
+  static [import_entity.entityKind] = "ClickHouseProjectionBuilder";
+  /** @internal */
+  config;
+  /** The projection body, e.g. ``sql`SELECT url, count() GROUP BY url` ``. */
+  as(query) {
+    this.config = { name: this.name, query };
+    return this;
+  }
+  /** @internal */
+  build(table) {
+    if (!this.config) {
+      throw new Error(`Projection "${this.name}" is missing its query; call .as(sql\`\u2026\`)`);
+    }
+    return new Projection(this.config, table);
+  }
+}
+class Projection {
+  static [import_entity.entityKind] = "ClickHouseProjection";
+  config;
+  constructor(config, table) {
+    this.config = { ...config, table };
+  }
+  /** Renders the `PROJECTION …` entry as it appears inside `CREATE TABLE`. */
+  getSQL() {
+    return import_sql.sql`PROJECTION ${import_sql.sql.identifier(this.config.name)} (${this.config.query})`;
+  }
+}
+function projection(name) {
+  return new ProjectionBuilder(name);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  Index,
+  IndexBuilder,
+  IndexBuilderOn,
+  Projection,
+  ProjectionBuilder,
+  index,
+  projection
+});
diff --git a/clickhouse-core/indexes.d.cts b/clickhouse-core/indexes.d.cts
new file mode 100644
index 0000000..c013a9c
--- /dev/null
+++ b/clickhouse-core/indexes.d.cts
@@ -0,0 +1,102 @@
+import { entityKind } from '../entity.cjs';
+import type { SQL } from '../sql/sql.cjs';
+import type { ClickHouseColumn } from './columns/common.cjs';
+import type { ClickHouseTable } from './table.cjs';
+/**
+ * The data-skipping index types ClickHouse supports.
+ *
+ * These are not row-lookup indexes: each one summarises a block of `GRANULARITY × index_granularity`
+ * rows so that blocks which cannot match a predicate are skipped entirely.
+ */
+export type ClickHouseIndexType = 'minmax' | 'set' | 'bloom_filter' | 'ngrambf_v1' | 'tokenbf_v1';
+export interface IndexConfig {
+    name: string;
+    /** The expression the index summarises. */
+    expressions: (ClickHouseColumn | SQL)[];
+    type: ClickHouseIndexType;
+    /** Positional arguments for the index type, e.g. the false-positive rate of `bloom_filter`. */
+    typeArgs: number[];
+    /** How many table granules each index granule covers. */
+    granularity: number | undefined;
+}
+export declare class IndexBuilderOn {
+    private name;
+    static readonly [entityKind]: string;
+    constructor(name: string);
+    /** The column or expression to index. */
+    on(...expressions: (ClickHouseColumn | SQL)[]): IndexBuilder;
+}
+export declare class IndexBuilder {
+    static readonly [entityKind]: string;
+    constructor(name: string, expressions: (ClickHouseColumn | SQL)[]);
+    /** `TYPE minmax` — stores the extremes of the expression per granule. */
+    minmax(): this;
+    /**
+     * `TYPE set(max_rows)` — stores up to `maxRows` distinct values per granule, or all of them when
+     * `maxRows` is `0`.
+     */
+    set(maxRows: number): this;
+    /** `TYPE bloom_filter([false_positive_rate])` — a Bloom filter over the expression's values. */
+    bloomFilter(falsePositiveRate?: number): this;
+    /**
+     * `TYPE ngrambf_v1(n, size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — a
+     * Bloom filter over character n-grams, which speeds up `LIKE` and substring searches.
+     */
+    ngrambf(n: number, sizeOfBloomFilterInBytes: number, numberOfHashFunctions: number, randomSeed: number): this;
+    /**
+     * `TYPE tokenbf_v1(size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — like
+     * {@link ngrambf} but tokenised on non-alphanumeric characters.
+     */
+    tokenbf(sizeOfBloomFilterInBytes: number, numberOfHashFunctions: number, randomSeed: number): this;
+    /** `GRANULARITY n` — how many table granules each index entry covers. Defaults to `1`. */
+    granularity(value: number): this;
+}
+export declare class Index {
+    static readonly [entityKind]: string;
+    readonly config: IndexConfig & {
+        table: ClickHouseTable;
+    };
+    constructor(config: IndexConfig, table: ClickHouseTable);
+    /** Renders the `INDEX …` entry as it appears inside `CREATE TABLE`. */
+    getSQL(): SQL;
+}
+export type AnyIndexBuilder = IndexBuilder;
+/**
+ * Declares a data-skipping index.
+ *
+ * ```ts
+ * index('idx_url').on(t.url).bloomFilter(0.01).granularity(4)
+ * index('idx_ts').on(t.ts).minmax()
+ * ```
+ */
+export declare function index(name: string): IndexBuilderOn;
+export interface ProjectionConfig {
+    name: string;
+    /** The projection's query, without the surrounding `SELECT` keyword being implied. */
+    query: SQL;
+}
+export declare class ProjectionBuilder {
+    private name;
+    static readonly [entityKind]: string;
+    constructor(name: string);
+    /** The projection body, e.g. ``sql`SELECT url, count() GROUP BY url` ``. */
+    as(query: SQL): this;
+}
+export declare class Projection {
+    static readonly [entityKind]: string;
+    readonly config: ProjectionConfig & {
+        table: ClickHouseTable;
+    };
+    constructor(config: ProjectionConfig, table: ClickHouseTable);
+    /** Renders the `PROJECTION …` entry as it appears inside `CREATE TABLE`. */
+    getSQL(): SQL;
+}
+/**
+ * Declares a projection — an alternate physical ordering or pre-aggregation of the table that
+ * ClickHouse maintains automatically and picks up when it makes a query cheaper.
+ *
+ * ```ts
+ * projection('by_url').as(sql`SELECT url, count() GROUP BY url`)
+ * ```
+ */
+export declare function projection(name: string): ProjectionBuilder;
diff --git a/clickhouse-core/indexes.d.ts b/clickhouse-core/indexes.d.ts
new file mode 100644
index 0000000..80a274a
--- /dev/null
+++ b/clickhouse-core/indexes.d.ts
@@ -0,0 +1,102 @@
+import { entityKind } from '../entity.js';
+import type { SQL } from '../sql/sql.js';
+import type { ClickHouseColumn } from './columns/common.js';
+import type { ClickHouseTable } from './table.js';
+/**
+ * The data-skipping index types ClickHouse supports.
+ *
+ * These are not row-lookup indexes: each one summarises a block of `GRANULARITY × index_granularity`
+ * rows so that blocks which cannot match a predicate are skipped entirely.
+ */
+export type ClickHouseIndexType = 'minmax' | 'set' | 'bloom_filter' | 'ngrambf_v1' | 'tokenbf_v1';
+export interface IndexConfig {
+    name: string;
+    /** The expression the index summarises. */
+    expressions: (ClickHouseColumn | SQL)[];
+    type: ClickHouseIndexType;
+    /** Positional arguments for the index type, e.g. the false-positive rate of `bloom_filter`. */
+    typeArgs: number[];
+    /** How many table granules each index granule covers. */
+    granularity: number | undefined;
+}
+export declare class IndexBuilderOn {
+    private name;
+    static readonly [entityKind]: string;
+    constructor(name: string);
+    /** The column or expression to index. */
+    on(...expressions: (ClickHouseColumn | SQL)[]): IndexBuilder;
+}
+export declare class IndexBuilder {
+    static readonly [entityKind]: string;
+    constructor(name: string, expressions: (ClickHouseColumn | SQL)[]);
+    /** `TYPE minmax` — stores the extremes of the expression per granule. */
+    minmax(): this;
+    /**
+     * `TYPE set(max_rows)` — stores up to `maxRows` distinct values per granule, or all of them when
+     * `maxRows` is `0`.
+     */
+    set(maxRows: number): this;
+    /** `TYPE bloom_filter([false_positive_rate])` — a Bloom filter over the expression's values. */
+    bloomFilter(falsePositiveRate?: number): this;
+    /**
+     * `TYPE ngrambf_v1(n, size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — a
+     * Bloom filter over character n-grams, which speeds up `LIKE` and substring searches.
+     */
+    ngrambf(n: number, sizeOfBloomFilterInBytes: number, numberOfHashFunctions: number, randomSeed: number): this;
+    /**
+     * `TYPE tokenbf_v1(size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — like
+     * {@link ngrambf} but tokenised on non-alphanumeric characters.
+     */
+    tokenbf(sizeOfBloomFilterInBytes: number, numberOfHashFunctions: number, randomSeed: number): this;
+    /** `GRANULARITY n` — how many table granules each index entry covers. Defaults to `1`. */
+    granularity(value: number): this;
+}
+export declare class Index {
+    static readonly [entityKind]: string;
+    readonly config: IndexConfig & {
+        table: ClickHouseTable;
+    };
+    constructor(config: IndexConfig, table: ClickHouseTable);
+    /** Renders the `INDEX …` entry as it appears inside `CREATE TABLE`. */
+    getSQL(): SQL;
+}
+export type AnyIndexBuilder = IndexBuilder;
+/**
+ * Declares a data-skipping index.
+ *
+ * ```ts
+ * index('idx_url').on(t.url).bloomFilter(0.01).granularity(4)
+ * index('idx_ts').on(t.ts).minmax()
+ * ```
+ */
+export declare function index(name: string): IndexBuilderOn;
+export interface ProjectionConfig {
+    name: string;
+    /** The projection's query, without the surrounding `SELECT` keyword being implied. */
+    query: SQL;
+}
+export declare class ProjectionBuilder {
+    private name;
+    static readonly [entityKind]: string;
+    constructor(name: string);
+    /** The projection body, e.g. ``sql`SELECT url, count() GROUP BY url` ``. */
+    as(query: SQL): this;
+}
+export declare class Projection {
+    static readonly [entityKind]: string;
+    readonly config: ProjectionConfig & {
+        table: ClickHouseTable;
+    };
+    constructor(config: ProjectionConfig, table: ClickHouseTable);
+    /** Renders the `PROJECTION …` entry as it appears inside `CREATE TABLE`. */
+    getSQL(): SQL;
+}
+/**
+ * Declares a projection — an alternate physical ordering or pre-aggregation of the table that
+ * ClickHouse maintains automatically and picks up when it makes a query cheaper.
+ *
+ * ```ts
+ * projection('by_url').as(sql`SELECT url, count() GROUP BY url`)
+ * ```
+ */
+export declare function projection(name: string): ProjectionBuilder;
diff --git a/clickhouse-core/indexes.js b/clickhouse-core/indexes.js
new file mode 100644
index 0000000..565bbcb
--- /dev/null
+++ b/clickhouse-core/indexes.js
@@ -0,0 +1,140 @@
+import { entityKind } from "../entity.js";
+import { sql } from "../sql/sql.js";
+class IndexBuilderOn {
+  constructor(name) {
+    this.name = name;
+  }
+  name;
+  static [entityKind] = "ClickHouseIndexBuilderOn";
+  /** The column or expression to index. */
+  on(...expressions) {
+    if (expressions.length === 0) {
+      throw new Error(`Index "${this.name}" must be declared on at least one column or expression`);
+    }
+    return new IndexBuilder(this.name, expressions);
+  }
+}
+class IndexBuilder {
+  static [entityKind] = "ClickHouseIndexBuilder";
+  /** @internal */
+  config;
+  constructor(name, expressions) {
+    this.config = {
+      name,
+      expressions,
+      // `minmax` is the cheapest index and the one that helps most often, so it is the default.
+      type: "minmax",
+      typeArgs: [],
+      granularity: void 0
+    };
+  }
+  /** `TYPE minmax` — stores the extremes of the expression per granule. */
+  minmax() {
+    this.config.type = "minmax";
+    this.config.typeArgs = [];
+    return this;
+  }
+  /**
+   * `TYPE set(max_rows)` — stores up to `maxRows` distinct values per granule, or all of them when
+   * `maxRows` is `0`.
+   */
+  set(maxRows) {
+    this.config.type = "set";
+    this.config.typeArgs = [maxRows];
+    return this;
+  }
+  /** `TYPE bloom_filter([false_positive_rate])` — a Bloom filter over the expression's values. */
+  bloomFilter(falsePositiveRate) {
+    this.config.type = "bloom_filter";
+    this.config.typeArgs = falsePositiveRate === void 0 ? [] : [falsePositiveRate];
+    return this;
+  }
+  /**
+   * `TYPE ngrambf_v1(n, size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — a
+   * Bloom filter over character n-grams, which speeds up `LIKE` and substring searches.
+   */
+  ngrambf(n, sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed) {
+    this.config.type = "ngrambf_v1";
+    this.config.typeArgs = [n, sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed];
+    return this;
+  }
+  /**
+   * `TYPE tokenbf_v1(size_of_bloom_filter_in_bytes, number_of_hash_functions, random_seed)` — like
+   * {@link ngrambf} but tokenised on non-alphanumeric characters.
+   */
+  tokenbf(sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed) {
+    this.config.type = "tokenbf_v1";
+    this.config.typeArgs = [sizeOfBloomFilterInBytes, numberOfHashFunctions, randomSeed];
+    return this;
+  }
+  /** `GRANULARITY n` — how many table granules each index entry covers. Defaults to `1`. */
+  granularity(value) {
+    this.config.granularity = value;
+    return this;
+  }
+  /** @internal */
+  build(table) {
+    return new Index(this.config, table);
+  }
+}
+class Index {
+  static [entityKind] = "ClickHouseIndex";
+  config;
+  constructor(config, table) {
+    this.config = { ...config, table };
+  }
+  /** Renders the `INDEX …` entry as it appears inside `CREATE TABLE`. */
+  getSQL() {
+    const { name, expressions, type, typeArgs, granularity } = this.config;
+    const expression = expressions.length === 1 ? sql`${expressions[0]}` : sql`(${sql.join(expressions, sql`, `)})`;
+    const typeSQL = typeArgs.length > 0 ? sql`${sql.raw(type)}(${sql.raw(typeArgs.join(", "))})` : sql`${sql.raw(type)}`;
+    return sql`INDEX ${sql.identifier(name)} ${expression} TYPE ${typeSQL} GRANULARITY ${sql.raw(String(granularity ?? 1))}`;
+  }
+}
+function index(name) {
+  return new IndexBuilderOn(name);
+}
+class ProjectionBuilder {
+  constructor(name) {
+    this.name = name;
+  }
+  name;
+  static [entityKind] = "ClickHouseProjectionBuilder";
+  /** @internal */
+  config;
+  /** The projection body, e.g. ``sql`SELECT url, count() GROUP BY url` ``. */
+  as(query) {
+    this.config = { name: this.name, query };
+    return this;
+  }
+  /** @internal */
+  build(table) {
+    if (!this.config) {
+      throw new Error(`Projection "${this.name}" is missing its query; call .as(sql\`\u2026\`)`);
+    }
+    return new Projection(this.config, table);
+  }
+}
+class Projection {
+  static [entityKind] = "ClickHouseProjection";
+  config;
+  constructor(config, table) {
+    this.config = { ...config, table };
+  }
+  /** Renders the `PROJECTION …` entry as it appears inside `CREATE TABLE`. */
+  getSQL() {
+    return sql`PROJECTION ${sql.identifier(this.config.name)} (${this.config.query})`;
+  }
+}
+function projection(name) {
+  return new ProjectionBuilder(name);
+}
+export {
+  Index,
+  IndexBuilder,
+  IndexBuilderOn,
+  Projection,
+  ProjectionBuilder,
+  index,
+  projection
+};
diff --git a/clickhouse-core/literals.cjs b/clickhouse-core/literals.cjs
new file mode 100644
index 0000000..ca1f94e
--- /dev/null
+++ b/clickhouse-core/literals.cjs
@@ -0,0 +1,66 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var literals_exports = {};
+__export(literals_exports, {
+  castFromString: () => castFromString,
+  escapeClickHouseIdentifier: () => escapeClickHouseIdentifier,
+  escapeClickHouseString: () => escapeClickHouseString,
+  numberToText: () => numberToText,
+  numericLiteral: () => numericLiteral
+});
+module.exports = __toCommonJS(literals_exports);
+var import_sql = require("../sql/sql.cjs");
+const NUMERIC_LITERAL = /^[+-]?(?:\d+(?:\.\d+)?(?:[eE][+-]?\d+)?)$/;
+function escapeClickHouseString(value) {
+  return `'${value.replace(/\\/g, "\\\\").replace(/'/g, "\\'")}'`;
+}
+function escapeClickHouseIdentifier(value) {
+  return `\`${value.replace(/\\/g, "\\\\").replace(/`/g, "\\`")}\``;
+}
+function numericLiteral(value) {
+  const text = typeof value === "number" ? numberToText(value) : String(value);
+  if (!NUMERIC_LITERAL.test(text)) {
+    if (text === "nan" || text === "inf" || text === "-inf") {
+      return import_sql.sql.raw(text);
+    }
+    throw new Error(`Invalid numeric value for ClickHouse: ${String(value)}`);
+  }
+  return import_sql.sql.raw(text);
+}
+function numberToText(value) {
+  if (Number.isNaN(value)) return "nan";
+  if (value === Number.POSITIVE_INFINITY) return "inf";
+  if (value === Number.NEGATIVE_INFINITY) return "-inf";
+  return String(value);
+}
+function castFromString(fn, value, ...rest) {
+  const args = [import_sql.sql`${value}`];
+  for (const arg of rest) {
+    args.push(typeof arg === "number" ? import_sql.sql.raw(String(arg)) : import_sql.sql`${arg}`);
+  }
+  return import_sql.sql`${import_sql.sql.raw(fn)}(${import_sql.sql.join(args, import_sql.sql`, `)})`;
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  castFromString,
+  escapeClickHouseIdentifier,
+  escapeClickHouseString,
+  numberToText,
+  numericLiteral
+});
diff --git a/clickhouse-core/literals.d.cts b/clickhouse-core/literals.d.cts
new file mode 100644
index 0000000..7afcd94
--- /dev/null
+++ b/clickhouse-core/literals.d.cts
@@ -0,0 +1,17 @@
+import type { SQL } from '../sql/sql.cjs';
+/** Wraps a string in single quotes, escaping backslashes and quotes the way ClickHouse expects. */
+export declare function escapeClickHouseString(value: string): string;
+/** Wraps an identifier in backticks, escaping backslashes and backticks. */
+export declare function escapeClickHouseIdentifier(value: string): string;
+/**
+ * Emits a bare numeric literal, so that wide integers (`Int64` and up) and decimals survive without
+ * being coerced through a JavaScript `number`.
+ *
+ * Throws on anything that is not a plain number, which keeps `sql.raw` from becoming an injection
+ * vector when a caller hands us an unexpected value.
+ */
+export declare function numericLiteral(value: bigint | number | string): SQL;
+/** ClickHouse spells the non-finite floats `nan`, `inf` and `-inf`. */
+export declare function numberToText(value: number): string;
+/** Builds `fn(<string literal>, ...rest)`, e.g. `toUUID('…')` or `toDateTime64('…', 3, 'UTC')`. */
+export declare function castFromString(fn: string, value: string, ...rest: (string | number)[]): SQL;
diff --git a/clickhouse-core/literals.d.ts b/clickhouse-core/literals.d.ts
new file mode 100644
index 0000000..b92cf03
--- /dev/null
+++ b/clickhouse-core/literals.d.ts
@@ -0,0 +1,17 @@
+import type { SQL } from '../sql/sql.js';
+/** Wraps a string in single quotes, escaping backslashes and quotes the way ClickHouse expects. */
+export declare function escapeClickHouseString(value: string): string;
+/** Wraps an identifier in backticks, escaping backslashes and backticks. */
+export declare function escapeClickHouseIdentifier(value: string): string;
+/**
+ * Emits a bare numeric literal, so that wide integers (`Int64` and up) and decimals survive without
+ * being coerced through a JavaScript `number`.
+ *
+ * Throws on anything that is not a plain number, which keeps `sql.raw` from becoming an injection
+ * vector when a caller hands us an unexpected value.
+ */
+export declare function numericLiteral(value: bigint | number | string): SQL;
+/** ClickHouse spells the non-finite floats `nan`, `inf` and `-inf`. */
+export declare function numberToText(value: number): string;
+/** Builds `fn(<string literal>, ...rest)`, e.g. `toUUID('…')` or `toDateTime64('…', 3, 'UTC')`. */
+export declare function castFromString(fn: string, value: string, ...rest: (string | number)[]): SQL;
diff --git a/clickhouse-core/literals.js b/clickhouse-core/literals.js
new file mode 100644
index 0000000..8aa77da
--- /dev/null
+++ b/clickhouse-core/literals.js
@@ -0,0 +1,38 @@
+import { sql } from "../sql/sql.js";
+const NUMERIC_LITERAL = /^[+-]?(?:\d+(?:\.\d+)?(?:[eE][+-]?\d+)?)$/;
+function escapeClickHouseString(value) {
+  return `'${value.replace(/\\/g, "\\\\").replace(/'/g, "\\'")}'`;
+}
+function escapeClickHouseIdentifier(value) {
+  return `\`${value.replace(/\\/g, "\\\\").replace(/`/g, "\\`")}\``;
+}
+function numericLiteral(value) {
+  const text = typeof value === "number" ? numberToText(value) : String(value);
+  if (!NUMERIC_LITERAL.test(text)) {
+    if (text === "nan" || text === "inf" || text === "-inf") {
+      return sql.raw(text);
+    }
+    throw new Error(`Invalid numeric value for ClickHouse: ${String(value)}`);
+  }
+  return sql.raw(text);
+}
+function numberToText(value) {
+  if (Number.isNaN(value)) return "nan";
+  if (value === Number.POSITIVE_INFINITY) return "inf";
+  if (value === Number.NEGATIVE_INFINITY) return "-inf";
+  return String(value);
+}
+function castFromString(fn, value, ...rest) {
+  const args = [sql`${value}`];
+  for (const arg of rest) {
+    args.push(typeof arg === "number" ? sql.raw(String(arg)) : sql`${arg}`);
+  }
+  return sql`${sql.raw(fn)}(${sql.join(args, sql`, `)})`;
+}
+export {
+  castFromString,
+  escapeClickHouseIdentifier,
+  escapeClickHouseString,
+  numberToText,
+  numericLiteral
+};
diff --git a/clickhouse-core/query-builders/count.cjs b/clickhouse-core/query-builders/count.cjs
new file mode 100644
index 0000000..822e49b
--- /dev/null
+++ b/clickhouse-core/query-builders/count.cjs
@@ -0,0 +1,73 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var count_exports = {};
+__export(count_exports, {
+  ClickHouseCountBuilder: () => ClickHouseCountBuilder
+});
+module.exports = __toCommonJS(count_exports);
+var import_entity = require("../../entity.cjs");
+var import_sql = require("../../sql/sql.cjs");
+class ClickHouseCountBuilder extends import_sql.SQL {
+  constructor(params) {
+    super(ClickHouseCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
+    this.params = params;
+    this.mapWith(Number);
+    this.session = params.session;
+    this.sql = ClickHouseCountBuilder.buildCount(
+      params.source,
+      params.filters
+    );
+  }
+  params;
+  sql;
+  static [import_entity.entityKind] = "ClickHouseCountBuilder";
+  [Symbol.toStringTag] = "ClickHouseCountBuilder";
+  session;
+  static buildEmbeddedCount(source, filters) {
+    return import_sql.sql`(select count(*) from ${source}${import_sql.sql.raw(" where ").if(filters)}${filters})`;
+  }
+  static buildCount(source, filters) {
+    return import_sql.sql`select count(*) as count from ${source}${import_sql.sql.raw(" where ").if(filters)}${filters}`;
+  }
+  then(onfulfilled, onrejected) {
+    return Promise.resolve(this.session.count(this.sql)).then(
+      onfulfilled,
+      onrejected
+    );
+  }
+  catch(onRejected) {
+    return this.then(void 0, onRejected);
+  }
+  finally(onFinally) {
+    return this.then(
+      (value) => {
+        onFinally?.();
+        return value;
+      },
+      (reason) => {
+        onFinally?.();
+        throw reason;
+      }
+    );
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseCountBuilder
+});
diff --git a/clickhouse-core/query-builders/count.d.cts b/clickhouse-core/query-builders/count.d.cts
new file mode 100644
index 0000000..8223f7f
--- /dev/null
+++ b/clickhouse-core/query-builders/count.d.cts
@@ -0,0 +1,25 @@
+import { entityKind } from '../../entity.cjs';
+import { SQL, type SQLWrapper } from '../../sql/sql.cjs';
+import type { ClickHouseSession } from '../session.cjs';
+import type { ClickHouseTable } from '../table.cjs';
+export declare class ClickHouseCountBuilder<TSession extends ClickHouseSession<any, any>> extends SQL<number> implements Promise<number>, SQLWrapper {
+    readonly params: {
+        source: ClickHouseTable | /* ClickHouseViewBase | */ SQL | SQLWrapper;
+        filters?: SQL<unknown>;
+        session: TSession;
+    };
+    private sql;
+    static readonly [entityKind] = "ClickHouseCountBuilder";
+    [Symbol.toStringTag]: string;
+    private session;
+    private static buildEmbeddedCount;
+    private static buildCount;
+    constructor(params: {
+        source: ClickHouseTable | /* ClickHouseViewBase | */ SQL | SQLWrapper;
+        filters?: SQL<unknown>;
+        session: TSession;
+    });
+    then<TResult1 = number, TResult2 = never>(onfulfilled?: ((value: number) => TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: ((reason: any) => TResult2 | PromiseLike<TResult2>) | null | undefined): Promise<TResult1 | TResult2>;
+    catch(onRejected?: ((reason: any) => never | PromiseLike<never>) | null | undefined): Promise<number>;
+    finally(onFinally?: (() => void) | null | undefined): Promise<number>;
+}
diff --git a/clickhouse-core/query-builders/count.d.ts b/clickhouse-core/query-builders/count.d.ts
new file mode 100644
index 0000000..d422eb4
--- /dev/null
+++ b/clickhouse-core/query-builders/count.d.ts
@@ -0,0 +1,25 @@
+import { entityKind } from '../../entity.js';
+import { SQL, type SQLWrapper } from '../../sql/sql.js';
+import type { ClickHouseSession } from '../session.js';
+import type { ClickHouseTable } from '../table.js';
+export declare class ClickHouseCountBuilder<TSession extends ClickHouseSession<any, any>> extends SQL<number> implements Promise<number>, SQLWrapper {
+    readonly params: {
+        source: ClickHouseTable | /* ClickHouseViewBase | */ SQL | SQLWrapper;
+        filters?: SQL<unknown>;
+        session: TSession;
+    };
+    private sql;
+    static readonly [entityKind] = "ClickHouseCountBuilder";
+    [Symbol.toStringTag]: string;
+    private session;
+    private static buildEmbeddedCount;
+    private static buildCount;
+    constructor(params: {
+        source: ClickHouseTable | /* ClickHouseViewBase | */ SQL | SQLWrapper;
+        filters?: SQL<unknown>;
+        session: TSession;
+    });
+    then<TResult1 = number, TResult2 = never>(onfulfilled?: ((value: number) => TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: ((reason: any) => TResult2 | PromiseLike<TResult2>) | null | undefined): Promise<TResult1 | TResult2>;
+    catch(onRejected?: ((reason: any) => never | PromiseLike<never>) | null | undefined): Promise<number>;
+    finally(onFinally?: (() => void) | null | undefined): Promise<number>;
+}
diff --git a/clickhouse-core/query-builders/count.js b/clickhouse-core/query-builders/count.js
new file mode 100644
index 0000000..d671a9d
--- /dev/null
+++ b/clickhouse-core/query-builders/count.js
@@ -0,0 +1,49 @@
+import { entityKind } from "../../entity.js";
+import { SQL, sql } from "../../sql/sql.js";
+class ClickHouseCountBuilder extends SQL {
+  constructor(params) {
+    super(ClickHouseCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
+    this.params = params;
+    this.mapWith(Number);
+    this.session = params.session;
+    this.sql = ClickHouseCountBuilder.buildCount(
+      params.source,
+      params.filters
+    );
+  }
+  params;
+  sql;
+  static [entityKind] = "ClickHouseCountBuilder";
+  [Symbol.toStringTag] = "ClickHouseCountBuilder";
+  session;
+  static buildEmbeddedCount(source, filters) {
+    return sql`(select count(*) from ${source}${sql.raw(" where ").if(filters)}${filters})`;
+  }
+  static buildCount(source, filters) {
+    return sql`select count(*) as count from ${source}${sql.raw(" where ").if(filters)}${filters}`;
+  }
+  then(onfulfilled, onrejected) {
+    return Promise.resolve(this.session.count(this.sql)).then(
+      onfulfilled,
+      onrejected
+    );
+  }
+  catch(onRejected) {
+    return this.then(void 0, onRejected);
+  }
+  finally(onFinally) {
+    return this.then(
+      (value) => {
+        onFinally?.();
+        return value;
+      },
+      (reason) => {
+        onFinally?.();
+        throw reason;
+      }
+    );
+  }
+}
+export {
+  ClickHouseCountBuilder
+};
diff --git a/clickhouse-core/query-builders/delete.cjs b/clickhouse-core/query-builders/delete.cjs
new file mode 100644
index 0000000..3c9756d
--- /dev/null
+++ b/clickhouse-core/query-builders/delete.cjs
@@ -0,0 +1,127 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var delete_exports = {};
+__export(delete_exports, {
+  ClickHouseDeleteBase: () => ClickHouseDeleteBase
+});
+module.exports = __toCommonJS(delete_exports);
+var import_entity = require("../../entity.cjs");
+var import_query_promise = require("../../query-promise.cjs");
+var import_utils = require("../utils.cjs");
+class ClickHouseDeleteBase extends import_query_promise.QueryPromise {
+  constructor(table, session, dialect) {
+    super();
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { table };
+  }
+  table;
+  session;
+  dialect;
+  static [import_entity.entityKind] = "ClickHouseDelete";
+  config;
+  /**
+   * Adds a `where` clause to the query.
+   *
+   * Calling this method will delete only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/delete}
+   *
+   * @param where the `where` clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be deleted.
+   *
+   * ```ts
+   * // Delete all cars with green color
+   * db.delete(cars).where(eq(cars.color, 'green'));
+   * // or
+   * db.delete(cars).where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Delete all BMW cars with a green color
+   * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Delete all cars with the green or blue color
+   * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    this.config.where = where;
+    return this;
+  }
+  /**
+   * Issues the delete as an `ALTER TABLE … DELETE` mutation rather than a lightweight `DELETE FROM`.
+   *
+   * Lightweight deletes (the default) mark rows deleted immediately and are cheap; mutations rewrite
+   * every affected part and run asynchronously in the background, but are supported on older servers
+   * and on table engines where lightweight deletes are not.
+   *
+   * Either way the delete is *not* transactional and not immediately reflected on disk.
+   */
+  mutation() {
+    this.config.mutation = true;
+    return this;
+  }
+  /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for a mutation. */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildDeleteQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "delete",
+        tables: (0, import_utils.extractUsedTable)(this.config.table)
+      }
+    );
+  }
+  execute = (placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseDeleteBase
+});
diff --git a/clickhouse-core/query-builders/delete.d.cts b/clickhouse-core/query-builders/delete.d.cts
new file mode 100644
index 0000000..46af21f
--- /dev/null
+++ b/clickhouse-core/query-builders/delete.d.cts
@@ -0,0 +1,91 @@
+import type { ClickHouseDialect } from '../dialect.cjs';
+import type { ClickHouseSettings } from '../engines.cjs';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.cjs';
+import type { ClickHouseTable } from '../table.cjs';
+import { entityKind } from '../../entity.cjs';
+import { QueryPromise } from '../../query-promise.cjs';
+import type { Query, SQL, SQLWrapper } from '../../sql/sql.cjs';
+export type ClickHouseDeleteWithout<T extends AnyClickHouseDeleteBase, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseDeleteBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseDelete<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseDeleteBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export interface ClickHouseDeleteConfig {
+    where?: SQL | undefined;
+    table: ClickHouseTable;
+    /**
+     * When set, the delete is issued as an `ALTER TABLE … DELETE` mutation instead of a lightweight
+     * `DELETE FROM`. See {@link ClickHouseDeleteBase.mutation}.
+     */
+    mutation?: boolean;
+    settings?: ClickHouseSettings;
+}
+export type ClickHouseDeletePrepare<T extends AnyClickHouseDeleteBase> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+type ClickHouseDeleteDynamic<T extends AnyClickHouseDeleteBase> = ClickHouseDelete<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+type AnyClickHouseDeleteBase = ClickHouseDeleteBase<any, any, any, any, any>;
+export interface ClickHouseDeleteBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> {
+    readonly _: {
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+    };
+}
+export declare class ClickHouseDeleteBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements SQLWrapper {
+    private table;
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    private config;
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a `where` clause to the query.
+     *
+     * Calling this method will delete only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/delete}
+     *
+     * @param where the `where` clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be deleted.
+     *
+     * ```ts
+     * // Delete all cars with green color
+     * db.delete(cars).where(eq(cars.color, 'green'));
+     * // or
+     * db.delete(cars).where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Delete all BMW cars with a green color
+     * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Delete all cars with the green or blue color
+     * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: SQL | undefined): ClickHouseDeleteWithout<this, TDynamic, 'where'>;
+    /**
+     * Issues the delete as an `ALTER TABLE … DELETE` mutation rather than a lightweight `DELETE FROM`.
+     *
+     * Lightweight deletes (the default) mark rows deleted immediately and are cheap; mutations rewrite
+     * every affected part and run asynchronously in the background, but are supported on older servers
+     * and on table engines where lightweight deletes are not.
+     *
+     * Either way the delete is *not* transactional and not immediately reflected on disk.
+     */
+    mutation(): ClickHouseDeleteWithout<this, TDynamic, 'mutation'>;
+    /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for a mutation. */
+    settings(settings: ClickHouseSettings): ClickHouseDeleteWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    prepare(): ClickHouseDeletePrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseDeleteDynamic<this>;
+}
+export {};
diff --git a/clickhouse-core/query-builders/delete.d.ts b/clickhouse-core/query-builders/delete.d.ts
new file mode 100644
index 0000000..1bf846c
--- /dev/null
+++ b/clickhouse-core/query-builders/delete.d.ts
@@ -0,0 +1,91 @@
+import type { ClickHouseDialect } from '../dialect.js';
+import type { ClickHouseSettings } from '../engines.js';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.js';
+import type { ClickHouseTable } from '../table.js';
+import { entityKind } from '../../entity.js';
+import { QueryPromise } from '../../query-promise.js';
+import type { Query, SQL, SQLWrapper } from '../../sql/sql.js';
+export type ClickHouseDeleteWithout<T extends AnyClickHouseDeleteBase, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseDeleteBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseDelete<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseDeleteBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export interface ClickHouseDeleteConfig {
+    where?: SQL | undefined;
+    table: ClickHouseTable;
+    /**
+     * When set, the delete is issued as an `ALTER TABLE … DELETE` mutation instead of a lightweight
+     * `DELETE FROM`. See {@link ClickHouseDeleteBase.mutation}.
+     */
+    mutation?: boolean;
+    settings?: ClickHouseSettings;
+}
+export type ClickHouseDeletePrepare<T extends AnyClickHouseDeleteBase> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+type ClickHouseDeleteDynamic<T extends AnyClickHouseDeleteBase> = ClickHouseDelete<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+type AnyClickHouseDeleteBase = ClickHouseDeleteBase<any, any, any, any, any>;
+export interface ClickHouseDeleteBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> {
+    readonly _: {
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+    };
+}
+export declare class ClickHouseDeleteBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements SQLWrapper {
+    private table;
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    private config;
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a `where` clause to the query.
+     *
+     * Calling this method will delete only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/delete}
+     *
+     * @param where the `where` clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be deleted.
+     *
+     * ```ts
+     * // Delete all cars with green color
+     * db.delete(cars).where(eq(cars.color, 'green'));
+     * // or
+     * db.delete(cars).where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Delete all BMW cars with a green color
+     * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Delete all cars with the green or blue color
+     * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: SQL | undefined): ClickHouseDeleteWithout<this, TDynamic, 'where'>;
+    /**
+     * Issues the delete as an `ALTER TABLE … DELETE` mutation rather than a lightweight `DELETE FROM`.
+     *
+     * Lightweight deletes (the default) mark rows deleted immediately and are cheap; mutations rewrite
+     * every affected part and run asynchronously in the background, but are supported on older servers
+     * and on table engines where lightweight deletes are not.
+     *
+     * Either way the delete is *not* transactional and not immediately reflected on disk.
+     */
+    mutation(): ClickHouseDeleteWithout<this, TDynamic, 'mutation'>;
+    /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for a mutation. */
+    settings(settings: ClickHouseSettings): ClickHouseDeleteWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    prepare(): ClickHouseDeletePrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseDeleteDynamic<this>;
+}
+export {};
diff --git a/clickhouse-core/query-builders/delete.js b/clickhouse-core/query-builders/delete.js
new file mode 100644
index 0000000..54707f1
--- /dev/null
+++ b/clickhouse-core/query-builders/delete.js
@@ -0,0 +1,103 @@
+import { entityKind } from "../../entity.js";
+import { QueryPromise } from "../../query-promise.js";
+import { extractUsedTable } from "../utils.js";
+class ClickHouseDeleteBase extends QueryPromise {
+  constructor(table, session, dialect) {
+    super();
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { table };
+  }
+  table;
+  session;
+  dialect;
+  static [entityKind] = "ClickHouseDelete";
+  config;
+  /**
+   * Adds a `where` clause to the query.
+   *
+   * Calling this method will delete only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/delete}
+   *
+   * @param where the `where` clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be deleted.
+   *
+   * ```ts
+   * // Delete all cars with green color
+   * db.delete(cars).where(eq(cars.color, 'green'));
+   * // or
+   * db.delete(cars).where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Delete all BMW cars with a green color
+   * db.delete(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Delete all cars with the green or blue color
+   * db.delete(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    this.config.where = where;
+    return this;
+  }
+  /**
+   * Issues the delete as an `ALTER TABLE … DELETE` mutation rather than a lightweight `DELETE FROM`.
+   *
+   * Lightweight deletes (the default) mark rows deleted immediately and are cheap; mutations rewrite
+   * every affected part and run asynchronously in the background, but are supported on older servers
+   * and on table engines where lightweight deletes are not.
+   *
+   * Either way the delete is *not* transactional and not immediately reflected on disk.
+   */
+  mutation() {
+    this.config.mutation = true;
+    return this;
+  }
+  /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for a mutation. */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildDeleteQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "delete",
+        tables: extractUsedTable(this.config.table)
+      }
+    );
+  }
+  execute = (placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+export {
+  ClickHouseDeleteBase
+};
diff --git a/clickhouse-core/query-builders/index.cjs b/clickhouse-core/query-builders/index.cjs
new file mode 100644
index 0000000..ac3ad32
--- /dev/null
+++ b/clickhouse-core/query-builders/index.cjs
@@ -0,0 +1,34 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __reExport = (target, mod, secondTarget) => (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var query_builders_exports = {};
+module.exports = __toCommonJS(query_builders_exports);
+__reExport(query_builders_exports, require("./count.cjs"), module.exports);
+__reExport(query_builders_exports, require("./delete.cjs"), module.exports);
+__reExport(query_builders_exports, require("./insert.cjs"), module.exports);
+__reExport(query_builders_exports, require("./query-builder.cjs"), module.exports);
+__reExport(query_builders_exports, require("./select.cjs"), module.exports);
+__reExport(query_builders_exports, require("./select.types.cjs"), module.exports);
+__reExport(query_builders_exports, require("./update.cjs"), module.exports);
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ...require("./count.cjs"),
+  ...require("./delete.cjs"),
+  ...require("./insert.cjs"),
+  ...require("./query-builder.cjs"),
+  ...require("./select.cjs"),
+  ...require("./select.types.cjs"),
+  ...require("./update.cjs")
+});
diff --git a/clickhouse-core/query-builders/index.d.cts b/clickhouse-core/query-builders/index.d.cts
new file mode 100644
index 0000000..23fcdf7
--- /dev/null
+++ b/clickhouse-core/query-builders/index.d.cts
@@ -0,0 +1,7 @@
+export * from './count.cjs';
+export * from './delete.cjs';
+export * from './insert.cjs';
+export * from './query-builder.cjs';
+export * from './select.cjs';
+export * from './select.types.cjs';
+export * from './update.cjs';
diff --git a/clickhouse-core/query-builders/index.d.ts b/clickhouse-core/query-builders/index.d.ts
new file mode 100644
index 0000000..9c78a79
--- /dev/null
+++ b/clickhouse-core/query-builders/index.d.ts
@@ -0,0 +1,7 @@
+export * from './count.js';
+export * from './delete.js';
+export * from './insert.js';
+export * from './query-builder.js';
+export * from './select.js';
+export * from './select.types.js';
+export * from './update.js';
diff --git a/clickhouse-core/query-builders/index.js b/clickhouse-core/query-builders/index.js
new file mode 100644
index 0000000..ac9c041
--- /dev/null
+++ b/clickhouse-core/query-builders/index.js
@@ -0,0 +1,7 @@
+export * from "./count.js";
+export * from "./delete.js";
+export * from "./insert.js";
+export * from "./query-builder.js";
+export * from "./select.js";
+export * from "./select.types.js";
+export * from "./update.js";
diff --git a/clickhouse-core/query-builders/insert.cjs b/clickhouse-core/query-builders/insert.cjs
new file mode 100644
index 0000000..cf3bf07
--- /dev/null
+++ b/clickhouse-core/query-builders/insert.cjs
@@ -0,0 +1,226 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var insert_exports = {};
+__export(insert_exports, {
+  CLICKHOUSE_INSERT_FORMAT: () => CLICKHOUSE_INSERT_FORMAT,
+  ClickHouseInsertBase: () => ClickHouseInsertBase,
+  ClickHouseInsertBuilder: () => ClickHouseInsertBuilder
+});
+module.exports = __toCommonJS(insert_exports);
+var import_entity = require("../../entity.cjs");
+var import_query_promise = require("../../query-promise.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_table = require("../../table.cjs");
+var import_utils = require("../utils.cjs");
+const CLICKHOUSE_INSERT_FORMAT = "JSONEachRow";
+function isStreamedRows(value) {
+  if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
+  return Symbol.iterator in value || Symbol.asyncIterator in value;
+}
+async function* toAsyncIterable(rows) {
+  yield* rows;
+}
+class ClickHouseInsertBuilder {
+  constructor(table, session, dialect, forceStatement = false) {
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+    this.forceStatement = forceStatement;
+  }
+  table;
+  session;
+  dialect;
+  forceStatement;
+  static [import_entity.entityKind] = "ClickHouseInsertBuilder";
+  /**
+   * Puts the rows in the statement, as `INSERT … VALUES (…), (…)`, rather than sending them as a
+   * body.
+   *
+   * The default is the body, because it streams and because the server parses a row format far more
+   * cheaply than a list of SQL expressions. This exists for when the statement itself is the
+   * artefact — something to log, hand to another tool, or run where no driver is available — and it
+   * necessarily materialises the batch.
+   *
+   * ```ts
+   * db.insert(events).inline().values(rows).toSQL().sql
+   * ```
+   */
+  inline() {
+    return new ClickHouseInsertBuilder(this.table, this.session, this.dialect, true);
+  }
+  values(values) {
+    if (isStreamedRows(values)) {
+      if (this.forceStatement) {
+        throw new Error(
+          "inline() needs the rows in memory. Pass an array to values() rather than a stream."
+        );
+      }
+      return new ClickHouseInsertBase(
+        this.table,
+        { rows: this.toRowStream(values) },
+        this.session,
+        this.dialect
+      );
+    }
+    const list = Array.isArray(values) ? values : [values];
+    if (list.length === 0) {
+      throw new Error("values() must be called with at least one value");
+    }
+    const hasExpression = list.some(
+      (entry) => Object.values(entry).some(
+        (value) => (0, import_entity.is)(value, import_sql.SQL) || (0, import_entity.is)(value, import_sql.Placeholder)
+      )
+    );
+    if (!hasExpression && !this.forceStatement) {
+      const mapped = list.map(
+        (entry) => this.dialect.mapRowForInsert(this.table, entry)
+      );
+      return new ClickHouseInsertBase(
+        this.table,
+        { rows: toAsyncIterable(mapped) },
+        this.session,
+        this.dialect
+      );
+    }
+    return new ClickHouseInsertBase(
+      this.table,
+      { values: this.toStatementValues(list) },
+      this.session,
+      this.dialect
+    );
+  }
+  /** @internal Wraps rows so each is mapped to its row-format form as it is pulled. */
+  async *toRowStream(source) {
+    for await (const entry of source) {
+      yield this.dialect.mapRowForInsert(this.table, entry);
+    }
+  }
+  /** @internal */
+  toStatementValues(list) {
+    const cols = this.table[import_table.Table.Symbol.Columns];
+    return list.map((entry) => {
+      const result = {};
+      for (const colKey of Object.keys(entry)) {
+        const colValue = entry[colKey];
+        result[colKey] = (0, import_entity.is)(colValue, import_sql.SQL) ? colValue : new import_sql.Param(colValue, cols[colKey]);
+      }
+      return result;
+    });
+  }
+  /**
+   * Populates the table from a query rather than from literal rows, emitting
+   * `INSERT INTO … SELECT …`.
+   *
+   * ClickHouse matches the selected columns to the table's columns *by position*, not by name, so
+   * the projection has to line up with the table's column order.
+   *
+   * @example
+   * ```ts
+   * await db.insert(dailyStats).select(
+   * 	db.select({ day: sql`toDate(${events.ts})`, hits: count() }).from(events).groupBy(sql`1`),
+   * );
+   * ```
+   */
+  select(query) {
+    const selectSQL = (0, import_entity.is)(query, import_sql.SQL) ? query : query.getSQL();
+    return new ClickHouseInsertBase(this.table, { select: selectSQL }, this.session, this.dialect);
+  }
+}
+class ClickHouseInsertBase extends import_query_promise.QueryPromise {
+  constructor(table, source, session, dialect) {
+    super();
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { table, ...source };
+  }
+  session;
+  dialect;
+  static [import_entity.entityKind] = "ClickHouseInsert";
+  config;
+  /**
+   * Adds a query-level `SETTINGS` clause.
+   *
+   * @example
+   * ```ts
+   * await db.insert(events).values(rows).settings({ async_insert: 1, wait_for_async_insert: 0 });
+   * ```
+   */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** Whether this insert sends its rows as a body rather than inside the statement. */
+  get isRowFormat() {
+    return this.config.rows !== void 0;
+  }
+  /** @internal */
+  getSQL() {
+    return this.isRowFormat ? this.dialect.buildInsertRowsQuery(this.config.table, CLICKHOUSE_INSERT_FORMAT) : this.dialect.buildInsertQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    if (this.isRowFormat) {
+      throw new Error(
+        "A row-format insert has no statement to prepare \u2014 its rows travel as a body. Await it directly, or call inline() to build an INSERT \u2026 VALUES statement instead."
+      );
+    }
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "insert",
+        tables: (0, import_utils.extractUsedTable)(this.config.table)
+      }
+    );
+  }
+  execute = async (placeholderValues) => {
+    if (this.config.rows !== void 0) {
+      const result = await this.session.insertRows(
+        this.dialect.insertTargetName(this.config.table),
+        this.config.rows,
+        {
+          settings: this.config.settings,
+          metadata: { type: "insert", tables: (0, import_utils.extractUsedTable)(this.config.table) }
+        }
+      );
+      return { rows: [], ...result };
+    }
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  CLICKHOUSE_INSERT_FORMAT,
+  ClickHouseInsertBase,
+  ClickHouseInsertBuilder
+});
diff --git a/clickhouse-core/query-builders/insert.d.cts b/clickhouse-core/query-builders/insert.d.cts
new file mode 100644
index 0000000..a9f1aee
--- /dev/null
+++ b/clickhouse-core/query-builders/insert.d.cts
@@ -0,0 +1,142 @@
+import type { ClickHouseDialect } from '../dialect.cjs';
+import type { ClickHouseSettings } from '../engines.cjs';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.cjs';
+import type { ClickHouseTable } from '../table.cjs';
+import { entityKind } from '../../entity.cjs';
+import type { TypedQueryBuilder } from '../../query-builders/query-builder.cjs';
+import { QueryPromise } from '../../query-promise.cjs';
+import type { RunnableQuery } from '../../runnable-query.cjs';
+import type { Query, SQLWrapper } from '../../sql/sql.cjs';
+import { Param, Placeholder, SQL } from '../../sql/sql.cjs';
+export interface ClickHouseInsertConfig<TTable extends ClickHouseTable = ClickHouseTable> {
+    table: TTable;
+    /** Literal rows, inlined into the statement. Mutually exclusive with {@link rows} and {@link select}. */
+    values?: Record<string, Param | SQL>[];
+    /**
+     * Rows sent as a body in a row format. Mutually exclusive with {@link values} and {@link select}.
+     *
+     * Async because the point of this path is that it streams: the source can be larger than memory
+     * and the driver pulls from it as the socket drains.
+     */
+    rows?: AsyncIterable<Record<string, unknown>>;
+    /** An `INSERT INTO … SELECT` source. Mutually exclusive with {@link values} and {@link rows}. */
+    select?: SQL;
+    settings?: ClickHouseSettings;
+}
+/** The row format a body insert uses. `JSONEachRow` matches columns by name, so rows may omit them. */
+export declare const CLICKHOUSE_INSERT_FORMAT = "JSONEachRow";
+export type AnyClickHouseInsertConfig = ClickHouseInsertConfig<ClickHouseTable>;
+export type ClickHouseInsertValue<TTable extends ClickHouseTable> = {
+    [Key in keyof TTable['$inferInsert']]: TTable['$inferInsert'][Key] | SQL | Placeholder;
+} & {};
+export declare class ClickHouseInsertBuilder<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase> {
+    private table;
+    private session;
+    private dialect;
+    /** Set by {@link inline}: keep the rows in the statement rather than sending a body. */
+    private forceStatement;
+    static readonly [entityKind]: string;
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect, 
+    /** Set by {@link inline}: keep the rows in the statement rather than sending a body. */
+    forceStatement?: boolean);
+    /**
+     * Puts the rows in the statement, as `INSERT … VALUES (…), (…)`, rather than sending them as a
+     * body.
+     *
+     * The default is the body, because it streams and because the server parses a row format far more
+     * cheaply than a list of SQL expressions. This exists for when the statement itself is the
+     * artefact — something to log, hand to another tool, or run where no driver is available — and it
+     * necessarily materialises the batch.
+     *
+     * ```ts
+     * db.insert(events).inline().values(rows).toSQL().sql
+     * ```
+     */
+    inline(): ClickHouseInsertBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * The rows to insert.
+     *
+     * **Rows go as a `JSONEachRow` body, not inside the statement.** ClickHouse has no
+     * prepared-statement protocol, so the inline form has to render every value as a SQL literal —
+     * which means building the whole batch as one string here and re-parsing every field as an
+     * expression there. That is fine for three rows and wrong for three hundred thousand, and since
+     * the driver can stream a body, the body is the default.
+     *
+     * Two things send a batch down the inline path instead, both automatic: a value that is a SQL
+     * expression (`sql\`now()\``) or a placeholder, because a body has nowhere to put one. Call
+     * {@link ClickHouseInsertBase.inline} to force it.
+     *
+     * Passing an iterable or async iterable streams: the driver pulls rows as the socket drains and
+     * nothing materialises the batch. A stream cannot be scanned for SQL values ahead of time, so one
+     * that yields a row containing an expression throws when it reaches it.
+     */
+    values(value: ClickHouseInsertValue<TTable>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    values(values: ClickHouseInsertValue<TTable>[]): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    values(values: Iterable<ClickHouseInsertValue<TTable>> | AsyncIterable<ClickHouseInsertValue<TTable>>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Populates the table from a query rather than from literal rows, emitting
+     * `INSERT INTO … SELECT …`.
+     *
+     * ClickHouse matches the selected columns to the table's columns *by position*, not by name, so
+     * the projection has to line up with the table's column order.
+     *
+     * @example
+     * ```ts
+     * await db.insert(dailyStats).select(
+     * 	db.select({ day: sql`toDate(${events.ts})`, hits: count() }).from(events).groupBy(sql`1`),
+     * );
+     * ```
+     */
+    select(query: SQL | TypedQueryBuilder<any, any>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+}
+export type ClickHouseInsertWithout<T extends AnyClickHouseInsert, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseInsertBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseInsertDynamic<T extends AnyClickHouseInsert> = ClickHouseInsert<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+export type ClickHouseInsertPrepare<T extends AnyClickHouseInsert> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+export type ClickHouseInsert<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export type AnyClickHouseInsert = ClickHouseInsertBase<any, any, any, any, any>;
+export interface ClickHouseInsertBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>>, RunnableQuery<ClickHouseQueryResultKind<TQueryResult, never>, 'clickhouse'>, SQLWrapper {
+    readonly _: {
+        readonly dialect: 'clickhouse';
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: ClickHouseQueryResultKind<TQueryResult, never>;
+    };
+}
+/**
+ * An insert into a ClickHouse table.
+ *
+ * There is no `ON CONFLICT`/`ON DUPLICATE KEY` equivalent and no `RETURNING`: ClickHouse does not
+ * enforce primary-key uniqueness at write time, and inserts do not report generated values back.
+ * Deduplication is a property of the engine — see `ReplacingMergeTree` — not of the insert.
+ */
+export declare class ClickHouseInsertBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements RunnableQuery<ClickHouseQueryResultKind<TQueryResult, never>, 'clickhouse'>, SQLWrapper {
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    protected $table: TTable;
+    private config;
+    constructor(table: TTable, source: Pick<ClickHouseInsertConfig<TTable>, 'values' | 'rows' | 'select'>, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a query-level `SETTINGS` clause.
+     *
+     * @example
+     * ```ts
+     * await db.insert(events).values(rows).settings({ async_insert: 1, wait_for_async_insert: 0 });
+     * ```
+     */
+    settings(settings: ClickHouseSettings): ClickHouseInsertWithout<this, TDynamic, 'settings'>;
+    /** Whether this insert sends its rows as a body rather than inside the statement. */
+    private get isRowFormat();
+    toSQL(): Query;
+    prepare(): ClickHouseInsertPrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseInsertDynamic<this>;
+}
diff --git a/clickhouse-core/query-builders/insert.d.ts b/clickhouse-core/query-builders/insert.d.ts
new file mode 100644
index 0000000..7dbecea
--- /dev/null
+++ b/clickhouse-core/query-builders/insert.d.ts
@@ -0,0 +1,142 @@
+import type { ClickHouseDialect } from '../dialect.js';
+import type { ClickHouseSettings } from '../engines.js';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.js';
+import type { ClickHouseTable } from '../table.js';
+import { entityKind } from '../../entity.js';
+import type { TypedQueryBuilder } from '../../query-builders/query-builder.js';
+import { QueryPromise } from '../../query-promise.js';
+import type { RunnableQuery } from '../../runnable-query.js';
+import type { Query, SQLWrapper } from '../../sql/sql.js';
+import { Param, Placeholder, SQL } from '../../sql/sql.js';
+export interface ClickHouseInsertConfig<TTable extends ClickHouseTable = ClickHouseTable> {
+    table: TTable;
+    /** Literal rows, inlined into the statement. Mutually exclusive with {@link rows} and {@link select}. */
+    values?: Record<string, Param | SQL>[];
+    /**
+     * Rows sent as a body in a row format. Mutually exclusive with {@link values} and {@link select}.
+     *
+     * Async because the point of this path is that it streams: the source can be larger than memory
+     * and the driver pulls from it as the socket drains.
+     */
+    rows?: AsyncIterable<Record<string, unknown>>;
+    /** An `INSERT INTO … SELECT` source. Mutually exclusive with {@link values} and {@link rows}. */
+    select?: SQL;
+    settings?: ClickHouseSettings;
+}
+/** The row format a body insert uses. `JSONEachRow` matches columns by name, so rows may omit them. */
+export declare const CLICKHOUSE_INSERT_FORMAT = "JSONEachRow";
+export type AnyClickHouseInsertConfig = ClickHouseInsertConfig<ClickHouseTable>;
+export type ClickHouseInsertValue<TTable extends ClickHouseTable> = {
+    [Key in keyof TTable['$inferInsert']]: TTable['$inferInsert'][Key] | SQL | Placeholder;
+} & {};
+export declare class ClickHouseInsertBuilder<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase> {
+    private table;
+    private session;
+    private dialect;
+    /** Set by {@link inline}: keep the rows in the statement rather than sending a body. */
+    private forceStatement;
+    static readonly [entityKind]: string;
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect, 
+    /** Set by {@link inline}: keep the rows in the statement rather than sending a body. */
+    forceStatement?: boolean);
+    /**
+     * Puts the rows in the statement, as `INSERT … VALUES (…), (…)`, rather than sending them as a
+     * body.
+     *
+     * The default is the body, because it streams and because the server parses a row format far more
+     * cheaply than a list of SQL expressions. This exists for when the statement itself is the
+     * artefact — something to log, hand to another tool, or run where no driver is available — and it
+     * necessarily materialises the batch.
+     *
+     * ```ts
+     * db.insert(events).inline().values(rows).toSQL().sql
+     * ```
+     */
+    inline(): ClickHouseInsertBuilder<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * The rows to insert.
+     *
+     * **Rows go as a `JSONEachRow` body, not inside the statement.** ClickHouse has no
+     * prepared-statement protocol, so the inline form has to render every value as a SQL literal —
+     * which means building the whole batch as one string here and re-parsing every field as an
+     * expression there. That is fine for three rows and wrong for three hundred thousand, and since
+     * the driver can stream a body, the body is the default.
+     *
+     * Two things send a batch down the inline path instead, both automatic: a value that is a SQL
+     * expression (`sql\`now()\``) or a placeholder, because a body has nowhere to put one. Call
+     * {@link ClickHouseInsertBase.inline} to force it.
+     *
+     * Passing an iterable or async iterable streams: the driver pulls rows as the socket drains and
+     * nothing materialises the batch. A stream cannot be scanned for SQL values ahead of time, so one
+     * that yields a row containing an expression throws when it reaches it.
+     */
+    values(value: ClickHouseInsertValue<TTable>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    values(values: ClickHouseInsertValue<TTable>[]): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    values(values: Iterable<ClickHouseInsertValue<TTable>> | AsyncIterable<ClickHouseInsertValue<TTable>>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+    /**
+     * Populates the table from a query rather than from literal rows, emitting
+     * `INSERT INTO … SELECT …`.
+     *
+     * ClickHouse matches the selected columns to the table's columns *by position*, not by name, so
+     * the projection has to line up with the table's column order.
+     *
+     * @example
+     * ```ts
+     * await db.insert(dailyStats).select(
+     * 	db.select({ day: sql`toDate(${events.ts})`, hits: count() }).from(events).groupBy(sql`1`),
+     * );
+     * ```
+     */
+    select(query: SQL | TypedQueryBuilder<any, any>): ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT>;
+}
+export type ClickHouseInsertWithout<T extends AnyClickHouseInsert, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseInsertBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseInsertDynamic<T extends AnyClickHouseInsert> = ClickHouseInsert<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+export type ClickHouseInsertPrepare<T extends AnyClickHouseInsert> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+export type ClickHouseInsert<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseInsertBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export type AnyClickHouseInsert = ClickHouseInsertBase<any, any, any, any, any>;
+export interface ClickHouseInsertBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>>, RunnableQuery<ClickHouseQueryResultKind<TQueryResult, never>, 'clickhouse'>, SQLWrapper {
+    readonly _: {
+        readonly dialect: 'clickhouse';
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: ClickHouseQueryResultKind<TQueryResult, never>;
+    };
+}
+/**
+ * An insert into a ClickHouse table.
+ *
+ * There is no `ON CONFLICT`/`ON DUPLICATE KEY` equivalent and no `RETURNING`: ClickHouse does not
+ * enforce primary-key uniqueness at write time, and inserts do not report generated values back.
+ * Deduplication is a property of the engine — see `ReplacingMergeTree` — not of the insert.
+ */
+export declare class ClickHouseInsertBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements RunnableQuery<ClickHouseQueryResultKind<TQueryResult, never>, 'clickhouse'>, SQLWrapper {
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    protected $table: TTable;
+    private config;
+    constructor(table: TTable, source: Pick<ClickHouseInsertConfig<TTable>, 'values' | 'rows' | 'select'>, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a query-level `SETTINGS` clause.
+     *
+     * @example
+     * ```ts
+     * await db.insert(events).values(rows).settings({ async_insert: 1, wait_for_async_insert: 0 });
+     * ```
+     */
+    settings(settings: ClickHouseSettings): ClickHouseInsertWithout<this, TDynamic, 'settings'>;
+    /** Whether this insert sends its rows as a body rather than inside the statement. */
+    private get isRowFormat();
+    toSQL(): Query;
+    prepare(): ClickHouseInsertPrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseInsertDynamic<this>;
+}
diff --git a/clickhouse-core/query-builders/insert.js b/clickhouse-core/query-builders/insert.js
new file mode 100644
index 0000000..0d67345
--- /dev/null
+++ b/clickhouse-core/query-builders/insert.js
@@ -0,0 +1,200 @@
+import { entityKind, is } from "../../entity.js";
+import { QueryPromise } from "../../query-promise.js";
+import { Param, Placeholder, SQL } from "../../sql/sql.js";
+import { Table } from "../../table.js";
+import { extractUsedTable } from "../utils.js";
+const CLICKHOUSE_INSERT_FORMAT = "JSONEachRow";
+function isStreamedRows(value) {
+  if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
+  return Symbol.iterator in value || Symbol.asyncIterator in value;
+}
+async function* toAsyncIterable(rows) {
+  yield* rows;
+}
+class ClickHouseInsertBuilder {
+  constructor(table, session, dialect, forceStatement = false) {
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+    this.forceStatement = forceStatement;
+  }
+  table;
+  session;
+  dialect;
+  forceStatement;
+  static [entityKind] = "ClickHouseInsertBuilder";
+  /**
+   * Puts the rows in the statement, as `INSERT … VALUES (…), (…)`, rather than sending them as a
+   * body.
+   *
+   * The default is the body, because it streams and because the server parses a row format far more
+   * cheaply than a list of SQL expressions. This exists for when the statement itself is the
+   * artefact — something to log, hand to another tool, or run where no driver is available — and it
+   * necessarily materialises the batch.
+   *
+   * ```ts
+   * db.insert(events).inline().values(rows).toSQL().sql
+   * ```
+   */
+  inline() {
+    return new ClickHouseInsertBuilder(this.table, this.session, this.dialect, true);
+  }
+  values(values) {
+    if (isStreamedRows(values)) {
+      if (this.forceStatement) {
+        throw new Error(
+          "inline() needs the rows in memory. Pass an array to values() rather than a stream."
+        );
+      }
+      return new ClickHouseInsertBase(
+        this.table,
+        { rows: this.toRowStream(values) },
+        this.session,
+        this.dialect
+      );
+    }
+    const list = Array.isArray(values) ? values : [values];
+    if (list.length === 0) {
+      throw new Error("values() must be called with at least one value");
+    }
+    const hasExpression = list.some(
+      (entry) => Object.values(entry).some(
+        (value) => is(value, SQL) || is(value, Placeholder)
+      )
+    );
+    if (!hasExpression && !this.forceStatement) {
+      const mapped = list.map(
+        (entry) => this.dialect.mapRowForInsert(this.table, entry)
+      );
+      return new ClickHouseInsertBase(
+        this.table,
+        { rows: toAsyncIterable(mapped) },
+        this.session,
+        this.dialect
+      );
+    }
+    return new ClickHouseInsertBase(
+      this.table,
+      { values: this.toStatementValues(list) },
+      this.session,
+      this.dialect
+    );
+  }
+  /** @internal Wraps rows so each is mapped to its row-format form as it is pulled. */
+  async *toRowStream(source) {
+    for await (const entry of source) {
+      yield this.dialect.mapRowForInsert(this.table, entry);
+    }
+  }
+  /** @internal */
+  toStatementValues(list) {
+    const cols = this.table[Table.Symbol.Columns];
+    return list.map((entry) => {
+      const result = {};
+      for (const colKey of Object.keys(entry)) {
+        const colValue = entry[colKey];
+        result[colKey] = is(colValue, SQL) ? colValue : new Param(colValue, cols[colKey]);
+      }
+      return result;
+    });
+  }
+  /**
+   * Populates the table from a query rather than from literal rows, emitting
+   * `INSERT INTO … SELECT …`.
+   *
+   * ClickHouse matches the selected columns to the table's columns *by position*, not by name, so
+   * the projection has to line up with the table's column order.
+   *
+   * @example
+   * ```ts
+   * await db.insert(dailyStats).select(
+   * 	db.select({ day: sql`toDate(${events.ts})`, hits: count() }).from(events).groupBy(sql`1`),
+   * );
+   * ```
+   */
+  select(query) {
+    const selectSQL = is(query, SQL) ? query : query.getSQL();
+    return new ClickHouseInsertBase(this.table, { select: selectSQL }, this.session, this.dialect);
+  }
+}
+class ClickHouseInsertBase extends QueryPromise {
+  constructor(table, source, session, dialect) {
+    super();
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { table, ...source };
+  }
+  session;
+  dialect;
+  static [entityKind] = "ClickHouseInsert";
+  config;
+  /**
+   * Adds a query-level `SETTINGS` clause.
+   *
+   * @example
+   * ```ts
+   * await db.insert(events).values(rows).settings({ async_insert: 1, wait_for_async_insert: 0 });
+   * ```
+   */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** Whether this insert sends its rows as a body rather than inside the statement. */
+  get isRowFormat() {
+    return this.config.rows !== void 0;
+  }
+  /** @internal */
+  getSQL() {
+    return this.isRowFormat ? this.dialect.buildInsertRowsQuery(this.config.table, CLICKHOUSE_INSERT_FORMAT) : this.dialect.buildInsertQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    if (this.isRowFormat) {
+      throw new Error(
+        "A row-format insert has no statement to prepare \u2014 its rows travel as a body. Await it directly, or call inline() to build an INSERT \u2026 VALUES statement instead."
+      );
+    }
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "insert",
+        tables: extractUsedTable(this.config.table)
+      }
+    );
+  }
+  execute = async (placeholderValues) => {
+    if (this.config.rows !== void 0) {
+      const result = await this.session.insertRows(
+        this.dialect.insertTargetName(this.config.table),
+        this.config.rows,
+        {
+          settings: this.config.settings,
+          metadata: { type: "insert", tables: extractUsedTable(this.config.table) }
+        }
+      );
+      return { rows: [], ...result };
+    }
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+export {
+  CLICKHOUSE_INSERT_FORMAT,
+  ClickHouseInsertBase,
+  ClickHouseInsertBuilder
+};
diff --git a/clickhouse-core/query-builders/query-builder.cjs b/clickhouse-core/query-builders/query-builder.cjs
new file mode 100644
index 0000000..a860db2
--- /dev/null
+++ b/clickhouse-core/query-builders/query-builder.cjs
@@ -0,0 +1,102 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var query_builder_exports = {};
+__export(query_builder_exports, {
+  QueryBuilder: () => QueryBuilder
+});
+module.exports = __toCommonJS(query_builder_exports);
+var import_dialect = require("../dialect.cjs");
+var import_entity = require("../../entity.cjs");
+var import_selection_proxy = require("../../selection-proxy.cjs");
+var import_subquery = require("../../subquery.cjs");
+var import_select = require("./select.cjs");
+class QueryBuilder {
+  static [import_entity.entityKind] = "ClickHouseQueryBuilder";
+  dialect;
+  dialectConfig;
+  constructor(dialect) {
+    this.dialect = (0, import_entity.is)(dialect, import_dialect.ClickHouseDialect) ? dialect : void 0;
+    this.dialectConfig = (0, import_entity.is)(dialect, import_dialect.ClickHouseDialect) ? void 0 : dialect;
+  }
+  $with = (alias, selection) => {
+    const queryBuilder = this;
+    const as = (qb) => {
+      if (typeof qb === "function") {
+        qb = qb(queryBuilder);
+      }
+      return new Proxy(
+        new import_subquery.WithSubquery(
+          qb.getSQL(),
+          selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
+          alias,
+          true
+        ),
+        new import_selection_proxy.SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+      );
+    };
+    return { as };
+  };
+  with(...queries) {
+    const self = this;
+    function select(fields) {
+      return new import_select.ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: void 0,
+        dialect: self.getDialect(),
+        withList: queries
+      });
+    }
+    function selectDistinct(fields) {
+      return new import_select.ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: void 0,
+        dialect: self.getDialect(),
+        withList: queries,
+        distinct: true
+      });
+    }
+    return { select, selectDistinct };
+  }
+  select(fields) {
+    return new import_select.ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: void 0,
+      dialect: this.getDialect()
+    });
+  }
+  selectDistinct(fields) {
+    return new import_select.ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: void 0,
+      dialect: this.getDialect(),
+      distinct: true
+    });
+  }
+  // Lazy load dialect to avoid circular dependency
+  getDialect() {
+    if (!this.dialect) {
+      this.dialect = new import_dialect.ClickHouseDialect(this.dialectConfig);
+    }
+    return this.dialect;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  QueryBuilder
+});
diff --git a/clickhouse-core/query-builders/query-builder.d.cts b/clickhouse-core/query-builders/query-builder.d.cts
new file mode 100644
index 0000000..4bf3433
--- /dev/null
+++ b/clickhouse-core/query-builders/query-builder.d.cts
@@ -0,0 +1,29 @@
+import type { ClickHouseDialectConfig } from '../dialect.cjs';
+import { ClickHouseDialect } from '../dialect.cjs';
+import type { WithBuilder } from '../subquery.cjs';
+import { entityKind } from '../../entity.cjs';
+import { WithSubquery } from '../../subquery.cjs';
+import { ClickHouseSelectBuilder } from './select.cjs';
+import type { SelectedFields } from './select.types.cjs';
+export declare class QueryBuilder {
+    static readonly [entityKind]: string;
+    private dialect;
+    private dialectConfig;
+    constructor(dialect?: ClickHouseDialect | ClickHouseDialectConfig);
+    $with: WithBuilder;
+    with(...queries: WithSubquery[]): {
+        select: {
+            (): ClickHouseSelectBuilder<undefined, never, "qb">;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, "qb">;
+        };
+        selectDistinct: {
+            (): ClickHouseSelectBuilder<undefined, never, "qb">;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, "qb">;
+        };
+    };
+    select(): ClickHouseSelectBuilder<undefined, never, 'qb'>;
+    select<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, 'qb'>;
+    selectDistinct(): ClickHouseSelectBuilder<undefined, never, 'qb'>;
+    selectDistinct<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, 'qb'>;
+    private getDialect;
+}
diff --git a/clickhouse-core/query-builders/query-builder.d.ts b/clickhouse-core/query-builders/query-builder.d.ts
new file mode 100644
index 0000000..08944a9
--- /dev/null
+++ b/clickhouse-core/query-builders/query-builder.d.ts
@@ -0,0 +1,29 @@
+import type { ClickHouseDialectConfig } from '../dialect.js';
+import { ClickHouseDialect } from '../dialect.js';
+import type { WithBuilder } from '../subquery.js';
+import { entityKind } from '../../entity.js';
+import { WithSubquery } from '../../subquery.js';
+import { ClickHouseSelectBuilder } from './select.js';
+import type { SelectedFields } from './select.types.js';
+export declare class QueryBuilder {
+    static readonly [entityKind]: string;
+    private dialect;
+    private dialectConfig;
+    constructor(dialect?: ClickHouseDialect | ClickHouseDialectConfig);
+    $with: WithBuilder;
+    with(...queries: WithSubquery[]): {
+        select: {
+            (): ClickHouseSelectBuilder<undefined, never, "qb">;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, "qb">;
+        };
+        selectDistinct: {
+            (): ClickHouseSelectBuilder<undefined, never, "qb">;
+            <TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, "qb">;
+        };
+    };
+    select(): ClickHouseSelectBuilder<undefined, never, 'qb'>;
+    select<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, 'qb'>;
+    selectDistinct(): ClickHouseSelectBuilder<undefined, never, 'qb'>;
+    selectDistinct<TSelection extends SelectedFields>(fields: TSelection): ClickHouseSelectBuilder<TSelection, never, 'qb'>;
+    private getDialect;
+}
diff --git a/clickhouse-core/query-builders/query-builder.js b/clickhouse-core/query-builders/query-builder.js
new file mode 100644
index 0000000..be17967
--- /dev/null
+++ b/clickhouse-core/query-builders/query-builder.js
@@ -0,0 +1,78 @@
+import { ClickHouseDialect } from "../dialect.js";
+import { entityKind, is } from "../../entity.js";
+import { SelectionProxyHandler } from "../../selection-proxy.js";
+import { WithSubquery } from "../../subquery.js";
+import { ClickHouseSelectBuilder } from "./select.js";
+class QueryBuilder {
+  static [entityKind] = "ClickHouseQueryBuilder";
+  dialect;
+  dialectConfig;
+  constructor(dialect) {
+    this.dialect = is(dialect, ClickHouseDialect) ? dialect : void 0;
+    this.dialectConfig = is(dialect, ClickHouseDialect) ? void 0 : dialect;
+  }
+  $with = (alias, selection) => {
+    const queryBuilder = this;
+    const as = (qb) => {
+      if (typeof qb === "function") {
+        qb = qb(queryBuilder);
+      }
+      return new Proxy(
+        new WithSubquery(
+          qb.getSQL(),
+          selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}),
+          alias,
+          true
+        ),
+        new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+      );
+    };
+    return { as };
+  };
+  with(...queries) {
+    const self = this;
+    function select(fields) {
+      return new ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: void 0,
+        dialect: self.getDialect(),
+        withList: queries
+      });
+    }
+    function selectDistinct(fields) {
+      return new ClickHouseSelectBuilder({
+        fields: fields ?? void 0,
+        session: void 0,
+        dialect: self.getDialect(),
+        withList: queries,
+        distinct: true
+      });
+    }
+    return { select, selectDistinct };
+  }
+  select(fields) {
+    return new ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: void 0,
+      dialect: this.getDialect()
+    });
+  }
+  selectDistinct(fields) {
+    return new ClickHouseSelectBuilder({
+      fields: fields ?? void 0,
+      session: void 0,
+      dialect: this.getDialect(),
+      distinct: true
+    });
+  }
+  // Lazy load dialect to avoid circular dependency
+  getDialect() {
+    if (!this.dialect) {
+      this.dialect = new ClickHouseDialect(this.dialectConfig);
+    }
+    return this.dialect;
+  }
+}
+export {
+  QueryBuilder
+};
diff --git a/clickhouse-core/query-builders/select.cjs b/clickhouse-core/query-builders/select.cjs
new file mode 100644
index 0000000..0411401
--- /dev/null
+++ b/clickhouse-core/query-builders/select.cjs
@@ -0,0 +1,875 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except2, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except2)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var select_exports = {};
+__export(select_exports, {
+  ClickHouseSelectBase: () => ClickHouseSelectBase,
+  ClickHouseSelectBuilder: () => ClickHouseSelectBuilder,
+  ClickHouseSelectQueryBuilderBase: () => ClickHouseSelectQueryBuilderBase,
+  except: () => except,
+  intersect: () => intersect,
+  minus: () => minus,
+  union: () => union,
+  unionAll: () => unionAll
+});
+module.exports = __toCommonJS(select_exports);
+var import_entity = require("../../entity.cjs");
+var import_query_builder = require("../../query-builders/query-builder.cjs");
+var import_query_promise = require("../../query-promise.cjs");
+var import_selection_proxy = require("../../selection-proxy.cjs");
+var import_sql = require("../../sql/sql.cjs");
+var import_subquery = require("../../subquery.cjs");
+var import_table = require("../../table.cjs");
+var import_utils = require("../../utils.cjs");
+var import_utils2 = require("../utils.cjs");
+class ClickHouseSelectBuilder {
+  static [import_entity.entityKind] = "ClickHouseSelectBuilder";
+  fields;
+  session;
+  dialect;
+  withList = [];
+  distinct;
+  constructor(config) {
+    this.fields = config.fields;
+    this.session = config.session;
+    this.dialect = config.dialect;
+    if (config.withList) {
+      this.withList = config.withList;
+    }
+    this.distinct = config.distinct;
+  }
+  from(source) {
+    const isPartialSelect = !!this.fields;
+    let fields;
+    if (this.fields) {
+      fields = this.fields;
+    } else if ((0, import_entity.is)(source, import_subquery.Subquery)) {
+      fields = Object.fromEntries(
+        Object.keys(source._.selectedFields).map((key) => [key, source[key]])
+      );
+    } else if ((0, import_entity.is)(source, import_sql.SQL)) {
+      fields = {};
+    } else {
+      fields = (0, import_utils.getTableColumns)(source);
+    }
+    return new ClickHouseSelectBase(
+      {
+        table: source,
+        fields,
+        isPartialSelect,
+        session: this.session,
+        dialect: this.dialect,
+        withList: this.withList,
+        distinct: this.distinct
+      }
+    );
+  }
+}
+class ClickHouseSelectQueryBuilderBase extends import_query_builder.TypedQueryBuilder {
+  static [import_entity.entityKind] = "ClickHouseSelectQueryBuilder";
+  _;
+  config;
+  joinsNotNullableMap;
+  tableName;
+  isPartialSelect;
+  /** @internal */
+  session;
+  dialect;
+  cacheConfig = void 0;
+  usedTables = /* @__PURE__ */ new Set();
+  constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }) {
+    super();
+    this.config = {
+      withList,
+      table,
+      fields: { ...fields },
+      distinct,
+      setOperators: []
+    };
+    this.isPartialSelect = isPartialSelect;
+    this.session = session;
+    this.dialect = dialect;
+    this._ = {
+      selectedFields: fields,
+      config: this.config
+    };
+    this.tableName = (0, import_utils.getTableLikeName)(table);
+    this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
+    for (const item of (0, import_utils2.extractUsedTable)(table)) this.usedTables.add(item);
+  }
+  /** @internal */
+  getUsedTables() {
+    return [...this.usedTables];
+  }
+  createJoin(joinType, lateral) {
+    return (table, on) => {
+      const baseTableName = this.tableName;
+      const tableName = (0, import_utils.getTableLikeName)(table);
+      for (const item of (0, import_utils2.extractUsedTable)(table)) this.usedTables.add(item);
+      if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
+        throw new Error(`Alias "${tableName}" is already used in this query`);
+      }
+      if (!this.isPartialSelect) {
+        if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
+          this.config.fields = {
+            [baseTableName]: this.config.fields
+          };
+        }
+        if (typeof tableName === "string" && !(0, import_entity.is)(table, import_sql.SQL)) {
+          const selection = (0, import_entity.is)(table, import_subquery.Subquery) ? table._.selectedFields : table[import_table.Table.Symbol.Columns];
+          this.config.fields[tableName] = selection;
+        }
+      }
+      if (typeof on === "function") {
+        on = on(
+          new Proxy(
+            this.config.fields,
+            new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+          )
+        );
+      }
+      if (!this.config.joins) {
+        this.config.joins = [];
+      }
+      this.config.joins.push({ on, table, joinType, alias: tableName, lateral });
+      if (typeof tableName === "string") {
+        switch (joinType) {
+          case "left": {
+            this.joinsNotNullableMap[tableName] = false;
+            break;
+          }
+          case "right": {
+            this.joinsNotNullableMap = Object.fromEntries(
+              Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
+            );
+            this.joinsNotNullableMap[tableName] = true;
+            break;
+          }
+          case "cross":
+          case "inner": {
+            this.joinsNotNullableMap[tableName] = true;
+            break;
+          }
+          case "full": {
+            this.joinsNotNullableMap = Object.fromEntries(
+              Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
+            );
+            this.joinsNotNullableMap[tableName] = false;
+            break;
+          }
+        }
+      }
+      return this;
+    };
+  }
+  /**
+   * Executes a `left join` operation by adding another table to the current query.
+   *
+   * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User; pets: Pet | null; }[] = await db.select()
+   *   .from(users)
+   *   .leftJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number | null; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .leftJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  leftJoin = this.createJoin("left", false);
+  /**
+   * Executes a `left join lateral` operation by adding subquery to the current query.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#left-join-lateral}
+   *
+   * @param table the subquery to join.
+   * @param on the `on` clause.
+   */
+  leftJoinLateral = this.createJoin("left", true);
+  /**
+   * Executes a `right join` operation by adding another table to the current query.
+   *
+   * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User | null; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .rightJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number | null; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .rightJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  rightJoin = this.createJoin("right", false);
+  /**
+   * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
+   *
+   * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .innerJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .innerJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  innerJoin = this.createJoin("inner", false);
+  /**
+   * Executes an `inner join lateral` operation, creating a new table by combining rows from two queries that have matching values.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join-lateral}
+   *
+   * @param table the subquery to join.
+   * @param on the `on` clause.
+   */
+  innerJoinLateral = this.createJoin("inner", true);
+  /**
+   * Executes a `full join` operation by combining rows from two tables into a new table.
+   *
+   * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User | null; pets: Pet | null; }[] = await db.select()
+   *   .from(users)
+   *   .fullJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number | null; petId: number | null; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .fullJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  fullJoin = this.createJoin("full", false);
+  /**
+   * Executes a `cross join` operation by combining rows from two tables into a new table.
+   *
+   * Calling this method retrieves all rows from both main and joined tables, merging all rows from each table.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join}
+   *
+   * @param table the table to join.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users, each user with every pet
+   * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .crossJoin(pets)
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .crossJoin(pets)
+   * ```
+   */
+  crossJoin = this.createJoin("cross", false);
+  /**
+   * Executes a `cross join lateral` operation by combining rows from two queries into a new table.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method retrieves all rows from both main and joined queries, merging all rows from each query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join-lateral}
+   *
+   * @param table the query to join.
+   */
+  crossJoinLateral = this.createJoin("cross", true);
+  createSetOperator(type, isAll) {
+    return (rightSelection) => {
+      const rightSelect = typeof rightSelection === "function" ? rightSelection(getClickHouseSetOperators()) : rightSelection;
+      if (!(0, import_utils.haveSameKeys)(this.getSelectedFields(), rightSelect.getSelectedFields())) {
+        throw new Error(
+          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
+        );
+      }
+      this.config.setOperators.push({ type, isAll, rightSelect });
+      return this;
+    };
+  }
+  /**
+   * Adds `union` set operator to the query.
+   *
+   * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all unique names from customers and users tables
+   * await db.select({ name: users.name })
+   *   .from(users)
+   *   .union(
+   *     db.select({ name: customers.name }).from(customers)
+   *   );
+   * // or
+   * import { union } from 'drizzle-orm/clickhouse-core'
+   *
+   * await union(
+   *   db.select({ name: users.name }).from(users),
+   *   db.select({ name: customers.name }).from(customers)
+   * );
+   * ```
+   */
+  union = this.createSetOperator("union", false);
+  /**
+   * Adds `union all` set operator to the query.
+   *
+   * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all transaction ids from both online and in-store sales
+   * await db.select({ transaction: onlineSales.transactionId })
+   *   .from(onlineSales)
+   *   .unionAll(
+   *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+   *   );
+   * // or
+   * import { unionAll } from 'drizzle-orm/clickhouse-core'
+   *
+   * await unionAll(
+   *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+   *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+   * );
+   * ```
+   */
+  unionAll = this.createSetOperator("union", true);
+  /**
+   * Adds `intersect` set operator to the query.
+   *
+   * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select course names that are offered in both departments A and B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .intersect(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { intersect } from 'drizzle-orm/clickhouse-core'
+   *
+   * await intersect(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  intersect = this.createSetOperator("intersect", false);
+  /**
+   * Adds `except` set operator to the query.
+   *
+   * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all courses offered in department A but not in department B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .except(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { except } from 'drizzle-orm/clickhouse-core'
+   *
+   * await except(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  except = this.createSetOperator("except", false);
+  /**
+   * Adds `minus` set operator to the query.
+   *
+   * This is an alias of `except` supported by ClickHouse.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all courses offered in department A but not in department B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .minus(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { minus } from 'drizzle-orm/clickhouse-core'
+   *
+   * await minus(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  minus = this.createSetOperator("except", false);
+  /** @internal */
+  addSetOperators(setOperators) {
+    this.config.setOperators.push(...setOperators);
+    return this;
+  }
+  /**
+   * Adds a `where` clause to the query.
+   *
+   * Calling this method will select only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
+   *
+   * @param where the `where` clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be selected.
+   *
+   * ```ts
+   * // Select all cars with green color
+   * await db.select().from(cars).where(eq(cars.color, 'green'));
+   * // or
+   * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Select all BMW cars with a green color
+   * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Select all cars with the green or blue color
+   * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    if (typeof where === "function") {
+      where = where(
+        new Proxy(
+          this.config.fields,
+          new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.where = where;
+    return this;
+  }
+  /**
+   * Adds a `having` clause to the query.
+   *
+   * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+   *
+   * @param having the `having` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all brands with more than one car
+   * await db.select({
+   * 	brand: cars.brand,
+   * 	count: sql<number>`cast(count(${cars.id}) as int)`,
+   * })
+   *   .from(cars)
+   *   .groupBy(cars.brand)
+   *   .having(({ count }) => gt(count, 1));
+   * ```
+   */
+  having(having) {
+    if (typeof having === "function") {
+      having = having(
+        new Proxy(
+          this.config.fields,
+          new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.having = having;
+    return this;
+  }
+  groupBy(...columns) {
+    if (typeof columns[0] === "function") {
+      const groupBy = columns[0](
+        new Proxy(
+          this.config.fields,
+          new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
+        )
+      );
+      this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
+    } else {
+      this.config.groupBy = columns;
+    }
+    return this;
+  }
+  orderBy(...columns) {
+    if (typeof columns[0] === "function") {
+      const orderBy = columns[0](
+        new Proxy(
+          this.config.fields,
+          new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
+        )
+      );
+      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
+      if (this.config.setOperators.length > 0) {
+        this.config.setOperators.at(-1).orderBy = orderByArray;
+      } else {
+        this.config.orderBy = orderByArray;
+      }
+    } else {
+      const orderByArray = columns;
+      if (this.config.setOperators.length > 0) {
+        this.config.setOperators.at(-1).orderBy = orderByArray;
+      } else {
+        this.config.orderBy = orderByArray;
+      }
+    }
+    return this;
+  }
+  /**
+   * Adds a `limit` clause to the query.
+   *
+   * Calling this method will set the maximum number of rows that will be returned by this query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+   *
+   * @param limit the `limit` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Get the first 10 people from this query.
+   * await db.select().from(people).limit(10);
+   * ```
+   */
+  limit(limit) {
+    if (this.config.setOperators.length > 0) {
+      this.config.setOperators.at(-1).limit = limit;
+    } else {
+      this.config.limit = limit;
+    }
+    return this;
+  }
+  /**
+   * Adds an `offset` clause to the query.
+   *
+   * Calling this method will skip a number of rows when returning results from this query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+   *
+   * @param offset the `offset` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Get the 10th-20th people from this query.
+   * await db.select().from(people).offset(10).limit(10);
+   * ```
+   */
+  offset(offset) {
+    if (this.config.setOperators.length > 0) {
+      this.config.setOperators.at(-1).offset = offset;
+    } else {
+      this.config.offset = offset;
+    }
+    return this;
+  }
+  /**
+   * Adds a `FINAL` modifier to the table being read.
+   *
+   * MergeTree variants that collapse or replace rows — `ReplacingMergeTree`, `CollapsingMergeTree`,
+   * `SummingMergeTree` and friends — only do so during background merges, so a plain read can see
+   * rows that are logically superseded. `FINAL` merges the relevant parts at query time.
+   *
+   * It is expensive: ClickHouse has to read and merge whole parts rather than stream granules.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(users).final().where(eq(users.id, 42));
+   * ```
+   */
+  final() {
+    this.config.final = true;
+    return this;
+  }
+  /**
+   * Adds a `PREWHERE` clause.
+   *
+   * `PREWHERE` is applied before the remaining columns are read from disk, so filtering on a small,
+   * cheap column can avoid reading large ones entirely. ClickHouse moves conditions there on its own
+   * when `optimize_move_to_prewhere` is on; this is the manual override.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).prewhere(eq(events.type, 'click')).where(gt(events.ts, since));
+   * ```
+   */
+  prewhere(where) {
+    if (typeof where === "function") {
+      where = where(
+        new Proxy(
+          this.config.fields,
+          new import_selection_proxy.SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.prewhere = where;
+    return this;
+  }
+  /**
+   * Adds a `SAMPLE` clause, reading an approximate subset of the table.
+   *
+   * Requires the table to declare a `SAMPLE BY` key. A value below 1 is a fraction of rows; a value
+   * of 1 or more is an approximate row count.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).sample(0.1);  // roughly a tenth of the rows
+   * ```
+   */
+  sample(value, offset) {
+    this.config.sample = { value, offset };
+    return this;
+  }
+  /**
+   * Adds an `ARRAY JOIN` clause, which unfolds an array column into one row per element.
+   *
+   * @example
+   * ```ts
+   * await db.select({ tag: events.tags }).from(events).arrayJoin(events.tags);
+   * ```
+   */
+  arrayJoin(...expressions) {
+    this.config.arrayJoins = [...this.config.arrayJoins ?? [], { expressions, left: false }];
+    return this;
+  }
+  /**
+   * Adds a `LEFT ARRAY JOIN` clause, which keeps rows whose array is empty by emitting the element
+   * type's default value instead of dropping the row.
+   */
+  leftArrayJoin(...expressions) {
+    this.config.arrayJoins = [...this.config.arrayJoins ?? [], { expressions, left: true }];
+    return this;
+  }
+  /**
+   * Adds a `LIMIT n BY expr` clause, which keeps the first `n` rows *for each distinct key* rather
+   * than `n` rows overall.
+   *
+   * @example
+   * ```ts
+   * // the three most recent events per user
+   * await db.select().from(events).orderBy(desc(events.ts)).limitBy(3, events.userId);
+   * ```
+   */
+  limitBy(limit, ...expressions) {
+    this.config.limitBy = { limit, expressions };
+    return this;
+  }
+  /**
+   * Adds `WITH TOTALS` to the `GROUP BY` clause, which appends a row aggregating every group.
+   *
+   * The totals row is delivered separately by ClickHouse's JSON formats, so it does not appear in
+   * the returned rows; read it from `db.execute()` if you need it.
+   */
+  withTotals() {
+    this.config.withTotals = true;
+    return this;
+  }
+  /**
+   * Adds a query-level `SETTINGS` clause.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).settings({ max_execution_time: 30, max_threads: 4 });
+   * ```
+   */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildSelectQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  as(alias) {
+    const usedTables = [];
+    usedTables.push(...(0, import_utils2.extractUsedTable)(this.config.table));
+    if (this.config.joins) {
+      for (const it of this.config.joins) usedTables.push(...(0, import_utils2.extractUsedTable)(it.table));
+    }
+    return new Proxy(
+      new import_subquery.Subquery(this.getSQL(), this.config.fields, alias, false, [...new Set(usedTables)]),
+      new import_selection_proxy.SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+    );
+  }
+  /** @internal */
+  getSelectedFields() {
+    return new Proxy(
+      this.config.fields,
+      new import_selection_proxy.SelectionProxyHandler({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+    );
+  }
+  $dynamic() {
+    return this;
+  }
+}
+class ClickHouseSelectBase extends ClickHouseSelectQueryBuilderBase {
+  static [import_entity.entityKind] = "ClickHouseSelect";
+  prepare() {
+    if (!this.session) {
+      throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
+    }
+    const fieldsList = (0, import_utils.orderSelectedFields)(this.config.fields);
+    const query = this.session.prepareQuery(this.dialect.sqlToQuery(this.getSQL()), fieldsList, void 0, {
+      type: "select",
+      tables: [...this.usedTables]
+    }, this.cacheConfig);
+    query.joinsNotNullableMap = this.joinsNotNullableMap;
+    return query;
+  }
+  $withCache(config) {
+    this.cacheConfig = config === void 0 ? { config: {}, enable: true, autoInvalidate: true } : config === false ? { enable: false } : { enable: true, autoInvalidate: true, ...config };
+    return this;
+  }
+  execute = ((placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  });
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+}
+(0, import_utils.applyMixins)(ClickHouseSelectBase, [import_query_promise.QueryPromise]);
+function createSetOperator(type, isAll) {
+  return (leftSelect, rightSelect, ...restSelects) => {
+    const setOperators = [rightSelect, ...restSelects].map((select) => ({
+      type,
+      isAll,
+      rightSelect: select
+    }));
+    for (const setOperator of setOperators) {
+      if (!(0, import_utils.haveSameKeys)(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
+        throw new Error(
+          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
+        );
+      }
+    }
+    return leftSelect.addSetOperators(setOperators);
+  };
+}
+const getClickHouseSetOperators = () => ({
+  union,
+  unionAll,
+  intersect,
+  except,
+  minus
+});
+const union = createSetOperator("union", false);
+const unionAll = createSetOperator("union", true);
+const intersect = createSetOperator("intersect", false);
+const except = createSetOperator("except", false);
+const minus = createSetOperator("except", true);
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseSelectBase,
+  ClickHouseSelectBuilder,
+  ClickHouseSelectQueryBuilderBase,
+  except,
+  intersect,
+  minus,
+  union,
+  unionAll
+});
diff --git a/clickhouse-core/query-builders/select.d.cts b/clickhouse-core/query-builders/select.d.cts
new file mode 100644
index 0000000..b5beda9
--- /dev/null
+++ b/clickhouse-core/query-builders/select.d.cts
@@ -0,0 +1,732 @@
+import type { CacheConfig, WithCacheConfig } from '../../cache/core/types.cjs';
+import type { ClickHouseColumn } from '../columns/index.cjs';
+import type { ClickHouseDialect } from '../dialect.cjs';
+import type { ClickHouseSettings } from '../engines.cjs';
+import type { ClickHouseSession, PreparedQueryHKTBase } from '../session.cjs';
+import type { SubqueryWithSelection } from '../subquery.cjs';
+import type { ClickHouseTable } from '../table.cjs';
+import { entityKind } from '../../entity.cjs';
+import { TypedQueryBuilder } from '../../query-builders/query-builder.cjs';
+import type { BuildSubquerySelection, GetSelectTableName, GetSelectTableSelection, JoinNullability, SelectMode, SelectResult } from '../../query-builders/select.types.cjs';
+import { QueryPromise } from '../../query-promise.cjs';
+import type { ColumnsSelection, Placeholder, Query } from '../../sql/sql.cjs';
+import { SQL } from '../../sql/sql.cjs';
+import { Subquery } from '../../subquery.cjs';
+import { type ValueOrArray } from '../../utils.cjs';
+import type { ClickHouseCreateSetOperatorFn, ClickHouseCrossJoinFn, ClickHouseJoinFn, ClickHouseSelectConfig, ClickHouseSelectDynamic, ClickHouseSelectHKT, ClickHouseSelectHKTBase, ClickHouseSelectPrepare, ClickHouseSelectWithout, ClickHouseSetOperatorExcludedMethods, ClickHouseSetOperatorWithResult, CreateClickHouseSelectFromBuilderMode, GetClickHouseSetOperators, SelectedFields, SetOperatorRightSelect } from './select.types.cjs';
+export declare class ClickHouseSelectBuilder<TSelection extends SelectedFields | undefined, TPreparedQueryHKT extends PreparedQueryHKTBase, TBuilderMode extends 'db' | 'qb' = 'db'> {
+    static readonly [entityKind]: string;
+    private fields;
+    private session;
+    private dialect;
+    private withList;
+    private distinct;
+    constructor(config: {
+        fields: TSelection;
+        session: ClickHouseSession | undefined;
+        dialect: ClickHouseDialect;
+        withList?: Subquery[];
+        distinct?: boolean;
+    });
+    from<TFrom extends ClickHouseTable | Subquery | SQL>(// | ClickHouseViewBase
+    source: TFrom): CreateClickHouseSelectFromBuilderMode<TBuilderMode, GetSelectTableName<TFrom>, TSelection extends undefined ? GetSelectTableSelection<TFrom> : TSelection, TSelection extends undefined ? 'single' : 'partial', TPreparedQueryHKT>;
+}
+export declare abstract class ClickHouseSelectQueryBuilderBase<THKT extends ClickHouseSelectHKTBase, TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> extends TypedQueryBuilder<TSelectedFields, TResult> {
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly hkt: THKT;
+        readonly tableName: TTableName;
+        readonly selection: TSelection;
+        readonly selectMode: TSelectMode;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly nullabilityMap: TNullabilityMap;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: TResult;
+        readonly selectedFields: TSelectedFields;
+        readonly config: ClickHouseSelectConfig;
+    };
+    protected config: ClickHouseSelectConfig;
+    protected joinsNotNullableMap: Record<string, boolean>;
+    private tableName;
+    private isPartialSelect;
+    protected dialect: ClickHouseDialect;
+    protected cacheConfig?: WithCacheConfig;
+    protected usedTables: Set<string>;
+    constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }: {
+        table: ClickHouseSelectConfig['table'];
+        fields: ClickHouseSelectConfig['fields'];
+        isPartialSelect: boolean;
+        session: ClickHouseSession | undefined;
+        dialect: ClickHouseDialect;
+        withList: Subquery[];
+        distinct: boolean | undefined;
+    });
+    private createJoin;
+    /**
+     * Executes a `left join` operation by adding another table to the current query.
+     *
+     * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User; pets: Pet | null; }[] = await db.select()
+     *   .from(users)
+     *   .leftJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number | null; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .leftJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    leftJoin: ClickHouseJoinFn<this, TDynamic, "left", false>;
+    /**
+     * Executes a `left join lateral` operation by adding subquery to the current query.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#left-join-lateral}
+     *
+     * @param table the subquery to join.
+     * @param on the `on` clause.
+     */
+    leftJoinLateral: ClickHouseJoinFn<this, TDynamic, "left", true>;
+    /**
+     * Executes a `right join` operation by adding another table to the current query.
+     *
+     * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User | null; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .rightJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number | null; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .rightJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    rightJoin: ClickHouseJoinFn<this, TDynamic, "right", false>;
+    /**
+     * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
+     *
+     * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .innerJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .innerJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    innerJoin: ClickHouseJoinFn<this, TDynamic, "inner", false>;
+    /**
+     * Executes an `inner join lateral` operation, creating a new table by combining rows from two queries that have matching values.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join-lateral}
+     *
+     * @param table the subquery to join.
+     * @param on the `on` clause.
+     */
+    innerJoinLateral: ClickHouseJoinFn<this, TDynamic, "inner", true>;
+    /**
+     * Executes a `full join` operation by combining rows from two tables into a new table.
+     *
+     * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User | null; pets: Pet | null; }[] = await db.select()
+     *   .from(users)
+     *   .fullJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number | null; petId: number | null; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .fullJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    fullJoin: ClickHouseJoinFn<this, TDynamic, "full", false>;
+    /**
+     * Executes a `cross join` operation by combining rows from two tables into a new table.
+     *
+     * Calling this method retrieves all rows from both main and joined tables, merging all rows from each table.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join}
+     *
+     * @param table the table to join.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users, each user with every pet
+     * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .crossJoin(pets)
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .crossJoin(pets)
+     * ```
+     */
+    crossJoin: ClickHouseCrossJoinFn<this, TDynamic, false>;
+    /**
+     * Executes a `cross join lateral` operation by combining rows from two queries into a new table.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method retrieves all rows from both main and joined queries, merging all rows from each query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join-lateral}
+     *
+     * @param table the query to join.
+     */
+    crossJoinLateral: ClickHouseCrossJoinFn<this, TDynamic, true>;
+    private createSetOperator;
+    /**
+     * Adds `union` set operator to the query.
+     *
+     * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all unique names from customers and users tables
+     * await db.select({ name: users.name })
+     *   .from(users)
+     *   .union(
+     *     db.select({ name: customers.name }).from(customers)
+     *   );
+     * // or
+     * import { union } from 'drizzle-orm/clickhouse-core'
+     *
+     * await union(
+     *   db.select({ name: users.name }).from(users),
+     *   db.select({ name: customers.name }).from(customers)
+     * );
+     * ```
+     */
+    union: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `union all` set operator to the query.
+     *
+     * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all transaction ids from both online and in-store sales
+     * await db.select({ transaction: onlineSales.transactionId })
+     *   .from(onlineSales)
+     *   .unionAll(
+     *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+     *   );
+     * // or
+     * import { unionAll } from 'drizzle-orm/clickhouse-core'
+     *
+     * await unionAll(
+     *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+     *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+     * );
+     * ```
+     */
+    unionAll: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `intersect` set operator to the query.
+     *
+     * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select course names that are offered in both departments A and B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .intersect(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { intersect } from 'drizzle-orm/clickhouse-core'
+     *
+     * await intersect(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    intersect: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `except` set operator to the query.
+     *
+     * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all courses offered in department A but not in department B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .except(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { except } from 'drizzle-orm/clickhouse-core'
+     *
+     * await except(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    except: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `minus` set operator to the query.
+     *
+     * This is an alias of `except` supported by ClickHouse.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all courses offered in department A but not in department B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .minus(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { minus } from 'drizzle-orm/clickhouse-core'
+     *
+     * await minus(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    minus: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds a `where` clause to the query.
+     *
+     * Calling this method will select only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
+     *
+     * @param where the `where` clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be selected.
+     *
+     * ```ts
+     * // Select all cars with green color
+     * await db.select().from(cars).where(eq(cars.color, 'green'));
+     * // or
+     * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Select all BMW cars with a green color
+     * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Select all cars with the green or blue color
+     * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'where'>;
+    /**
+     * Adds a `having` clause to the query.
+     *
+     * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+     *
+     * @param having the `having` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all brands with more than one car
+     * await db.select({
+     * 	brand: cars.brand,
+     * 	count: sql<number>`cast(count(${cars.id}) as int)`,
+     * })
+     *   .from(cars)
+     *   .groupBy(cars.brand)
+     *   .having(({ count }) => gt(count, 1));
+     * ```
+     */
+    having(having: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'having'>;
+    /**
+     * Adds a `group by` clause to the query.
+     *
+     * Calling this method will group rows that have the same values into summary rows, often used for aggregation purposes.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+     *
+     * @example
+     *
+     * ```ts
+     * // Group and count people by their last names
+     * await db.select({
+     *    lastName: people.lastName,
+     *    count: sql<number>`cast(count(*) as int)`
+     * })
+     *   .from(people)
+     *   .groupBy(people.lastName);
+     * ```
+     */
+    groupBy(builder: (aliases: this['_']['selection']) => ValueOrArray<ClickHouseColumn | SQL | SQL.Aliased>): ClickHouseSelectWithout<this, TDynamic, 'groupBy'>;
+    groupBy(...columns: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'groupBy'>;
+    /**
+     * Adds an `order by` clause to the query.
+     *
+     * Calling this method will sort the result-set in ascending or descending order. By default, the sort order is ascending.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#order-by}
+     *
+     * @example
+     *
+     * ```
+     * // Select cars ordered by year
+     * await db.select().from(cars).orderBy(cars.year);
+     * ```
+     *
+     * You can specify whether results are in ascending or descending order with the `asc()` and `desc()` operators.
+     *
+     * ```ts
+     * // Select cars ordered by year in descending order
+     * await db.select().from(cars).orderBy(desc(cars.year));
+     *
+     * // Select cars ordered by year and price
+     * await db.select().from(cars).orderBy(asc(cars.year), desc(cars.price));
+     * ```
+     */
+    orderBy(builder: (aliases: this['_']['selection']) => ValueOrArray<ClickHouseColumn | SQL | SQL.Aliased>): ClickHouseSelectWithout<this, TDynamic, 'orderBy'>;
+    orderBy(...columns: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'orderBy'>;
+    /**
+     * Adds a `limit` clause to the query.
+     *
+     * Calling this method will set the maximum number of rows that will be returned by this query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+     *
+     * @param limit the `limit` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Get the first 10 people from this query.
+     * await db.select().from(people).limit(10);
+     * ```
+     */
+    limit(limit: number): ClickHouseSelectWithout<this, TDynamic, 'limit'>;
+    /**
+     * Adds an `offset` clause to the query.
+     *
+     * Calling this method will skip a number of rows when returning results from this query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+     *
+     * @param offset the `offset` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Get the 10th-20th people from this query.
+     * await db.select().from(people).offset(10).limit(10);
+     * ```
+     */
+    offset(offset: number): ClickHouseSelectWithout<this, TDynamic, 'offset'>;
+    /**
+     * Adds a `FINAL` modifier to the table being read.
+     *
+     * MergeTree variants that collapse or replace rows — `ReplacingMergeTree`, `CollapsingMergeTree`,
+     * `SummingMergeTree` and friends — only do so during background merges, so a plain read can see
+     * rows that are logically superseded. `FINAL` merges the relevant parts at query time.
+     *
+     * It is expensive: ClickHouse has to read and merge whole parts rather than stream granules.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(users).final().where(eq(users.id, 42));
+     * ```
+     */
+    final(): ClickHouseSelectWithout<this, TDynamic, 'final'>;
+    /**
+     * Adds a `PREWHERE` clause.
+     *
+     * `PREWHERE` is applied before the remaining columns are read from disk, so filtering on a small,
+     * cheap column can avoid reading large ones entirely. ClickHouse moves conditions there on its own
+     * when `optimize_move_to_prewhere` is on; this is the manual override.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).prewhere(eq(events.type, 'click')).where(gt(events.ts, since));
+     * ```
+     */
+    prewhere(where: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'prewhere'>;
+    /**
+     * Adds a `SAMPLE` clause, reading an approximate subset of the table.
+     *
+     * Requires the table to declare a `SAMPLE BY` key. A value below 1 is a fraction of rows; a value
+     * of 1 or more is an approximate row count.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).sample(0.1);  // roughly a tenth of the rows
+     * ```
+     */
+    sample(value: number, offset?: number): ClickHouseSelectWithout<this, TDynamic, 'sample'>;
+    /**
+     * Adds an `ARRAY JOIN` clause, which unfolds an array column into one row per element.
+     *
+     * @example
+     * ```ts
+     * await db.select({ tag: events.tags }).from(events).arrayJoin(events.tags);
+     * ```
+     */
+    arrayJoin(...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, never>;
+    /**
+     * Adds a `LEFT ARRAY JOIN` clause, which keeps rows whose array is empty by emitting the element
+     * type's default value instead of dropping the row.
+     */
+    leftArrayJoin(...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, never>;
+    /**
+     * Adds a `LIMIT n BY expr` clause, which keeps the first `n` rows *for each distinct key* rather
+     * than `n` rows overall.
+     *
+     * @example
+     * ```ts
+     * // the three most recent events per user
+     * await db.select().from(events).orderBy(desc(events.ts)).limitBy(3, events.userId);
+     * ```
+     */
+    limitBy(limit: number | Placeholder, ...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'limitBy'>;
+    /**
+     * Adds `WITH TOTALS` to the `GROUP BY` clause, which appends a row aggregating every group.
+     *
+     * The totals row is delivered separately by ClickHouse's JSON formats, so it does not appear in
+     * the returned rows; read it from `db.execute()` if you need it.
+     */
+    withTotals(): ClickHouseSelectWithout<this, TDynamic, 'withTotals'>;
+    /**
+     * Adds a query-level `SETTINGS` clause.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).settings({ max_execution_time: 30, max_threads: 4 });
+     * ```
+     */
+    settings(settings: ClickHouseSettings): ClickHouseSelectWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    as<TAlias extends string>(alias: TAlias): SubqueryWithSelection<this['_']['selectedFields'], TAlias>;
+    $dynamic(): ClickHouseSelectDynamic<this>;
+}
+export interface ClickHouseSelectBase<TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> extends ClickHouseSelectQueryBuilderBase<ClickHouseSelectHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, QueryPromise<TResult> {
+}
+export declare class ClickHouseSelectBase<TTableName extends string | undefined, TSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields = BuildSubquerySelection<TSelection, TNullabilityMap>> extends ClickHouseSelectQueryBuilderBase<ClickHouseSelectHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields> {
+    static readonly [entityKind]: string;
+    prepare(): ClickHouseSelectPrepare<this>;
+    $withCache(config?: {
+        config?: CacheConfig;
+        tag?: string;
+        autoInvalidate?: boolean;
+    } | false): this;
+    execute: ReturnType<this["prepare"]>["execute"];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+}
+/**
+ * Adds `union` set operator to the query.
+ *
+ * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all unique names from customers and users tables
+ * import { union } from 'drizzle-orm/clickhouse-core'
+ *
+ * await union(
+ *   db.select({ name: users.name }).from(users),
+ *   db.select({ name: customers.name }).from(customers)
+ * );
+ * // or
+ * await db.select({ name: users.name })
+ *   .from(users)
+ *   .union(
+ *     db.select({ name: customers.name }).from(customers)
+ *   );
+ * ```
+ */
+export declare const union: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `union all` set operator to the query.
+ *
+ * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all transaction ids from both online and in-store sales
+ * import { unionAll } from 'drizzle-orm/clickhouse-core'
+ *
+ * await unionAll(
+ *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+ *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+ * );
+ * // or
+ * await db.select({ transaction: onlineSales.transactionId })
+ *   .from(onlineSales)
+ *   .unionAll(
+ *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+ *   );
+ * ```
+ */
+export declare const unionAll: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `intersect` set operator to the query.
+ *
+ * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select course names that are offered in both departments A and B
+ * import { intersect } from 'drizzle-orm/clickhouse-core'
+ *
+ * await intersect(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .intersect(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const intersect: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `except` set operator to the query.
+ *
+ * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all courses offered in department A but not in department B
+ * import { except } from 'drizzle-orm/clickhouse-core'
+ *
+ * await except(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .except(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const except: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `minus` set operator to the query.
+ *
+ * This is an alias of `except` supported by ClickHouse.
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all courses offered in department A but not in department B
+ * import { minus } from 'drizzle-orm/clickhouse-core'
+ *
+ * await minus(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .minus(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const minus: ClickHouseCreateSetOperatorFn;
diff --git a/clickhouse-core/query-builders/select.d.ts b/clickhouse-core/query-builders/select.d.ts
new file mode 100644
index 0000000..23d0451
--- /dev/null
+++ b/clickhouse-core/query-builders/select.d.ts
@@ -0,0 +1,732 @@
+import type { CacheConfig, WithCacheConfig } from '../../cache/core/types.js';
+import type { ClickHouseColumn } from '../columns/index.js';
+import type { ClickHouseDialect } from '../dialect.js';
+import type { ClickHouseSettings } from '../engines.js';
+import type { ClickHouseSession, PreparedQueryHKTBase } from '../session.js';
+import type { SubqueryWithSelection } from '../subquery.js';
+import type { ClickHouseTable } from '../table.js';
+import { entityKind } from '../../entity.js';
+import { TypedQueryBuilder } from '../../query-builders/query-builder.js';
+import type { BuildSubquerySelection, GetSelectTableName, GetSelectTableSelection, JoinNullability, SelectMode, SelectResult } from '../../query-builders/select.types.js';
+import { QueryPromise } from '../../query-promise.js';
+import type { ColumnsSelection, Placeholder, Query } from '../../sql/sql.js';
+import { SQL } from '../../sql/sql.js';
+import { Subquery } from '../../subquery.js';
+import { type ValueOrArray } from '../../utils.js';
+import type { ClickHouseCreateSetOperatorFn, ClickHouseCrossJoinFn, ClickHouseJoinFn, ClickHouseSelectConfig, ClickHouseSelectDynamic, ClickHouseSelectHKT, ClickHouseSelectHKTBase, ClickHouseSelectPrepare, ClickHouseSelectWithout, ClickHouseSetOperatorExcludedMethods, ClickHouseSetOperatorWithResult, CreateClickHouseSelectFromBuilderMode, GetClickHouseSetOperators, SelectedFields, SetOperatorRightSelect } from './select.types.js';
+export declare class ClickHouseSelectBuilder<TSelection extends SelectedFields | undefined, TPreparedQueryHKT extends PreparedQueryHKTBase, TBuilderMode extends 'db' | 'qb' = 'db'> {
+    static readonly [entityKind]: string;
+    private fields;
+    private session;
+    private dialect;
+    private withList;
+    private distinct;
+    constructor(config: {
+        fields: TSelection;
+        session: ClickHouseSession | undefined;
+        dialect: ClickHouseDialect;
+        withList?: Subquery[];
+        distinct?: boolean;
+    });
+    from<TFrom extends ClickHouseTable | Subquery | SQL>(// | ClickHouseViewBase
+    source: TFrom): CreateClickHouseSelectFromBuilderMode<TBuilderMode, GetSelectTableName<TFrom>, TSelection extends undefined ? GetSelectTableSelection<TFrom> : TSelection, TSelection extends undefined ? 'single' : 'partial', TPreparedQueryHKT>;
+}
+export declare abstract class ClickHouseSelectQueryBuilderBase<THKT extends ClickHouseSelectHKTBase, TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> extends TypedQueryBuilder<TSelectedFields, TResult> {
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly hkt: THKT;
+        readonly tableName: TTableName;
+        readonly selection: TSelection;
+        readonly selectMode: TSelectMode;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly nullabilityMap: TNullabilityMap;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: TResult;
+        readonly selectedFields: TSelectedFields;
+        readonly config: ClickHouseSelectConfig;
+    };
+    protected config: ClickHouseSelectConfig;
+    protected joinsNotNullableMap: Record<string, boolean>;
+    private tableName;
+    private isPartialSelect;
+    protected dialect: ClickHouseDialect;
+    protected cacheConfig?: WithCacheConfig;
+    protected usedTables: Set<string>;
+    constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }: {
+        table: ClickHouseSelectConfig['table'];
+        fields: ClickHouseSelectConfig['fields'];
+        isPartialSelect: boolean;
+        session: ClickHouseSession | undefined;
+        dialect: ClickHouseDialect;
+        withList: Subquery[];
+        distinct: boolean | undefined;
+    });
+    private createJoin;
+    /**
+     * Executes a `left join` operation by adding another table to the current query.
+     *
+     * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User; pets: Pet | null; }[] = await db.select()
+     *   .from(users)
+     *   .leftJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number | null; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .leftJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    leftJoin: ClickHouseJoinFn<this, TDynamic, "left", false>;
+    /**
+     * Executes a `left join lateral` operation by adding subquery to the current query.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#left-join-lateral}
+     *
+     * @param table the subquery to join.
+     * @param on the `on` clause.
+     */
+    leftJoinLateral: ClickHouseJoinFn<this, TDynamic, "left", true>;
+    /**
+     * Executes a `right join` operation by adding another table to the current query.
+     *
+     * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User | null; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .rightJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number | null; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .rightJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    rightJoin: ClickHouseJoinFn<this, TDynamic, "right", false>;
+    /**
+     * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
+     *
+     * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .innerJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .innerJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    innerJoin: ClickHouseJoinFn<this, TDynamic, "inner", false>;
+    /**
+     * Executes an `inner join lateral` operation, creating a new table by combining rows from two queries that have matching values.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join-lateral}
+     *
+     * @param table the subquery to join.
+     * @param on the `on` clause.
+     */
+    innerJoinLateral: ClickHouseJoinFn<this, TDynamic, "inner", true>;
+    /**
+     * Executes a `full join` operation by combining rows from two tables into a new table.
+     *
+     * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
+     *
+     * @param table the table to join.
+     * @param on the `on` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users and their pets
+     * const usersWithPets: { user: User | null; pets: Pet | null; }[] = await db.select()
+     *   .from(users)
+     *   .fullJoin(pets, eq(users.id, pets.ownerId))
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number | null; petId: number | null; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .fullJoin(pets, eq(users.id, pets.ownerId))
+     * ```
+     */
+    fullJoin: ClickHouseJoinFn<this, TDynamic, "full", false>;
+    /**
+     * Executes a `cross join` operation by combining rows from two tables into a new table.
+     *
+     * Calling this method retrieves all rows from both main and joined tables, merging all rows from each table.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join}
+     *
+     * @param table the table to join.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all users, each user with every pet
+     * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+     *   .from(users)
+     *   .crossJoin(pets)
+     *
+     * // Select userId and petId
+     * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+     *   userId: users.id,
+     *   petId: pets.id,
+     * })
+     *   .from(users)
+     *   .crossJoin(pets)
+     * ```
+     */
+    crossJoin: ClickHouseCrossJoinFn<this, TDynamic, false>;
+    /**
+     * Executes a `cross join lateral` operation by combining rows from two queries into a new table.
+     *
+     * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+     *
+     * Calling this method retrieves all rows from both main and joined queries, merging all rows from each query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join-lateral}
+     *
+     * @param table the query to join.
+     */
+    crossJoinLateral: ClickHouseCrossJoinFn<this, TDynamic, true>;
+    private createSetOperator;
+    /**
+     * Adds `union` set operator to the query.
+     *
+     * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all unique names from customers and users tables
+     * await db.select({ name: users.name })
+     *   .from(users)
+     *   .union(
+     *     db.select({ name: customers.name }).from(customers)
+     *   );
+     * // or
+     * import { union } from 'drizzle-orm/clickhouse-core'
+     *
+     * await union(
+     *   db.select({ name: users.name }).from(users),
+     *   db.select({ name: customers.name }).from(customers)
+     * );
+     * ```
+     */
+    union: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `union all` set operator to the query.
+     *
+     * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all transaction ids from both online and in-store sales
+     * await db.select({ transaction: onlineSales.transactionId })
+     *   .from(onlineSales)
+     *   .unionAll(
+     *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+     *   );
+     * // or
+     * import { unionAll } from 'drizzle-orm/clickhouse-core'
+     *
+     * await unionAll(
+     *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+     *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+     * );
+     * ```
+     */
+    unionAll: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `intersect` set operator to the query.
+     *
+     * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select course names that are offered in both departments A and B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .intersect(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { intersect } from 'drizzle-orm/clickhouse-core'
+     *
+     * await intersect(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    intersect: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `except` set operator to the query.
+     *
+     * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all courses offered in department A but not in department B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .except(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { except } from 'drizzle-orm/clickhouse-core'
+     *
+     * await except(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    except: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds `minus` set operator to the query.
+     *
+     * This is an alias of `except` supported by ClickHouse.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all courses offered in department A but not in department B
+     * await db.select({ courseName: depA.courseName })
+     *   .from(depA)
+     *   .minus(
+     *     db.select({ courseName: depB.courseName }).from(depB)
+     *   );
+     * // or
+     * import { minus } from 'drizzle-orm/clickhouse-core'
+     *
+     * await minus(
+     *   db.select({ courseName: depA.courseName }).from(depA),
+     *   db.select({ courseName: depB.courseName }).from(depB)
+     * );
+     * ```
+     */
+    minus: <TValue extends ClickHouseSetOperatorWithResult<TResult>>(rightSelection: ((setOperators: GetClickHouseSetOperators) => SetOperatorRightSelect<TValue, TResult>) | SetOperatorRightSelect<TValue, TResult>) => ClickHouseSelectWithout<this, TDynamic, ClickHouseSetOperatorExcludedMethods, true>;
+    /**
+     * Adds a `where` clause to the query.
+     *
+     * Calling this method will select only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
+     *
+     * @param where the `where` clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be selected.
+     *
+     * ```ts
+     * // Select all cars with green color
+     * await db.select().from(cars).where(eq(cars.color, 'green'));
+     * // or
+     * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Select all BMW cars with a green color
+     * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Select all cars with the green or blue color
+     * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'where'>;
+    /**
+     * Adds a `having` clause to the query.
+     *
+     * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+     *
+     * @param having the `having` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Select all brands with more than one car
+     * await db.select({
+     * 	brand: cars.brand,
+     * 	count: sql<number>`cast(count(${cars.id}) as int)`,
+     * })
+     *   .from(cars)
+     *   .groupBy(cars.brand)
+     *   .having(({ count }) => gt(count, 1));
+     * ```
+     */
+    having(having: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'having'>;
+    /**
+     * Adds a `group by` clause to the query.
+     *
+     * Calling this method will group rows that have the same values into summary rows, often used for aggregation purposes.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+     *
+     * @example
+     *
+     * ```ts
+     * // Group and count people by their last names
+     * await db.select({
+     *    lastName: people.lastName,
+     *    count: sql<number>`cast(count(*) as int)`
+     * })
+     *   .from(people)
+     *   .groupBy(people.lastName);
+     * ```
+     */
+    groupBy(builder: (aliases: this['_']['selection']) => ValueOrArray<ClickHouseColumn | SQL | SQL.Aliased>): ClickHouseSelectWithout<this, TDynamic, 'groupBy'>;
+    groupBy(...columns: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'groupBy'>;
+    /**
+     * Adds an `order by` clause to the query.
+     *
+     * Calling this method will sort the result-set in ascending or descending order. By default, the sort order is ascending.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#order-by}
+     *
+     * @example
+     *
+     * ```
+     * // Select cars ordered by year
+     * await db.select().from(cars).orderBy(cars.year);
+     * ```
+     *
+     * You can specify whether results are in ascending or descending order with the `asc()` and `desc()` operators.
+     *
+     * ```ts
+     * // Select cars ordered by year in descending order
+     * await db.select().from(cars).orderBy(desc(cars.year));
+     *
+     * // Select cars ordered by year and price
+     * await db.select().from(cars).orderBy(asc(cars.year), desc(cars.price));
+     * ```
+     */
+    orderBy(builder: (aliases: this['_']['selection']) => ValueOrArray<ClickHouseColumn | SQL | SQL.Aliased>): ClickHouseSelectWithout<this, TDynamic, 'orderBy'>;
+    orderBy(...columns: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'orderBy'>;
+    /**
+     * Adds a `limit` clause to the query.
+     *
+     * Calling this method will set the maximum number of rows that will be returned by this query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+     *
+     * @param limit the `limit` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Get the first 10 people from this query.
+     * await db.select().from(people).limit(10);
+     * ```
+     */
+    limit(limit: number): ClickHouseSelectWithout<this, TDynamic, 'limit'>;
+    /**
+     * Adds an `offset` clause to the query.
+     *
+     * Calling this method will skip a number of rows when returning results from this query.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+     *
+     * @param offset the `offset` clause.
+     *
+     * @example
+     *
+     * ```ts
+     * // Get the 10th-20th people from this query.
+     * await db.select().from(people).offset(10).limit(10);
+     * ```
+     */
+    offset(offset: number): ClickHouseSelectWithout<this, TDynamic, 'offset'>;
+    /**
+     * Adds a `FINAL` modifier to the table being read.
+     *
+     * MergeTree variants that collapse or replace rows — `ReplacingMergeTree`, `CollapsingMergeTree`,
+     * `SummingMergeTree` and friends — only do so during background merges, so a plain read can see
+     * rows that are logically superseded. `FINAL` merges the relevant parts at query time.
+     *
+     * It is expensive: ClickHouse has to read and merge whole parts rather than stream granules.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(users).final().where(eq(users.id, 42));
+     * ```
+     */
+    final(): ClickHouseSelectWithout<this, TDynamic, 'final'>;
+    /**
+     * Adds a `PREWHERE` clause.
+     *
+     * `PREWHERE` is applied before the remaining columns are read from disk, so filtering on a small,
+     * cheap column can avoid reading large ones entirely. ClickHouse moves conditions there on its own
+     * when `optimize_move_to_prewhere` is on; this is the manual override.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).prewhere(eq(events.type, 'click')).where(gt(events.ts, since));
+     * ```
+     */
+    prewhere(where: ((aliases: this['_']['selection']) => SQL | undefined) | SQL | undefined): ClickHouseSelectWithout<this, TDynamic, 'prewhere'>;
+    /**
+     * Adds a `SAMPLE` clause, reading an approximate subset of the table.
+     *
+     * Requires the table to declare a `SAMPLE BY` key. A value below 1 is a fraction of rows; a value
+     * of 1 or more is an approximate row count.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).sample(0.1);  // roughly a tenth of the rows
+     * ```
+     */
+    sample(value: number, offset?: number): ClickHouseSelectWithout<this, TDynamic, 'sample'>;
+    /**
+     * Adds an `ARRAY JOIN` clause, which unfolds an array column into one row per element.
+     *
+     * @example
+     * ```ts
+     * await db.select({ tag: events.tags }).from(events).arrayJoin(events.tags);
+     * ```
+     */
+    arrayJoin(...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, never>;
+    /**
+     * Adds a `LEFT ARRAY JOIN` clause, which keeps rows whose array is empty by emitting the element
+     * type's default value instead of dropping the row.
+     */
+    leftArrayJoin(...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, never>;
+    /**
+     * Adds a `LIMIT n BY expr` clause, which keeps the first `n` rows *for each distinct key* rather
+     * than `n` rows overall.
+     *
+     * @example
+     * ```ts
+     * // the three most recent events per user
+     * await db.select().from(events).orderBy(desc(events.ts)).limitBy(3, events.userId);
+     * ```
+     */
+    limitBy(limit: number | Placeholder, ...expressions: (ClickHouseColumn | SQL | SQL.Aliased)[]): ClickHouseSelectWithout<this, TDynamic, 'limitBy'>;
+    /**
+     * Adds `WITH TOTALS` to the `GROUP BY` clause, which appends a row aggregating every group.
+     *
+     * The totals row is delivered separately by ClickHouse's JSON formats, so it does not appear in
+     * the returned rows; read it from `db.execute()` if you need it.
+     */
+    withTotals(): ClickHouseSelectWithout<this, TDynamic, 'withTotals'>;
+    /**
+     * Adds a query-level `SETTINGS` clause.
+     *
+     * @example
+     * ```ts
+     * await db.select().from(events).settings({ max_execution_time: 30, max_threads: 4 });
+     * ```
+     */
+    settings(settings: ClickHouseSettings): ClickHouseSelectWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    as<TAlias extends string>(alias: TAlias): SubqueryWithSelection<this['_']['selectedFields'], TAlias>;
+    $dynamic(): ClickHouseSelectDynamic<this>;
+}
+export interface ClickHouseSelectBase<TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> extends ClickHouseSelectQueryBuilderBase<ClickHouseSelectHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, QueryPromise<TResult> {
+}
+export declare class ClickHouseSelectBase<TTableName extends string | undefined, TSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields = BuildSubquerySelection<TSelection, TNullabilityMap>> extends ClickHouseSelectQueryBuilderBase<ClickHouseSelectHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields> {
+    static readonly [entityKind]: string;
+    prepare(): ClickHouseSelectPrepare<this>;
+    $withCache(config?: {
+        config?: CacheConfig;
+        tag?: string;
+        autoInvalidate?: boolean;
+    } | false): this;
+    execute: ReturnType<this["prepare"]>["execute"];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+}
+/**
+ * Adds `union` set operator to the query.
+ *
+ * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all unique names from customers and users tables
+ * import { union } from 'drizzle-orm/clickhouse-core'
+ *
+ * await union(
+ *   db.select({ name: users.name }).from(users),
+ *   db.select({ name: customers.name }).from(customers)
+ * );
+ * // or
+ * await db.select({ name: users.name })
+ *   .from(users)
+ *   .union(
+ *     db.select({ name: customers.name }).from(customers)
+ *   );
+ * ```
+ */
+export declare const union: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `union all` set operator to the query.
+ *
+ * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all transaction ids from both online and in-store sales
+ * import { unionAll } from 'drizzle-orm/clickhouse-core'
+ *
+ * await unionAll(
+ *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+ *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+ * );
+ * // or
+ * await db.select({ transaction: onlineSales.transactionId })
+ *   .from(onlineSales)
+ *   .unionAll(
+ *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+ *   );
+ * ```
+ */
+export declare const unionAll: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `intersect` set operator to the query.
+ *
+ * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select course names that are offered in both departments A and B
+ * import { intersect } from 'drizzle-orm/clickhouse-core'
+ *
+ * await intersect(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .intersect(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const intersect: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `except` set operator to the query.
+ *
+ * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+ *
+ * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all courses offered in department A but not in department B
+ * import { except } from 'drizzle-orm/clickhouse-core'
+ *
+ * await except(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .except(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const except: ClickHouseCreateSetOperatorFn;
+/**
+ * Adds `minus` set operator to the query.
+ *
+ * This is an alias of `except` supported by ClickHouse.
+ *
+ * @example
+ *
+ * ```ts
+ * // Select all courses offered in department A but not in department B
+ * import { minus } from 'drizzle-orm/clickhouse-core'
+ *
+ * await minus(
+ *   db.select({ courseName: depA.courseName }).from(depA),
+ *   db.select({ courseName: depB.courseName }).from(depB)
+ * );
+ * // or
+ * await db.select({ courseName: depA.courseName })
+ *   .from(depA)
+ *   .minus(
+ *     db.select({ courseName: depB.courseName }).from(depB)
+ *   );
+ * ```
+ */
+export declare const minus: ClickHouseCreateSetOperatorFn;
diff --git a/clickhouse-core/query-builders/select.js b/clickhouse-core/query-builders/select.js
new file mode 100644
index 0000000..2a57e2a
--- /dev/null
+++ b/clickhouse-core/query-builders/select.js
@@ -0,0 +1,850 @@
+import { entityKind, is } from "../../entity.js";
+import { TypedQueryBuilder } from "../../query-builders/query-builder.js";
+import { QueryPromise } from "../../query-promise.js";
+import { SelectionProxyHandler } from "../../selection-proxy.js";
+import { SQL } from "../../sql/sql.js";
+import { Subquery } from "../../subquery.js";
+import { Table } from "../../table.js";
+import {
+  applyMixins,
+  getTableColumns,
+  getTableLikeName,
+  haveSameKeys,
+  orderSelectedFields
+} from "../../utils.js";
+import { extractUsedTable } from "../utils.js";
+class ClickHouseSelectBuilder {
+  static [entityKind] = "ClickHouseSelectBuilder";
+  fields;
+  session;
+  dialect;
+  withList = [];
+  distinct;
+  constructor(config) {
+    this.fields = config.fields;
+    this.session = config.session;
+    this.dialect = config.dialect;
+    if (config.withList) {
+      this.withList = config.withList;
+    }
+    this.distinct = config.distinct;
+  }
+  from(source) {
+    const isPartialSelect = !!this.fields;
+    let fields;
+    if (this.fields) {
+      fields = this.fields;
+    } else if (is(source, Subquery)) {
+      fields = Object.fromEntries(
+        Object.keys(source._.selectedFields).map((key) => [key, source[key]])
+      );
+    } else if (is(source, SQL)) {
+      fields = {};
+    } else {
+      fields = getTableColumns(source);
+    }
+    return new ClickHouseSelectBase(
+      {
+        table: source,
+        fields,
+        isPartialSelect,
+        session: this.session,
+        dialect: this.dialect,
+        withList: this.withList,
+        distinct: this.distinct
+      }
+    );
+  }
+}
+class ClickHouseSelectQueryBuilderBase extends TypedQueryBuilder {
+  static [entityKind] = "ClickHouseSelectQueryBuilder";
+  _;
+  config;
+  joinsNotNullableMap;
+  tableName;
+  isPartialSelect;
+  /** @internal */
+  session;
+  dialect;
+  cacheConfig = void 0;
+  usedTables = /* @__PURE__ */ new Set();
+  constructor({ table, fields, isPartialSelect, session, dialect, withList, distinct }) {
+    super();
+    this.config = {
+      withList,
+      table,
+      fields: { ...fields },
+      distinct,
+      setOperators: []
+    };
+    this.isPartialSelect = isPartialSelect;
+    this.session = session;
+    this.dialect = dialect;
+    this._ = {
+      selectedFields: fields,
+      config: this.config
+    };
+    this.tableName = getTableLikeName(table);
+    this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
+    for (const item of extractUsedTable(table)) this.usedTables.add(item);
+  }
+  /** @internal */
+  getUsedTables() {
+    return [...this.usedTables];
+  }
+  createJoin(joinType, lateral) {
+    return (table, on) => {
+      const baseTableName = this.tableName;
+      const tableName = getTableLikeName(table);
+      for (const item of extractUsedTable(table)) this.usedTables.add(item);
+      if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
+        throw new Error(`Alias "${tableName}" is already used in this query`);
+      }
+      if (!this.isPartialSelect) {
+        if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
+          this.config.fields = {
+            [baseTableName]: this.config.fields
+          };
+        }
+        if (typeof tableName === "string" && !is(table, SQL)) {
+          const selection = is(table, Subquery) ? table._.selectedFields : table[Table.Symbol.Columns];
+          this.config.fields[tableName] = selection;
+        }
+      }
+      if (typeof on === "function") {
+        on = on(
+          new Proxy(
+            this.config.fields,
+            new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+          )
+        );
+      }
+      if (!this.config.joins) {
+        this.config.joins = [];
+      }
+      this.config.joins.push({ on, table, joinType, alias: tableName, lateral });
+      if (typeof tableName === "string") {
+        switch (joinType) {
+          case "left": {
+            this.joinsNotNullableMap[tableName] = false;
+            break;
+          }
+          case "right": {
+            this.joinsNotNullableMap = Object.fromEntries(
+              Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
+            );
+            this.joinsNotNullableMap[tableName] = true;
+            break;
+          }
+          case "cross":
+          case "inner": {
+            this.joinsNotNullableMap[tableName] = true;
+            break;
+          }
+          case "full": {
+            this.joinsNotNullableMap = Object.fromEntries(
+              Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false])
+            );
+            this.joinsNotNullableMap[tableName] = false;
+            break;
+          }
+        }
+      }
+      return this;
+    };
+  }
+  /**
+   * Executes a `left join` operation by adding another table to the current query.
+   *
+   * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#left-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User; pets: Pet | null; }[] = await db.select()
+   *   .from(users)
+   *   .leftJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number | null; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .leftJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  leftJoin = this.createJoin("left", false);
+  /**
+   * Executes a `left join lateral` operation by adding subquery to the current query.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method associates each row of the table with the corresponding row from the joined table, if a match is found. If no matching row exists, it sets all columns of the joined table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#left-join-lateral}
+   *
+   * @param table the subquery to join.
+   * @param on the `on` clause.
+   */
+  leftJoinLateral = this.createJoin("left", true);
+  /**
+   * Executes a `right join` operation by adding another table to the current query.
+   *
+   * Calling this method associates each row of the joined table with the corresponding row from the main table, if a match is found. If no matching row exists, it sets all columns of the main table to null.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#right-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User | null; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .rightJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number | null; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .rightJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  rightJoin = this.createJoin("right", false);
+  /**
+   * Executes an `inner join` operation, creating a new table by combining rows from two tables that have matching values.
+   *
+   * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .innerJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .innerJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  innerJoin = this.createJoin("inner", false);
+  /**
+   * Executes an `inner join lateral` operation, creating a new table by combining rows from two queries that have matching values.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method retrieves rows that have corresponding entries in both joined tables. Rows without matching entries in either table are excluded, resulting in a table that includes only matching pairs.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#inner-join-lateral}
+   *
+   * @param table the subquery to join.
+   * @param on the `on` clause.
+   */
+  innerJoinLateral = this.createJoin("inner", true);
+  /**
+   * Executes a `full join` operation by combining rows from two tables into a new table.
+   *
+   * Calling this method retrieves all rows from both main and joined tables, merging rows with matching values and filling in `null` for non-matching columns.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#full-join}
+   *
+   * @param table the table to join.
+   * @param on the `on` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users and their pets
+   * const usersWithPets: { user: User | null; pets: Pet | null; }[] = await db.select()
+   *   .from(users)
+   *   .fullJoin(pets, eq(users.id, pets.ownerId))
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number | null; petId: number | null; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .fullJoin(pets, eq(users.id, pets.ownerId))
+   * ```
+   */
+  fullJoin = this.createJoin("full", false);
+  /**
+   * Executes a `cross join` operation by combining rows from two tables into a new table.
+   *
+   * Calling this method retrieves all rows from both main and joined tables, merging all rows from each table.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join}
+   *
+   * @param table the table to join.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all users, each user with every pet
+   * const usersWithPets: { user: User; pets: Pet; }[] = await db.select()
+   *   .from(users)
+   *   .crossJoin(pets)
+   *
+   * // Select userId and petId
+   * const usersIdsAndPetIds: { userId: number; petId: number; }[] = await db.select({
+   *   userId: users.id,
+   *   petId: pets.id,
+   * })
+   *   .from(users)
+   *   .crossJoin(pets)
+   * ```
+   */
+  crossJoin = this.createJoin("cross", false);
+  /**
+   * Executes a `cross join lateral` operation by combining rows from two queries into a new table.
+   *
+   * A `lateral` join allows the right-hand expression to refer to columns from the left-hand side.
+   *
+   * Calling this method retrieves all rows from both main and joined queries, merging all rows from each query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/joins#cross-join-lateral}
+   *
+   * @param table the query to join.
+   */
+  crossJoinLateral = this.createJoin("cross", true);
+  createSetOperator(type, isAll) {
+    return (rightSelection) => {
+      const rightSelect = typeof rightSelection === "function" ? rightSelection(getClickHouseSetOperators()) : rightSelection;
+      if (!haveSameKeys(this.getSelectedFields(), rightSelect.getSelectedFields())) {
+        throw new Error(
+          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
+        );
+      }
+      this.config.setOperators.push({ type, isAll, rightSelect });
+      return this;
+    };
+  }
+  /**
+   * Adds `union` set operator to the query.
+   *
+   * Calling this method will combine the result sets of the `select` statements and remove any duplicate rows that appear across them.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#union}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all unique names from customers and users tables
+   * await db.select({ name: users.name })
+   *   .from(users)
+   *   .union(
+   *     db.select({ name: customers.name }).from(customers)
+   *   );
+   * // or
+   * import { union } from 'drizzle-orm/clickhouse-core'
+   *
+   * await union(
+   *   db.select({ name: users.name }).from(users),
+   *   db.select({ name: customers.name }).from(customers)
+   * );
+   * ```
+   */
+  union = this.createSetOperator("union", false);
+  /**
+   * Adds `union all` set operator to the query.
+   *
+   * Calling this method will combine the result-set of the `select` statements and keep all duplicate rows that appear across them.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#union-all}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all transaction ids from both online and in-store sales
+   * await db.select({ transaction: onlineSales.transactionId })
+   *   .from(onlineSales)
+   *   .unionAll(
+   *     db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+   *   );
+   * // or
+   * import { unionAll } from 'drizzle-orm/clickhouse-core'
+   *
+   * await unionAll(
+   *   db.select({ transaction: onlineSales.transactionId }).from(onlineSales),
+   *   db.select({ transaction: inStoreSales.transactionId }).from(inStoreSales)
+   * );
+   * ```
+   */
+  unionAll = this.createSetOperator("union", true);
+  /**
+   * Adds `intersect` set operator to the query.
+   *
+   * Calling this method will retain only the rows that are present in both result sets and eliminate duplicates.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#intersect}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select course names that are offered in both departments A and B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .intersect(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { intersect } from 'drizzle-orm/clickhouse-core'
+   *
+   * await intersect(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  intersect = this.createSetOperator("intersect", false);
+  /**
+   * Adds `except` set operator to the query.
+   *
+   * Calling this method will retrieve all unique rows from the left query, except for the rows that are present in the result set of the right query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/set-operations#except}
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all courses offered in department A but not in department B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .except(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { except } from 'drizzle-orm/clickhouse-core'
+   *
+   * await except(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  except = this.createSetOperator("except", false);
+  /**
+   * Adds `minus` set operator to the query.
+   *
+   * This is an alias of `except` supported by ClickHouse.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all courses offered in department A but not in department B
+   * await db.select({ courseName: depA.courseName })
+   *   .from(depA)
+   *   .minus(
+   *     db.select({ courseName: depB.courseName }).from(depB)
+   *   );
+   * // or
+   * import { minus } from 'drizzle-orm/clickhouse-core'
+   *
+   * await minus(
+   *   db.select({ courseName: depA.courseName }).from(depA),
+   *   db.select({ courseName: depB.courseName }).from(depB)
+   * );
+   * ```
+   */
+  minus = this.createSetOperator("except", false);
+  /** @internal */
+  addSetOperators(setOperators) {
+    this.config.setOperators.push(...setOperators);
+    return this;
+  }
+  /**
+   * Adds a `where` clause to the query.
+   *
+   * Calling this method will select only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#filtering}
+   *
+   * @param where the `where` clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be selected.
+   *
+   * ```ts
+   * // Select all cars with green color
+   * await db.select().from(cars).where(eq(cars.color, 'green'));
+   * // or
+   * await db.select().from(cars).where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Select all BMW cars with a green color
+   * await db.select().from(cars).where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Select all cars with the green or blue color
+   * await db.select().from(cars).where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    if (typeof where === "function") {
+      where = where(
+        new Proxy(
+          this.config.fields,
+          new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.where = where;
+    return this;
+  }
+  /**
+   * Adds a `having` clause to the query.
+   *
+   * Calling this method will select only those rows that fulfill a specified condition. It is typically used with aggregate functions to filter the aggregated data based on a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#aggregations}
+   *
+   * @param having the `having` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Select all brands with more than one car
+   * await db.select({
+   * 	brand: cars.brand,
+   * 	count: sql<number>`cast(count(${cars.id}) as int)`,
+   * })
+   *   .from(cars)
+   *   .groupBy(cars.brand)
+   *   .having(({ count }) => gt(count, 1));
+   * ```
+   */
+  having(having) {
+    if (typeof having === "function") {
+      having = having(
+        new Proxy(
+          this.config.fields,
+          new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.having = having;
+    return this;
+  }
+  groupBy(...columns) {
+    if (typeof columns[0] === "function") {
+      const groupBy = columns[0](
+        new Proxy(
+          this.config.fields,
+          new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
+        )
+      );
+      this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
+    } else {
+      this.config.groupBy = columns;
+    }
+    return this;
+  }
+  orderBy(...columns) {
+    if (typeof columns[0] === "function") {
+      const orderBy = columns[0](
+        new Proxy(
+          this.config.fields,
+          new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })
+        )
+      );
+      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
+      if (this.config.setOperators.length > 0) {
+        this.config.setOperators.at(-1).orderBy = orderByArray;
+      } else {
+        this.config.orderBy = orderByArray;
+      }
+    } else {
+      const orderByArray = columns;
+      if (this.config.setOperators.length > 0) {
+        this.config.setOperators.at(-1).orderBy = orderByArray;
+      } else {
+        this.config.orderBy = orderByArray;
+      }
+    }
+    return this;
+  }
+  /**
+   * Adds a `limit` clause to the query.
+   *
+   * Calling this method will set the maximum number of rows that will be returned by this query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+   *
+   * @param limit the `limit` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Get the first 10 people from this query.
+   * await db.select().from(people).limit(10);
+   * ```
+   */
+  limit(limit) {
+    if (this.config.setOperators.length > 0) {
+      this.config.setOperators.at(-1).limit = limit;
+    } else {
+      this.config.limit = limit;
+    }
+    return this;
+  }
+  /**
+   * Adds an `offset` clause to the query.
+   *
+   * Calling this method will skip a number of rows when returning results from this query.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/select#limit--offset}
+   *
+   * @param offset the `offset` clause.
+   *
+   * @example
+   *
+   * ```ts
+   * // Get the 10th-20th people from this query.
+   * await db.select().from(people).offset(10).limit(10);
+   * ```
+   */
+  offset(offset) {
+    if (this.config.setOperators.length > 0) {
+      this.config.setOperators.at(-1).offset = offset;
+    } else {
+      this.config.offset = offset;
+    }
+    return this;
+  }
+  /**
+   * Adds a `FINAL` modifier to the table being read.
+   *
+   * MergeTree variants that collapse or replace rows — `ReplacingMergeTree`, `CollapsingMergeTree`,
+   * `SummingMergeTree` and friends — only do so during background merges, so a plain read can see
+   * rows that are logically superseded. `FINAL` merges the relevant parts at query time.
+   *
+   * It is expensive: ClickHouse has to read and merge whole parts rather than stream granules.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(users).final().where(eq(users.id, 42));
+   * ```
+   */
+  final() {
+    this.config.final = true;
+    return this;
+  }
+  /**
+   * Adds a `PREWHERE` clause.
+   *
+   * `PREWHERE` is applied before the remaining columns are read from disk, so filtering on a small,
+   * cheap column can avoid reading large ones entirely. ClickHouse moves conditions there on its own
+   * when `optimize_move_to_prewhere` is on; this is the manual override.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).prewhere(eq(events.type, 'click')).where(gt(events.ts, since));
+   * ```
+   */
+  prewhere(where) {
+    if (typeof where === "function") {
+      where = where(
+        new Proxy(
+          this.config.fields,
+          new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })
+        )
+      );
+    }
+    this.config.prewhere = where;
+    return this;
+  }
+  /**
+   * Adds a `SAMPLE` clause, reading an approximate subset of the table.
+   *
+   * Requires the table to declare a `SAMPLE BY` key. A value below 1 is a fraction of rows; a value
+   * of 1 or more is an approximate row count.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).sample(0.1);  // roughly a tenth of the rows
+   * ```
+   */
+  sample(value, offset) {
+    this.config.sample = { value, offset };
+    return this;
+  }
+  /**
+   * Adds an `ARRAY JOIN` clause, which unfolds an array column into one row per element.
+   *
+   * @example
+   * ```ts
+   * await db.select({ tag: events.tags }).from(events).arrayJoin(events.tags);
+   * ```
+   */
+  arrayJoin(...expressions) {
+    this.config.arrayJoins = [...this.config.arrayJoins ?? [], { expressions, left: false }];
+    return this;
+  }
+  /**
+   * Adds a `LEFT ARRAY JOIN` clause, which keeps rows whose array is empty by emitting the element
+   * type's default value instead of dropping the row.
+   */
+  leftArrayJoin(...expressions) {
+    this.config.arrayJoins = [...this.config.arrayJoins ?? [], { expressions, left: true }];
+    return this;
+  }
+  /**
+   * Adds a `LIMIT n BY expr` clause, which keeps the first `n` rows *for each distinct key* rather
+   * than `n` rows overall.
+   *
+   * @example
+   * ```ts
+   * // the three most recent events per user
+   * await db.select().from(events).orderBy(desc(events.ts)).limitBy(3, events.userId);
+   * ```
+   */
+  limitBy(limit, ...expressions) {
+    this.config.limitBy = { limit, expressions };
+    return this;
+  }
+  /**
+   * Adds `WITH TOTALS` to the `GROUP BY` clause, which appends a row aggregating every group.
+   *
+   * The totals row is delivered separately by ClickHouse's JSON formats, so it does not appear in
+   * the returned rows; read it from `db.execute()` if you need it.
+   */
+  withTotals() {
+    this.config.withTotals = true;
+    return this;
+  }
+  /**
+   * Adds a query-level `SETTINGS` clause.
+   *
+   * @example
+   * ```ts
+   * await db.select().from(events).settings({ max_execution_time: 30, max_threads: 4 });
+   * ```
+   */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildSelectQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  as(alias) {
+    const usedTables = [];
+    usedTables.push(...extractUsedTable(this.config.table));
+    if (this.config.joins) {
+      for (const it of this.config.joins) usedTables.push(...extractUsedTable(it.table));
+    }
+    return new Proxy(
+      new Subquery(this.getSQL(), this.config.fields, alias, false, [...new Set(usedTables)]),
+      new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+    );
+  }
+  /** @internal */
+  getSelectedFields() {
+    return new Proxy(
+      this.config.fields,
+      new SelectionProxyHandler({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" })
+    );
+  }
+  $dynamic() {
+    return this;
+  }
+}
+class ClickHouseSelectBase extends ClickHouseSelectQueryBuilderBase {
+  static [entityKind] = "ClickHouseSelect";
+  prepare() {
+    if (!this.session) {
+      throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
+    }
+    const fieldsList = orderSelectedFields(this.config.fields);
+    const query = this.session.prepareQuery(this.dialect.sqlToQuery(this.getSQL()), fieldsList, void 0, {
+      type: "select",
+      tables: [...this.usedTables]
+    }, this.cacheConfig);
+    query.joinsNotNullableMap = this.joinsNotNullableMap;
+    return query;
+  }
+  $withCache(config) {
+    this.cacheConfig = config === void 0 ? { config: {}, enable: true, autoInvalidate: true } : config === false ? { enable: false } : { enable: true, autoInvalidate: true, ...config };
+    return this;
+  }
+  execute = ((placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  });
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+}
+applyMixins(ClickHouseSelectBase, [QueryPromise]);
+function createSetOperator(type, isAll) {
+  return (leftSelect, rightSelect, ...restSelects) => {
+    const setOperators = [rightSelect, ...restSelects].map((select) => ({
+      type,
+      isAll,
+      rightSelect: select
+    }));
+    for (const setOperator of setOperators) {
+      if (!haveSameKeys(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
+        throw new Error(
+          "Set operator error (union / intersect / except): selected fields are not the same or are in a different order"
+        );
+      }
+    }
+    return leftSelect.addSetOperators(setOperators);
+  };
+}
+const getClickHouseSetOperators = () => ({
+  union,
+  unionAll,
+  intersect,
+  except,
+  minus
+});
+const union = createSetOperator("union", false);
+const unionAll = createSetOperator("union", true);
+const intersect = createSetOperator("intersect", false);
+const except = createSetOperator("except", false);
+const minus = createSetOperator("except", true);
+export {
+  ClickHouseSelectBase,
+  ClickHouseSelectBuilder,
+  ClickHouseSelectQueryBuilderBase,
+  except,
+  intersect,
+  minus,
+  union,
+  unionAll
+};
diff --git a/clickhouse-core/query-builders/select.types.cjs b/clickhouse-core/query-builders/select.types.cjs
new file mode 100644
index 0000000..bd921f3
--- /dev/null
+++ b/clickhouse-core/query-builders/select.types.cjs
@@ -0,0 +1,16 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var select_types_exports = {};
+module.exports = __toCommonJS(select_types_exports);
diff --git a/clickhouse-core/query-builders/select.types.d.cts b/clickhouse-core/query-builders/select.types.d.cts
new file mode 100644
index 0000000..2ade58c
--- /dev/null
+++ b/clickhouse-core/query-builders/select.types.d.cts
@@ -0,0 +1,166 @@
+import type { ClickHouseColumn } from '../columns/index.cjs';
+import type { ClickHouseSettings } from '../engines.cjs';
+import type { ClickHouseTable, ClickHouseTableWithColumns } from '../table.cjs';
+import type { SelectedFields as SelectedFieldsBase, SelectedFieldsFlat as SelectedFieldsFlatBase, SelectedFieldsOrdered as SelectedFieldsOrderedBase } from '../../operations.cjs';
+import type { TypedQueryBuilder } from '../../query-builders/query-builder.cjs';
+import type { AppendToNullabilityMap, AppendToResult, BuildSubquerySelection, GetSelectTableName, JoinNullability, JoinType, MapColumnsToTableAlias, SelectMode, SelectResult, SetOperator } from '../../query-builders/select.types.cjs';
+import type { ColumnsSelection, Placeholder, SQL, View } from '../../sql/sql.cjs';
+import type { Subquery } from '../../subquery.cjs';
+import type { Table, UpdateTableConfig } from '../../table.cjs';
+import type { Assume, ValidateShape } from '../../utils.cjs';
+import type { ClickHousePreparedQueryConfig, PreparedQueryHKTBase, PreparedQueryKind } from '../session.cjs';
+import type { ClickHouseSelectBase, ClickHouseSelectQueryBuilderBase } from './select.cjs';
+export interface ClickHouseSelectJoinConfig {
+    on: SQL | undefined;
+    table: ClickHouseTable | Subquery | SQL;
+    alias: string | undefined;
+    joinType: JoinType;
+    lateral?: boolean;
+    /**
+     * ClickHouse's strictness modifier, written between the join type and `JOIN`.
+     *
+     * `ALL` (the default) yields a row per matching pair, `ANY` stops at the first match on the right,
+     * and `ASOF` joins on the closest preceding value of the last condition.
+     */
+    strictness?: ClickHouseJoinStrictness;
+    /** Emits `GLOBAL JOIN`, which broadcasts the right-hand side to every shard. */
+    global?: boolean;
+}
+/** ClickHouse's join strictness modifiers. */
+export type ClickHouseJoinStrictness = 'all' | 'any' | 'asof' | 'semi' | 'anti';
+/** An `ARRAY JOIN` clause, which unfolds array columns into one row per element. */
+export interface ClickHouseArrayJoinConfig {
+    expressions: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    /** `LEFT ARRAY JOIN` keeps rows whose array is empty, emitting a default value instead. */
+    left: boolean;
+}
+/** A `LIMIT n [OFFSET m] BY expr` clause, which limits rows *per distinct key*. */
+export interface ClickHouseLimitByConfig {
+    limit: number | Placeholder;
+    offset?: number | Placeholder;
+    expressions: (ClickHouseColumn | SQL | SQL.Aliased)[];
+}
+/** A `SAMPLE k [OFFSET m]` clause. */
+export interface ClickHouseSampleConfig {
+    /** A fraction of rows when below 1, or an absolute row count when 1 or above. */
+    value: number;
+    offset?: number;
+}
+export type BuildAliasTable<TTable extends ClickHouseTable | View, TAlias extends string> = TTable extends Table ? ClickHouseTableWithColumns<UpdateTableConfig<TTable['_']['config'], {
+    name: TAlias;
+    columns: MapColumnsToTableAlias<TTable['_']['columns'], TAlias, 'clickhouse'>;
+}>> : never;
+export interface ClickHouseSelectConfig {
+    withList?: Subquery[];
+    fields: Record<string, unknown>;
+    fieldsFlat?: SelectedFieldsOrdered;
+    where?: SQL;
+    having?: SQL;
+    table: ClickHouseTable | Subquery | SQL;
+    limit?: number | Placeholder;
+    offset?: number | Placeholder;
+    joins?: ClickHouseSelectJoinConfig[];
+    orderBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    groupBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    distinct?: boolean;
+    /** `FINAL` — merge parts at read time, so that ReplacingMergeTree/Collapsing tables read cleanly. */
+    final?: boolean;
+    /** `PREWHERE` — a filter ClickHouse applies before reading the remaining columns. */
+    prewhere?: SQL;
+    /** `SAMPLE` — read an approximate subset of the table. */
+    sample?: ClickHouseSampleConfig;
+    /** `ARRAY JOIN` clauses. */
+    arrayJoins?: ClickHouseArrayJoinConfig[];
+    /** `LIMIT … BY` — limits rows per distinct key rather than over the whole result. */
+    limitBy?: ClickHouseLimitByConfig;
+    /** `GROUP BY … WITH TOTALS` — appends a totals row to the result. */
+    withTotals?: boolean;
+    /** Query-level `SETTINGS`. */
+    settings?: ClickHouseSettings;
+    setOperators: {
+        rightSelect: TypedQueryBuilder<any, any>;
+        type: SetOperator;
+        isAll: boolean;
+        orderBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+        limit?: number | Placeholder;
+        offset?: number | Placeholder;
+    }[];
+}
+export type ClickHouseJoin<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TJoinType extends JoinType, TJoinedTable extends ClickHouseTable | Subquery | SQL, // | ClickHouseViewBase
+TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>> = T extends any ? ClickHouseSelectWithout<ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], AppendToResult<T['_']['tableName'], T['_']['selection'], TJoinedName, TJoinedTable extends ClickHouseTable ? TJoinedTable['_']['columns'] : TJoinedTable extends Subquery ? Assume<TJoinedTable['_']['selectedFields'], SelectedFields> : never, T['_']['selectMode']>, T['_']['selectMode'] extends 'partial' ? T['_']['selectMode'] : 'multiple', T['_']['preparedQueryHKT'], AppendToNullabilityMap<T['_']['nullabilityMap'], TJoinedName, TJoinType>, TDynamic, T['_']['excludedMethods']>, TDynamic, T['_']['excludedMethods']> : never;
+export type ClickHouseJoinFn<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TJoinType extends JoinType, TIsLateral extends boolean> = <TJoinedTable extends (TIsLateral extends true ? Subquery | SQL : ClickHouseTable | Subquery | SQL), TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>>(table: TJoinedTable, on: ((aliases: T['_']['selection']) => SQL | undefined) | SQL | undefined) => ClickHouseJoin<T, TDynamic, TJoinType, TJoinedTable, TJoinedName>;
+export type ClickHouseCrossJoinFn<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TIsLateral extends boolean> = <TJoinedTable extends (TIsLateral extends true ? Subquery | SQL : ClickHouseTable | Subquery | SQL), TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>>(table: TJoinedTable) => ClickHouseJoin<T, TDynamic, 'cross', TJoinedTable, TJoinedName>;
+export type SelectedFieldsFlat = SelectedFieldsFlatBase<ClickHouseColumn>;
+export type SelectedFields = SelectedFieldsBase<ClickHouseColumn, ClickHouseTable>;
+export type SelectedFieldsOrdered = SelectedFieldsOrderedBase<ClickHouseColumn>;
+export interface ClickHouseSelectHKTBase {
+    tableName: string | undefined;
+    selection: unknown;
+    selectMode: SelectMode;
+    preparedQueryHKT: unknown;
+    nullabilityMap: unknown;
+    dynamic: boolean;
+    excludedMethods: string;
+    result: unknown;
+    selectedFields: unknown;
+    _type: unknown;
+}
+export type ClickHouseSelectKind<T extends ClickHouseSelectHKTBase, TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability>, TDynamic extends boolean, TExcludedMethods extends string, TResult = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields = BuildSubquerySelection<TSelection, TNullabilityMap>> = (T & {
+    tableName: TTableName;
+    selection: TSelection;
+    selectMode: TSelectMode;
+    preparedQueryHKT: TPreparedQueryHKT;
+    nullabilityMap: TNullabilityMap;
+    dynamic: TDynamic;
+    excludedMethods: TExcludedMethods;
+    result: TResult;
+    selectedFields: TSelectedFields;
+})['_type'];
+export interface ClickHouseSelectQueryBuilderHKT extends ClickHouseSelectHKTBase {
+    _type: ClickHouseSelectQueryBuilderBase<ClickHouseSelectQueryBuilderHKT, this['tableName'], Assume<this['selection'], ColumnsSelection>, this['selectMode'], Assume<this['preparedQueryHKT'], PreparedQueryHKTBase>, Assume<this['nullabilityMap'], Record<string, JoinNullability>>, this['dynamic'], this['excludedMethods'], Assume<this['result'], any[]>, Assume<this['selectedFields'], ColumnsSelection>>;
+}
+export interface ClickHouseSelectHKT extends ClickHouseSelectHKTBase {
+    _type: ClickHouseSelectBase<this['tableName'], Assume<this['selection'], ColumnsSelection>, this['selectMode'], Assume<this['preparedQueryHKT'], PreparedQueryHKTBase>, Assume<this['nullabilityMap'], Record<string, JoinNullability>>, this['dynamic'], this['excludedMethods'], Assume<this['result'], any[]>, Assume<this['selectedFields'], ColumnsSelection>>;
+}
+export type ClickHouseSetOperatorExcludedMethods = 'where' | 'having' | 'groupBy' | 'session' | 'leftJoin' | 'rightJoin' | 'innerJoin' | 'fullJoin' | 'final' | 'prewhere' | 'sample' | 'limitBy' | 'withTotals' | 'settings';
+export type ClickHouseSelectWithout<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, K extends keyof T & string, TResetExcluded extends boolean = false> = TDynamic extends true ? T : Omit<ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], T['_']['selection'], T['_']['selectMode'], T['_']['preparedQueryHKT'], T['_']['nullabilityMap'], TDynamic, TResetExcluded extends true ? K : T['_']['excludedMethods'] | K, T['_']['result'], T['_']['selectedFields']>, TResetExcluded extends true ? K : T['_']['excludedMethods'] | K>;
+export type ClickHouseSelectPrepare<T extends AnyClickHouseSelect> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: T['_']['result'];
+    iterator: T['_']['result'][number];
+}, true>;
+export type ClickHouseSelectDynamic<T extends AnyClickHouseSelectQueryBuilder> = ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], T['_']['selection'], T['_']['selectMode'], T['_']['preparedQueryHKT'], T['_']['nullabilityMap'], true, never, T['_']['result'], T['_']['selectedFields']>;
+export type CreateClickHouseSelectFromBuilderMode<TBuilderMode extends 'db' | 'qb', TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase> = TBuilderMode extends 'db' ? ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT> : ClickHouseSelectQueryBuilderBase<ClickHouseSelectQueryBuilderHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT>;
+export type ClickHouseSelectQueryBuilder<THKT extends ClickHouseSelectHKTBase = ClickHouseSelectQueryBuilderHKT, TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = ColumnsSelection, TSelectMode extends SelectMode = SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>, TResult extends any[] = unknown[], TSelectedFields extends ColumnsSelection = ColumnsSelection> = ClickHouseSelectQueryBuilderBase<THKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, true, never, TResult, TSelectedFields>;
+export type AnyClickHouseSelectQueryBuilder = ClickHouseSelectQueryBuilderBase<any, any, any, any, any, any, any, any, any>;
+export type AnyClickHouseSetOperatorInterface = ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, any, any>;
+export interface ClickHouseSetOperatorInterface<TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> {
+    _: {
+        readonly hkt: ClickHouseSelectHKT;
+        readonly tableName: TTableName;
+        readonly selection: TSelection;
+        readonly selectMode: TSelectMode;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly nullabilityMap: TNullabilityMap;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: TResult;
+        readonly selectedFields: TSelectedFields;
+    };
+}
+export type ClickHouseSetOperatorWithResult<TResult extends any[]> = ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, TResult, any>;
+export type ClickHouseSelect<TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = Record<string, any>, TSelectMode extends SelectMode = SelectMode, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>> = ClickHouseSelectBase<TTableName, TSelection, TSelectMode, PreparedQueryHKTBase, TNullabilityMap, true, never>;
+export type AnyClickHouseSelect = ClickHouseSelectBase<any, any, any, any, any, any, any, any>;
+export type ClickHouseSetOperator<TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = Record<string, any>, TSelectMode extends SelectMode = SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>> = ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, true, ClickHouseSetOperatorExcludedMethods>;
+export type SetOperatorRightSelect<TValue extends ClickHouseSetOperatorWithResult<TResult>, TResult extends any[]> = TValue extends ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, infer TValueResult, any> ? ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>> : TValue;
+export type SetOperatorRestSelect<TValue extends readonly ClickHouseSetOperatorWithResult<TResult>[], TResult extends any[]> = TValue extends [infer First, ...infer Rest] ? First extends ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, infer TValueResult, any> ? Rest extends AnyClickHouseSetOperatorInterface[] ? [
+    ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>>,
+    ...SetOperatorRestSelect<Rest, TResult>
+] : ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>[]> : never : TValue;
+export type ClickHouseCreateSetOperatorFn = <TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TValue extends ClickHouseSetOperatorWithResult<TResult>, TRest extends ClickHouseSetOperatorWithResult<TResult>[], TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>>(leftSelect: ClickHouseSetOperatorInterface<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, rightSelect: SetOperatorRightSelect<TValue, TResult>, ...restSelects: SetOperatorRestSelect<TRest, TResult>) => ClickHouseSelectWithout<ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, false, ClickHouseSetOperatorExcludedMethods, true>;
+export type GetClickHouseSetOperators = {
+    union: ClickHouseCreateSetOperatorFn;
+    intersect: ClickHouseCreateSetOperatorFn;
+    except: ClickHouseCreateSetOperatorFn;
+    unionAll: ClickHouseCreateSetOperatorFn;
+    minus: ClickHouseCreateSetOperatorFn;
+};
diff --git a/clickhouse-core/query-builders/select.types.d.ts b/clickhouse-core/query-builders/select.types.d.ts
new file mode 100644
index 0000000..8e0dae7
--- /dev/null
+++ b/clickhouse-core/query-builders/select.types.d.ts
@@ -0,0 +1,166 @@
+import type { ClickHouseColumn } from '../columns/index.js';
+import type { ClickHouseSettings } from '../engines.js';
+import type { ClickHouseTable, ClickHouseTableWithColumns } from '../table.js';
+import type { SelectedFields as SelectedFieldsBase, SelectedFieldsFlat as SelectedFieldsFlatBase, SelectedFieldsOrdered as SelectedFieldsOrderedBase } from '../../operations.js';
+import type { TypedQueryBuilder } from '../../query-builders/query-builder.js';
+import type { AppendToNullabilityMap, AppendToResult, BuildSubquerySelection, GetSelectTableName, JoinNullability, JoinType, MapColumnsToTableAlias, SelectMode, SelectResult, SetOperator } from '../../query-builders/select.types.js';
+import type { ColumnsSelection, Placeholder, SQL, View } from '../../sql/sql.js';
+import type { Subquery } from '../../subquery.js';
+import type { Table, UpdateTableConfig } from '../../table.js';
+import type { Assume, ValidateShape } from '../../utils.js';
+import type { ClickHousePreparedQueryConfig, PreparedQueryHKTBase, PreparedQueryKind } from '../session.js';
+import type { ClickHouseSelectBase, ClickHouseSelectQueryBuilderBase } from './select.js';
+export interface ClickHouseSelectJoinConfig {
+    on: SQL | undefined;
+    table: ClickHouseTable | Subquery | SQL;
+    alias: string | undefined;
+    joinType: JoinType;
+    lateral?: boolean;
+    /**
+     * ClickHouse's strictness modifier, written between the join type and `JOIN`.
+     *
+     * `ALL` (the default) yields a row per matching pair, `ANY` stops at the first match on the right,
+     * and `ASOF` joins on the closest preceding value of the last condition.
+     */
+    strictness?: ClickHouseJoinStrictness;
+    /** Emits `GLOBAL JOIN`, which broadcasts the right-hand side to every shard. */
+    global?: boolean;
+}
+/** ClickHouse's join strictness modifiers. */
+export type ClickHouseJoinStrictness = 'all' | 'any' | 'asof' | 'semi' | 'anti';
+/** An `ARRAY JOIN` clause, which unfolds array columns into one row per element. */
+export interface ClickHouseArrayJoinConfig {
+    expressions: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    /** `LEFT ARRAY JOIN` keeps rows whose array is empty, emitting a default value instead. */
+    left: boolean;
+}
+/** A `LIMIT n [OFFSET m] BY expr` clause, which limits rows *per distinct key*. */
+export interface ClickHouseLimitByConfig {
+    limit: number | Placeholder;
+    offset?: number | Placeholder;
+    expressions: (ClickHouseColumn | SQL | SQL.Aliased)[];
+}
+/** A `SAMPLE k [OFFSET m]` clause. */
+export interface ClickHouseSampleConfig {
+    /** A fraction of rows when below 1, or an absolute row count when 1 or above. */
+    value: number;
+    offset?: number;
+}
+export type BuildAliasTable<TTable extends ClickHouseTable | View, TAlias extends string> = TTable extends Table ? ClickHouseTableWithColumns<UpdateTableConfig<TTable['_']['config'], {
+    name: TAlias;
+    columns: MapColumnsToTableAlias<TTable['_']['columns'], TAlias, 'clickhouse'>;
+}>> : never;
+export interface ClickHouseSelectConfig {
+    withList?: Subquery[];
+    fields: Record<string, unknown>;
+    fieldsFlat?: SelectedFieldsOrdered;
+    where?: SQL;
+    having?: SQL;
+    table: ClickHouseTable | Subquery | SQL;
+    limit?: number | Placeholder;
+    offset?: number | Placeholder;
+    joins?: ClickHouseSelectJoinConfig[];
+    orderBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    groupBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+    distinct?: boolean;
+    /** `FINAL` — merge parts at read time, so that ReplacingMergeTree/Collapsing tables read cleanly. */
+    final?: boolean;
+    /** `PREWHERE` — a filter ClickHouse applies before reading the remaining columns. */
+    prewhere?: SQL;
+    /** `SAMPLE` — read an approximate subset of the table. */
+    sample?: ClickHouseSampleConfig;
+    /** `ARRAY JOIN` clauses. */
+    arrayJoins?: ClickHouseArrayJoinConfig[];
+    /** `LIMIT … BY` — limits rows per distinct key rather than over the whole result. */
+    limitBy?: ClickHouseLimitByConfig;
+    /** `GROUP BY … WITH TOTALS` — appends a totals row to the result. */
+    withTotals?: boolean;
+    /** Query-level `SETTINGS`. */
+    settings?: ClickHouseSettings;
+    setOperators: {
+        rightSelect: TypedQueryBuilder<any, any>;
+        type: SetOperator;
+        isAll: boolean;
+        orderBy?: (ClickHouseColumn | SQL | SQL.Aliased)[];
+        limit?: number | Placeholder;
+        offset?: number | Placeholder;
+    }[];
+}
+export type ClickHouseJoin<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TJoinType extends JoinType, TJoinedTable extends ClickHouseTable | Subquery | SQL, // | ClickHouseViewBase
+TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>> = T extends any ? ClickHouseSelectWithout<ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], AppendToResult<T['_']['tableName'], T['_']['selection'], TJoinedName, TJoinedTable extends ClickHouseTable ? TJoinedTable['_']['columns'] : TJoinedTable extends Subquery ? Assume<TJoinedTable['_']['selectedFields'], SelectedFields> : never, T['_']['selectMode']>, T['_']['selectMode'] extends 'partial' ? T['_']['selectMode'] : 'multiple', T['_']['preparedQueryHKT'], AppendToNullabilityMap<T['_']['nullabilityMap'], TJoinedName, TJoinType>, TDynamic, T['_']['excludedMethods']>, TDynamic, T['_']['excludedMethods']> : never;
+export type ClickHouseJoinFn<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TJoinType extends JoinType, TIsLateral extends boolean> = <TJoinedTable extends (TIsLateral extends true ? Subquery | SQL : ClickHouseTable | Subquery | SQL), TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>>(table: TJoinedTable, on: ((aliases: T['_']['selection']) => SQL | undefined) | SQL | undefined) => ClickHouseJoin<T, TDynamic, TJoinType, TJoinedTable, TJoinedName>;
+export type ClickHouseCrossJoinFn<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, TIsLateral extends boolean> = <TJoinedTable extends (TIsLateral extends true ? Subquery | SQL : ClickHouseTable | Subquery | SQL), TJoinedName extends GetSelectTableName<TJoinedTable> = GetSelectTableName<TJoinedTable>>(table: TJoinedTable) => ClickHouseJoin<T, TDynamic, 'cross', TJoinedTable, TJoinedName>;
+export type SelectedFieldsFlat = SelectedFieldsFlatBase<ClickHouseColumn>;
+export type SelectedFields = SelectedFieldsBase<ClickHouseColumn, ClickHouseTable>;
+export type SelectedFieldsOrdered = SelectedFieldsOrderedBase<ClickHouseColumn>;
+export interface ClickHouseSelectHKTBase {
+    tableName: string | undefined;
+    selection: unknown;
+    selectMode: SelectMode;
+    preparedQueryHKT: unknown;
+    nullabilityMap: unknown;
+    dynamic: boolean;
+    excludedMethods: string;
+    result: unknown;
+    selectedFields: unknown;
+    _type: unknown;
+}
+export type ClickHouseSelectKind<T extends ClickHouseSelectHKTBase, TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability>, TDynamic extends boolean, TExcludedMethods extends string, TResult = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields = BuildSubquerySelection<TSelection, TNullabilityMap>> = (T & {
+    tableName: TTableName;
+    selection: TSelection;
+    selectMode: TSelectMode;
+    preparedQueryHKT: TPreparedQueryHKT;
+    nullabilityMap: TNullabilityMap;
+    dynamic: TDynamic;
+    excludedMethods: TExcludedMethods;
+    result: TResult;
+    selectedFields: TSelectedFields;
+})['_type'];
+export interface ClickHouseSelectQueryBuilderHKT extends ClickHouseSelectHKTBase {
+    _type: ClickHouseSelectQueryBuilderBase<ClickHouseSelectQueryBuilderHKT, this['tableName'], Assume<this['selection'], ColumnsSelection>, this['selectMode'], Assume<this['preparedQueryHKT'], PreparedQueryHKTBase>, Assume<this['nullabilityMap'], Record<string, JoinNullability>>, this['dynamic'], this['excludedMethods'], Assume<this['result'], any[]>, Assume<this['selectedFields'], ColumnsSelection>>;
+}
+export interface ClickHouseSelectHKT extends ClickHouseSelectHKTBase {
+    _type: ClickHouseSelectBase<this['tableName'], Assume<this['selection'], ColumnsSelection>, this['selectMode'], Assume<this['preparedQueryHKT'], PreparedQueryHKTBase>, Assume<this['nullabilityMap'], Record<string, JoinNullability>>, this['dynamic'], this['excludedMethods'], Assume<this['result'], any[]>, Assume<this['selectedFields'], ColumnsSelection>>;
+}
+export type ClickHouseSetOperatorExcludedMethods = 'where' | 'having' | 'groupBy' | 'session' | 'leftJoin' | 'rightJoin' | 'innerJoin' | 'fullJoin' | 'final' | 'prewhere' | 'sample' | 'limitBy' | 'withTotals' | 'settings';
+export type ClickHouseSelectWithout<T extends AnyClickHouseSelectQueryBuilder, TDynamic extends boolean, K extends keyof T & string, TResetExcluded extends boolean = false> = TDynamic extends true ? T : Omit<ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], T['_']['selection'], T['_']['selectMode'], T['_']['preparedQueryHKT'], T['_']['nullabilityMap'], TDynamic, TResetExcluded extends true ? K : T['_']['excludedMethods'] | K, T['_']['result'], T['_']['selectedFields']>, TResetExcluded extends true ? K : T['_']['excludedMethods'] | K>;
+export type ClickHouseSelectPrepare<T extends AnyClickHouseSelect> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: T['_']['result'];
+    iterator: T['_']['result'][number];
+}, true>;
+export type ClickHouseSelectDynamic<T extends AnyClickHouseSelectQueryBuilder> = ClickHouseSelectKind<T['_']['hkt'], T['_']['tableName'], T['_']['selection'], T['_']['selectMode'], T['_']['preparedQueryHKT'], T['_']['nullabilityMap'], true, never, T['_']['result'], T['_']['selectedFields']>;
+export type CreateClickHouseSelectFromBuilderMode<TBuilderMode extends 'db' | 'qb', TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase> = TBuilderMode extends 'db' ? ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT> : ClickHouseSelectQueryBuilderBase<ClickHouseSelectQueryBuilderHKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT>;
+export type ClickHouseSelectQueryBuilder<THKT extends ClickHouseSelectHKTBase = ClickHouseSelectQueryBuilderHKT, TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = ColumnsSelection, TSelectMode extends SelectMode = SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>, TResult extends any[] = unknown[], TSelectedFields extends ColumnsSelection = ColumnsSelection> = ClickHouseSelectQueryBuilderBase<THKT, TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, true, never, TResult, TSelectedFields>;
+export type AnyClickHouseSelectQueryBuilder = ClickHouseSelectQueryBuilderBase<any, any, any, any, any, any, any, any, any>;
+export type AnyClickHouseSetOperatorInterface = ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, any, any>;
+export interface ClickHouseSetOperatorInterface<TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>> {
+    _: {
+        readonly hkt: ClickHouseSelectHKT;
+        readonly tableName: TTableName;
+        readonly selection: TSelection;
+        readonly selectMode: TSelectMode;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly nullabilityMap: TNullabilityMap;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+        readonly result: TResult;
+        readonly selectedFields: TSelectedFields;
+    };
+}
+export type ClickHouseSetOperatorWithResult<TResult extends any[]> = ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, TResult, any>;
+export type ClickHouseSelect<TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = Record<string, any>, TSelectMode extends SelectMode = SelectMode, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>> = ClickHouseSelectBase<TTableName, TSelection, TSelectMode, PreparedQueryHKTBase, TNullabilityMap, true, never>;
+export type AnyClickHouseSelect = ClickHouseSelectBase<any, any, any, any, any, any, any, any>;
+export type ClickHouseSetOperator<TTableName extends string | undefined = string | undefined, TSelection extends ColumnsSelection = Record<string, any>, TSelectMode extends SelectMode = SelectMode, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = Record<string, JoinNullability>> = ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, true, ClickHouseSetOperatorExcludedMethods>;
+export type SetOperatorRightSelect<TValue extends ClickHouseSetOperatorWithResult<TResult>, TResult extends any[]> = TValue extends ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, infer TValueResult, any> ? ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>> : TValue;
+export type SetOperatorRestSelect<TValue extends readonly ClickHouseSetOperatorWithResult<TResult>[], TResult extends any[]> = TValue extends [infer First, ...infer Rest] ? First extends ClickHouseSetOperatorInterface<any, any, any, any, any, any, any, infer TValueResult, any> ? Rest extends AnyClickHouseSetOperatorInterface[] ? [
+    ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>>,
+    ...SetOperatorRestSelect<Rest, TResult>
+] : ValidateShape<TValueResult[number], TResult[number], TypedQueryBuilder<any, TValueResult>[]> : never : TValue;
+export type ClickHouseCreateSetOperatorFn = <TTableName extends string | undefined, TSelection extends ColumnsSelection, TSelectMode extends SelectMode, TValue extends ClickHouseSetOperatorWithResult<TResult>, TRest extends ClickHouseSetOperatorWithResult<TResult>[], TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase, TNullabilityMap extends Record<string, JoinNullability> = TTableName extends string ? Record<TTableName, 'not-null'> : {}, TDynamic extends boolean = false, TExcludedMethods extends string = never, TResult extends any[] = SelectResult<TSelection, TSelectMode, TNullabilityMap>[], TSelectedFields extends ColumnsSelection = BuildSubquerySelection<TSelection, TNullabilityMap>>(leftSelect: ClickHouseSetOperatorInterface<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, rightSelect: SetOperatorRightSelect<TValue, TResult>, ...restSelects: SetOperatorRestSelect<TRest, TResult>) => ClickHouseSelectWithout<ClickHouseSelectBase<TTableName, TSelection, TSelectMode, TPreparedQueryHKT, TNullabilityMap, TDynamic, TExcludedMethods, TResult, TSelectedFields>, false, ClickHouseSetOperatorExcludedMethods, true>;
+export type GetClickHouseSetOperators = {
+    union: ClickHouseCreateSetOperatorFn;
+    intersect: ClickHouseCreateSetOperatorFn;
+    except: ClickHouseCreateSetOperatorFn;
+    unionAll: ClickHouseCreateSetOperatorFn;
+    minus: ClickHouseCreateSetOperatorFn;
+};
diff --git a/clickhouse-core/query-builders/select.types.js b/clickhouse-core/query-builders/select.types.js
new file mode 100644
index 0000000..e69de29
diff --git a/clickhouse-core/query-builders/update.cjs b/clickhouse-core/query-builders/update.cjs
new file mode 100644
index 0000000..ba3eb0e
--- /dev/null
+++ b/clickhouse-core/query-builders/update.cjs
@@ -0,0 +1,138 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var update_exports = {};
+__export(update_exports, {
+  ClickHouseUpdateBase: () => ClickHouseUpdateBase,
+  ClickHouseUpdateBuilder: () => ClickHouseUpdateBuilder
+});
+module.exports = __toCommonJS(update_exports);
+var import_entity = require("../../entity.cjs");
+var import_query_promise = require("../../query-promise.cjs");
+var import_utils = require("../../utils.cjs");
+var import_utils2 = require("../utils.cjs");
+class ClickHouseUpdateBuilder {
+  constructor(table, session, dialect) {
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+  }
+  table;
+  session;
+  dialect;
+  static [import_entity.entityKind] = "ClickHouseUpdateBuilder";
+  set(values) {
+    return new ClickHouseUpdateBase(
+      this.table,
+      (0, import_utils.mapUpdateSet)(this.table, values),
+      this.session,
+      this.dialect
+    );
+  }
+}
+class ClickHouseUpdateBase extends import_query_promise.QueryPromise {
+  constructor(table, set, session, dialect) {
+    super();
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { set, table };
+  }
+  session;
+  dialect;
+  static [import_entity.entityKind] = "ClickHouseUpdate";
+  config;
+  /**
+   * Adds a 'where' clause to the query.
+   *
+   * Calling this method will update only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/update}
+   *
+   * @param where the 'where' clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be updated.
+   *
+   * ```ts
+   * // Update all cars with green color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(eq(cars.color, 'green'));
+   * // or
+   * db.update(cars).set({ color: 'red' })
+   *   .where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Update all BMW cars with a green color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Update all cars with the green or blue color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    this.config.where = where;
+    return this;
+  }
+  /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for the mutation. */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildUpdateQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "update",
+        tables: (0, import_utils2.extractUsedTable)(this.config.table)
+      }
+    );
+  }
+  execute = (placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseUpdateBase,
+  ClickHouseUpdateBuilder
+});
diff --git a/clickhouse-core/query-builders/update.d.cts b/clickhouse-core/query-builders/update.d.cts
new file mode 100644
index 0000000..dcab1b2
--- /dev/null
+++ b/clickhouse-core/query-builders/update.d.cts
@@ -0,0 +1,95 @@
+import type { ClickHouseDialect } from '../dialect.cjs';
+import type { ClickHouseSettings } from '../engines.cjs';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.cjs';
+import type { ClickHouseTable } from '../table.cjs';
+import type { GetColumnData } from '../../column.cjs';
+import { entityKind } from '../../entity.cjs';
+import { QueryPromise } from '../../query-promise.cjs';
+import type { Query, SQL, SQLWrapper } from '../../sql/sql.cjs';
+import { type UpdateSet } from '../../utils.cjs';
+export interface ClickHouseUpdateConfig {
+    where?: SQL | undefined;
+    set: UpdateSet;
+    table: ClickHouseTable;
+    settings?: ClickHouseSettings;
+}
+export type ClickHouseUpdateSetSource<TTable extends ClickHouseTable> = {
+    [Key in keyof TTable['$inferInsert']]?: GetColumnData<TTable['_']['columns'][Key], 'query'> | SQL | undefined;
+} & {};
+export declare class ClickHouseUpdateBuilder<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase> {
+    private table;
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly table: TTable;
+    };
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect);
+    set(values: ClickHouseUpdateSetSource<TTable>): ClickHouseUpdateBase<TTable, TQueryResult, TPreparedQueryHKT>;
+}
+export type ClickHouseUpdateWithout<T extends AnyClickHouseUpdateBase, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseUpdateBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseUpdatePrepare<T extends AnyClickHouseUpdateBase> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+export type ClickHouseUpdateDynamic<T extends AnyClickHouseUpdateBase> = ClickHouseUpdate<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+export type ClickHouseUpdate<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseUpdateBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export type AnyClickHouseUpdateBase = ClickHouseUpdateBase<any, any, any, any, any>;
+export interface ClickHouseUpdateBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>>, SQLWrapper {
+    readonly _: {
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+    };
+}
+export declare class ClickHouseUpdateBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements SQLWrapper {
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    private config;
+    constructor(table: TTable, set: UpdateSet, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a 'where' clause to the query.
+     *
+     * Calling this method will update only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/update}
+     *
+     * @param where the 'where' clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be updated.
+     *
+     * ```ts
+     * // Update all cars with green color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(eq(cars.color, 'green'));
+     * // or
+     * db.update(cars).set({ color: 'red' })
+     *   .where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Update all BMW cars with a green color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Update all cars with the green or blue color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: SQL | undefined): ClickHouseUpdateWithout<this, TDynamic, 'where'>;
+    /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for the mutation. */
+    settings(settings: ClickHouseSettings): ClickHouseUpdateWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    prepare(): ClickHouseUpdatePrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseUpdateDynamic<this>;
+}
diff --git a/clickhouse-core/query-builders/update.d.ts b/clickhouse-core/query-builders/update.d.ts
new file mode 100644
index 0000000..a615cc2
--- /dev/null
+++ b/clickhouse-core/query-builders/update.d.ts
@@ -0,0 +1,95 @@
+import type { ClickHouseDialect } from '../dialect.js';
+import type { ClickHouseSettings } from '../engines.js';
+import type { AnyClickHouseQueryResultHKT, ClickHousePreparedQueryConfig, ClickHouseQueryResultHKT, ClickHouseQueryResultKind, ClickHouseSession, PreparedQueryHKTBase, PreparedQueryKind } from '../session.js';
+import type { ClickHouseTable } from '../table.js';
+import type { GetColumnData } from '../../column.js';
+import { entityKind } from '../../entity.js';
+import { QueryPromise } from '../../query-promise.js';
+import type { Query, SQL, SQLWrapper } from '../../sql/sql.js';
+import { type UpdateSet } from '../../utils.js';
+export interface ClickHouseUpdateConfig {
+    where?: SQL | undefined;
+    set: UpdateSet;
+    table: ClickHouseTable;
+    settings?: ClickHouseSettings;
+}
+export type ClickHouseUpdateSetSource<TTable extends ClickHouseTable> = {
+    [Key in keyof TTable['$inferInsert']]?: GetColumnData<TTable['_']['columns'][Key], 'query'> | SQL | undefined;
+} & {};
+export declare class ClickHouseUpdateBuilder<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase> {
+    private table;
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    readonly _: {
+        readonly table: TTable;
+    };
+    constructor(table: TTable, session: ClickHouseSession, dialect: ClickHouseDialect);
+    set(values: ClickHouseUpdateSetSource<TTable>): ClickHouseUpdateBase<TTable, TQueryResult, TPreparedQueryHKT>;
+}
+export type ClickHouseUpdateWithout<T extends AnyClickHouseUpdateBase, TDynamic extends boolean, K extends keyof T & string> = TDynamic extends true ? T : Omit<ClickHouseUpdateBase<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT'], TDynamic, T['_']['excludedMethods'] | K>, T['_']['excludedMethods'] | K>;
+export type ClickHouseUpdatePrepare<T extends AnyClickHouseUpdateBase> = PreparedQueryKind<T['_']['preparedQueryHKT'], ClickHousePreparedQueryConfig & {
+    execute: ClickHouseQueryResultKind<T['_']['queryResult'], never>;
+    iterator: never;
+}, true>;
+export type ClickHouseUpdateDynamic<T extends AnyClickHouseUpdateBase> = ClickHouseUpdate<T['_']['table'], T['_']['queryResult'], T['_']['preparedQueryHKT']>;
+export type ClickHouseUpdate<TTable extends ClickHouseTable = ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT = AnyClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> = ClickHouseUpdateBase<TTable, TQueryResult, TPreparedQueryHKT, true, never>;
+export type AnyClickHouseUpdateBase = ClickHouseUpdateBase<any, any, any, any, any>;
+export interface ClickHouseUpdateBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>>, SQLWrapper {
+    readonly _: {
+        readonly table: TTable;
+        readonly queryResult: TQueryResult;
+        readonly preparedQueryHKT: TPreparedQueryHKT;
+        readonly dynamic: TDynamic;
+        readonly excludedMethods: TExcludedMethods;
+    };
+}
+export declare class ClickHouseUpdateBase<TTable extends ClickHouseTable, TQueryResult extends ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase, TDynamic extends boolean = false, TExcludedMethods extends string = never> extends QueryPromise<ClickHouseQueryResultKind<TQueryResult, never>> implements SQLWrapper {
+    private session;
+    private dialect;
+    static readonly [entityKind]: string;
+    private config;
+    constructor(table: TTable, set: UpdateSet, session: ClickHouseSession, dialect: ClickHouseDialect);
+    /**
+     * Adds a 'where' clause to the query.
+     *
+     * Calling this method will update only those rows that fulfill a specified condition.
+     *
+     * See docs: {@link https://orm.drizzle.team/docs/update}
+     *
+     * @param where the 'where' clause.
+     *
+     * @example
+     * You can use conditional operators and `sql function` to filter the rows to be updated.
+     *
+     * ```ts
+     * // Update all cars with green color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(eq(cars.color, 'green'));
+     * // or
+     * db.update(cars).set({ color: 'red' })
+     *   .where(sql`${cars.color} = 'green'`)
+     * ```
+     *
+     * You can logically combine conditional operators with `and()` and `or()` operators:
+     *
+     * ```ts
+     * // Update all BMW cars with a green color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+     *
+     * // Update all cars with the green or blue color
+     * db.update(cars).set({ color: 'red' })
+     *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+     * ```
+     */
+    where(where: SQL | undefined): ClickHouseUpdateWithout<this, TDynamic, 'where'>;
+    /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for the mutation. */
+    settings(settings: ClickHouseSettings): ClickHouseUpdateWithout<this, TDynamic, 'settings'>;
+    toSQL(): Query;
+    prepare(): ClickHouseUpdatePrepare<this>;
+    execute: ReturnType<this['prepare']>['execute'];
+    private createIterator;
+    iterator: ReturnType<this["prepare"]>["iterator"];
+    $dynamic(): ClickHouseUpdateDynamic<this>;
+}
diff --git a/clickhouse-core/query-builders/update.js b/clickhouse-core/query-builders/update.js
new file mode 100644
index 0000000..5bc5511
--- /dev/null
+++ b/clickhouse-core/query-builders/update.js
@@ -0,0 +1,113 @@
+import { entityKind } from "../../entity.js";
+import { QueryPromise } from "../../query-promise.js";
+import { mapUpdateSet } from "../../utils.js";
+import { extractUsedTable } from "../utils.js";
+class ClickHouseUpdateBuilder {
+  constructor(table, session, dialect) {
+    this.table = table;
+    this.session = session;
+    this.dialect = dialect;
+  }
+  table;
+  session;
+  dialect;
+  static [entityKind] = "ClickHouseUpdateBuilder";
+  set(values) {
+    return new ClickHouseUpdateBase(
+      this.table,
+      mapUpdateSet(this.table, values),
+      this.session,
+      this.dialect
+    );
+  }
+}
+class ClickHouseUpdateBase extends QueryPromise {
+  constructor(table, set, session, dialect) {
+    super();
+    this.session = session;
+    this.dialect = dialect;
+    this.config = { set, table };
+  }
+  session;
+  dialect;
+  static [entityKind] = "ClickHouseUpdate";
+  config;
+  /**
+   * Adds a 'where' clause to the query.
+   *
+   * Calling this method will update only those rows that fulfill a specified condition.
+   *
+   * See docs: {@link https://orm.drizzle.team/docs/update}
+   *
+   * @param where the 'where' clause.
+   *
+   * @example
+   * You can use conditional operators and `sql function` to filter the rows to be updated.
+   *
+   * ```ts
+   * // Update all cars with green color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(eq(cars.color, 'green'));
+   * // or
+   * db.update(cars).set({ color: 'red' })
+   *   .where(sql`${cars.color} = 'green'`)
+   * ```
+   *
+   * You can logically combine conditional operators with `and()` and `or()` operators:
+   *
+   * ```ts
+   * // Update all BMW cars with a green color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(and(eq(cars.color, 'green'), eq(cars.brand, 'BMW')));
+   *
+   * // Update all cars with the green or blue color
+   * db.update(cars).set({ color: 'red' })
+   *   .where(or(eq(cars.color, 'green'), eq(cars.color, 'blue')));
+   * ```
+   */
+  where(where) {
+    this.config.where = where;
+    return this;
+  }
+  /** Adds a query-level `SETTINGS` clause, e.g. `{ mutations_sync: 2 }` to wait for the mutation. */
+  settings(settings) {
+    this.config.settings = { ...this.config.settings, ...settings };
+    return this;
+  }
+  /** @internal */
+  getSQL() {
+    return this.dialect.buildUpdateQuery(this.config);
+  }
+  toSQL() {
+    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
+    return rest;
+  }
+  prepare() {
+    return this.session.prepareQuery(
+      this.dialect.sqlToQuery(this.getSQL()),
+      void 0,
+      void 0,
+      {
+        type: "update",
+        tables: extractUsedTable(this.config.table)
+      }
+    );
+  }
+  execute = (placeholderValues) => {
+    return this.prepare().execute(placeholderValues);
+  };
+  createIterator = () => {
+    const self = this;
+    return async function* (placeholderValues) {
+      yield* self.prepare().iterator(placeholderValues);
+    };
+  };
+  iterator = this.createIterator();
+  $dynamic() {
+    return this;
+  }
+}
+export {
+  ClickHouseUpdateBase,
+  ClickHouseUpdateBuilder
+};
diff --git a/clickhouse-core/schema.cjs b/clickhouse-core/schema.cjs
new file mode 100644
index 0000000..76a3a87
--- /dev/null
+++ b/clickhouse-core/schema.cjs
@@ -0,0 +1,52 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var schema_exports = {};
+__export(schema_exports, {
+  ClickHouseSchema: () => ClickHouseSchema,
+  clickhouseDatabase: () => clickhouseDatabase,
+  clickhouseSchema: () => clickhouseSchema,
+  isClickHouseSchema: () => isClickHouseSchema
+});
+module.exports = __toCommonJS(schema_exports);
+var import_entity = require("../entity.cjs");
+var import_table = require("./table.cjs");
+class ClickHouseSchema {
+  constructor(schemaName) {
+    this.schemaName = schemaName;
+  }
+  schemaName;
+  static [import_entity.entityKind] = "ClickHouseSchema";
+  table = (name, columns, extraConfig) => {
+    return (0, import_table.clickhouseTableWithSchema)(name, columns, extraConfig, this.schemaName);
+  };
+}
+function clickhouseDatabase(name) {
+  return new ClickHouseSchema(name);
+}
+const clickhouseSchema = clickhouseDatabase;
+function isClickHouseSchema(obj) {
+  return (0, import_entity.is)(obj, ClickHouseSchema);
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseSchema,
+  clickhouseDatabase,
+  clickhouseSchema,
+  isClickHouseSchema
+});
diff --git a/clickhouse-core/schema.d.cts b/clickhouse-core/schema.d.cts
new file mode 100644
index 0000000..d5c9bad
--- /dev/null
+++ b/clickhouse-core/schema.d.cts
@@ -0,0 +1,23 @@
+import { entityKind } from '../entity.cjs';
+import { type ClickHouseTableFn } from './table.cjs';
+/**
+ * A ClickHouse database, which is what the `SCHEMA`/`DATABASE` qualifier in a table name refers to.
+ */
+export declare class ClickHouseSchema<TName extends string = string> {
+    readonly schemaName: TName;
+    static readonly [entityKind]: string;
+    constructor(schemaName: TName);
+    table: ClickHouseTableFn<TName>;
+}
+/**
+ * Declares a ClickHouse database to qualify tables with.
+ *
+ * ```ts
+ * export const analytics = clickhouseDatabase('analytics');
+ * export const events = analytics.table('events', { … });
+ * ```
+ */
+export declare function clickhouseDatabase<TName extends string>(name: TName): ClickHouseSchema<TName>;
+/** @see clickhouseDatabase */
+export declare const clickhouseSchema: typeof clickhouseDatabase;
+export declare function isClickHouseSchema(obj: unknown): obj is ClickHouseSchema;
diff --git a/clickhouse-core/schema.d.ts b/clickhouse-core/schema.d.ts
new file mode 100644
index 0000000..1eca010
--- /dev/null
+++ b/clickhouse-core/schema.d.ts
@@ -0,0 +1,23 @@
+import { entityKind } from '../entity.js';
+import { type ClickHouseTableFn } from './table.js';
+/**
+ * A ClickHouse database, which is what the `SCHEMA`/`DATABASE` qualifier in a table name refers to.
+ */
+export declare class ClickHouseSchema<TName extends string = string> {
+    readonly schemaName: TName;
+    static readonly [entityKind]: string;
+    constructor(schemaName: TName);
+    table: ClickHouseTableFn<TName>;
+}
+/**
+ * Declares a ClickHouse database to qualify tables with.
+ *
+ * ```ts
+ * export const analytics = clickhouseDatabase('analytics');
+ * export const events = analytics.table('events', { … });
+ * ```
+ */
+export declare function clickhouseDatabase<TName extends string>(name: TName): ClickHouseSchema<TName>;
+/** @see clickhouseDatabase */
+export declare const clickhouseSchema: typeof clickhouseDatabase;
+export declare function isClickHouseSchema(obj: unknown): obj is ClickHouseSchema;
diff --git a/clickhouse-core/schema.js b/clickhouse-core/schema.js
new file mode 100644
index 0000000..7b4b54a
--- /dev/null
+++ b/clickhouse-core/schema.js
@@ -0,0 +1,25 @@
+import { entityKind, is } from "../entity.js";
+import { clickhouseTableWithSchema } from "./table.js";
+class ClickHouseSchema {
+  constructor(schemaName) {
+    this.schemaName = schemaName;
+  }
+  schemaName;
+  static [entityKind] = "ClickHouseSchema";
+  table = (name, columns, extraConfig) => {
+    return clickhouseTableWithSchema(name, columns, extraConfig, this.schemaName);
+  };
+}
+function clickhouseDatabase(name) {
+  return new ClickHouseSchema(name);
+}
+const clickhouseSchema = clickhouseDatabase;
+function isClickHouseSchema(obj) {
+  return is(obj, ClickHouseSchema);
+}
+export {
+  ClickHouseSchema,
+  clickhouseDatabase,
+  clickhouseSchema,
+  isClickHouseSchema
+};
diff --git a/clickhouse-core/session.cjs b/clickhouse-core/session.cjs
new file mode 100644
index 0000000..d4df30a
--- /dev/null
+++ b/clickhouse-core/session.cjs
@@ -0,0 +1,134 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var session_exports = {};
+__export(session_exports, {
+  ClickHousePreparedQuery: () => ClickHousePreparedQuery,
+  ClickHouseSession: () => ClickHouseSession
+});
+module.exports = __toCommonJS(session_exports);
+var import_cache = require("../cache/core/cache.cjs");
+var import_entity = require("../entity.cjs");
+var import_errors = require("../errors.cjs");
+class ClickHousePreparedQuery {
+  constructor(cache, queryMetadata, cacheConfig) {
+    this.cache = cache;
+    this.queryMetadata = queryMetadata;
+    this.cacheConfig = cacheConfig;
+    if (cache && cache.strategy() === "all" && cacheConfig === void 0) {
+      this.cacheConfig = { enable: true, autoInvalidate: true };
+    }
+    if (!this.cacheConfig?.enable) {
+      this.cacheConfig = void 0;
+    }
+  }
+  cache;
+  queryMetadata;
+  cacheConfig;
+  static [import_entity.entityKind] = "ClickHousePreparedQuery";
+  /** @internal */
+  async queryWithCache(queryString, params, query) {
+    if (this.cache === void 0 || (0, import_entity.is)(this.cache, import_cache.NoopCache) || this.queryMetadata === void 0) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new import_errors.DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (this.cacheConfig && !this.cacheConfig.enable) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new import_errors.DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if ((this.queryMetadata.type === "insert" || this.queryMetadata.type === "update" || this.queryMetadata.type === "delete") && this.queryMetadata.tables.length > 0) {
+      try {
+        const [res] = await Promise.all([
+          query(),
+          this.cache.onMutate({ tables: this.queryMetadata.tables })
+        ]);
+        return res;
+      } catch (e) {
+        throw new import_errors.DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (!this.cacheConfig) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new import_errors.DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (this.queryMetadata.type === "select") {
+      const fromCache = await this.cache.get(
+        this.cacheConfig.tag ?? await (0, import_cache.hashQuery)(queryString, params),
+        this.queryMetadata.tables,
+        this.cacheConfig.tag !== void 0,
+        this.cacheConfig.autoInvalidate
+      );
+      if (fromCache === void 0) {
+        let result;
+        try {
+          result = await query();
+        } catch (e) {
+          throw new import_errors.DrizzleQueryError(queryString, params, e);
+        }
+        await this.cache.put(
+          this.cacheConfig.tag ?? await (0, import_cache.hashQuery)(queryString, params),
+          result,
+          // make sure we send tables that were used in a query only if user wants to invalidate it on each write
+          this.cacheConfig.autoInvalidate ? this.queryMetadata.tables : [],
+          this.cacheConfig.tag !== void 0,
+          this.cacheConfig.config
+        );
+        return result;
+      }
+      return fromCache;
+    }
+    try {
+      return await query();
+    } catch (e) {
+      throw new import_errors.DrizzleQueryError(queryString, params, e);
+    }
+  }
+  /** @internal */
+  joinsNotNullableMap;
+}
+class ClickHouseSession {
+  constructor(dialect) {
+    this.dialect = dialect;
+  }
+  dialect;
+  static [import_entity.entityKind] = "ClickHouseSession";
+  execute(query) {
+    return this.prepareQuery(
+      this.dialect.sqlToQuery(query),
+      void 0
+    ).execute();
+  }
+  async count(sql) {
+    const rows = await this.all(sql);
+    return Number(rows[0]?.["count"] ?? 0);
+  }
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHousePreparedQuery,
+  ClickHouseSession
+});
diff --git a/clickhouse-core/session.d.cts b/clickhouse-core/session.d.cts
new file mode 100644
index 0000000..a3b8084
--- /dev/null
+++ b/clickhouse-core/session.d.cts
@@ -0,0 +1,88 @@
+import { type Cache } from '../cache/core/cache.cjs';
+import type { WithCacheConfig } from '../cache/core/types.cjs';
+import { entityKind } from '../entity.cjs';
+import type { Query, SQL } from '../sql/sql.cjs';
+import type { Assume, Equal } from '../utils.cjs';
+import type { ClickHouseDialect } from './dialect.cjs';
+import type { ClickHouseSettings } from './engines.cjs';
+import type { SelectedFieldsOrdered } from './query-builders/select.types.cjs';
+export interface ClickHouseQueryResultHKT {
+    readonly $brand: 'ClickHouseQueryResultHKT';
+    readonly row: unknown;
+    readonly type: unknown;
+}
+export interface AnyClickHouseQueryResultHKT extends ClickHouseQueryResultHKT {
+    readonly type: any;
+}
+export type ClickHouseQueryResultKind<TKind extends ClickHouseQueryResultHKT, TRow> = (TKind & {
+    readonly row: TRow;
+})['type'];
+export interface ClickHousePreparedQueryConfig {
+    execute: unknown;
+    iterator: unknown;
+}
+export interface ClickHousePreparedQueryHKT {
+    readonly $brand: 'ClickHousePreparedQueryHKT';
+    readonly config: unknown;
+    readonly type: unknown;
+}
+export type PreparedQueryKind<TKind extends ClickHousePreparedQueryHKT, TConfig extends ClickHousePreparedQueryConfig, TAssume extends boolean = false> = Equal<TAssume, true> extends true ? Assume<(TKind & {
+    readonly config: TConfig;
+})['type'], ClickHousePreparedQuery<TConfig>> : (TKind & {
+    readonly config: TConfig;
+})['type'];
+/** What a prepared query does, for the purposes of cache invalidation. */
+export interface ClickHouseQueryMetadata {
+    type: 'select' | 'update' | 'delete' | 'insert';
+    tables: string[];
+}
+export declare abstract class ClickHousePreparedQuery<T extends ClickHousePreparedQueryConfig> {
+    private cache?;
+    private queryMetadata?;
+    private cacheConfig?;
+    static readonly [entityKind]: string;
+    constructor(cache?: Cache | undefined, queryMetadata?: ClickHouseQueryMetadata | undefined, cacheConfig?: WithCacheConfig | undefined);
+    abstract execute(placeholderValues?: Record<string, unknown>): Promise<T['execute']>;
+    abstract iterator(placeholderValues?: Record<string, unknown>): AsyncGenerator<T['iterator']>;
+}
+/**
+ * A ClickHouse session.
+ *
+ * There is no `transaction()`: ClickHouse's transaction support is experimental, limited to a single
+ * node and off by default, so Drizzle does not pretend to offer atomicity it cannot deliver. Batch
+ * writes instead — a single multi-row `INSERT` is atomic per part.
+ */
+export declare abstract class ClickHouseSession<TQueryResult extends ClickHouseQueryResultHKT = ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> {
+    protected dialect: ClickHouseDialect;
+    static readonly [entityKind]: string;
+    constructor(dialect: ClickHouseDialect);
+    abstract prepareQuery<T extends ClickHousePreparedQueryConfig, TPreparedQueryHKT extends ClickHousePreparedQueryHKT>(query: Query, fields: SelectedFieldsOrdered | undefined, customResultMapper?: (rows: unknown[][]) => T['execute'], queryMetadata?: ClickHouseQueryMetadata, cacheConfig?: WithCacheConfig): PreparedQueryKind<TPreparedQueryHKT, T>;
+    execute<T>(query: SQL): Promise<T>;
+    abstract all<T = unknown>(query: SQL): Promise<T[]>;
+    /**
+     * Inserts rows as a *body* rather than as part of the statement.
+     *
+     * ClickHouse takes data by two routes. `INSERT … VALUES (…), (…)` puts it in the statement, which
+     * means the client builds the whole batch as one string and the server re-parses every field as a
+     * SQL expression. A row format sends `INSERT INTO t FORMAT JSONEachRow` followed by the rows,
+     * which streams, and is the route ClickHouse's own documentation points at for bulk loads.
+     *
+     * `rows` is an async iterable so a caller can hand over a source larger than memory; the driver
+     * pulls from it as the socket drains. Keys are ClickHouse column names, values have already been
+     * through {@link ClickHouseColumn.mapToRowValue}, and a row may omit a column — `JSONEachRow`
+     * matches by name and the server applies that column's `DEFAULT`.
+     */
+    abstract insertRows(table: string, rows: AsyncIterable<Record<string, unknown>>, options?: {
+        settings?: ClickHouseSettings;
+        metadata?: ClickHouseQueryMetadata;
+    }): Promise<{
+        query_id: string;
+    }>;
+    count(sql: SQL): Promise<number>;
+    /** Declared for symmetry with the other dialects; `TQueryResult` is only used at the type level. */
+    readonly $queryResult: TQueryResult;
+    readonly $preparedQueryHKT: TPreparedQueryHKT;
+}
+export interface PreparedQueryHKTBase extends ClickHousePreparedQueryHKT {
+    type: ClickHousePreparedQuery<Assume<this['config'], ClickHousePreparedQueryConfig>>;
+}
diff --git a/clickhouse-core/session.d.ts b/clickhouse-core/session.d.ts
new file mode 100644
index 0000000..ccafad3
--- /dev/null
+++ b/clickhouse-core/session.d.ts
@@ -0,0 +1,88 @@
+import { type Cache } from '../cache/core/cache.js';
+import type { WithCacheConfig } from '../cache/core/types.js';
+import { entityKind } from '../entity.js';
+import type { Query, SQL } from '../sql/sql.js';
+import type { Assume, Equal } from '../utils.js';
+import type { ClickHouseDialect } from './dialect.js';
+import type { ClickHouseSettings } from './engines.js';
+import type { SelectedFieldsOrdered } from './query-builders/select.types.js';
+export interface ClickHouseQueryResultHKT {
+    readonly $brand: 'ClickHouseQueryResultHKT';
+    readonly row: unknown;
+    readonly type: unknown;
+}
+export interface AnyClickHouseQueryResultHKT extends ClickHouseQueryResultHKT {
+    readonly type: any;
+}
+export type ClickHouseQueryResultKind<TKind extends ClickHouseQueryResultHKT, TRow> = (TKind & {
+    readonly row: TRow;
+})['type'];
+export interface ClickHousePreparedQueryConfig {
+    execute: unknown;
+    iterator: unknown;
+}
+export interface ClickHousePreparedQueryHKT {
+    readonly $brand: 'ClickHousePreparedQueryHKT';
+    readonly config: unknown;
+    readonly type: unknown;
+}
+export type PreparedQueryKind<TKind extends ClickHousePreparedQueryHKT, TConfig extends ClickHousePreparedQueryConfig, TAssume extends boolean = false> = Equal<TAssume, true> extends true ? Assume<(TKind & {
+    readonly config: TConfig;
+})['type'], ClickHousePreparedQuery<TConfig>> : (TKind & {
+    readonly config: TConfig;
+})['type'];
+/** What a prepared query does, for the purposes of cache invalidation. */
+export interface ClickHouseQueryMetadata {
+    type: 'select' | 'update' | 'delete' | 'insert';
+    tables: string[];
+}
+export declare abstract class ClickHousePreparedQuery<T extends ClickHousePreparedQueryConfig> {
+    private cache?;
+    private queryMetadata?;
+    private cacheConfig?;
+    static readonly [entityKind]: string;
+    constructor(cache?: Cache | undefined, queryMetadata?: ClickHouseQueryMetadata | undefined, cacheConfig?: WithCacheConfig | undefined);
+    abstract execute(placeholderValues?: Record<string, unknown>): Promise<T['execute']>;
+    abstract iterator(placeholderValues?: Record<string, unknown>): AsyncGenerator<T['iterator']>;
+}
+/**
+ * A ClickHouse session.
+ *
+ * There is no `transaction()`: ClickHouse's transaction support is experimental, limited to a single
+ * node and off by default, so Drizzle does not pretend to offer atomicity it cannot deliver. Batch
+ * writes instead — a single multi-row `INSERT` is atomic per part.
+ */
+export declare abstract class ClickHouseSession<TQueryResult extends ClickHouseQueryResultHKT = ClickHouseQueryResultHKT, TPreparedQueryHKT extends PreparedQueryHKTBase = PreparedQueryHKTBase> {
+    protected dialect: ClickHouseDialect;
+    static readonly [entityKind]: string;
+    constructor(dialect: ClickHouseDialect);
+    abstract prepareQuery<T extends ClickHousePreparedQueryConfig, TPreparedQueryHKT extends ClickHousePreparedQueryHKT>(query: Query, fields: SelectedFieldsOrdered | undefined, customResultMapper?: (rows: unknown[][]) => T['execute'], queryMetadata?: ClickHouseQueryMetadata, cacheConfig?: WithCacheConfig): PreparedQueryKind<TPreparedQueryHKT, T>;
+    execute<T>(query: SQL): Promise<T>;
+    abstract all<T = unknown>(query: SQL): Promise<T[]>;
+    /**
+     * Inserts rows as a *body* rather than as part of the statement.
+     *
+     * ClickHouse takes data by two routes. `INSERT … VALUES (…), (…)` puts it in the statement, which
+     * means the client builds the whole batch as one string and the server re-parses every field as a
+     * SQL expression. A row format sends `INSERT INTO t FORMAT JSONEachRow` followed by the rows,
+     * which streams, and is the route ClickHouse's own documentation points at for bulk loads.
+     *
+     * `rows` is an async iterable so a caller can hand over a source larger than memory; the driver
+     * pulls from it as the socket drains. Keys are ClickHouse column names, values have already been
+     * through {@link ClickHouseColumn.mapToRowValue}, and a row may omit a column — `JSONEachRow`
+     * matches by name and the server applies that column's `DEFAULT`.
+     */
+    abstract insertRows(table: string, rows: AsyncIterable<Record<string, unknown>>, options?: {
+        settings?: ClickHouseSettings;
+        metadata?: ClickHouseQueryMetadata;
+    }): Promise<{
+        query_id: string;
+    }>;
+    count(sql: SQL): Promise<number>;
+    /** Declared for symmetry with the other dialects; `TQueryResult` is only used at the type level. */
+    readonly $queryResult: TQueryResult;
+    readonly $preparedQueryHKT: TPreparedQueryHKT;
+}
+export interface PreparedQueryHKTBase extends ClickHousePreparedQueryHKT {
+    type: ClickHousePreparedQuery<Assume<this['config'], ClickHousePreparedQueryConfig>>;
+}
diff --git a/clickhouse-core/session.js b/clickhouse-core/session.js
new file mode 100644
index 0000000..8b0510f
--- /dev/null
+++ b/clickhouse-core/session.js
@@ -0,0 +1,109 @@
+import { hashQuery, NoopCache } from "../cache/core/cache.js";
+import { entityKind, is } from "../entity.js";
+import { DrizzleQueryError } from "../errors.js";
+class ClickHousePreparedQuery {
+  constructor(cache, queryMetadata, cacheConfig) {
+    this.cache = cache;
+    this.queryMetadata = queryMetadata;
+    this.cacheConfig = cacheConfig;
+    if (cache && cache.strategy() === "all" && cacheConfig === void 0) {
+      this.cacheConfig = { enable: true, autoInvalidate: true };
+    }
+    if (!this.cacheConfig?.enable) {
+      this.cacheConfig = void 0;
+    }
+  }
+  cache;
+  queryMetadata;
+  cacheConfig;
+  static [entityKind] = "ClickHousePreparedQuery";
+  /** @internal */
+  async queryWithCache(queryString, params, query) {
+    if (this.cache === void 0 || is(this.cache, NoopCache) || this.queryMetadata === void 0) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (this.cacheConfig && !this.cacheConfig.enable) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if ((this.queryMetadata.type === "insert" || this.queryMetadata.type === "update" || this.queryMetadata.type === "delete") && this.queryMetadata.tables.length > 0) {
+      try {
+        const [res] = await Promise.all([
+          query(),
+          this.cache.onMutate({ tables: this.queryMetadata.tables })
+        ]);
+        return res;
+      } catch (e) {
+        throw new DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (!this.cacheConfig) {
+      try {
+        return await query();
+      } catch (e) {
+        throw new DrizzleQueryError(queryString, params, e);
+      }
+    }
+    if (this.queryMetadata.type === "select") {
+      const fromCache = await this.cache.get(
+        this.cacheConfig.tag ?? await hashQuery(queryString, params),
+        this.queryMetadata.tables,
+        this.cacheConfig.tag !== void 0,
+        this.cacheConfig.autoInvalidate
+      );
+      if (fromCache === void 0) {
+        let result;
+        try {
+          result = await query();
+        } catch (e) {
+          throw new DrizzleQueryError(queryString, params, e);
+        }
+        await this.cache.put(
+          this.cacheConfig.tag ?? await hashQuery(queryString, params),
+          result,
+          // make sure we send tables that were used in a query only if user wants to invalidate it on each write
+          this.cacheConfig.autoInvalidate ? this.queryMetadata.tables : [],
+          this.cacheConfig.tag !== void 0,
+          this.cacheConfig.config
+        );
+        return result;
+      }
+      return fromCache;
+    }
+    try {
+      return await query();
+    } catch (e) {
+      throw new DrizzleQueryError(queryString, params, e);
+    }
+  }
+  /** @internal */
+  joinsNotNullableMap;
+}
+class ClickHouseSession {
+  constructor(dialect) {
+    this.dialect = dialect;
+  }
+  dialect;
+  static [entityKind] = "ClickHouseSession";
+  execute(query) {
+    return this.prepareQuery(
+      this.dialect.sqlToQuery(query),
+      void 0
+    ).execute();
+  }
+  async count(sql) {
+    const rows = await this.all(sql);
+    return Number(rows[0]?.["count"] ?? 0);
+  }
+}
+export {
+  ClickHousePreparedQuery,
+  ClickHouseSession
+};
diff --git a/clickhouse-core/subquery.cjs b/clickhouse-core/subquery.cjs
new file mode 100644
index 0000000..008f64e
--- /dev/null
+++ b/clickhouse-core/subquery.cjs
@@ -0,0 +1,16 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var subquery_exports = {};
+module.exports = __toCommonJS(subquery_exports);
diff --git a/clickhouse-core/subquery.d.cts b/clickhouse-core/subquery.d.cts
new file mode 100644
index 0000000..fea28ad
--- /dev/null
+++ b/clickhouse-core/subquery.d.cts
@@ -0,0 +1,18 @@
+import type { TypedQueryBuilder } from '../query-builders/query-builder.cjs';
+import type { AddAliasToSelection } from '../query-builders/select.types.cjs';
+import type { ColumnsSelection, SQL } from '../sql/sql.cjs';
+import type { Subquery, WithSubquery, WithSubqueryWithoutSelection } from '../subquery.cjs';
+import type { QueryBuilder } from './query-builders/query-builder.cjs';
+export type SubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = Subquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'clickhouse'>> & AddAliasToSelection<TSelection, TAlias, 'clickhouse'>;
+export type WithSubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = WithSubquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'clickhouse'>> & AddAliasToSelection<TSelection, TAlias, 'clickhouse'>;
+export interface WithBuilder {
+    <TAlias extends string>(alias: TAlias): {
+        as: {
+            <TSelection extends ColumnsSelection>(qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>)): WithSubqueryWithSelection<TSelection, TAlias>;
+            (qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>)): WithSubqueryWithoutSelection<TAlias>;
+        };
+    };
+    <TAlias extends string, TSelection extends ColumnsSelection>(alias: TAlias, selection: TSelection): {
+        as: (qb: SQL | ((qb: QueryBuilder) => SQL)) => WithSubqueryWithSelection<TSelection, TAlias>;
+    };
+}
diff --git a/clickhouse-core/subquery.d.ts b/clickhouse-core/subquery.d.ts
new file mode 100644
index 0000000..2b8db99
--- /dev/null
+++ b/clickhouse-core/subquery.d.ts
@@ -0,0 +1,18 @@
+import type { TypedQueryBuilder } from '../query-builders/query-builder.js';
+import type { AddAliasToSelection } from '../query-builders/select.types.js';
+import type { ColumnsSelection, SQL } from '../sql/sql.js';
+import type { Subquery, WithSubquery, WithSubqueryWithoutSelection } from '../subquery.js';
+import type { QueryBuilder } from './query-builders/query-builder.js';
+export type SubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = Subquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'clickhouse'>> & AddAliasToSelection<TSelection, TAlias, 'clickhouse'>;
+export type WithSubqueryWithSelection<TSelection extends ColumnsSelection, TAlias extends string> = WithSubquery<TAlias, AddAliasToSelection<TSelection, TAlias, 'clickhouse'>> & AddAliasToSelection<TSelection, TAlias, 'clickhouse'>;
+export interface WithBuilder {
+    <TAlias extends string>(alias: TAlias): {
+        as: {
+            <TSelection extends ColumnsSelection>(qb: TypedQueryBuilder<TSelection> | ((qb: QueryBuilder) => TypedQueryBuilder<TSelection>)): WithSubqueryWithSelection<TSelection, TAlias>;
+            (qb: TypedQueryBuilder<undefined> | ((qb: QueryBuilder) => TypedQueryBuilder<undefined>)): WithSubqueryWithoutSelection<TAlias>;
+        };
+    };
+    <TAlias extends string, TSelection extends ColumnsSelection>(alias: TAlias, selection: TSelection): {
+        as: (qb: SQL | ((qb: QueryBuilder) => SQL)) => WithSubqueryWithSelection<TSelection, TAlias>;
+    };
+}
diff --git a/clickhouse-core/subquery.js b/clickhouse-core/subquery.js
new file mode 100644
index 0000000..e69de29
diff --git a/clickhouse-core/table.cjs b/clickhouse-core/table.cjs
new file mode 100644
index 0000000..4f6b4f4
--- /dev/null
+++ b/clickhouse-core/table.cjs
@@ -0,0 +1,72 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var table_exports = {};
+__export(table_exports, {
+  ClickHouseTable: () => ClickHouseTable,
+  clickhouseTable: () => clickhouseTable,
+  clickhouseTableCreator: () => clickhouseTableCreator,
+  clickhouseTableWithSchema: () => clickhouseTableWithSchema
+});
+module.exports = __toCommonJS(table_exports);
+var import_entity = require("../entity.cjs");
+var import_table = require("../table.cjs");
+var import_all = require("./columns/all.cjs");
+class ClickHouseTable extends import_table.Table {
+  static [import_entity.entityKind] = "ClickHouseTable";
+  /** @internal */
+  static Symbol = Object.assign({}, import_table.Table.Symbol, {});
+  /** @internal */
+  [import_table.Table.Symbol.Columns];
+  /** @internal */
+  [import_table.Table.Symbol.ExtraConfigBuilder] = void 0;
+}
+function clickhouseTableWithSchema(name, columns, extraConfig, schema, baseName = name) {
+  const rawTable = new ClickHouseTable(name, schema, baseName);
+  const parsedColumns = typeof columns === "function" ? columns((0, import_all.getClickHouseColumnBuilders)()) : columns;
+  const builtColumns = Object.fromEntries(
+    Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
+      const colBuilder = colBuilderBase;
+      colBuilder.setName(name2);
+      const column = colBuilder.build(rawTable);
+      return [name2, column];
+    })
+  );
+  const table = Object.assign(rawTable, builtColumns);
+  table[import_table.Table.Symbol.Columns] = builtColumns;
+  table[import_table.Table.Symbol.ExtraConfigColumns] = builtColumns;
+  if (extraConfig) {
+    table[ClickHouseTable.Symbol.ExtraConfigBuilder] = extraConfig;
+  }
+  return table;
+}
+const clickhouseTable = (name, columns, extraConfig) => {
+  return clickhouseTableWithSchema(name, columns, extraConfig, void 0, name);
+};
+function clickhouseTableCreator(customizeTableName) {
+  return (name, columns, extraConfig) => {
+    return clickhouseTableWithSchema(customizeTableName(name), columns, extraConfig, void 0, name);
+  };
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseTable,
+  clickhouseTable,
+  clickhouseTableCreator,
+  clickhouseTableWithSchema
+});
diff --git a/clickhouse-core/table.d.cts b/clickhouse-core/table.d.cts
new file mode 100644
index 0000000..e18b245
--- /dev/null
+++ b/clickhouse-core/table.d.cts
@@ -0,0 +1,64 @@
+import type { BuildColumns } from '../column-builder.cjs';
+import { entityKind } from '../entity.cjs';
+import { Table, type TableConfig as TableConfigBase, type UpdateTableConfig } from '../table.cjs';
+import { type ClickHouseColumnBuilders } from './columns/all.cjs';
+import type { ClickHouseColumn, ClickHouseColumnBuilderBase } from './columns/common.cjs';
+import type { ClickHouseTableEngine } from './engines.cjs';
+import type { AnyIndexBuilder, ProjectionBuilder } from './indexes.cjs';
+/**
+ * Anything a ClickHouse table can declare alongside its columns: the table engine, data-skipping
+ * indexes and projections.
+ */
+export type ClickHouseTableExtraConfigValue = ClickHouseTableEngine | AnyIndexBuilder | ProjectionBuilder;
+export type TableConfig = TableConfigBase<ClickHouseColumn>;
+export declare class ClickHouseTable<T extends TableConfig = TableConfig> extends Table<T> {
+    static readonly [entityKind]: string;
+    protected $columns: T['columns'];
+}
+export type AnyClickHouseTable<TPartial extends Partial<TableConfig> = {}> = ClickHouseTable<UpdateTableConfig<TableConfig, TPartial>>;
+export type ClickHouseTableWithColumns<T extends TableConfig> = ClickHouseTable<T> & {
+    [Key in keyof T['columns']]: T['columns'][Key];
+};
+export declare function clickhouseTableWithSchema<TTableName extends string, TSchemaName extends string | undefined, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: TColumnsMap | ((columnTypes: ClickHouseColumnBuilders) => TColumnsMap), extraConfig: ((self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]) | undefined, schema: TSchemaName, baseName?: TTableName): ClickHouseTableWithColumns<{
+    name: TTableName;
+    schema: TSchemaName;
+    columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+    dialect: 'clickhouse';
+}>;
+export interface ClickHouseTableFn<TSchemaName extends string | undefined = undefined> {
+    <TTableName extends string, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: TColumnsMap, extraConfig?: (self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]): ClickHouseTableWithColumns<{
+        name: TTableName;
+        schema: TSchemaName;
+        columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+        dialect: 'clickhouse';
+    }>;
+    <TTableName extends string, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: (columnTypes: ClickHouseColumnBuilders) => TColumnsMap, extraConfig?: (self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]): ClickHouseTableWithColumns<{
+        name: TTableName;
+        schema: TSchemaName;
+        columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+        dialect: 'clickhouse';
+    }>;
+}
+/**
+ * Declares a ClickHouse table.
+ *
+ * Unlike the row-store dialects, the third argument is where the *engine* is declared — ClickHouse
+ * tables have no meaningful default beyond `MergeTree`, and the sorting key is what determines how
+ * the table performs.
+ *
+ * ```ts
+ * export const events = clickhouseTable('events', {
+ * 	id: uint64().notNull(),
+ * 	ts: dateTime({ timezone: 'UTC' }).notNull(),
+ * 	url: lowCardinality(string()).notNull(),
+ * }, (t) => [
+ * 	mergeTree({
+ * 		orderBy: [t.ts, t.id],
+ * 		partitionBy: sql`toYYYYMM(${t.ts})`,
+ * 	}),
+ * 	index('idx_url').on(t.url).bloomFilter(0.01).granularity(4),
+ * ]);
+ * ```
+ */
+export declare const clickhouseTable: ClickHouseTableFn;
+export declare function clickhouseTableCreator(customizeTableName: (name: string) => string): ClickHouseTableFn;
diff --git a/clickhouse-core/table.d.ts b/clickhouse-core/table.d.ts
new file mode 100644
index 0000000..a016cc6
--- /dev/null
+++ b/clickhouse-core/table.d.ts
@@ -0,0 +1,64 @@
+import type { BuildColumns } from '../column-builder.js';
+import { entityKind } from '../entity.js';
+import { Table, type TableConfig as TableConfigBase, type UpdateTableConfig } from '../table.js';
+import { type ClickHouseColumnBuilders } from './columns/all.js';
+import type { ClickHouseColumn, ClickHouseColumnBuilderBase } from './columns/common.js';
+import type { ClickHouseTableEngine } from './engines.js';
+import type { AnyIndexBuilder, ProjectionBuilder } from './indexes.js';
+/**
+ * Anything a ClickHouse table can declare alongside its columns: the table engine, data-skipping
+ * indexes and projections.
+ */
+export type ClickHouseTableExtraConfigValue = ClickHouseTableEngine | AnyIndexBuilder | ProjectionBuilder;
+export type TableConfig = TableConfigBase<ClickHouseColumn>;
+export declare class ClickHouseTable<T extends TableConfig = TableConfig> extends Table<T> {
+    static readonly [entityKind]: string;
+    protected $columns: T['columns'];
+}
+export type AnyClickHouseTable<TPartial extends Partial<TableConfig> = {}> = ClickHouseTable<UpdateTableConfig<TableConfig, TPartial>>;
+export type ClickHouseTableWithColumns<T extends TableConfig> = ClickHouseTable<T> & {
+    [Key in keyof T['columns']]: T['columns'][Key];
+};
+export declare function clickhouseTableWithSchema<TTableName extends string, TSchemaName extends string | undefined, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: TColumnsMap | ((columnTypes: ClickHouseColumnBuilders) => TColumnsMap), extraConfig: ((self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]) | undefined, schema: TSchemaName, baseName?: TTableName): ClickHouseTableWithColumns<{
+    name: TTableName;
+    schema: TSchemaName;
+    columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+    dialect: 'clickhouse';
+}>;
+export interface ClickHouseTableFn<TSchemaName extends string | undefined = undefined> {
+    <TTableName extends string, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: TColumnsMap, extraConfig?: (self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]): ClickHouseTableWithColumns<{
+        name: TTableName;
+        schema: TSchemaName;
+        columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+        dialect: 'clickhouse';
+    }>;
+    <TTableName extends string, TColumnsMap extends Record<string, ClickHouseColumnBuilderBase>>(name: TTableName, columns: (columnTypes: ClickHouseColumnBuilders) => TColumnsMap, extraConfig?: (self: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>) => ClickHouseTableExtraConfigValue[]): ClickHouseTableWithColumns<{
+        name: TTableName;
+        schema: TSchemaName;
+        columns: BuildColumns<TTableName, TColumnsMap, 'clickhouse'>;
+        dialect: 'clickhouse';
+    }>;
+}
+/**
+ * Declares a ClickHouse table.
+ *
+ * Unlike the row-store dialects, the third argument is where the *engine* is declared — ClickHouse
+ * tables have no meaningful default beyond `MergeTree`, and the sorting key is what determines how
+ * the table performs.
+ *
+ * ```ts
+ * export const events = clickhouseTable('events', {
+ * 	id: uint64().notNull(),
+ * 	ts: dateTime({ timezone: 'UTC' }).notNull(),
+ * 	url: lowCardinality(string()).notNull(),
+ * }, (t) => [
+ * 	mergeTree({
+ * 		orderBy: [t.ts, t.id],
+ * 		partitionBy: sql`toYYYYMM(${t.ts})`,
+ * 	}),
+ * 	index('idx_url').on(t.url).bloomFilter(0.01).granularity(4),
+ * ]);
+ * ```
+ */
+export declare const clickhouseTable: ClickHouseTableFn;
+export declare function clickhouseTableCreator(customizeTableName: (name: string) => string): ClickHouseTableFn;
diff --git a/clickhouse-core/table.js b/clickhouse-core/table.js
new file mode 100644
index 0000000..4fe5b33
--- /dev/null
+++ b/clickhouse-core/table.js
@@ -0,0 +1,45 @@
+import { entityKind } from "../entity.js";
+import { Table } from "../table.js";
+import { getClickHouseColumnBuilders } from "./columns/all.js";
+class ClickHouseTable extends Table {
+  static [entityKind] = "ClickHouseTable";
+  /** @internal */
+  static Symbol = Object.assign({}, Table.Symbol, {});
+  /** @internal */
+  [Table.Symbol.Columns];
+  /** @internal */
+  [Table.Symbol.ExtraConfigBuilder] = void 0;
+}
+function clickhouseTableWithSchema(name, columns, extraConfig, schema, baseName = name) {
+  const rawTable = new ClickHouseTable(name, schema, baseName);
+  const parsedColumns = typeof columns === "function" ? columns(getClickHouseColumnBuilders()) : columns;
+  const builtColumns = Object.fromEntries(
+    Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
+      const colBuilder = colBuilderBase;
+      colBuilder.setName(name2);
+      const column = colBuilder.build(rawTable);
+      return [name2, column];
+    })
+  );
+  const table = Object.assign(rawTable, builtColumns);
+  table[Table.Symbol.Columns] = builtColumns;
+  table[Table.Symbol.ExtraConfigColumns] = builtColumns;
+  if (extraConfig) {
+    table[ClickHouseTable.Symbol.ExtraConfigBuilder] = extraConfig;
+  }
+  return table;
+}
+const clickhouseTable = (name, columns, extraConfig) => {
+  return clickhouseTableWithSchema(name, columns, extraConfig, void 0, name);
+};
+function clickhouseTableCreator(customizeTableName) {
+  return (name, columns, extraConfig) => {
+    return clickhouseTableWithSchema(customizeTableName(name), columns, extraConfig, void 0, name);
+  };
+}
+export {
+  ClickHouseTable,
+  clickhouseTable,
+  clickhouseTableCreator,
+  clickhouseTableWithSchema
+};
diff --git a/clickhouse-core/utils.cjs b/clickhouse-core/utils.cjs
new file mode 100644
index 0000000..f9a4e4b
--- /dev/null
+++ b/clickhouse-core/utils.cjs
@@ -0,0 +1,82 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var utils_exports = {};
+__export(utils_exports, {
+  extractUsedTable: () => extractUsedTable,
+  getTableConfig: () => getTableConfig
+});
+module.exports = __toCommonJS(utils_exports);
+var import_entity = require("../entity.cjs");
+var import_sql = require("../sql/sql.cjs");
+var import_subquery = require("../subquery.cjs");
+var import_table = require("../table.cjs");
+var import_engines = require("./engines.cjs");
+var import_indexes = require("./indexes.cjs");
+var import_table2 = require("./table.cjs");
+function extractUsedTable(table) {
+  if ((0, import_entity.is)(table, import_table2.ClickHouseTable)) {
+    return [`${table[import_table.Table.Symbol.BaseName]}`];
+  }
+  if ((0, import_entity.is)(table, import_subquery.Subquery)) {
+    return table._.usedTables ?? [];
+  }
+  if ((0, import_entity.is)(table, import_sql.SQL)) {
+    return table.usedTables ?? [];
+  }
+  return [];
+}
+function getTableConfig(table) {
+  const columns = Object.values(table[import_table2.ClickHouseTable.Symbol.Columns]);
+  const indexes = [];
+  const projections = [];
+  let engine;
+  const name = table[import_table.Table.Symbol.Name];
+  const schema = table[import_table.Table.Symbol.Schema];
+  const baseName = table[import_table.Table.Symbol.BaseName];
+  const extraConfigBuilder = table[import_table2.ClickHouseTable.Symbol.ExtraConfigBuilder];
+  if (extraConfigBuilder !== void 0) {
+    const extraConfig = extraConfigBuilder(table[import_table2.ClickHouseTable.Symbol.Columns]);
+    for (const builder of Array.isArray(extraConfig) ? extraConfig.flat(1) : Object.values(extraConfig)) {
+      if ((0, import_entity.is)(builder, import_engines.ClickHouseTableEngine)) {
+        if (engine !== void 0) {
+          throw new Error(`Table "${name}" declares more than one engine`);
+        }
+        engine = builder;
+      } else if ((0, import_entity.is)(builder, import_indexes.IndexBuilder)) {
+        indexes.push(builder.build(table));
+      } else if ((0, import_entity.is)(builder, import_indexes.ProjectionBuilder)) {
+        projections.push(builder.build(table));
+      }
+    }
+  }
+  return {
+    columns,
+    engine,
+    indexes,
+    projections,
+    name,
+    schema,
+    baseName
+  };
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  extractUsedTable,
+  getTableConfig
+});
diff --git a/clickhouse-core/utils.d.cts b/clickhouse-core/utils.d.cts
new file mode 100644
index 0000000..dfcf960
--- /dev/null
+++ b/clickhouse-core/utils.d.cts
@@ -0,0 +1,22 @@
+import { SQL } from '../sql/sql.cjs';
+import { Subquery } from '../subquery.cjs';
+import { ClickHouseTableEngine } from './engines.cjs';
+import { type Index, type Projection } from './indexes.cjs';
+import { ClickHouseTable } from './table.cjs';
+export declare function extractUsedTable(table: ClickHouseTable | Subquery | SQL): string[];
+/**
+ * Reads back everything a table declared: its columns, its engine and the indexes and projections
+ * from the extra-config array.
+ *
+ * This is what drizzle-kit reads to generate DDL, and is also the supported way for application code
+ * to introspect a schema.
+ */
+export declare function getTableConfig(table: ClickHouseTable): {
+    columns: import("./index.cjs").ClickHouseColumn<import("../column.cjs").ColumnBaseConfig<import("../column-builder.cjs").ColumnDataType, string>, {}, {}>[];
+    engine: ClickHouseTableEngine | undefined;
+    indexes: Index[];
+    projections: Projection[];
+    name: string;
+    schema: string | undefined;
+    baseName: string;
+};
diff --git a/clickhouse-core/utils.d.ts b/clickhouse-core/utils.d.ts
new file mode 100644
index 0000000..ad9dfd0
--- /dev/null
+++ b/clickhouse-core/utils.d.ts
@@ -0,0 +1,22 @@
+import { SQL } from '../sql/sql.js';
+import { Subquery } from '../subquery.js';
+import { ClickHouseTableEngine } from './engines.js';
+import { type Index, type Projection } from './indexes.js';
+import { ClickHouseTable } from './table.js';
+export declare function extractUsedTable(table: ClickHouseTable | Subquery | SQL): string[];
+/**
+ * Reads back everything a table declared: its columns, its engine and the indexes and projections
+ * from the extra-config array.
+ *
+ * This is what drizzle-kit reads to generate DDL, and is also the supported way for application code
+ * to introspect a schema.
+ */
+export declare function getTableConfig(table: ClickHouseTable): {
+    columns: import("./index.js").ClickHouseColumn<import("../column.js").ColumnBaseConfig<import("../column-builder.js").ColumnDataType, string>, {}, {}>[];
+    engine: ClickHouseTableEngine | undefined;
+    indexes: Index[];
+    projections: Projection[];
+    name: string;
+    schema: string | undefined;
+    baseName: string;
+};
diff --git a/clickhouse-core/utils.js b/clickhouse-core/utils.js
new file mode 100644
index 0000000..20432a6
--- /dev/null
+++ b/clickhouse-core/utils.js
@@ -0,0 +1,57 @@
+import { is } from "../entity.js";
+import { SQL } from "../sql/sql.js";
+import { Subquery } from "../subquery.js";
+import { Table } from "../table.js";
+import { ClickHouseTableEngine } from "./engines.js";
+import { IndexBuilder, ProjectionBuilder } from "./indexes.js";
+import { ClickHouseTable } from "./table.js";
+function extractUsedTable(table) {
+  if (is(table, ClickHouseTable)) {
+    return [`${table[Table.Symbol.BaseName]}`];
+  }
+  if (is(table, Subquery)) {
+    return table._.usedTables ?? [];
+  }
+  if (is(table, SQL)) {
+    return table.usedTables ?? [];
+  }
+  return [];
+}
+function getTableConfig(table) {
+  const columns = Object.values(table[ClickHouseTable.Symbol.Columns]);
+  const indexes = [];
+  const projections = [];
+  let engine;
+  const name = table[Table.Symbol.Name];
+  const schema = table[Table.Symbol.Schema];
+  const baseName = table[Table.Symbol.BaseName];
+  const extraConfigBuilder = table[ClickHouseTable.Symbol.ExtraConfigBuilder];
+  if (extraConfigBuilder !== void 0) {
+    const extraConfig = extraConfigBuilder(table[ClickHouseTable.Symbol.Columns]);
+    for (const builder of Array.isArray(extraConfig) ? extraConfig.flat(1) : Object.values(extraConfig)) {
+      if (is(builder, ClickHouseTableEngine)) {
+        if (engine !== void 0) {
+          throw new Error(`Table "${name}" declares more than one engine`);
+        }
+        engine = builder;
+      } else if (is(builder, IndexBuilder)) {
+        indexes.push(builder.build(table));
+      } else if (is(builder, ProjectionBuilder)) {
+        projections.push(builder.build(table));
+      }
+    }
+  }
+  return {
+    columns,
+    engine,
+    indexes,
+    projections,
+    name,
+    schema,
+    baseName
+  };
+}
+export {
+  extractUsedTable,
+  getTableConfig
+};
diff --git a/clickhouse-core/view-base.cjs b/clickhouse-core/view-base.cjs
new file mode 100644
index 0000000..8f389d0
--- /dev/null
+++ b/clickhouse-core/view-base.cjs
@@ -0,0 +1,32 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var view_base_exports = {};
+__export(view_base_exports, {
+  ClickHouseViewBase: () => ClickHouseViewBase
+});
+module.exports = __toCommonJS(view_base_exports);
+var import_entity = require("../entity.cjs");
+var import_sql = require("../sql/sql.cjs");
+class ClickHouseViewBase extends import_sql.View {
+  static [import_entity.entityKind] = "ClickHouseViewBase";
+}
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseViewBase
+});
diff --git a/clickhouse-core/view-base.d.cts b/clickhouse-core/view-base.d.cts
new file mode 100644
index 0000000..a81e298
--- /dev/null
+++ b/clickhouse-core/view-base.d.cts
@@ -0,0 +1,9 @@
+import { entityKind } from '../entity.cjs';
+import type { ColumnsSelection } from '../sql/sql.cjs';
+import { View } from '../sql/sql.cjs';
+export declare abstract class ClickHouseViewBase<TName extends string = string, TExisting extends boolean = boolean, TSelectedFields extends ColumnsSelection = ColumnsSelection> extends View<TName, TExisting, TSelectedFields> {
+    static readonly [entityKind]: string;
+    readonly _: View<TName, TExisting, TSelectedFields>['_'] & {
+        readonly viewBrand: 'ClickHouseViewBase';
+    };
+}
diff --git a/clickhouse-core/view-base.d.ts b/clickhouse-core/view-base.d.ts
new file mode 100644
index 0000000..89dec99
--- /dev/null
+++ b/clickhouse-core/view-base.d.ts
@@ -0,0 +1,9 @@
+import { entityKind } from '../entity.js';
+import type { ColumnsSelection } from '../sql/sql.js';
+import { View } from '../sql/sql.js';
+export declare abstract class ClickHouseViewBase<TName extends string = string, TExisting extends boolean = boolean, TSelectedFields extends ColumnsSelection = ColumnsSelection> extends View<TName, TExisting, TSelectedFields> {
+    static readonly [entityKind]: string;
+    readonly _: View<TName, TExisting, TSelectedFields>['_'] & {
+        readonly viewBrand: 'ClickHouseViewBase';
+    };
+}
diff --git a/clickhouse-core/view-base.js b/clickhouse-core/view-base.js
new file mode 100644
index 0000000..1dfa652
--- /dev/null
+++ b/clickhouse-core/view-base.js
@@ -0,0 +1,8 @@
+import { entityKind } from "../entity.js";
+import { View } from "../sql/sql.js";
+class ClickHouseViewBase extends View {
+  static [entityKind] = "ClickHouseViewBase";
+}
+export {
+  ClickHouseViewBase
+};
diff --git a/clickhouse-core/view-common.cjs b/clickhouse-core/view-common.cjs
new file mode 100644
index 0000000..5c8ea2d
--- /dev/null
+++ b/clickhouse-core/view-common.cjs
@@ -0,0 +1,28 @@
+"use strict";
+var __defProp = Object.defineProperty;
+var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
+var __getOwnPropNames = Object.getOwnPropertyNames;
+var __hasOwnProp = Object.prototype.hasOwnProperty;
+var __export = (target, all) => {
+  for (var name in all)
+    __defProp(target, name, { get: all[name], enumerable: true });
+};
+var __copyProps = (to, from, except, desc) => {
+  if (from && typeof from === "object" || typeof from === "function") {
+    for (let key of __getOwnPropNames(from))
+      if (!__hasOwnProp.call(to, key) && key !== except)
+        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
+  }
+  return to;
+};
+var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
+var view_common_exports = {};
+__export(view_common_exports, {
+  ClickHouseViewConfig: () => ClickHouseViewConfig
+});
+module.exports = __toCommonJS(view_common_exports);
+const ClickHouseViewConfig = /* @__PURE__ */ Symbol.for("drizzle:ClickHouseViewConfig");
+// Annotate the CommonJS export names for ESM import in node:
+0 && (module.exports = {
+  ClickHouseViewConfig
+});
diff --git a/clickhouse-core/view-common.d.cts b/clickhouse-core/view-common.d.cts
new file mode 100644
index 0000000..26f095f
--- /dev/null
+++ b/clickhouse-core/view-common.d.cts
@@ -0,0 +1 @@
+export declare const ClickHouseViewConfig: unique symbol;
diff --git a/clickhouse-core/view-common.d.ts b/clickhouse-core/view-common.d.ts
new file mode 100644
index 0000000..26f095f
--- /dev/null
+++ b/clickhouse-core/view-common.d.ts
@@ -0,0 +1 @@
+export declare const ClickHouseViewConfig: unique symbol;
diff --git a/clickhouse-core/view-common.js b/clickhouse-core/view-common.js
new file mode 100644
index 0000000..919a7a2
--- /dev/null
+++ b/clickhouse-core/view-common.js
@@ -0,0 +1,4 @@
+const ClickHouseViewConfig = /* @__PURE__ */ Symbol.for("drizzle:ClickHouseViewConfig");
+export {
+  ClickHouseViewConfig
+};
diff --git a/column-builder.d.cts b/column-builder.d.cts
index 37a2bfb..5ca9230 100644
--- a/column-builder.d.cts
+++ b/column-builder.d.cts
@@ -1,4 +1,5 @@
 import { entityKind } from "./entity.cjs";
+import type { ClickHouseColumn } from "./clickhouse-core/columns/common.cjs";
 import type { Column } from "./column.cjs";
 import type { GelColumn, GelExtraConfigColumn } from "./gel-core/index.cjs";
 import type { MySqlColumn } from "./mysql-core/index.cjs";
@@ -8,7 +9,7 @@ import type { SQL } from "./sql/sql.cjs";
 import type { SQLiteColumn } from "./sqlite-core/index.cjs";
 import type { Assume, Simplify } from "./utils.cjs";
 export type ColumnDataType = 'string' | 'number' | 'boolean' | 'array' | 'json' | 'date' | 'bigint' | 'custom' | 'buffer' | 'dateDuration' | 'duration' | 'relDuration' | 'localTime' | 'localDate' | 'localDateTime';
-export type Dialect = 'pg' | 'mysql' | 'sqlite' | 'singlestore' | 'common' | 'gel';
+export type Dialect = 'pg' | 'mysql' | 'sqlite' | 'singlestore' | 'clickhouse' | 'common' | 'gel';
 export type GeneratedStorageMode = 'virtual' | 'stored';
 export type GeneratedType = 'always' | 'byDefault';
 export type GeneratedColumnConfig<TDataType> = {
@@ -227,7 +228,7 @@ export declare abstract class ColumnBuilder<T extends ColumnBuilderBaseConfig<Co
         type: 'always';
     }>;
 }
-export type BuildColumn<TTableName extends string, TBuilder extends ColumnBuilderBase, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'mysqlColumnBuilderBrand'>>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'common' ? Column<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'singlestoreColumnBuilderBrand'>>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : never;
+export type BuildColumn<TTableName extends string, TBuilder extends ColumnBuilderBase, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'mysqlColumnBuilderBrand'>>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'common' ? Column<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'singlestoreColumnBuilderBrand'>>> : TDialect extends 'clickhouse' ? ClickHouseColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : never;
 export type BuildIndexColumn<TDialect extends Dialect> = TDialect extends 'pg' ? ExtraConfigColumn : TDialect extends 'gel' ? GelExtraConfigColumn : never;
 export type BuildColumns<TTableName extends string, TConfigMap extends Record<string, ColumnBuilderBase>, TDialect extends Dialect> = {
     [Key in keyof TConfigMap]: BuildColumn<TTableName, {
@@ -239,4 +240,4 @@ export type BuildColumns<TTableName extends string, TConfigMap extends Record<st
 export type BuildExtraConfigColumns<_TTableName extends string, TConfigMap extends Record<string, ColumnBuilderBase>, TDialect extends Dialect> = {
     [Key in keyof TConfigMap]: BuildIndexColumn<TDialect>;
 } & {};
-export type ChangeColumnTableName<TColumn extends Column, TAlias extends string, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TColumn['_'], TAlias>> : never;
+export type ChangeColumnTableName<TColumn extends Column, TAlias extends string, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'clickhouse' ? ClickHouseColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TColumn['_'], TAlias>> : never;
diff --git a/column-builder.d.ts b/column-builder.d.ts
index 07dfef9..e8f8c85 100644
--- a/column-builder.d.ts
+++ b/column-builder.d.ts
@@ -1,4 +1,5 @@
 import { entityKind } from "./entity.js";
+import type { ClickHouseColumn } from "./clickhouse-core/columns/common.js";
 import type { Column } from "./column.js";
 import type { GelColumn, GelExtraConfigColumn } from "./gel-core/index.js";
 import type { MySqlColumn } from "./mysql-core/index.js";
@@ -8,7 +9,7 @@ import type { SQL } from "./sql/sql.js";
 import type { SQLiteColumn } from "./sqlite-core/index.js";
 import type { Assume, Simplify } from "./utils.js";
 export type ColumnDataType = 'string' | 'number' | 'boolean' | 'array' | 'json' | 'date' | 'bigint' | 'custom' | 'buffer' | 'dateDuration' | 'duration' | 'relDuration' | 'localTime' | 'localDate' | 'localDateTime';
-export type Dialect = 'pg' | 'mysql' | 'sqlite' | 'singlestore' | 'common' | 'gel';
+export type Dialect = 'pg' | 'mysql' | 'sqlite' | 'singlestore' | 'clickhouse' | 'common' | 'gel';
 export type GeneratedStorageMode = 'virtual' | 'stored';
 export type GeneratedType = 'always' | 'byDefault';
 export type GeneratedColumnConfig<TDataType> = {
@@ -227,7 +228,7 @@ export declare abstract class ColumnBuilder<T extends ColumnBuilderBaseConfig<Co
         type: 'always';
     }>;
 }
-export type BuildColumn<TTableName extends string, TBuilder extends ColumnBuilderBase, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'mysqlColumnBuilderBrand'>>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'common' ? Column<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'singlestoreColumnBuilderBrand'>>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : never;
+export type BuildColumn<TTableName extends string, TBuilder extends ColumnBuilderBase, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'mysqlColumnBuilderBrand'>>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'common' ? Column<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect' | 'primaryKeyHasDefault' | 'singlestoreColumnBuilderBrand'>>> : TDialect extends 'clickhouse' ? ClickHouseColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TBuilder['_'], TTableName>, {}, Simplify<Omit<TBuilder['_'], keyof MakeColumnConfig<TBuilder['_'], TTableName> | 'brand' | 'dialect'>>> : never;
 export type BuildIndexColumn<TDialect extends Dialect> = TDialect extends 'pg' ? ExtraConfigColumn : TDialect extends 'gel' ? GelExtraConfigColumn : never;
 export type BuildColumns<TTableName extends string, TConfigMap extends Record<string, ColumnBuilderBase>, TDialect extends Dialect> = {
     [Key in keyof TConfigMap]: BuildColumn<TTableName, {
@@ -239,4 +240,4 @@ export type BuildColumns<TTableName extends string, TConfigMap extends Record<st
 export type BuildExtraConfigColumns<_TTableName extends string, TConfigMap extends Record<string, ColumnBuilderBase>, TDialect extends Dialect> = {
     [Key in keyof TConfigMap]: BuildIndexColumn<TDialect>;
 } & {};
-export type ChangeColumnTableName<TColumn extends Column, TAlias extends string, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TColumn['_'], TAlias>> : never;
+export type ChangeColumnTableName<TColumn extends Column, TAlias extends string, TDialect extends Dialect> = TDialect extends 'pg' ? PgColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'mysql' ? MySqlColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'singlestore' ? SingleStoreColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'clickhouse' ? ClickHouseColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'sqlite' ? SQLiteColumn<MakeColumnConfig<TColumn['_'], TAlias>> : TDialect extends 'gel' ? GelColumn<MakeColumnConfig<TColumn['_'], TAlias>> : never;
diff --git a/package.json b/package.json
index 74f3ae7..8405259 100644
--- a/package.json
+++ b/package.json
@@ -72,7 +72,8 @@
     "sql.js": ">=1",
     "sqlite3": ">=5",
     "gel": ">=2",
-    "@upstash/redis": ">=1.34.7"
+    "@upstash/redis": ">=1.34.7",
+    "@clickhouse/client": ">=1"
   },
   "peerDependenciesMeta": {
     "mysql2": {
@@ -161,6 +162,9 @@
     },
     "@upstash/redis": {
       "optional": true
+    },
+    "@clickhouse/client": {
+      "optional": true
     }
   },
   "devDependencies": {
@@ -5524,6 +5528,618 @@
       },
       "types": "./pg-core/columns/vector_extension/vector.d.ts",
       "default": "./pg-core/columns/vector_extension/vector.js"
+    },
+    "./clickhouse-core/alias": {
+      "import": {
+        "types": "./clickhouse-core/alias.d.ts",
+        "default": "./clickhouse-core/alias.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/alias.d.cts",
+        "default": "./clickhouse-core/alias.cjs"
+      },
+      "types": "./clickhouse-core/alias.d.ts",
+      "default": "./clickhouse-core/alias.js"
+    },
+    "./clickhouse-core/columns/all": {
+      "import": {
+        "types": "./clickhouse-core/columns/all.d.ts",
+        "default": "./clickhouse-core/columns/all.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/all.d.cts",
+        "default": "./clickhouse-core/columns/all.cjs"
+      },
+      "types": "./clickhouse-core/columns/all.d.ts",
+      "default": "./clickhouse-core/columns/all.js"
+    },
+    "./clickhouse-core/columns/array": {
+      "import": {
+        "types": "./clickhouse-core/columns/array.d.ts",
+        "default": "./clickhouse-core/columns/array.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/array.d.cts",
+        "default": "./clickhouse-core/columns/array.cjs"
+      },
+      "types": "./clickhouse-core/columns/array.d.ts",
+      "default": "./clickhouse-core/columns/array.js"
+    },
+    "./clickhouse-core/columns/bigint": {
+      "import": {
+        "types": "./clickhouse-core/columns/bigint.d.ts",
+        "default": "./clickhouse-core/columns/bigint.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/bigint.d.cts",
+        "default": "./clickhouse-core/columns/bigint.cjs"
+      },
+      "types": "./clickhouse-core/columns/bigint.d.ts",
+      "default": "./clickhouse-core/columns/bigint.js"
+    },
+    "./clickhouse-core/columns/bool": {
+      "import": {
+        "types": "./clickhouse-core/columns/bool.d.ts",
+        "default": "./clickhouse-core/columns/bool.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/bool.d.cts",
+        "default": "./clickhouse-core/columns/bool.cjs"
+      },
+      "types": "./clickhouse-core/columns/bool.d.ts",
+      "default": "./clickhouse-core/columns/bool.js"
+    },
+    "./clickhouse-core/columns/common": {
+      "import": {
+        "types": "./clickhouse-core/columns/common.d.ts",
+        "default": "./clickhouse-core/columns/common.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/common.d.cts",
+        "default": "./clickhouse-core/columns/common.cjs"
+      },
+      "types": "./clickhouse-core/columns/common.d.ts",
+      "default": "./clickhouse-core/columns/common.js"
+    },
+    "./clickhouse-core/columns/custom": {
+      "import": {
+        "types": "./clickhouse-core/columns/custom.d.ts",
+        "default": "./clickhouse-core/columns/custom.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/custom.d.cts",
+        "default": "./clickhouse-core/columns/custom.cjs"
+      },
+      "types": "./clickhouse-core/columns/custom.d.ts",
+      "default": "./clickhouse-core/columns/custom.js"
+    },
+    "./clickhouse-core/columns/date": {
+      "import": {
+        "types": "./clickhouse-core/columns/date.d.ts",
+        "default": "./clickhouse-core/columns/date.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/date.d.cts",
+        "default": "./clickhouse-core/columns/date.cjs"
+      },
+      "types": "./clickhouse-core/columns/date.d.ts",
+      "default": "./clickhouse-core/columns/date.js"
+    },
+    "./clickhouse-core/columns/date.common": {
+      "import": {
+        "types": "./clickhouse-core/columns/date.common.d.ts",
+        "default": "./clickhouse-core/columns/date.common.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/date.common.d.cts",
+        "default": "./clickhouse-core/columns/date.common.cjs"
+      },
+      "types": "./clickhouse-core/columns/date.common.d.ts",
+      "default": "./clickhouse-core/columns/date.common.js"
+    },
+    "./clickhouse-core/columns/datetime": {
+      "import": {
+        "types": "./clickhouse-core/columns/datetime.d.ts",
+        "default": "./clickhouse-core/columns/datetime.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/datetime.d.cts",
+        "default": "./clickhouse-core/columns/datetime.cjs"
+      },
+      "types": "./clickhouse-core/columns/datetime.d.ts",
+      "default": "./clickhouse-core/columns/datetime.js"
+    },
+    "./clickhouse-core/columns/decimal": {
+      "import": {
+        "types": "./clickhouse-core/columns/decimal.d.ts",
+        "default": "./clickhouse-core/columns/decimal.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/decimal.d.cts",
+        "default": "./clickhouse-core/columns/decimal.cjs"
+      },
+      "types": "./clickhouse-core/columns/decimal.d.ts",
+      "default": "./clickhouse-core/columns/decimal.js"
+    },
+    "./clickhouse-core/columns/enum": {
+      "import": {
+        "types": "./clickhouse-core/columns/enum.d.ts",
+        "default": "./clickhouse-core/columns/enum.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/enum.d.cts",
+        "default": "./clickhouse-core/columns/enum.cjs"
+      },
+      "types": "./clickhouse-core/columns/enum.d.ts",
+      "default": "./clickhouse-core/columns/enum.js"
+    },
+    "./clickhouse-core/columns/float": {
+      "import": {
+        "types": "./clickhouse-core/columns/float.d.ts",
+        "default": "./clickhouse-core/columns/float.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/float.d.cts",
+        "default": "./clickhouse-core/columns/float.cjs"
+      },
+      "types": "./clickhouse-core/columns/float.d.ts",
+      "default": "./clickhouse-core/columns/float.js"
+    },
+    "./clickhouse-core/columns": {
+      "import": {
+        "types": "./clickhouse-core/columns/index.d.ts",
+        "default": "./clickhouse-core/columns/index.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/index.d.cts",
+        "default": "./clickhouse-core/columns/index.cjs"
+      },
+      "types": "./clickhouse-core/columns/index.d.ts",
+      "default": "./clickhouse-core/columns/index.js"
+    },
+    "./clickhouse-core/columns/int": {
+      "import": {
+        "types": "./clickhouse-core/columns/int.d.ts",
+        "default": "./clickhouse-core/columns/int.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/int.d.cts",
+        "default": "./clickhouse-core/columns/int.cjs"
+      },
+      "types": "./clickhouse-core/columns/int.d.ts",
+      "default": "./clickhouse-core/columns/int.js"
+    },
+    "./clickhouse-core/columns/ip": {
+      "import": {
+        "types": "./clickhouse-core/columns/ip.d.ts",
+        "default": "./clickhouse-core/columns/ip.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/ip.d.cts",
+        "default": "./clickhouse-core/columns/ip.cjs"
+      },
+      "types": "./clickhouse-core/columns/ip.d.ts",
+      "default": "./clickhouse-core/columns/ip.js"
+    },
+    "./clickhouse-core/columns/json": {
+      "import": {
+        "types": "./clickhouse-core/columns/json.d.ts",
+        "default": "./clickhouse-core/columns/json.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/json.d.cts",
+        "default": "./clickhouse-core/columns/json.cjs"
+      },
+      "types": "./clickhouse-core/columns/json.d.ts",
+      "default": "./clickhouse-core/columns/json.js"
+    },
+    "./clickhouse-core/columns/low-cardinality": {
+      "import": {
+        "types": "./clickhouse-core/columns/low-cardinality.d.ts",
+        "default": "./clickhouse-core/columns/low-cardinality.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/low-cardinality.d.cts",
+        "default": "./clickhouse-core/columns/low-cardinality.cjs"
+      },
+      "types": "./clickhouse-core/columns/low-cardinality.d.ts",
+      "default": "./clickhouse-core/columns/low-cardinality.js"
+    },
+    "./clickhouse-core/columns/map": {
+      "import": {
+        "types": "./clickhouse-core/columns/map.d.ts",
+        "default": "./clickhouse-core/columns/map.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/map.d.cts",
+        "default": "./clickhouse-core/columns/map.cjs"
+      },
+      "types": "./clickhouse-core/columns/map.d.ts",
+      "default": "./clickhouse-core/columns/map.js"
+    },
+    "./clickhouse-core/columns/nullable": {
+      "import": {
+        "types": "./clickhouse-core/columns/nullable.d.ts",
+        "default": "./clickhouse-core/columns/nullable.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/nullable.d.cts",
+        "default": "./clickhouse-core/columns/nullable.cjs"
+      },
+      "types": "./clickhouse-core/columns/nullable.d.ts",
+      "default": "./clickhouse-core/columns/nullable.js"
+    },
+    "./clickhouse-core/columns/string": {
+      "import": {
+        "types": "./clickhouse-core/columns/string.d.ts",
+        "default": "./clickhouse-core/columns/string.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/string.d.cts",
+        "default": "./clickhouse-core/columns/string.cjs"
+      },
+      "types": "./clickhouse-core/columns/string.d.ts",
+      "default": "./clickhouse-core/columns/string.js"
+    },
+    "./clickhouse-core/columns/tuple": {
+      "import": {
+        "types": "./clickhouse-core/columns/tuple.d.ts",
+        "default": "./clickhouse-core/columns/tuple.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/tuple.d.cts",
+        "default": "./clickhouse-core/columns/tuple.cjs"
+      },
+      "types": "./clickhouse-core/columns/tuple.d.ts",
+      "default": "./clickhouse-core/columns/tuple.js"
+    },
+    "./clickhouse-core/columns/uuid": {
+      "import": {
+        "types": "./clickhouse-core/columns/uuid.d.ts",
+        "default": "./clickhouse-core/columns/uuid.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/columns/uuid.d.cts",
+        "default": "./clickhouse-core/columns/uuid.cjs"
+      },
+      "types": "./clickhouse-core/columns/uuid.d.ts",
+      "default": "./clickhouse-core/columns/uuid.js"
+    },
+    "./clickhouse-core/db": {
+      "import": {
+        "types": "./clickhouse-core/db.d.ts",
+        "default": "./clickhouse-core/db.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/db.d.cts",
+        "default": "./clickhouse-core/db.cjs"
+      },
+      "types": "./clickhouse-core/db.d.ts",
+      "default": "./clickhouse-core/db.js"
+    },
+    "./clickhouse-core/ddl": {
+      "import": {
+        "types": "./clickhouse-core/ddl.d.ts",
+        "default": "./clickhouse-core/ddl.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/ddl.d.cts",
+        "default": "./clickhouse-core/ddl.cjs"
+      },
+      "types": "./clickhouse-core/ddl.d.ts",
+      "default": "./clickhouse-core/ddl.js"
+    },
+    "./clickhouse-core/dialect": {
+      "import": {
+        "types": "./clickhouse-core/dialect.d.ts",
+        "default": "./clickhouse-core/dialect.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/dialect.d.cts",
+        "default": "./clickhouse-core/dialect.cjs"
+      },
+      "types": "./clickhouse-core/dialect.d.ts",
+      "default": "./clickhouse-core/dialect.js"
+    },
+    "./clickhouse-core/engines": {
+      "import": {
+        "types": "./clickhouse-core/engines.d.ts",
+        "default": "./clickhouse-core/engines.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/engines.d.cts",
+        "default": "./clickhouse-core/engines.cjs"
+      },
+      "types": "./clickhouse-core/engines.d.ts",
+      "default": "./clickhouse-core/engines.js"
+    },
+    "./clickhouse-core/errors": {
+      "import": {
+        "types": "./clickhouse-core/errors.d.ts",
+        "default": "./clickhouse-core/errors.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/errors.d.cts",
+        "default": "./clickhouse-core/errors.cjs"
+      },
+      "types": "./clickhouse-core/errors.d.ts",
+      "default": "./clickhouse-core/errors.js"
+    },
+    "./clickhouse-core/expressions": {
+      "import": {
+        "types": "./clickhouse-core/expressions.d.ts",
+        "default": "./clickhouse-core/expressions.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/expressions.d.cts",
+        "default": "./clickhouse-core/expressions.cjs"
+      },
+      "types": "./clickhouse-core/expressions.d.ts",
+      "default": "./clickhouse-core/expressions.js"
+    },
+    "./clickhouse-core": {
+      "import": {
+        "types": "./clickhouse-core/index.d.ts",
+        "default": "./clickhouse-core/index.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/index.d.cts",
+        "default": "./clickhouse-core/index.cjs"
+      },
+      "types": "./clickhouse-core/index.d.ts",
+      "default": "./clickhouse-core/index.js"
+    },
+    "./clickhouse-core/indexes": {
+      "import": {
+        "types": "./clickhouse-core/indexes.d.ts",
+        "default": "./clickhouse-core/indexes.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/indexes.d.cts",
+        "default": "./clickhouse-core/indexes.cjs"
+      },
+      "types": "./clickhouse-core/indexes.d.ts",
+      "default": "./clickhouse-core/indexes.js"
+    },
+    "./clickhouse-core/literals": {
+      "import": {
+        "types": "./clickhouse-core/literals.d.ts",
+        "default": "./clickhouse-core/literals.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/literals.d.cts",
+        "default": "./clickhouse-core/literals.cjs"
+      },
+      "types": "./clickhouse-core/literals.d.ts",
+      "default": "./clickhouse-core/literals.js"
+    },
+    "./clickhouse-core/query-builders/count": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/count.d.ts",
+        "default": "./clickhouse-core/query-builders/count.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/count.d.cts",
+        "default": "./clickhouse-core/query-builders/count.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/count.d.ts",
+      "default": "./clickhouse-core/query-builders/count.js"
+    },
+    "./clickhouse-core/query-builders/delete": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/delete.d.ts",
+        "default": "./clickhouse-core/query-builders/delete.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/delete.d.cts",
+        "default": "./clickhouse-core/query-builders/delete.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/delete.d.ts",
+      "default": "./clickhouse-core/query-builders/delete.js"
+    },
+    "./clickhouse-core/query-builders": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/index.d.ts",
+        "default": "./clickhouse-core/query-builders/index.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/index.d.cts",
+        "default": "./clickhouse-core/query-builders/index.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/index.d.ts",
+      "default": "./clickhouse-core/query-builders/index.js"
+    },
+    "./clickhouse-core/query-builders/insert": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/insert.d.ts",
+        "default": "./clickhouse-core/query-builders/insert.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/insert.d.cts",
+        "default": "./clickhouse-core/query-builders/insert.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/insert.d.ts",
+      "default": "./clickhouse-core/query-builders/insert.js"
+    },
+    "./clickhouse-core/query-builders/query-builder": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/query-builder.d.ts",
+        "default": "./clickhouse-core/query-builders/query-builder.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/query-builder.d.cts",
+        "default": "./clickhouse-core/query-builders/query-builder.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/query-builder.d.ts",
+      "default": "./clickhouse-core/query-builders/query-builder.js"
+    },
+    "./clickhouse-core/query-builders/select": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/select.d.ts",
+        "default": "./clickhouse-core/query-builders/select.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/select.d.cts",
+        "default": "./clickhouse-core/query-builders/select.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/select.d.ts",
+      "default": "./clickhouse-core/query-builders/select.js"
+    },
+    "./clickhouse-core/query-builders/select.types": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/select.types.d.ts",
+        "default": "./clickhouse-core/query-builders/select.types.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/select.types.d.cts",
+        "default": "./clickhouse-core/query-builders/select.types.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/select.types.d.ts",
+      "default": "./clickhouse-core/query-builders/select.types.js"
+    },
+    "./clickhouse-core/query-builders/update": {
+      "import": {
+        "types": "./clickhouse-core/query-builders/update.d.ts",
+        "default": "./clickhouse-core/query-builders/update.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/query-builders/update.d.cts",
+        "default": "./clickhouse-core/query-builders/update.cjs"
+      },
+      "types": "./clickhouse-core/query-builders/update.d.ts",
+      "default": "./clickhouse-core/query-builders/update.js"
+    },
+    "./clickhouse-core/schema": {
+      "import": {
+        "types": "./clickhouse-core/schema.d.ts",
+        "default": "./clickhouse-core/schema.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/schema.d.cts",
+        "default": "./clickhouse-core/schema.cjs"
+      },
+      "types": "./clickhouse-core/schema.d.ts",
+      "default": "./clickhouse-core/schema.js"
+    },
+    "./clickhouse-core/session": {
+      "import": {
+        "types": "./clickhouse-core/session.d.ts",
+        "default": "./clickhouse-core/session.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/session.d.cts",
+        "default": "./clickhouse-core/session.cjs"
+      },
+      "types": "./clickhouse-core/session.d.ts",
+      "default": "./clickhouse-core/session.js"
+    },
+    "./clickhouse-core/subquery": {
+      "import": {
+        "types": "./clickhouse-core/subquery.d.ts",
+        "default": "./clickhouse-core/subquery.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/subquery.d.cts",
+        "default": "./clickhouse-core/subquery.cjs"
+      },
+      "types": "./clickhouse-core/subquery.d.ts",
+      "default": "./clickhouse-core/subquery.js"
+    },
+    "./clickhouse-core/table": {
+      "import": {
+        "types": "./clickhouse-core/table.d.ts",
+        "default": "./clickhouse-core/table.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/table.d.cts",
+        "default": "./clickhouse-core/table.cjs"
+      },
+      "types": "./clickhouse-core/table.d.ts",
+      "default": "./clickhouse-core/table.js"
+    },
+    "./clickhouse-core/utils": {
+      "import": {
+        "types": "./clickhouse-core/utils.d.ts",
+        "default": "./clickhouse-core/utils.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/utils.d.cts",
+        "default": "./clickhouse-core/utils.cjs"
+      },
+      "types": "./clickhouse-core/utils.d.ts",
+      "default": "./clickhouse-core/utils.js"
+    },
+    "./clickhouse-core/view-base": {
+      "import": {
+        "types": "./clickhouse-core/view-base.d.ts",
+        "default": "./clickhouse-core/view-base.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/view-base.d.cts",
+        "default": "./clickhouse-core/view-base.cjs"
+      },
+      "types": "./clickhouse-core/view-base.d.ts",
+      "default": "./clickhouse-core/view-base.js"
+    },
+    "./clickhouse-core/view-common": {
+      "import": {
+        "types": "./clickhouse-core/view-common.d.ts",
+        "default": "./clickhouse-core/view-common.js"
+      },
+      "require": {
+        "types": "./clickhouse-core/view-common.d.cts",
+        "default": "./clickhouse-core/view-common.cjs"
+      },
+      "types": "./clickhouse-core/view-common.d.ts",
+      "default": "./clickhouse-core/view-common.js"
+    },
+    "./clickhouse/driver": {
+      "import": {
+        "types": "./clickhouse/driver.d.ts",
+        "default": "./clickhouse/driver.js"
+      },
+      "require": {
+        "types": "./clickhouse/driver.d.cts",
+        "default": "./clickhouse/driver.cjs"
+      },
+      "types": "./clickhouse/driver.d.ts",
+      "default": "./clickhouse/driver.js"
+    },
+    "./clickhouse": {
+      "import": {
+        "types": "./clickhouse/index.d.ts",
+        "default": "./clickhouse/index.js"
+      },
+      "require": {
+        "types": "./clickhouse/index.d.cts",
+        "default": "./clickhouse/index.cjs"
+      },
+      "types": "./clickhouse/index.d.ts",
+      "default": "./clickhouse/index.js"
+    },
+    "./clickhouse/migrator": {
+      "import": {
+        "types": "./clickhouse/migrator.d.ts",
+        "default": "./clickhouse/migrator.js"
+      },
+      "require": {
+        "types": "./clickhouse/migrator.d.cts",
+        "default": "./clickhouse/migrator.cjs"
+      },
+      "types": "./clickhouse/migrator.d.ts",
+      "default": "./clickhouse/migrator.js"
+    },
+    "./clickhouse/session": {
+      "import": {
+        "types": "./clickhouse/session.d.ts",
+        "default": "./clickhouse/session.js"
+      },
+      "require": {
+        "types": "./clickhouse/session.d.cts",
+        "default": "./clickhouse/session.cjs"
+      },
+      "types": "./clickhouse/session.d.ts",
+      "default": "./clickhouse/session.js"
     }
   }
 }
