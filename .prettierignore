node_modules
dist
.turbo
.next
.tanstack
out
coverage
pnpm-lock.yaml
*.generated.*
routeTree.gen.ts
**/assets/generated
.expo
/sdk

# Terraform state — machine-written JSON, rewritten by every apply. Linting it
# only produces a false format:check failure right after you deploy.
*.tfstate
*.tfstate.*

# Generator-owned artifacts — `generate:openapi` and `drizzle-kit generate`
# write these wholesale in their own style. Formatting them just guarantees a
# spurious mega-diff on the next regeneration.
app/packages/web/openapi.json
app/packages/web/src/db/migrations/
